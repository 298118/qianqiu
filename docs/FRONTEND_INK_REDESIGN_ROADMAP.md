# S73-S77 前端水墨重构路线图

本文档是《千秋》前端大重构的活动任务书。目标是把现有浏览器界面从功能堆叠式页面，升级为中国古典水墨、手绘、泛黄宣纸与奏折质感的沉浸式史书/案头体验，同时继续保护 Mock 默认可玩、无构建前端、服务器裁决、存档安全与完整书生科举路径。

## 1. 设计目标

关键词：案头、卷轴、留白、淡墨、朱印、纸纹、微动。

整体方向：

- 首页是展开的画卷：顶部“千秋”，下方水墨云雾轻动，表单如题名册或户籍册，开始按钮如玉玺落印。
- 主界面是活的史书与案头工具：叙事正文优先阅读，地图、身份面板、人物、局势簿和专属功能按页签/抽屉/专题界面分层。
- 输入区固定底部：多行输入框伪作展开竹简或空白奏折，右侧“呈上”朱印按钮，支持 Enter 提交、Shift+Enter 换行，并在上方淡显最近一次 AI 行动提示。
- 设置统一右上角：AI 设置、存档/读档、返回首页、显示偏好和可访问性开关都进入“印匣/设置”入口，避免散落在页面各处。
- 专属场景全屏化：科举考试、放榜、殿试、朝议、县衙审案、军营、城市等重要场景有独立界面与插画，而不是只在侧栏追加小卡片。
- 身份面板差异化：书生、地方官、入仕官员、大臣、将领、皇帝拥有不同背景、色彩权重、交互入口和专属操作预留；皇帝需要预留奏折、圣旨、任免、朝议等未来扩展位。
- 地图升级复用 S72 PixiJS：不推倒 `mapRuntimeView` 和 `public/mapRenderer.js`，而是纳入新主界面布局、视觉皮肤、场景背景和设置抽屉。

视觉原则：

- 主背景使用宣纸米白、浅驼和旧绢色，建议范围 `#f5f0e6` 到 `#e8dcc8`。
- 正文使用深墨色，关键标题和行动按钮用朱砂红；朱砂只用于重要状态、榜文、印章和提交按钮。
- 边框使用淡墨线、折痕、骑缝章、竹简压边、残纸边缘，避免厚重雕花和高饱和装饰。
- 背景图案必须低对比，不遮挡文字。文字游戏的阅读舒适度高于装饰密度。
- 动效像墨气浮动、纸页轻展和朱印落下；避免现代弹跳、霓虹、玻璃拟态和过度闪烁。

## 2. 稳定边界

技术边界：

- 前端继续使用纯 HTML/CSS/JavaScript，无 build step。除非后续单独批准，不引入 React/Vue/Svelte 或 bundler。
- 现有 `public/app.js` 已承担大量状态和渲染职责，S74 起应逐步拆成浏览器原生模块或小文件，但保持 Express 静态加载即可运行。
- S72 的 PixiJS 地图继续使用本地 `pixi.js@7.4.3` vendor 优先和固定 CDN fallback；缺 PixiJS 或缺素材时必须静态降级，不阻断文字主流程。
- 存档仍走已有 `GET /api/game/saves` 与 `GET /api/game/player-state/:sessionId`；读档只读 redacted player state，不读取 raw session、raw audit 或 SQLite 原始行。
- AI 设置仍走 `GET/POST /api/ai/settings/:sessionId` 和安全 `aiSettingsView` / `aiControlAuditView`；前端不得读取 `.env`、本地路径、raw prompt、raw provider payload 或 hidden ledger。
- 返回首页只改变前端视图和当前本地 session 指针，不删除存档，不重写服务器状态。

内容边界：

- 所有 AI 生成或第三方候选视觉素材入库前，都必须由 Codex 视觉审核历史/水墨适配、缩放可读性、无水印、无现代元素、无可读乱码、无 hidden/raw 暗示、无本地路径和 key。
- 玩家与 NPC 立绘要求古代形象、成年、端庄、有身份感、俊美但不露骨；不得性化未成年人，不用幼态卖点，不生成过度暴露、挑逗或只强调身体的素材。
- 女官与女性 NPC 可以按世界观存在，长期目标可维持较均衡性别比例；表现重点是身份、服饰、姿态、神情和历史氛围。
- 科举、刑名、战争等场景可以有紧张感，但避免血腥猎奇。
- 榜文、奏折、书卷、试卷背景尽量不生成可读文字；真实可读文字由前端 HTML 渲染，避免图片乱码和错误史实。
- 立绘规模 3-4 百张属于长期目标，不在 S73 一次性全部生成。先建立风格基准、manifest、审核流程和少量代表样张，再按身份/剧情/重要 NPC 分批扩展。

安全视图边界：

- 新增首页、设置、存档、身份面板、考试界面、放榜界面、NPC 立绘面板只能组合已有 route view 或后续服务器新增的安全 projection。
- 若新增任何可见数据域、角色面板、NPC 私档摘要或场景工具入口，必须同步定义 AI read scope、actor intelligence、tool permissions、proposal boundaries、server adjudication、audit records 和 Mock/no-key fallback。
- `mapRuntimeView` 的显示坐标仍只供浏览器渲染，不进入 prompt、AI 工具或服务器裁决事实。

## 3. 信息架构草案

### 首页

第一视口必须直接传达“千秋”与古代史书/画卷气质：

- 顶部大标题“千秋”，下方水墨云雾动画。
- 中央表单置于画卷或题名册中：朝代下拉、自定义年份、身份、姓名、家境；书生身份才显示家境细分“贫寒/普通/世家”，并保留自定义背景。
- “开始”按钮为朱印/玉玺盖章效果。
- 右上角可打开设置/存档；首页存档可以以“案卷”列表呈现。
- 背景为巨幅水墨山水画卷，留白足够承载表单，不遮挡输入。

### 主界面

建议分为五个层次：

- 顶栏：日期、身份、地点/场景、核心资源、设置入口。
- 主叙事卷：中央阅读区，像史书正文或卷册页面，保留最近行动、AI 叙事、世界回响。
- 场景插画带：根据当前身份与场景显示淡墨背景或专属插画，如书斋、贡院、官署、军营、御案、城市。
- 功能页签/抽屉：舆图、人物、局势簿、科举档案、官职履历、AI 调动审计等进入分层界面，不全部常驻堆叠。
- 底部行动奏折：固定输入与“呈上”，响应身份 placeholder。

### 右上角设置

统一入口建议命名为“印匣”或“设置”：

- AI 设置：复用 `aiSettingsView`，展示 provider/model/输出长度/安全严格度/critic/safety，不暴露 key。
- 存档/读档：复用存档 metadata 列表，显示当前存档、最近更新时间、身份、回合、摘要。
- 返回首页：回到首页但保留当前 session；提供“继续本局”入口。
- 显示偏好：动效开关、字体大小、对比度、地图动效、自动滚动叙事。
- 本地诊断提示：仅显示安全摘要，不连接开发诊断 raw API。

### 专属界面

- 科举考试：全屏模态，贡院号舍淡墨背景，中央试卷，上方考题，下方宽大写作区，顶部考试阶段/局部时间，侧边虚拟考生或考场记录，“交卷”为火漆/朱印。
- 放榜：皇榜黄纸底，状元/榜眼/探花朱砂章强调，其他名次列表，玩家高亮，点击玩家名展开考官评语、防作弊和评分维度；背景为贡院外放榜场景。
- 皇帝：御案界面，奏折队列、圣旨书写、朝议入口、任免/赏罚预留位；所有后果仍由服务器 resolver 裁决。
- 地方官：县衙案牍、钱粮、水利、盗匪、词讼入口，未来接入地方案件和财政 resolver。
- 将领：军帐、粮饷、斥候、边患、军令草案和战报入口。
- 大臣/入仕官员：部院公文、同年/派系、考成、弹劾、奏疏入口。
- 书生：书斋、读书簿、师友、科期、文章练习和赶考入口。

## 4. S73 素材准备

目标：建立可复用视觉资产体系，而不是先大改 UI。S73 完成后，S74-S76 能直接按 manifest 与视觉规范接入。

### S73.0 前端水墨重构规划

状态：本路线图即 S73.0 的交付。

交付：

- 固定 S73-S77 范围、非目标、安全边界、素材方向和任务拆分。
- 记录 medium 子代理创意发散输入，Codex 负责最终口径、prompt 定稿、视觉审核和提交。
- 将后续工作写入 `docs/DEVELOPMENT_STEPS.md`、brief、README 和共享上下文。

### S73.1 视觉规范与素材契约

交付：

- 新增或扩展 `docs/FRONTEND_VISUAL_ASSET_GUIDE.md`，固定首页、主界面、场景、身份、NPC、UI 材质、动效素材的尺寸、格式、命名、颜色和审核标准。
- 新增前端资产 manifest 草案，建议路径 `public/assets/ui/ink-ui-manifest.json`，与 S72 地图 manifest 分离但可互相引用。
- 新增素材台账，建议路径 `docs/FRONTEND_ASSET_LEDGER.md`，记录生成工具、prompt 摘要、后处理、许可、视觉审核、公开发行许可和替换计划。
- 规定资产分层：背景大图、场景插画、身份背景、NPC 立绘、UI 材质、图标、动效、fallback。

Manifest 字段建议：

```json
{
  "id": "s73-home-scroll-bg-v1",
  "phase": "S73",
  "category": "backgrounds",
  "roleOrScene": "home",
  "path": "/assets/ui/s73-home-scroll-bg-v1.webp",
  "format": "webp",
  "dimensions": { "width": 1920, "height": 1080 },
  "transparent": false,
  "toneTags": ["水墨", "宣纸", "山水", "留白"],
  "usage": ["home.hero.background"],
  "sourceType": "ai_generated",
  "generator": "gpt-image-2",
  "promptSummary": "首页山水画卷背景，淡墨云雾，留白充足",
  "licenseNote": "project-generated",
  "visualReview": {
    "reviewer": "Codex",
    "status": "approved",
    "notes": "无文字、水印、现代元素；低对比，适合表单叠加"
  },
  "safetyReview": {
    "noSecrets": true,
    "noHiddenRaw": true,
    "adultOrNonHumanOnly": true,
    "nonSexualized": true
  },
  "fallback": "s73-paper-parchment-v1"
}
```

### S73.2 首批界面素材

素材清单：

- 首页：山水画卷背景、云雾透明层、宣纸/羊皮纸纹理、卷轴边缘、题名册底、玉玺/朱印按钮底。
- 主界面：史书正文纸面、奏折输入纸、竹简压边、淡墨分隔线、设置印匣纹理、fallback 纸底。
- 场景：贡院号舍、皇榜放榜、殿试金殿、书斋、县衙、公堂、军帐、御案、城市街巷。
- 身份背景：书生、地方官、入仕官员、大臣、将领、皇帝。
- 动效：水墨云雾、墨迹扩散、朱印落章、纸页轻展遮罩。

验收：

- 每个素材有 manifest、台账和 Codex 视觉审核。
- 大图在桌面与移动裁切后仍有留白。
- 不含可读文字、水印、现代元素、乱码、密钥、本地路径或 hidden/raw 内容。
- 有资源失败 fallback，不能阻断开局、读档、地图和科举。

### S73.3 NPC 与玩家立绘基准

目标是建立风格，不是一次性生成 3-4 百张。

首批样张：

- 玩家：成年男女书生、成年男女入仕官员、成年男女将领、成年男女皇帝/摄政视角头像或半身。
- NPC 类型：老师、考官、同年、县令、吏员、大臣、将领、宫廷人物、商贾、士绅、女官。
- 重要 NPC 预留：张居正、魏忠贤等历史/拟史人物先做 prompt 与审核规则，不必在 S73 全部实装。

立绘规范：

- 成人、端庄、身份明确、服饰符合古代语境。
- 同一人物后续可按姿态/状态扩展：常态、喜、怒、忧、病、入仕、战时、朝服等。
- 生成时避免幼态、露骨、现代妆造、可读文字、徽标、水印和夸张动漫化。
- 浏览器先通过 manifest `portraitRef` 预留接线，不把 hidden NPC 私档暴露给前端。

### S73.4 素材预览与 QA

交付：

- 简单静态素材预览页或测试脚本，列出 manifest 项目、尺寸、路径、透明度、用途和审核状态。
- Node 测试校验 manifest 路径只指向 `public/assets/`，字段不含 key/path/raw/prompt 原文泄漏，图片存在且尺寸符合预期。
- 若生成动效素材，增加 reduced-motion fallback 说明。

## 5. S74 信息架构与前端拆分

目标：在不改视觉大面积 CSS 前，先把现有前端从单一巨型文件拆出可维护边界。

建议拆分：

- `public/app.js` 保留启动和兼容桥接，逐步迁移。
- `public/ui/state.js`：当前 session、route view、设置、存档、视图状态。
- `public/ui/api.js`：start、turn、saves、player-state、AI settings、exam API。
- `public/ui/shell.js`：首页/游戏/模态/抽屉切换。
- `public/ui/settingsDrawer.js`：AI 设置、存档、返回首页、显示偏好。
- `public/ui/actionDock.js`：底部输入、快捷提交、AI 行动提示。
- `public/ui/rolePanels.js`：身份面板 registry。
- `public/ui/examSurfaces.js`：考试与放榜全屏界面。
- `public/ui/assets.js`：manifest 加载、fallback、CSS 变量注入。

验收：

- 不引入构建工具，script 加载顺序清晰。
- 现有 browser smoke 仍通过。
- `app.js` 不再继续无限膨胀；新模块职责清楚。
- 存档、AI 设置、地图、科举和完整书生路径不回退。

## 6. S75 首页与全局 Shell

目标：完成用户第一眼可见的水墨画卷首页与统一设置入口。

交付：

- 首页重做为全屏画卷：标题“千秋”、云雾动画、朝代/年份/身份/姓名/家境/自定义背景表单。
- 家境控件按身份显示，书生可选“贫寒/普通/世家”，并保留自定义文本。
- “开始”按钮采用玉玺/朱印视觉，有按下盖章反馈。
- 右上角设置入口上线，整合 AI 设置、存档/读档、返回首页、显示偏好。
- 首页存档列表改为案卷/档册视觉，可继续读取安全 metadata。
- 返回首页后能继续当前游戏或选择读档，不丢失当前 session。

验收：

- `npm run smoke:browser` 覆盖开局、读档、返回首页、设置抽屉。
- 移动端表单不溢出，按钮文字不挤压。
- 低动效偏好下云雾和盖章动效降级。

## 7. S76 身份、场景与专题界面

目标：把主游戏从“所有面板堆在侧栏”升级为身份和场景驱动的清爽多页体验。

交付：

- 主界面按身份使用 panel registry：书生、地方官、入仕官员、大臣、将领、皇帝。
- 每个身份有专属背景、核心指标、常用动作入口和未来玩法预留位。
- 地图作为“舆图”页整合到主界面，可在需要时扩大展示，但仍只读安全 `mapRuntimeView`。
- 底部行动奏折固定，支持 Enter 提交、Shift+Enter 换行、身份 placeholder 和 AI 行动提示。
- 科举考试全屏重构：号舍背景、试卷区域、考题、写作区、场内阶段、虚拟考生/考场记录、交卷朱印。
- 放榜全屏重构：皇榜背景、三鼎甲朱砂章、排名列表、玩家高亮、评分/防作弊/考官评语展开。
- 皇帝专属界面预留奏折和圣旨：前端只生成自然语言草稿或 proposal 表单，最终后果仍由服务器普通回合或后续 resolver 裁决。

验收：

- 书生完整科举路径继续通过：`scholar -> child_exam -> provincial_exam -> metropolitan_exam -> palace_exam -> official`。
- 皇帝、大臣、将领、地方官、入仕官员代表性开局与行动反馈继续可用。
- 新界面不读取 raw `worldState` hidden ledger、raw audit、provider proposal、prompt、本地路径或 key。

## 8. S77 验收、性能与归档

目标：把大重构收口为稳定可玩版本。

验收矩阵：

- 浏览器 smoke：桌面、移动、开局、读档、返回首页、设置抽屉、AI 设置、安全存档、地图、科举、放榜、代表身份行动。
- 像素/视觉检查：首页背景非空、地图 canvas 非空、考试/放榜背景加载、资源失败 fallback、生图无现代元素或可读乱码。
- 可访问性：键盘提交、Esc 关闭抽屉/模态、焦点回收、对比度、字体大小、reduced-motion。
- 安全：hidden/raw/key/path/prompt 污染防线，设置和存档只读安全 API。
- 性能：首屏资产懒加载，NPC 立绘按需加载，移动端无明显卡顿；不一次性加载 3-4 百张立绘。
- 文档：更新 README、brief、共享上下文、活动台账，必要时新增归档。

完成后：

- 若 S73-S77 全部完成，新增归档 `docs/FRONTEND_INK_REDESIGN_ARCHIVE.md`。
- 活动台账压缩为归档入口，后续再开新的小步骤扩展 NPC 批量立绘、皇帝玩法、场景 resolver 或地图深度玩法。

## 9. Prompt 草案

创意发散由 medium 子代理提供，以下为 Codex 收口后的可用草案。实际生成前仍需根据尺寸、用途、留白位置和负面约束定稿。

### 水墨背景

一幅适合中国古代历史模拟文字游戏的淡墨背景，泛黄宣纸质感，松烟墨晕染，远处有若隐若现的山脉、江河与城郭轮廓，大面积留白，低对比，不含任何文字、标志、水印、现代建筑或现代 UI。画面气质沉静、典雅，像案头展开的旧史书插页，适合叠加中文界面文字。

### 首页山水画卷

中国古典水墨长卷风格的首页主视觉，画卷在书案上展开，远山、江水、云雾、古城与驿道以淡墨绘成，画面中央上方留出放置“千秋”二字的空白，前景有极淡纸纹与卷轴边缘，整体温润泛黄，庄重但不压迫。不要出现真实文字、人物特写、现代物品、水印或花哨装饰，风格清爽，留白充足。

### 考试号舍

明清科举贡院号舍场景，窄小号舍、木板、考篮、砚台、卷纸、孤灯，雨后微湿的青砖与深夜寒意，水墨手绘插画风，宣纸底色，墨色克制，局部暖光。气氛紧张、清冷、专注，适合作为全屏考试界面背景。不要出现可读文字、现代灯具、现代桌椅、夸张戏剧表情或血腥内容。

### 皇榜放榜

古代皇榜放榜场景，城门或贡院外张贴榜文，士子与百姓聚集远观，朱砂榜纸醒目但榜上不可有可读文字，远处旗幡与飞檐以淡墨勾勒，画面有晨雾和金色微光。整体像中国手绘历史画卷，热闹但不拥挤，主体清晰，适合作为放榜全屏界面。无现代服饰、无现代建筑、无水印。

### 身份背景：书生

中国古代寒门书生的身份背景图，简朴书斋，木案、旧书、砚台、窗外竹影与远山，宣纸泛黄底，水墨手绘风，清寒而有志气。画面留出右侧或中部大块空白用于 UI 面板，不出现可读文字，不要华丽宫廷感，不要现代物品。

### 身份背景：地方官

中国古代地方官署背景，县衙厅堂、案牍、公文匣、屏风、庭院树影，以淡墨和少量朱砂表现制度感。画面端正、克制、可长期阅读，不拥挤，留出信息面板空间。不要出现可读文字、现代徽章或夸张刑具。

### 身份背景：皇帝

古代帝王视角的御案与殿阁背景，远处金殿轮廓，近处御案、奏折、玉玺、烛光，以水墨与低饱和金色表现威仪。庄重、深远、不过度奢华，留白充足，适合文字游戏主界面。不要出现真实文字、现代元素、夸张龙纹堆叠或强烈金光。

### NPC 立绘风格

中国古代历史模拟游戏 NPC 半身立绘风格基准，水墨手绘，淡彩上色，人物为成年，站姿端正，服饰符合古代士人、官员、将领、女官或市井人物气质，面部有克制表情与身份差异，背景透明或极淡宣纸底。线条干净，适合在 UI 面板中小尺寸显示。不要幼态化、不要露骨性化、不要夸张动漫眼、不要现代饰品、不要可读文字、水印或徽标。

## 10. 风险与后续优化

- 最大风险不是“不够古风”，而是“太古风导致难读”。所有装饰必须服务阅读与长期游玩。
- 大量素材会影响加载性能，S76 前必须引入懒加载、缩略图和 fallback，不一次性加载大批立绘。
- 现有 `public/app.js` 体量过大，若不在 S74 拆分，S75-S76 会变成难以审查的大 patch。
- 立绘长期规模很大，需要以人物 `portraitRef`、manifest、台账和审核状态驱动，而不是把图片路径硬编码在渲染函数里。
- 皇帝、地方官、将领等专属操作先做前端草稿和入口预留；真实玩法后果必须走后续服务器 resolver，不让前端或模型直接改状态。
