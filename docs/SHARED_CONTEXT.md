# Shared AI Development Context

这是 Codex 当前使用的压缩交接板。它只保留接手下一步必须知道的状态、边界、验证入口和内容保护规则；阶段细节请追溯 `docs/DEVELOPMENT_STEPS.md` 顶部归档索引和各专题归档。

## Read First

每次开发会话开始前必须读取：

1. `AGENTS.md` 或 `CLAUDE.md`
2. `docs/SHARED_CONTEXT.md`
3. `docs/QIANQIU_DEVELOPMENT_BRIEF.md`
4. `docs/DEVELOPMENT_STEPS.md`

稳定治理锚点见 `docs/DEVELOPMENT_GOVERNANCE.md`。重写 brief、路线图或交接文档时不得删弱受保护规则，并运行 `npm run check:docs-governance`；完整测试中的 `test/documentationGovernance.test.js` 也会守门。

## Current Snapshot

- Product: browser + Node.js historical simulation text game **Qianqiu / 千秋**。
- Runtime target: `npm install && npm start`，默认 `AI_PROVIDER=mock`，打开 `http://localhost:3000` 即可本地游玩。真实 provider 只作为可选配置，不能成为启动门槛。
- Frontend: React + TypeScript + Vite 在 `client/`，生产构建在 `dist/client/`，Express 默认 `/` 服务 React SPA。React Router Data Mode 管理首页、主卷、舆图、人物、囊箧、史册、科举、皇榜、朝议和设置。旧 `public/index.html`、`public/app.js`、`public/styles.css`、`public/mapPanel.js` 只作迁移参考；`public/assets/`、`public/vendor/`、`public/mapRenderer.js` 继续提供已审核素材和 S72 地图 runtime。
- Backend: Node.js + Express，当前以 CommonJS JavaScript 为主。S86 已建立渐进 TypeScript 检查地基，`npm run typecheck:server` 覆盖契约、API/view 类型、安全 projection、AI schema/provider facade、storage adapter 和核心 resolver 的首批边界；S87 已完成 route/API response shape 首轮覆盖，`src/contracts/serverContracts.ts` 固定 public response，`src/routes/routeResponses.js` 用局部 `@ts-check` helper 接入大型 route 并运行时拒绝 public `worldState` raw ledger key。不得为了启用类型检查一次性 whole-file `@ts-check` 大型 route 文件，也不得放宽 raw ledger 剥离、Ajv/runtime 校验或服务器裁决。
- Storage: 默认 JSON session files under `data/sessions/`；可选 `STORAGE_ADAPTER=sqlite` 使用本地 `schema_migrations`、`world_sessions`、audit tables、`geo_*`、`people_*`、`office_*`、`event_archive_index`、`prompt_retrieval_index` 和 `safe_search_index`。SQLite 派生行只从 `world_sessions.world_state_json` 单向修复，不是玩家 API、prompt 或服务器裁决的 raw truth source。
- Roadmap status: S49-S87 已完成并归档或压缩记录。S88 全面系统打磨已从活动台账长表迁出，阶段性归档见 `docs/QIANQIU_POLISHING_ARCHIVE.md`，原规划见 `docs/QIANQIU_POLISHING_ROADMAP.md`；S89.1 已完成 React 玩家可见文案与移动端覆盖层润色，S89.2 已完成 React 视觉矩阵、轻量专题页壳与高清立绘查看器公开说明，S89.3 已完成 React 设置入口、专题文案与错误空态收束，S89.4 已完成 React 首页旧案状态与史册信息密度 polish，S89.5 已完成 React 全局材质、覆盖层过渡与交互反馈 polish，S89.6 已完成高清立绘查看器人物小传与当前情况 polish，S89.7 已完成舆图交互与筛选提示 polish，S89.8 已完成高清立绘查看器画中所见 polish，S89.9 已完成人物页与立绘查看器材质题签 polish，S89.10 已完成史册信息密度与移动端长文本二轮 polish，S89.11 已完成舆图全关图层空态与筛选交互 polish，S89.12 已完成舆图筛选专题层体验 polish，S89.13 已完成右上角印匣与设置目录信息架构 polish，S89.14 已完成玩家身份标签中文化与 CSS 预算缓冲 polish，S89.15 已完成来函与账解证据读法 polish，S89.16 已完成全局壳与基础控件交互反馈 polish，S89.17 已完成朝议专题目录与官署案头索引 polish，S89.18 已完成科举与皇榜仪式读法 polish，S89.19 已完成设置与断卷状态读法 polish，S89.20 已完成前端 CSS 预算瘦身与材质变量清理，S89.21 已完成舆图局势读法与行动线索 polish，S89.22 已完成主卷本旬行止笺与草稿状态读法 polish，S89.23 已完成囊箧流转候批笺与移置候批读法 polish，S89.24 已完成前端 CSS 重复规则折叠与预算缓冲 polish，S89.25 已完成覆盖层液态玻璃与共享纵深 polish，S89.26 已完成人物交游候复笺与案头索引 polish，S89.27 已完成前端 CSS 构建预算校准，S89.28 已完成前端 JS 分包预算稳定，S89.29 已完成史册证据读法与旁注空态 polish，S89.30 已完成共享案卷材质与错落动效 polish，S89.31 已完成舆图态势罗盘与移动单点札记 polish，S89.32 已完成首页与全局壳材质动效及入口收束 polish，S89.33 已完成科举与皇榜二轮仪式材质动效 polish，S89.34 已完成主卷与朝议案头中枢二轮材质动效 polish，S89.35 已完成人物页与高清立绘查看器二轮高级展示 polish，S89.36 已完成朝议 / 人物 / 史册跨页追踪读法 polish，S89.37 已完成 CSS token 与可访问性渐进重构，S89.38 已完成 CSS 架构重构第二阶段，S89.39 已完成 CSS 语义纸面动效 utility 接入，S89.40 已完成 selected/empty 语义纸面动效 utility 接入，S89.41 已完成跨身份大面板语义纸面动效 utility 接入，S89.42 已完成静态纸面 surface utility 与结构 selector 收敛，S89.43 已完成皇榜与专题层静态 surface container 语义 hook 接入，S89.44 已完成舆图与史册静态读法 surface utility 接入，S89.45 已完成囊箧与账解静态 surface utility 接入，S89.46 已完成首页静态 surface utility 接入，S89.47 已完成主卷静态 surface utility 接入，S89.48 已完成人物页静态 surface utility 接入，S89.49 已完成科举页静态 surface utility 接入，活动台账见 `docs/DEVELOPMENT_STEPS.md`。若继续 S88/S89 残余方向，应继续新开可审查小步骤，而不是把 S88 长流水复制回活动台账。
- Current collaboration: 2026-05-14 起停止 Gemini CLI 协作。后续开发、素材生成/审核、验证、文档同步和 Git 提交由 Codex 负责；用户已授权本仓库使用 Codex 子代理，实施子代理不得提交，提交前复审子代理必须只读。
- Current local `.env`: 可能含用户 provider keys。`.env` 被 Git 忽略，不能打印、复制到文档或提交。

## Core Invariants

- 后续开发和维护不以“最小实现点”或“最小改动点”为目标；在安全边界、默认可运行、内容保护和可审查粒度不受损的前提下，优先交付完整、丰富、功能强大的游戏实现，并把必要的系统、交互、AI、数据、验证和文档一次设计到位。
- 复杂功能必须坚持前后端分离和大步骤拆分：后端/API/数据契约、AI 权限与服务器裁决、前端体验、验证与文档按可审查阶段分步交付；前端不得代替服务器裁决资源、身份、交易、NPC 行动、经济结果或隐藏信息。
- 后端契约、API/view 类型、安全 projection、AI schema/provider facade、storage adapter 和核心 resolver 新增或重构时，应优先使用 TypeScript 或纳入 TypeScript 检查；既有 JavaScript 允许渐进迁移，不得为语言迁移一次性重写大量稳定模块，TS 类型不能替代 Ajv 与服务器 runtime 校验。
- 后端 route/API response shape 新增或重构时，必须对齐 `src/contracts/serverContracts.ts` 或局部 JSDoc typedef，并运行 `npm run typecheck:server`；不得为了启用类型检查一次性 whole-file `@ts-check` 大型 route 文件，也不得放宽安全 projection、raw ledger 剥离、Ajv/runtime 校验或服务器裁决。
- 完整书生路径必须继续可用：`scholar -> child_exam -> provincial_exam -> metropolitan_exam -> palace_exam -> official`。
- AI 是《千秋》的核心世界引擎，但只能读取安全 view、生成叙事/建议/proposal 或受限 structured payload；服务器拥有状态边界、时间推进、科举晋级、作弊处罚、官场任免、长期事件、世界实体、世界议程、数据库写入和持久化裁决。
- 游戏规则、数值阈值、时间间隔、概率、UI 限制、fixture 规模和 prompt budget 等可调参数不得散落为 magic numbers / 魔法数字；新增或调整时优先集中到 `src/config/GameConfig.js` 或更贴近领域的 `src/game/*Config.js`，并写清单位、范围和默认值意图。
- 项目内协作文档、路线图、交接记录、领域注释和玩家可见文案优先使用中文。

## Content Protection

- API、prompt、浏览器和 SQLite 安全索引只能消费服务器整理后的 projection。不得暴露 raw audit、provider proposal、完整 prompt、本地路径、密钥、隐藏 notes、hidden intent、未公开任所、未公开关系、raw ledger、SQLite raw/index row 或 hidden raw rows。
- 浏览器只写本地草稿、route/surface 状态或安全设置；不得直接裁决资源、身份、交易、NPC 行动、经济结果、考试晋级、官职任免、地图行动、关系终局、婚姻、弹劾、定罪、背叛或 hidden 信息。
- `topic_draft`、`draftContext`、舆图行动、实体压力、领域后果、经济解释、NPC follow-up 和关系 evidence 都必须由服务器从当前安全 view 重新校验；伪造 refs、raw/provider/prompt/path/key/hidden/SQLite 污染和 visual-only refs 不得进入裁决。
- S73-S79 人物与素材显示必须通过已审核 `portraitRef`、缩略图、低清占位、精简 runtime manifest 和 fallback 使用。不得显示未审核素材、完整 source manifest、本地 artifacts 路径或 provider/key/raw/prompt 字段；AI 生成或第三方视觉资产入 manifest 前必须做视觉审核。

## Implemented Surface

- 书生主线：读书画像、三旬读书计划、备考压力、科场流程/反馈、多级考试、放榜、同年座师网络、授官轨迹、入仕首月差事、回署回执、奏折/朝议裁决、跟进、跨身份回应、续办链路和长期官场后果均由服务器安全 view 暴露。
- 六身份循环：`roleCycleView` 统一皇帝、大臣、将领、地方官、书生、入仕官员当前身份循环，只展开当前身份；非当前身份只显示待任占位。`roleCycleDomainAdjudication` 只接低风险市价、侦察和调粮接缝，并阻断高风险军务词、非当前领域身份和近 3 旬重复提交。
- 领域后果：`domainConsequenceView` 从已裁决 city/military/judicial/NPC economy 安全来源派生公开后果，按当前角色裁剪，并通过 `publicEchoRef` / canonical echo 与月报、world thread、topic surface、topic draft 和下一轮普通回合复核串联。
- NPC/关系：主动来函、后续簿、follow-up resolution、关系行动 trace、人物页交游议题、实体压力、`worldEntityView.recentImpacts` 和史册实体归档均只读消费安全 projection，不创建真实资源/关系/婚姻/弹劾/定罪/背叛结果。
- 资产经济：`economyTraceView` 解释资源、资产、囊箧、交易、委派、市价和 NPC 月账变化；交易、委派、经济 evidence 与普通/SSE/true streaming 回合均走服务器重校验。
- React 状态：route-local session guard、专题层案卷绑定、人物/囊箧/科举/皇榜安全空态、overlay 焦点、移动端/长文本/低动效 polish、route recovery、草稿/session 绑定和 NPC/囊箧 mutation 防串扰已经完成首轮。
- 舆图与素材：PixiJS 地图行动牌和 `map-runtime` draftContext 会由服务器重建 `mapRuntimeView` 复核；NPC 舆图锚点是 visual-only。runtime manifest、assetRegistry 和 browser smoke 守住审核状态、成年立绘、lazy-load budget、重要 NPC 池隔离和完整 source manifest 禁用。
- Provider 验收：no-key runtime fallback、provider long-run 经济 trace 验收和真实 provider 缺 key skip/显式失败边界已经建立首轮。

## Archives And Contracts

- 当前活动台账：[DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)。
- S88 阶段性归档：[QIANQIU_POLISHING_ARCHIVE.md](QIANQIU_POLISHING_ARCHIVE.md)，原规划：[QIANQIU_POLISHING_ROADMAP.md](QIANQIU_POLISHING_ROADMAP.md)。
- S86/S87 类型归档：[TYPESCRIPT_BACKEND_MIGRATION_ARCHIVE.md](TYPESCRIPT_BACKEND_MIGRATION_ARCHIVE.md)、[TYPESCRIPT_ROUTE_RESPONSE_COVERAGE_ARCHIVE.md](TYPESCRIPT_ROUTE_RESPONSE_COVERAGE_ARCHIVE.md)。
- S81-S85 NPC/资产归档：[NPC_INVENTORY_SYSTEM_ARCHIVE.md](NPC_INVENTORY_SYSTEM_ARCHIVE.md)。
- S73-S77 前端归档：[FRONTEND_INK_REDESIGN_ARCHIVE.md](FRONTEND_INK_REDESIGN_ARCHIVE.md)。
- 数据库与世界内容归档：[LOCAL_DATABASE_AND_WORLD_CONTENT_ARCHIVE.md](LOCAL_DATABASE_AND_WORLD_CONTENT_ARCHIVE.md)、[DATABASE_GAMEPLAY_RESOLVER_ARCHIVE.md](DATABASE_GAMEPLAY_RESOLVER_ARCHIVE.md)。
- AI 权限矩阵：[AI_CONTROL_AUDIT_MATRIX.md](AI_CONTROL_AUDIT_MATRIX.md)。

## Current Work Note

2026-05-26：S89.49 已完成科举页静态 surface utility 接入。科举页 `.examQuestionPanel`、当前/预览 `.examDesk`、两处 `.examRecordPanel`、`.examPeerPanel`、三处 `.examPreviewPanel` 和 `.examRecentSubmitPanel` 在保留既有业务 class、`data-polish-exam-paper="s89-33-exam-ceremony-material"`、`data-polish-exam-ledger="s89-18-exam-ritual"`、取题/推进考场/交卷 form handlers、考试 API 调用、反馈拟行动本地草稿、考试纸卷专属 `::before`、展开动画、低动效 selector、高对比 `.paperMotionSurface` 覆盖和移动端布局的同时新增 `.paperMotionSurface`；`polish-surfaces.css` 第一条共享纸面 selector 已收敛为 `.paperMotionCard`、`.paperMotionPanel`、`.paperMotionSurface`，不再依赖科举 route-specific class。`test/reactClientScaffold.test.js` 新增 S89.49 source canary，守住六类科举静态 surface hook、shared selector 纯语义化、未误接 `.paperMotionCard` / `.paperMotionPanel`、三类 exam handler/API 调用和专属 reduced-motion selector；`scripts/clientSmoke.js` 在取题前后检查所有已渲染科举静态面板均带 `.paperMotionSurface`，并在取题后确认当前试卷仍是 `[aria-label='当前试卷'].examDesk.paperMotionSurface[data-polish-exam-paper='s89-33-exam-ceremony-material']`。本步不新增依赖或素材，不改 React 数据流、后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest；浏览器仍只消费安全 view、route/surface 状态和本地草稿，不请求诊断接口，不读取 raw 存档、本地路径、source manifest 或完整 manifest，不裁决取题、推进考场、交卷、评分、舞弊、放榜、晋级、授官、资源、身份、交易、委派、NPC 行动、经济、官职、地图行动、关系、婚姻、弹劾、定罪、背叛或 hidden 信息。当前已通过：`node --check scripts/clientSmoke.js`、`node --test test/reactClientScaffold.test.js`（82 tests）、`npm run typecheck:client`、完整串行客户端套件 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`（6 files / 134 tests）、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、直接 browser smoke `node scripts/clientSmoke.js`（首次 360s 外层限时截断，600s 重跑通过）、截图矩阵 `npm run smoke:browser:visual`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 touched-files `git diff --check`。预算输出为 `JS 662.8 KiB / CSS 140.8 KiB / fonts 26288.4 KiB / client-assets 27092.0 KiB`，截图输出到 `artifacts/browser-visual-matrix`；构建仍输出既有 `/assets/ui/...` runtime asset resolution warning、npm `globalignorefile` warning 和 plugin timing warning。提交前只读复审 Hume 未发现阻断问题，确认考试 API/handler、route guard、反馈草稿、考试纸卷专属动画/伪元素/reduced-motion、移动端布局、后端 API/schema、AI 权限、存档、runtime manifest 和服务器裁决边界未变；非阻断建议为后续可显式给 `.examSafetyBoundary` 不接 `.paperMotionSurface` 增加 source canary。提交哈希待本轮完成后回填。

2026-05-26：S89.48 已完成人物页静态 surface utility 接入。人物页 `.npcGroupList`、`.npcDetailWorkbench` 和 `.portraitLedger` 在保留既有业务 class、`data-polish-people-*`、人物分组选择、`paperMotionSelected`、`data-gallery-selected`、画像懒加载、人物画屏、交游候复笺、跨页追索、对话/交易/委派/交游 handlers、本地草稿写入和移动端布局的同时新增 `.paperMotionSurface`；`polish-surfaces.css` 第一条共享纸面 selector 已从 `.portraitLedger`、`.npcGroupList`、`.npcDetailWorkbench` 收敛到 `.paperMotionSurface`。本轮按开工只读审计建议不纳入科举页 `.examQuestionPanel` / `.examDesk` / `.examRecordPanel` / `.examPeerPanel` / `.examPreviewPanel` / `.examRecentSubmitPanel`，把考试纸卷专属展开动画、reduced-motion 守门和取题/推进考场/交卷链路留给后续 S89.49 单独审查。`test/reactClientScaffold.test.js` 新增 S89.48 source canary，守住三处人物静态 surface hook、shared selector 不再依赖这些业务 class、未误接 `.paperMotionCard` / `.paperMotionPanel`，并确认 NPC 选中态与四类 mutation handler 仍在；`scripts/clientSmoke.js` 在人物页实际 DOM 检查三处 `.paperMotionSurface` 且无缺口。本步不新增依赖或素材，不改 React 数据流、后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest；浏览器仍只消费安全 view、route/surface 状态和本地草稿，不请求诊断接口，不读取 raw 存档、本地路径、source manifest 或完整 manifest，不裁决资源、身份、交易、委派、NPC 行动、经济、考试、官职、地图行动、关系、婚姻、弹劾、定罪、背叛或 hidden 信息。当前已通过：`node --check scripts/clientSmoke.js`、`node --test test/reactClientScaffold.test.js`（81 tests）、`npm run typecheck:client`、完整串行客户端套件 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`（6 files / 134 tests）、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、直接 browser smoke `node scripts/clientSmoke.js`、截图矩阵 `npm run smoke:browser:visual`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 touched-files `git diff --check`。预算输出为 `JS 662.6 KiB / CSS 140.9 KiB / fonts 26288.4 KiB / client-assets 27091.9 KiB`，截图输出到 `artifacts/browser-visual-matrix`；构建仍输出既有 `/assets/ui/...` runtime asset resolution warning、npm `globalignorefile` warning 和 plugin timing warning。提交前只读复审 Hooke 未发现阻断问题，确认范围只迁移人物页三处、科举页候选保留到 S89.49，且新增 `.paperMotionSurface` 继续承接原 shared selector 阴影，视觉风险很低；非阻断建议为后续若人物页新增同类容器需同步调整 `peopleStaticSurfaceCount === 3`。实现提交 `7f5e322f`。本次 hash 回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档维护，跳过子代理复审。

2026-05-26：S89.47 已完成主卷静态 surface utility 接入。主卷 `.narrativeScroll`、`.gameSideLedger` 和条件渲染的 `.openingClaimPanel` 在保留既有业务 class、`data-polish-game="s89-22-main-ledger-reader"`、`data-draft-state`、开局背景裁决读法、案头索引、草稿状态文案、主卷叙事、移动端布局和低动效降级的同时新增 `.paperMotionSurface`；`polish-surfaces.css` 第一条共享纸面 selector 已从 `.narrativeScroll`、`.gameSideLedger`、`.openingClaimPanel` 收敛到 `.paperMotionSurface`。本轮不纳入人物工作台或科举面板，避免同轮碰 NPC 对话/交易/委派/交游 mutation、取题/推进考场/交卷路径和考试纸卷专属动效。`test/reactClientScaffold.test.js` 新增 S89.47 source canary，守住主卷三块静态 surface hook、共享 selector 不再依赖这些业务 class、未误接 `.paperMotionCard` / `.paperMotionPanel`，并确认 `.gameSideLedger[data-draft-state="written"]` 的高对比/低动效守门仍存在；`scripts/clientSmoke.js` 在默认 Mock 开局主卷检查三处 `.paperMotionSurface`，并在书生面板写入本地草稿后确认 `.gameSideLedger.paperMotionSurface[data-draft-state='written']` 仍存在。本步不新增依赖或素材，不改 React 数据流、后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest；浏览器仍只消费安全 view、route/surface 状态和本地草稿，不请求诊断接口，不读取 raw 存档、本地路径、source manifest 或完整 manifest，不裁决资源、身份、交易、委派、NPC 行动、经济、考试、官职、地图行动、关系、婚姻、弹劾、定罪、背叛或 hidden 信息。当前已通过：`node --check scripts/clientSmoke.js`、`node --test test/reactClientScaffold.test.js`（80 tests）、`npm run typecheck:client`、完整串行客户端套件 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`（6 files / 134 tests）、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、直接 browser smoke `node scripts/clientSmoke.js`、截图矩阵 `npm run smoke:browser:visual`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`。预算输出为 `JS 662.5 KiB / CSS 141.0 KiB / fonts 26288.4 KiB / client-assets 27091.9 KiB`，截图输出到 `artifacts/browser-visual-matrix`；构建仍输出既有 `/assets/ui/...` runtime asset resolution warning、npm `globalignorefile` warning 和 plugin timing warning，`git diff --check` 仅输出未触碰归档/素材文件的既有 CRLF 替换提示。开工只读探索子代理 Nietzsche 确认 S89.47 适合迁移主卷三处，风险低于人物/NPC 工作台和科举面板，并提醒保留 `.gameSideLedger[data-draft-state="written"]` 与草稿后 smoke；本轮已按建议补运行时断言。提交前只读复审 Nash 未发现阻断问题，确认 `GamePage.tsx` 只追加三处 `.paperMotionSurface`、未改回合提交/草稿来源/`draftContext`/开局裁决数据或安全 view 消费路径，且 `.paperMotionSurface` 没有进入 hover、transform 或入场动画规则；非阻断建议为后续可把 `mainStaticSurfaceCount` 拆成分项计数以提升诊断速度。实现提交 `da3ff316`。本次 hash 回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档维护，跳过子代理复审。

2026-05-26：S89.46 已完成首页静态 surface utility 接入。首页开卷案桌 `.homeDesk`、当前本局 `.continueShelf` 和旧案架 `.saveShelf` 在保留既有业务 class、`data-polish-home-*`、开局表单、旧案读取/刷新、继续本局链接、状态文案、移动端布局和低动效降级的同时新增 `.paperMotionSurface`；`polish-surfaces.css` 第一条共享纸面 selector 已从 `.homeDesk`、`.saveShelf`、`.continueShelf` 收敛到 `.paperMotionSurface`。本轮不纳入主卷 `.narrativeScroll` / `.gameSideLedger` / `.openingClaimPanel`、人物工作台或科举面板，避免混入草稿状态、NPC 行动、交易/委派或考试交卷路径。`test/reactClientScaffold.test.js` 新增 S89.46 source canary，守住首页三块静态 surface hook、共享 selector 不再依赖这些业务 class、未误接 `.paperMotionCard` / `.paperMotionPanel` 且首页未引入回合提交或危险 HTML；`scripts/clientSmoke.js` 在默认首页、移动首页和返回首页继续本局路径检查 `.homeDesk.paperMotionSurface`、`.saveShelf.paperMotionSurface` 与 `.continueShelf.paperMotionSurface`。本步不新增依赖或素材，不改 React 数据流、后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest；浏览器仍只消费安全 view、本地显示/案卷状态和旧案列表，不请求诊断接口，不读取 raw 存档、本地路径、source manifest 或完整 manifest，不裁决资源、身份、交易、委派、NPC 行动、经济、考试、官职、地图行动、关系、婚姻、弹劾、定罪、背叛或 hidden 信息。当前已通过：`node --check scripts/clientSmoke.js`、`node --test test/reactClientScaffold.test.js`（79 tests）、`npm run typecheck:client`、完整串行客户端套件 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`（6 files / 134 tests）、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、直接 browser smoke `node scripts/clientSmoke.js`、截图矩阵 `npm run smoke:browser:visual`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`。预算输出为 `JS 662.5 KiB / CSS 141.0 KiB / fonts 26288.4 KiB / client-assets 27091.9 KiB`，截图输出到 `artifacts/browser-visual-matrix`；构建仍输出既有 `/assets/ui/...` runtime asset resolution warning、npm `globalignorefile` warning 和 plugin timing warning，`git diff --check` 仅输出未触碰归档/素材文件的既有 CRLF 替换提示。开工只读探索子代理 Nietzsche 建议 S89.46 选择首页三块静态 surface，并确认它们风险低于主卷、人物/NPC 工作台和科举页；本轮已按建议实施。提交前只读复审 Nash 未发现阻断问题，确认迁移保持原 shared shadow 归属、`.paperMotionSurface` 未进入 hover 抬升或入场动画规则、`HomePage.tsx` 只变更三处 className，开局表单、旧案刷新、继续本局链接和本地状态逻辑未改；非阻断建议为后续可把首页 browser smoke 的合计 surface count 拆成分项计数以提升失败诊断精度。实现提交 `26c604b8`。本次 hash 回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档维护，跳过子代理复审。

2026-05-26：S89.45 已完成囊箧与账解静态 surface utility 接入。囊箧页 `.inventoryContainerList`、`.inventoryItemList`、三处 `.inventoryLedgerBlock` 和两处 `.inventoryTransferPanel` 在保留既有业务 class、容器/物件选择、移置表单、候批笺标记、资源/资产/凭证摘要和移动端布局的同时新增 `.paperMotionSurface`；账解读法 `.economyTraceSection` 新增 `.paperMotionSurface`，保留 `data-polish-evidence`、边界标记、`::after` 朱痕、`backdrop-filter` 和草稿/证据只读逻辑。`polish-surfaces.css` 第一条共享纸面 selector 已从 `.inventoryContainerList`、`.inventoryItemList`、`.inventoryLedgerBlock`、`.inventoryTransferPanel`、`.economyTraceSection` 收敛到 `.paperMotionSurface`；人物案头、首页/主卷和考试案头等仍留待后续小步审计。`test/reactClientScaffold.test.js` 新增 S89.45 source canary，守住囊箧/账解 hook、共享 selector 不再依赖这些业务 class、未误接 `.paperMotionCard` / `.paperMotionPanel` 且账解不引入回合提交或危险 HTML；`scripts/clientSmoke.js` 在桌面/移动囊箧路线抽样 8 个静态 `.paperMotionSurface`。本步不新增依赖或素材，不改 React 数据流、后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest；浏览器仍只消费安全 view、route/surface 状态和本地表单/草稿，不请求诊断接口，不读取 raw 存档、本地路径、source manifest 或完整 manifest，不裁决资源、物品移置、交易、委派、NPC 行动、经济、身份、考试、官职、地图行动、关系、婚姻、弹劾、定罪、背叛或 hidden 信息。当前已通过：`node --check scripts/clientSmoke.js`、`node --test test/reactClientScaffold.test.js`（78 tests）、`npm run typecheck:client`、完整串行客户端套件 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`（6 files / 134 tests）、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、直接 browser smoke `node scripts/clientSmoke.js`、截图矩阵 `npm run smoke:browser:visual`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`。预算输出为 `JS 662.4 KiB / CSS 141.1 KiB / fonts 26288.4 KiB / client-assets 27091.9 KiB`，截图输出到 `artifacts/browser-visual-matrix`；构建仍输出既有 `/assets/ui/...` runtime asset resolution warning、npm `globalignorefile` warning 和 plugin timing warning，`git diff --check` 仅输出未触碰归档/素材文件的既有 CRLF 替换提示。开工只读探索子代理 McClintock 确认该切片低风险，建议囊箧 7 个容器与账解 1 个容器可同轮迁移，并提醒不要误用 card/panel、不要触碰移动端布局、表单行为、数据流、API/schema、AI 权限或服务器裁决；本轮已按建议实施。提交前只读复审 Leibniz 未发现阻断问题，确认 8 个 surface 计数合理，`.paperMotionSurface` 只承接共享阴影/基础 overflow/高对比边框，不进入 hover 或入场动画规则，移置表单 handler、账解 draft 回调和 S89.15/S89.23 证据标记未改变；非阻断建议为后续可把 browser smoke surface count 拆成分项数量以提升诊断精度。实现提交 `7fef02de`。本次 hash 回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档维护，跳过子代理复审。

2026-05-26：S89.44 已完成舆图与史册静态读法 surface utility 接入。舆图页 `.mapSituationLedger`、`.mapVisibleLayerDigest` 和 `.mapSituationIndex` 在保留既有业务 class、`data-polish-card`、`data-polish-map-empty`、`data-polish-map-situation`、`data-polish-map-reading`、按钮草稿状态和移动端布局的同时新增 `.paperMotionSurface`；史册页 `.archiveDigestBand` 与 `.archiveDigestIntro` 新增 `.paperMotionSurface`，近次线索、统计、空态、史册证据读法和跨页追索逻辑不变。`polish-surfaces.css` 第一条共享纸面 selector 已从 `.mapSituationLedger`、`.mapVisibleLayerDigest`、`.mapSituationIndex`、`.archiveDigestBand`、`.archiveDigestIntro` 收敛到 `.paperMotionSurface`；高对比 selector 同步移除 `.archiveDigestBand`，改由 `.paperMotionSurface` 统一承接边框增强。人物/囊箧工作台、首页/主卷、考试案头和账解区域仍有更复杂布局、状态、伪元素或交互规则，本轮不纳入，后续再小步评估。`test/reactClientScaffold.test.js` 新增 S89.44 source canary，并把 S89.43/S89.44 未误接 card/panel 的负向断言改为顺序无关 lookahead；`scripts/clientSmoke.js` 在桌面/移动舆图和史册索引中抽样 `.paperMotionSurface` 数量。本步不新增依赖或素材，不改 React 数据流、后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest；浏览器仍只消费安全 view、route/surface 状态和本地草稿，不请求诊断接口，不读取 raw 存档、本地路径、source manifest 或完整 manifest，不裁决资源、身份、交易、委派、NPC 行动、经济、考试、官职、地图行动、关系、婚姻、弹劾、定罪、背叛或 hidden 信息。当前已通过：`node --check scripts/clientSmoke.js`、`node --test test/reactClientScaffold.test.js`（77 tests）、`npm run typecheck:client`、完整串行客户端套件 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`（6 files / 134 tests）、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、直接 browser smoke `node scripts/clientSmoke.js`、截图矩阵 `npm run smoke:browser:visual`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`。预算输出为单独 `JS 662.2 KiB / CSS 141.3 KiB / fonts 26288.4 KiB / client-assets 27091.9 KiB`，截图矩阵内置预算为 `JS 662.3 KiB / CSS 141.2 KiB / fonts 26288.4 KiB / client-assets 27091.9 KiB`，截图输出到 `artifacts/browser-visual-matrix`；构建仍输出既有 `/assets/ui/...` runtime asset resolution warning、npm `globalignorefile` warning 和 plugin timing warning，`git diff --check` 输出仅有未触碰归档/素材文件的既有 CRLF 替换提示。开工只读探索子代理 Pauli 确认该切片低风险，建议同步移除高对比 `.archiveDigestBand` 并保留移动端布局规则；本轮已按建议实施。提交前只读复审 Russell 首轮未发现阻断问题，并指出负向 canary 仍可能受 class 顺序影响；本轮已改为顺序无关 lookahead并重跑快速验证。最终只读复审 Pasteur 未发现阻断或新的非阻断风险，认可提交。实现提交 `c3fa671f`。本次 hash 回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档维护，跳过子代理复审。

2026-05-26：S89.43 已完成皇榜与专题层静态 surface container 语义 hook 接入。皇榜全屏页的 `.rankingNoticeBoard`、`.rankingListPanel`、`.rankingDetailPanel`、`.rankingBoundary` 在保留既有业务 class、`data-polish-ranking-*`、focus、ARIA、专属金榜布局和榜行 `.paperMotionInteractive` / `data-selected` 门控的同时新增 `.paperMotionSurface`；专题层与舆图筛选的 `.topicSurfaceColumn`（含 `.topicDraftColumn`）新增 `.paperMotionSurface`，栏目内部材料卡、草稿槽、证据勾选、按钮和本地草稿逻辑不变。`polish-surfaces.css` 第一条共享纸面 selector 已从 `.rankingNoticeBoard`、`.rankingListPanel`、`.rankingDetailPanel`、`.rankingBoundary` 和 `.topicSurfaceColumn` 收敛到既有 `.paperMotionSurface` hook，继续保留主桌、考试案头、舆图局势、人物案头、囊箧/账解等仍有专属动效或更复杂 route CSS 的业务 selector，后续再分小步评估。`test/reactClientScaffold.test.js` 新增 S89.43 source canary，守住皇榜四个静态容器与专题层六个栏目接入 hook、共享 selector 不再依赖这些 route-specific class、未误接 `.paperMotionCard` / `.paperMotionPanel`；`scripts/clientSmoke.js` 在皇榜全屏、专题层、桌面/移动舆图筛选中抽样 `.paperMotionSurface` 数量。本步不新增依赖或素材，不改 React 数据流、后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest；浏览器仍只消费安全 view、route/surface 状态和本地草稿，不请求诊断接口，不读取 raw 存档、本地路径、source manifest 或完整 manifest，不裁决资源、身份、交易、委派、NPC 行动、经济、考试、官职、地图行动、关系、婚姻、弹劾、定罪、背叛或 hidden 信息。当前已通过：`node --check scripts/clientSmoke.js`、`node --test test/reactClientScaffold.test.js`（76 tests）、`npm run typecheck:client`、完整串行客户端套件 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`（6 files / 134 tests）、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、直接 browser smoke `node scripts/clientSmoke.js`、截图矩阵 `npm run smoke:browser:visual`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`。预算输出为 `JS 662.2 KiB / CSS 141.3 KiB / fonts 26288.4 KiB / client-assets 27091.9 KiB`，截图输出到 `artifacts/browser-visual-matrix`；构建仍输出既有 `/assets/ui/...` runtime asset resolution warning、npm `globalignorefile` warning 和 plugin timing warning。开工探索子代理 Maxwell 在完成前因远端 stream disconnected 失败，未产生可采纳报告；提交前只读复审 Curie 未发现阻断问题，认可提交，非阻断建议为后续可把 S89.43 source canary 中未误接 `.paperMotionCard` / `.paperMotionPanel` 的负向正则改成更顺序无关的检查。实现提交 `29ae3653`。本次 hash 回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档维护，跳过子代理复审。

2026-05-26：S89.42 已完成静态纸面 surface utility 与结构 selector 收敛。新增 `.paperMotionSurface` 作为只承接共享纸面阴影、不承担入场动画或交互抬升的静态 surface hook；主卷行止笺、囊箧候批笺、设置目录状态、朝议案头索引、史册证据读法、科举仪程、皇榜仪程、人物案头索引、专题层案卷口径和 AI 推演设置状态簿的 `.surfaceSafetyList` 行统一新增 `.surfaceSafetyRow paperMotionSurface`，AI 推演分工卡新增 `.aiTaskRoute paperMotionSurface`。`polish-surfaces.css` 已从 `.aiTaskRoute`、`.surfaceSafetyList div` 和 `.rankingList button` 收敛到语义 hook；皇榜榜行保留既有 `.paperMotionInteractive`、`aria-pressed` / `data-selected` 和专属金榜选中 CSS，不误接 `.paperMotionSelected`。`test/reactClientScaffold.test.js` 新增 S89.42 source canary，守住静态 surface hook、无旧结构 selector、皇榜榜行不误标 selected；`scripts/clientSmoke.js` 增加 `.paperMotionSurface`、`.surfaceSafetyRow.paperMotionSurface`、旧安全行缺口、`.aiTaskRoute.paperMotionSurface` 和皇榜 interactive/selected-hook gating 运行时抽样，并把 static surface 与 animated card/panel 的动画断言拆开。本步不新增依赖或素材，不改 React 数据流、后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest；浏览器仍只消费安全 view、route/surface 状态和本地草稿，不请求诊断接口，不读取 raw 存档、本地路径、source manifest 或完整 manifest，不裁决资源、身份、交易、委派、NPC 行动、经济、考试、官职、地图行动、关系、婚姻、弹劾、定罪、背叛或 hidden 信息。当前已通过：`node --check scripts/clientSmoke.js`、`node --test test/reactClientScaffold.test.js`（75 tests）、`npm run typecheck:client`、完整串行客户端套件 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`（6 files / 134 tests）、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、直接 browser smoke `node scripts/clientSmoke.js`、截图矩阵 `npm run smoke:browser:visual`。预算输出为 `JS 662.0 KiB / CSS 141.4 KiB / fonts 26288.4 KiB / client-assets 27091.8 KiB`，截图输出到 `artifacts/browser-visual-matrix`；直接 browser smoke 首次与截图矩阵并行时在 360s 外层超时，截图矩阵内置 browser smoke 通过，随后单独重跑 `node scripts/clientSmoke.js` 通过。构建仍输出既有 `/assets/ui/...` runtime asset resolution warning、npm `globalignorefile` warning 和 plugin timing warning。开工只读探索子代理 Huygens 确认 `.aiTaskRoute`、`.surfaceSafetyList` 行和 `.rankingList button` 是本轮低风险收敛点，并建议皇榜榜行不要接 `.paperMotionSelected`；本轮已按该风险建议保留皇榜专属选中态。提交前只读复审 Gibbs 未发现阻断或非阻断风险，认可提交。实现提交 `babb4ab9`。本次 hash 回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档维护，跳过子代理复审。

2026-05-26：S89.41 已完成跨身份大面板语义纸面动效 utility 接入。新增并接入 `.paperMotionPanel`，让所有 `.scholarPanelCard` 大面板在保留原业务 class、`aria-labelledby`、`data-polish-*`、`aria-label` 与 route 专用布局的同时复用共享材质/动效 hook；五身份面板和 `RoleCycleSection` 外层 article 额外接入 `.rolePanel`，但 `DomainConsequenceSection`、`NpcFollowUpEvidenceSection`、`CrossPageTraceRail` 和史册证据读法只接 `.paperMotionPanel`，不误标为身份面板。`polish-surfaces.css`、高对比和低动效共享 selector 已从 `.scholarPanelCard` 与已被 `.paperMotionCard` 覆盖的 `.npcRelationshipAgendaCard` 收敛到 `.paperMotionPanel`；`routes/game.css` 的 `.scholarPanelCard`、`.scholarPanelCardHeader`、面板内标题/按钮等业务布局规则保留，不删除仍有源码命中的 selector。`test/reactClientScaffold.test.js` 新增 S89.41 source canary，守住所有 `.scholarPanelCard` 生成点带 `.paperMotionPanel`、身份面板才带 `.rolePanel`、`RoleCycleSection` header 仍为 `scholarPanelCardHeader`、共享 selector 不再依赖 `.scholarPanelCard` / `.npcRelationshipAgendaCard`；`scripts/clientSmoke.js` 在书生面板后抽样守住 `.scholarPanelCard.paperMotionPanel` 与 `.scholarPanelCard.rolePanel`。本步不新增依赖或素材，不改 React 数据流、后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest；浏览器仍只消费安全 view、route/surface 状态和本地草稿，不请求诊断接口，不读取 raw 存档、本地路径、source manifest 或完整 manifest，不裁决资源、身份、交易、委派、NPC 行动、经济、考试、官职、地图行动、关系、婚姻、弹劾、定罪、背叛或 hidden 信息。当前已通过：`node --check scripts/clientSmoke.js`、`node --test test/reactClientScaffold.test.js`（74 tests）、`npm run typecheck:client`、完整串行客户端套件 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`（6 files / 134 tests）、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、直接 browser smoke `node scripts/clientSmoke.js`、截图矩阵 `npm run smoke:browser:visual`。预算输出为 `JS 660.8 KiB / CSS 141.4 KiB / fonts 26288.4 KiB / client-assets 27090.6 KiB`，截图输出到 `artifacts/browser-visual-matrix`；构建仍输出既有 `/assets/ui/...` runtime asset resolution warning、npm `globalignorefile` warning 和 plugin timing warning。开工只读探索子代理 Descartes 确认 `.scholarPanelCard` 生成点，指出 `RoleCycleSection` header 不得误改为 card surface，并建议非身份证据/追索面板不要挂 `.rolePanel`；本轮已按建议修正。提交前只读复审 Planck 未发现阻断或非阻断风险，认可提交。实现提交 `93a84ec4`。本次 hash 回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档维护，跳过子代理复审。

2026-05-26：S89.40 已完成 selected/empty 语义纸面动效 utility 接入。新增并接入 `.paperMotionSelected` 与 `.paperMotionEmpty`：人物名册、囊箧容器、议题草稿槽、印匣页签、人物工作台页签和舆图图层开关在保留原业务 class、`aria-pressed`、`aria-selected`、`checked`、`data-gallery-selected` 与 `data-layer-state` 的同时复用 selected hook；首页旧案空态、跨身份面板空态、快捷建议空态、史册空态、皇榜空态、舆图 runtime/全隐空态和 AI 设置矩阵 error 状态复用 empty hook，其中 AI 设置只在 `matrixState === "error"` 时加 `.paperMotionEmpty`。`polish-surfaces.css` 的 selected/empty 共享材质 selector 已改读语义 hook；高对比 selected 边框和低动效 selected 动画关闭都继续由真实 selected/checked 状态门控，避免未选中控件被朱印材质污染；browser smoke 现在记录 selected/empty 数量，检查未选中 semantic control 不带 selected 材质，并在舆图三层全隐时用 `{ empty: true }` 强制守住 `.paperMotionEmpty`。`test/reactClientScaffold.test.js` 新增 S89.40 source canary；`scripts/clientSmoke.js` 抽样守住 `.paperMotionSelected`、`.paperMotionEmpty`、unselected selected-hook gating、map selected hook 与全隐 empty hook。未删除各 route/component 的业务 class、专用 selected/empty CSS 或仍有源码命中的旧 selector；`scholarPanelCard` 跨身份大面板、`surfaceSafetyList div`、`aiTaskRoute`、`rankingList button` 等仍留待后续小步评估。本步不新增依赖或素材，不改 React 数据流、后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest；浏览器仍只消费安全 view、route/surface 状态和本地草稿，不请求诊断接口，不读取 raw 存档、本地路径、source manifest 或完整 manifest，不裁决资源、身份、交易、委派、NPC 行动、经济、考试、官职、地图行动、关系、婚姻、弹劾、定罪、背叛或 hidden 信息。当前已通过：`node --check scripts/clientSmoke.js`、`node --test test/reactClientScaffold.test.js`（73 tests）、`npm run typecheck:client`、完整串行客户端套件 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`（6 files / 134 tests）、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、直接 browser smoke `node scripts/clientSmoke.js`、截图矩阵 `npm run smoke:browser:visual`。预算输出为 `JS 659.8 KiB / CSS 141.6 KiB / fonts 26288.4 KiB / client-assets 27089.9 KiB`，截图输出到 `artifacts/browser-visual-matrix`；直接 browser smoke 首轮在首页 `networkidle` 30s 超时，重跑通过完整路线；补全 `{ empty: true }` 后再次运行 `node --check scripts/clientSmoke.js`、`node --test test/reactClientScaffold.test.js` 与直接 browser smoke 通过。构建仍输出既有 `/assets/ui/...` runtime asset resolution warning、npm `globalignorefile` warning 和 plugin timing warning。开工只读探索子代理 Mendel 确认 selected/empty 接入点并建议保持状态门控；提交前只读复审 Hegel 未发现阻断问题，指出 empty hook browser smoke 强制覆盖偏弱，本轮已按建议补舆图全隐 empty 守门；Hegel 补充只读复审确认该 delta 无新阻断。实现提交 `76db0feb`。本次 commit hash 回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档改动，跳过子代理复审。

2026-05-26：S89.39 已完成 CSS 语义纸面动效 utility 接入。新增并接入 `.paperMotionCard`、`.paperMotionInteractive`、`.paperMotionDraft`，让旧案卡、舆图行动/近事/人物锚点、史册线索/归档项、人物卡、交游信号/议题、囊箧资产/凭证/物件、来函证据、账解卡、皇榜前三印、榜行按钮、设置目录卡和专题层条目在保留原业务 class 的同时复用稳定语义 hook；`polish-surfaces.css`、高对比和低动效共享 selector 已从一批 route-specific class 收敛到这些 hook。selected/empty、`scholarPanelCard` 跨身份面板、`surfaceSafetyList div`、`aiTaskRoute` 等仍保留显式 selector，避免一次性扩大视觉影响；`.rolePanel`、`.statusSurface`、`.paperMotionEmpty` 和仍有源码命中的旧 CSS class 未删除。`test/reactClientScaffold.test.js` 现在递归解析 CSS `@import` 图，避免后续新增二级模块时漏读样式源，并新增 S89.39 source canary；`scripts/clientSmoke.js` 抽样守住 `.paperMotionCard.paperMotionInteractive` 与舆图 written draft 的 `.paperMotionDraft`。本步不新增依赖或素材，不改 React 数据流、后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest；浏览器仍只消费安全 view、route/surface 状态和本地草稿，不请求诊断接口，不读取 raw 存档、本地路径、source manifest 或完整 manifest，不裁决资源、身份、交易、委派、NPC 行动、经济、考试、官职、地图行动、关系、婚姻、弹劾、定罪、背叛或 hidden 信息。当前已通过：`node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`（72 tests）、完整串行客户端套件 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`（6 files / 134 tests）、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、直接 browser smoke `node scripts/clientSmoke.js`、截图矩阵 `npm run smoke:browser:visual` 和 `git diff --check`。预算输出为 `JS 659.5 KiB / CSS 141.7 KiB / fonts 26288.4 KiB / client-assets 27089.6 KiB`，截图输出到 `artifacts/browser-visual-matrix`；构建仍输出既有 `/assets/ui/...` runtime asset resolution warning、npm `globalignorefile` warning 和 plugin timing warning。开工只读探索子代理 Confucius 确认当前接入范围合理，建议保留 selected/empty 和跨身份大面板显式 selector，本轮按建议补人物交游议题和皇榜榜行按钮；提交前只读复审 Laplace 未发现阻断问题。实现提交 `54ad449d`。本次 commit hash 回填为低风险纯文档维护，未另行子代理复审。

2026-05-26：S89.38 已完成 CSS 架构重构第二阶段。`client/src/styles/global.css` 现在只作为 Vite 稳定入口，按顺序导入 `tokens/`、`base/`、`utilities/`、`components/`、`routes/`、`responsive/`、`motion/` 模块；`responsive/global-responsive.css` 进一步拆为移动端 layout、home、game-map、people-inventory、exam-ranking 五个 760px 模块。新增 `--qq-color-state-*`、`--qq-color-disabled-*`、`--qq-shadow-card-*`、`--qq-surface-disabled` 等 token，高对比模式同步覆盖状态色、disabled、卡片阴影和 disabled surface；`.statusSurface`、按钮 disabled、共享卡片 shadow 等已开始复用这些 token。共享材质动效从路线图编号 CSS hook 收敛到稳定 `data-material-motion="shared-paper"`，`data-polish-atmosphere="s89-30-shared-material-motion"` 仅保留作 smoke/source marker；短编号 `s895D` / `s895S` keyframes 改为 `drawerPanelFade` / `draftWrittenPulse`。`test/reactClientScaffold.test.js` 现在读取入口与全部拆分 CSS 模块的合并源，并新增 S89.38 import 顺序 canary；`scripts/clientSmoke.js` 增加稳定材质动效 hook 守门。只读探索子代理 Kuhn 确认 `.rolePanel` / `.statusSurface` 是当前未接入但有意保留的基础 utility，其余大块 selector 均有源码命中，因此本轮未删除旧 CSS；后续建议逐页把 `polish-surfaces.css`、高对比和低动效里的巨型 `:is(...)` 继续接入语义 utility。本步不新增依赖或素材，不改 React 数据流、后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest；浏览器仍只消费安全 view、route/surface 状态和本地草稿，不请求诊断接口，不读取 raw 存档、本地路径、source manifest 或完整 manifest，不裁决资源、身份、交易、委派、NPC 行动、经济、考试、官职、地图行动、关系、婚姻、弹劾、定罪、背叛或 hidden 信息。当前已通过：`node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`（71 tests）、完整串行客户端套件 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`（6 files / 134 tests）、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、直接 browser smoke `node scripts/clientSmoke.js`、截图矩阵 `npm run smoke:browser:visual` 和 `git diff --check`。预算输出为 `JS 658.5 KiB / CSS 143.3 KiB / fonts 26288.4 KiB / client-assets 27090.3 KiB`，截图输出到 `artifacts/browser-visual-matrix`；首次 `npm run build:client` 因本地 Rolldown optional native binding 缺失失败，运行 `npm install` 修复 `node_modules` 后通过且未改变 tracked files；直接 browser smoke 初次 240s 外层限时不足、视觉矩阵初次 420s 外层限时不足，均以更长限时重跑通过。构建仍输出既有 `/assets/ui/...` runtime asset resolution warning、npm `globalignorefile` warning 和 plugin timing warning。提交前只读复审 Epicurus 未发现阻断问题；实现提交 `4c9acd3f`。本次 commit hash 回填为低风险纯文档维护，未另行子代理复审。

2026-05-26：S89.37 已完成 CSS token 与可访问性渐进重构第一阶段。`global.css` 新增 `--qq-color-*`、`--qq-surface-*`、`--qq-space-*` 与 `--qq-motion-*` token，正文链接默认恢复朱色下划线，按钮/导航型链接通过 `a[class]`、`.scholarPanelActions a` 与既有 `.paperLink` / `.paperButton` 保持 reset；新增 `.paperSurface`、`.rolePanel`、`.statusSurface`、`.ledgerCard` 基础 surface utility，并先在 `CrossPageTraceRail` 接入 `.paperSurface` / `.ledgerCard`。`data-contrast="high"` 现在同步提高纸面、边框、focus 与按钮/卡片对比；本地 `data-motion="reduced"` 和系统 `prefers-reduced-motion` 共用 `--qq-motion-instant` 等变量。CSS 物理拆文件留作后续第二阶段，本步不新增依赖或素材，不改 React 数据流、后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest；浏览器仍只消费安全 view、route/surface 状态和本地草稿，不请求诊断接口，不读取 raw 存档、本地路径、source manifest 或完整 manifest，不裁决资源、身份、交易、委派、NPC 行动、经济、考试、官职、地图行动、关系、婚姻、弹劾、定罪、背叛或 hidden 信息。当前已通过：`node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`（70 tests）、完整串行客户端套件 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`（6 files / 134 tests）、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、直接 browser smoke `node scripts/clientSmoke.js`、截图矩阵 `npm run smoke:browser:visual` 和 `git diff --check`。预算输出为 `JS 658.5 KiB / CSS 142.6 KiB / fonts 26288.4 KiB / client-assets 27089.5 KiB`，截图输出到 `artifacts/browser-visual-matrix`；`npm run smoke:browser:visual` 首次因本地 Rolldown Windows optional native binding 缺失失败，运行 `npm install` 修复 `node_modules` 后通过且未改变 tracked files；构建仍输出既有 `/assets/ui/...` runtime asset resolution warning。提交前只读复审 Bohr 首轮指出无 class 的 `.scholarPanelActions a` 会继承正文链接下划线，本轮已补局部 reset 和 source canary 并重跑脚手架、类型检查、构建、预算与 diff check；Bohr 二轮复核通过，无阻断问题，非阻断建议为后续可把该 canary 正则再收窄到 block 内匹配。实现提交 `33a3a458`；本次哈希回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)、[QIANQIU_DEVELOPMENT_BRIEF.md](QIANQIU_DEVELOPMENT_BRIEF.md)，属于低风险纯文档改动，跳过子代理复审。

2026-05-26：S89.36 已完成朝议 / 人物 / 史册跨页追踪读法 polish。新增共享 `CrossPageTraceRail`，统一 `data-polish-cross-trace="s89-36-cross-page-trace"`、`data-cross-trace-page`、`data-cross-trace-state` 和 `data-cross-trace-target`；`CourtPage` 在官署议程后新增“跨页追索笺”，用当前 route session 匹配后的 `eventArchiveView`、`domainConsequenceView.recentConsequences`、`npcActiveRequestView.followUpEvidence`、`worldThreadView.activeThreads` 和 `economyTraceView.traceItems` 安全计数提示“查人物 / 查史册 / 回主卷候复”。`PeoplePage` 在交游候复笺后新增同一 rail，用既有已清洗 `peopleRows`、来函证据、交游议题、账解数量和本地草稿有无提示“留本页 / 入朝议 / 查史册 / 回主卷候复”。`ArchivePage` 在史册证据读法后新增同一 rail，用当前史册条目、公开后果、实体余波、来函线索和可拟草稿状态提示“留本页 / 查人物 / 入朝议 / 回主卷候复”。`global.css` 新增 rail 折纸材质、四列密度、卡片错落进入、hover/focus 朱边抬升、移动端单列和 `s8936TraceSlipIn`；本地低动效偏好和系统 `prefers-reduced-motion` 均关闭 rail 卡片动画但保留纸面层次、边框、链接状态和可读布局。本步不新增依赖或素材，不改后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest；浏览器仍只消费当前 route session 的安全 view、既有页面已清洗计数和本地草稿有无，不请求诊断接口，不读取 raw 存档、本地路径、source manifest 或完整 manifest，不裁决资源、身份、交易、委派、NPC 行动、经济、考试、官职、地图行动、关系、婚姻、弹劾、定罪、背叛或 hidden 信息；unsupported route 的追索链接统一退回首页。当前已通过：`node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`（69 tests）、focused `npx vitest --config vitest.config.mjs run client/src/__tests__/App.test.tsx -t "unsupported court|archive route entries|current people ledger|registry-backed local surfaces" --pool=vmThreads --fileParallelism=false --maxWorkers=1`（4 tests / 70 skipped）、完整串行客户端套件 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`（6 files / 134 tests）、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client` 和直接 browser smoke `node scripts/clientSmoke.js`。`npm run test:client` 复现既有 Vitest fork worker 启动超时：5 files / 97 tests 已通过，但 `client/src/state/uiState.test.ts` worker 未启动；已用上述串行池完整复核同一客户端套件。`npm run smoke:browser` wrapper 完成 runtime manifest、build 和 budget 后在 browser 段首页 `networkidle` 等待超时；直接 browser smoke 通过同一浏览器段。预算输出为 `JS 658.4 KiB / CSS 139.6 KiB / fonts 26288.4 KiB / client-assets 27086.4 KiB`；构建与 smoke 仍输出既有 npm `globalignorefile` warning、Vite `/assets/ui/...` runtime asset resolution warning 和 plugin timing warning。开工只读探索子代理 Schrodinger 建议聚焦共享跨页追索 rail、保持前端-only、安全 view-only、route session guard，不增加重复主要按钮，不新增 API/schema 或 surface id；本轮按建议实施。提交前只读复审 Raman 通过，未发现阻断问题；非阻断风险为 unsupported 状态下 rail 链接仍是可点击 `Link` 但安全退回首页，以及低动效下 hover/focus 仍保留轻微反馈，均符合本步目标。实现提交 `9748b336`；本次哈希回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档改动，跳过子代理复审。

2026-05-25：S89.35 已完成人物页与高清立绘查看器二轮高级展示 polish。`PeoplePage` 保留 S89.9/S89.26 marker，并新增 `data-polish-people-gallery="s89-35-people-portrait-gallery"`、`data-polish-people-gallery-band`、“人物画屏 / 入谱照面”、`data-portrait-ledger-state`、人物名册 `data-gallery-selected`、人物谱牒卡 `data-selected` / `data-remastered` 和 S89.35 ledger/workbench/detail/card marker；画屏只读显示当前案卷公开人物、本页/入谱计数、当前照面、公开小传、题签、高清数、来函证据数、账解数和本地草稿有无，不展示草稿正文、内部来源枚举、完整素材池或全量立绘池。`Portrait` 新增只读卡片状态 `data-portrait-state="zoomable|ready|fallback"` 与 S89.35 portrait-card marker，仍保持 `loading="lazy"`、runtime asset registry 和 fallback 行为。`SurfaceHost` 高清立绘查看器新增 `data-polish-portrait-viewer="s89-35-people-portrait-gallery"`、`data-viewer-state` 和“画屏案读”只读栏，复用已清洗的 `viewerCopy` 显示人物、身份、题签和观画状态。`global.css` 新增人物画屏绢帛/朱痕材质、人物卡选中/高清框线、名册按钮朱印反馈、viewer 案读栏、`s8935GalleryUnroll` 与 `s8935SlipRise`，本地低动效偏好和系统 `prefers-reduced-motion` 均关闭强动画但保留材质、边框、选中态和可读状态。范围限 React `PeoplePage` / `Portrait` / `SurfaceHost`、全局 CSS、客户端 smoke/source canary、前端测试、brief 与交接文档；不新增依赖或素材，不改后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest。浏览器仍只消费 `worldPeopleView`、`npcRosterView`、`npcDetailView.publicProfile`、既有 follow-up/economy/relationship 安全 view、route/local 状态和已审核 runtime 画像字段，不请求诊断接口，不读取 raw 存档、本地路径、source manifest 或完整 manifest，不裁决资源、身份、交易、委派、NPC 行动、经济、考试、官职、地图行动、关系、婚姻、弹劾、定罪、背叛或 hidden 信息。当前已通过：`node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`（68 tests）、focused `npx vitest --config vitest.config.mjs run client/src/__tests__/App.test.tsx -t "current people ledger" --pool=vmThreads --fileParallelism=false --maxWorkers=1`（1 test / 73 skipped）、完整串行客户端套件 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`（6 files / 134 tests）、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、`npm run smoke:browser`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check`。`npm run test:client` 复现既有 Vitest fork worker 启动超时：5 files / 131 tests 已通过，但 `client/src/components/Portrait.test.tsx` worker 未启动；已用上述串行池完整复核同一客户端套件。最终构建预算输出为 `JS 653.5 KiB / CSS 138.1 KiB / fonts 26288.4 KiB / client-assets 27080.0 KiB`；构建与 smoke 仍输出既有 npm `globalignorefile` warning、Vite `/assets/ui/...` runtime asset resolution warning 和 plugin timing warning。开工只读探索子代理 Huygens 建议聚焦人物画屏、名册/卡片状态、viewer 案读栏、低动效和 frontend-only safety，本轮按建议实施；提交前只读复审 Darwin 通过，未发现阻断问题，非阻断风险为 `Portrait` fallback 分支仍会把传入 `portraitRef` 写入 `data-portrait-ref`，本轮调用链来自安全 ref / registry，后续若新增调用方可考虑让 `Portrait` 自身只输出已解析安全 ref 或空值；CSS-heavy polish 仍有组合视口残余风险，但移动端单列、低动效、browser smoke、source canary 和预算检查已覆盖主要回归。实现提交 `d6ad9af4`；本次哈希回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档改动，跳过子代理复审。

2026-05-25：S89.34 已完成主卷与朝议案头中枢二轮材质动效 polish。`GamePage` 新增 `data-polish-game-center="s89-34-main-court-desk"`、主卷命令栏/场景带 marker、“案头中枢 / 本卷案桌”和 `data-desk-state`，按场景、卷宗、草稿、去处整理主卷读法，并把行旅、人物、账解、科举复核四类公开材料只显示计数与候复边界；`gameSideLedger` 只新增 `data-draft-state="written|empty"`，不展示草稿正文。`CourtPage` 保留 S89.17 目录 marker，并新增 `data-polish-court-agenda="s89-34-main-court-desk"`、“官署议程 / 御案传签”、`data-agenda-state` 和每个专题入口 `data-court-state`，六个既有专题按钮仍是唯一入口，畸形案卷继续禁用。`global.css` 新增案头中枢、官署议程、侧栏草稿状态、朝议 entry hover/focus、共享旧绢变量与 `s8934DeskUnroll`、`s8934SlipRise`、`s8934InkPulse`、`s8934SceneSheen`、`s8934SealSettle`、`s8934LedgerGlow`、`s8934CourtSealGlow`；本地低动效偏好和系统 `prefers-reduced-motion` 均关闭强动画但保留材质、边框和状态。范围限 React `GamePage` / `CourtPage`、全局 CSS、客户端 smoke/source canary、前端测试、brief 与交接文档；不新增依赖或素材，不改后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest。浏览器仍只消费安全 view、route/local 状态和本地草稿状态，不请求诊断接口，不读取 raw 存档，不裁决资源、身份、交易、NPC 行动、经济、考试、官职、地图行动、关系、婚姻、弹劾、定罪、背叛或 hidden 信息。当前已通过：`node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`（68 tests）、focused `npx vitest --config vitest.config.mjs run client/src/__tests__/App.test.tsx -t "main game shell|registry-backed local surfaces|unsupported court" --pool=vmThreads --fileParallelism=false --maxWorkers=1`（3 tests / 71 skipped）、完整串行客户端套件 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`（6 files / 134 tests）、`npm run test:client`（6 files / 134 tests）、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、`npm run smoke:browser`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`。预算输出为 `JS 650.0 KiB / CSS 133.4 KiB / fonts 26288.4 KiB / client-assets 27071.8 KiB`；构建与 smoke 仍输出既有 npm `globalignorefile` warning、Vite `/assets/ui/...` runtime asset resolution warning 和 plugin timing warning。开工只读子代理 Jason 建议本步聚焦主卷案头中枢、朝议议程、低动效、smoke/source canary，并保持 frontend-only、safe-view-only、browser-draft-only；本轮按建议实施。提交前只读复审 Volta 通过，未发现阻断问题；残余风险仅为 CSS-heavy polish 仍可能有视口组合细节，但 browser smoke、overflow 与低动效检查已覆盖主要回归。实现提交 `0bae6c8f`；本次哈希回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档改动，跳过子代理复审。

2026-05-25：S89.33 已完成科举与皇榜二轮仪式材质动效 polish。`ExamPage` 新增 `data-polish-exam-ceremony="s89-33-exam-ceremony-material"`、科举 hero marker、“科场仪幕”和试卷纸卷 marker，按肃场、启封、落墨、候榜整理当前试别、题纸、草稿字数和候榜状态；`RankingPage` 新增 `data-polish-ranking-ceremony="s89-33-ranking-golden-board"`、皇榜 hero/榜纸 marker、“金榜仪轨”和榜行 `aria-pressed` / `data-selected`，按张榜、我名、同年、授官整理公开榜文读法并强化选中反馈。`global.css` 新增科举轻墨光、仪幕纸层、试卷纸纹、交卷朱印反馈、皇榜金光、金榜仪轨、榜行选中和榜名详情纸层；新增 `s8933ExamPaperUnfurl`、`s8933ExamInkSettle`、`s8933RankingGoldSheen`、`s8933RankingRowSelect`，本地低动效偏好和系统 `prefers-reduced-motion` 均关闭强动画但保留边框、阴影和选中态。本步不新增依赖或素材，不改后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest；浏览器仍只消费安全 view、既有科举 API、route/local 状态和本地草稿，不请求诊断接口，不读取 raw 存档，不裁决取题、评分、舞弊、放榜、晋级、授官、同年座师关系、官职任免、资源、交易、NPC 行动、经济、关系、婚姻、弹劾、定罪、背叛或 hidden 信息。当前已通过：`node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`（67 tests）、focused `npx vitest --config vitest.config.mjs run client/src/__tests__/App.test.tsx -t "exam page|ranking page" --pool=vmThreads --fileParallelism=false --maxWorkers=1`（2 tests / 72 skipped）、完整串行客户端套件 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`（6 files / 134 tests）、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、`npm run smoke:browser`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`。`npm run test:client` 复现既有 Vitest fork worker 启动超时：5 files / 129 tests 已通过，但 `client/src/api/qianqiuClient.test.ts` worker 未启动；已用上述串行池完整复核同一客户端套件。预算输出为 `JS 646.8 KiB / CSS 125.7 KiB / fonts 26288.4 KiB / client-assets 27060.9 KiB`；构建与 smoke 仍输出既有 npm `globalignorefile` warning、Vite `/assets/ui/...` runtime asset resolution warning 和 plugin timing warning。开工只读子代理 Laplace 建议聚焦 S89.33 marker、科场仪幕、金榜仪轨、榜行选中反馈、低动效、smoke 和 source canary，本轮按建议实施；提交前只读复审 Hegel 通过，未发现阻断问题，非阻断建议为后续收紧榜行唯一选中断言并在需要时给科举 hero 光效补显式层级。实现提交 `dd47cb17`；本次哈希回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档改动，跳过子代理复审。

2026-05-25：S89.32 已完成首页与全局壳材质动效及入口收束 polish。`HomePage` 新增 `data-polish-home="s89-32-home-entry-scroll"`、`data-polish-home-entry="s89-32-opening-desk"` 与“开卷路径”读法，把题名、立身、候复三步收进新开案卷表单；首页样卷入口降权为“试阅样卷 / 样卷舆图”，主焦点继续是朱印开卷。`AppShell` 新增 `data-polish-entry="s89-32-shell-entry-glass"`、顶栏/导航/右上角工具区 S89.32 marker；`SurfaceHost` 印匣总览、分栏和面板新增 `s89-32-inkbox-glass-ledger` marker；`SettingsPage` 保持刷新路由目录定位并新增 `data-polish-settings-entry="s89-32-settings-directory-entry"` 与 `data-settings-tab`，不把设置放回主导航。`global.css` 新增顶栏克制毛玻璃、样卷入口弱化、首页案桌卷轴展开、印匣玻璃纸层、朱印页签反馈和 `s8932ScrollUnfurl`、`s8932InkPathRise`、`s8932InkboxGlassIn`、`s8932NavInkGlow`、`s8932SealPressBloom`；本地低动效偏好和系统 `prefers-reduced-motion` 均关闭强动画但保留边框、阴影和选中态。本步不新增依赖或素材，不改后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest；浏览器仍只消费安全 view、route/surface 状态、本地显示偏好和旧案列表，不请求诊断接口，不读取 raw 存档，不裁决资源、身份、交易、NPC 行动、经济、考试、官职、地图行动、关系、婚姻、弹劾、定罪、背叛或 hidden 信息。当前已通过：`node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`（66 tests）、`npm run test:client`（6 files / 134 tests）、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、`npm run smoke:browser`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`。预算输出为 `JS 644.5 KiB / CSS 119.4 KiB / fonts 26288.4 KiB / client-assets 27052.4 KiB`；Vite 仍输出既有 `/assets/ui/...` runtime asset resolution warning 和 plugin timing warning，npm 仍输出既有 `globalignorefile` warning。开工只读子代理 McClintock 建议此步聚焦首页、顶栏、右上角印匣/设置目录、样卷入口降权、低动效和唯一入口 smoke；本轮按建议实施。提交前只读复审 Euclid 通过，未发现阻断问题；非阻断建议为后续可把 `SurfaceHost.tsx` 纳入 S89.32 专项污染 bundle 的精细 strip、并把本地低动效首页动画关闭加入 browser smoke。实现提交 `111566ee`；本次哈希回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档改动，跳过子代理复审。

2026-05-25：S89.31 已完成舆图态势罗盘与移动单点札记 polish。`MapPage` 右侧舆图簿新增 `data-polish-map-tide="s89-31-map-tide-compass"` 的“舆图态势罗盘”，按近事、人物、后果、可拟四类整理公开舆图读法；罗盘拟稿只写本地 `map-runtime` 草稿，仍携带当前安全 target/source refs 等待服务器从当前 `mapRuntimeView` 重建复核。`InkMapRuntimeBridge` 的 tooltip 新增 `data-polish-tooltip-reading="s89-31-mobile-map-note"`、地点/近事/人物/驿路札记、可见度条、候复边界和移动端底部札记 sheet；舆图页与 runtime tooltip 的文本/ref sanitizer 同步过滤 `draftContext`、`schema`、`manifest`、`server adjudication`、`AI read scope`、`proposal boundary`、`safe view`、`resolver` 及其连字符/冒号 ref 形态；`global.css` 新增 `.mapTideCompass*`、`.inkMapTooltipReading`、`s8931MapTideGlow`、`s8931MapNoteIn`、`s8931MapNoteSheetIn`，并在本地低动效偏好和系统 `prefers-reduced-motion` 下关闭强动画。本步不新增 API、依赖或素材，不改 `mapRuntimeView` 契约、后端 schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest；浏览器仍只消费安全 view、图层状态和本地草稿，不请求诊断接口，不读取 raw 存档，不把坐标、layout、点击位置、route path、visual-only effect、NPC anchor 或 tooltip 变成资源、身份、交易、NPC 行动、经济、关系、地图行动、官职、考试、婚姻、弹劾、定罪、背叛或 hidden 裁决依据。当前已通过：`node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`（65 tests）、focused `npx vitest --config vitest.config.mjs run client/src/__tests__/App.test.tsx -t "map renderer" --pool=vmThreads --fileParallelism=false --maxWorkers=1`、完整串行客户端套件 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`（6 files / 134 tests）、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、直接 browser smoke `node scripts/clientSmoke.js`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`。`npm run test:client` 本轮复现既有 Vitest fork worker 启动超时：4 files / 119 tests 已通过，但 `client/src/assets/assetRegistry.test.ts` 与 `client/src/api/qianqiuClient.test.ts` worker 未启动；已用上述串行池完整复核同一客户端套件。`npm run smoke:browser` 串联 wrapper 完成 manifest/build/budget 后，在 browser 段首页 `networkidle` 等待超时；已用直接 `node scripts/clientSmoke.js` 通过同一浏览器段。预算输出为 `JS 643.0 KiB / CSS 111.4 KiB / fonts 26288.4 KiB / client-assets 27042.9 KiB`；Vite 仍输出既有 `/assets/ui/...` runtime asset resolution warning 和 plugin timing warning，npm 仍输出既有 `globalignorefile` warning。开工只读子代理 Euler 建议收窄为该舆图 polish 切片、保持前端-only/draft-only/safe-view-only 且不扩大 `mapRuntimeView` 契约或服务器裁决权；本轮按建议实施。提交前只读复审 Confucius 首轮发现工程词 sanitizer/canary 缺口，本轮已补文本/ref 双层清洗、污染 ref fixture 和 source canary；Confucius 复核通过，未发现新的阻断问题，非阻断建议为后续可另补 label/summary 污染 UI fixture。实现提交为 `88a5253c`；本次哈希回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档改动，跳过子代理复审。

2026-05-25：S89.30 已完成共享案卷材质与错落动效 polish。`AppShell` 新增 `data-polish-atmosphere="s89-30-shared-material-motion"`，`global.css` 用共享 selector 增强首页案桌/旧案架、主卷案头、舆图簿、人物卡、囊箧、史册、科举、皇榜、朝议、设置目录、专题层、状态行、选中控件、草稿已写入和空态的宣纸层次、轻浮起、错落进入与朱印反馈；新增 `s8930PaperRise`、`s8930StateWash`、`s8930SealBloom`，并在本地低动效偏好和系统 `prefers-reduced-motion` 下关闭强动画。`scripts/clientBuildBudget.js` 将 CSS 总预算/单 CSS asset 上限调整为 `220000` / `200000` bytes，作为本轮产品级共享材质层后的硬门余量，不表示取消预算治理；后续 CSS 继续增长时仍应优先拆分、删除无用选择器、合并重复规则或抽出共享样式。`scripts/clientSmoke.js` 在既有 S89.5 跨页材质 smoke 中检查 S89.30 shell marker、三组 keyframes、共享 surface、状态行、朱印选中态、空态纸底、低动效关闭和舆图草稿写入状态洗；`test/reactClientScaffold.test.js` 新增 S89.30 source canary，并把旧 CSS source guard 调整到当前预算口径。本步不新增依赖或素材，不改 React 数据来源、后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest；浏览器仍只消费安全 view、route/surface 状态和本地草稿，不请求诊断接口，不读取 raw 存档，不裁决资源、身份、交易、NPC 行动、经济、考试、官职、地图行动、关系、婚姻、弹劾、定罪、背叛或 hidden 信息。当前已通过：`node --check scripts/clientSmoke.js`、`node --check scripts/clientBuildBudget.js`、`node --test test/reactClientScaffold.test.js`（64 tests）、`npm run typecheck:client`、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、完整串行客户端套件 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`（6 files / 134 tests）、`npm run smoke:browser`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check` 和完整 `npm test`（1178 tests）。`npm run test:client` 复现既有 Vitest fork worker 启动超时：5 files / 129 tests 已通过，但 `client/src/api/qianqiuClient.test.ts` worker 未启动；已用上述串行池完整复核同一客户端套件。预算输出为 `JS 636.8 KiB / CSS 106.5 KiB / fonts 26288.4 KiB / client-assets 27031.8 KiB`；Vite 仍输出既有 `/assets/ui/...` runtime asset resolution warning 和 plugin timing warning。开工只读子代理 Poincare 建议选择共享材质、动效与候复状态反馈切片，并提醒更新 CSS canary、同时覆盖本地低动效与系统 media query、不得加入 turn submission/diagnostics/raw/provider/prompt/path/key/hidden 文案；本轮按建议实施。提交前只读复审 Hilbert 已通过，未发现阻断问题；非阻断建议为后续若需支持更旧浏览器，可给 `.mapLayerToggle:has(input:checked)` 增加 class/data 状态替代路径，并在继续加 CSS 时优先抽共享 class 或拆 route/component 样式。实现提交为 `76bdb67c`；本次哈希回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档改动，跳过子代理复审。

2026-05-25：S89.29 已完成史册证据读法与旁注空态 polish。`ArchivePage` 在“案卷索引 / 近次线索”和公开追踪主列之间新增带 `data-polish-archive-reader="s89-29-evidence-reader"` 的“史册追索笺 / 史册证据读法”，用既有 `scholarPanelCard`、`surfaceSafetyList`、`statusLine` 和 `eyebrow` 零新增 CSS 展示主列、旁注、追索、拟稿四行读法；旁注计数只来自当前案卷安全 `eventArchiveView` counts、`domainConsequenceView`、`npcActiveRequestView.followUpEvidence.counts.total` 或公开 evidence 分组长度，不显示 raw evidence refs、草稿正文、内部枚举或工程词。`scripts/clientSmoke.js` 在史册 digest smoke 中检查 S89.29 reader、4 行读法、boundary marker 和“按钮只写案头草稿，仍回主卷候复”文案；`test/reactClientScaffold.test.js` 守住 reader helper/marker、零 S89.29 CSS selector 和无 `/api/game/turn`/raw/provider/path/key/hidden 污染；`App.test.tsx` 的 archive route 用例断言 12 条近次、1 条后果、2 条来函、2 条实体余波和拟稿候复边界。本步不新增依赖、CSS 或素材，不改后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest；浏览器仍只消费安全 view、route/local 状态和本地草稿，不请求诊断接口，不读取 raw 存档，不裁决资源、身份、交易、NPC 行动、经济、考试、官职、关系、婚姻、弹劾、定罪、背叛或 hidden 信息。当前已通过：`node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`（63 tests）、focused `npx vitest --config vitest.config.mjs run client/src/__tests__/App.test.tsx -t "archive route entries" --pool=vmThreads --fileParallelism=false --maxWorkers=1`（1 test / 73 skipped）、`npm run test:client`（6 files / 134 tests）、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、`npm run smoke:browser`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`。最终预算输出为 `JS 636.7 KiB / CSS 99.5 KiB / fonts 26288.4 KiB / client-assets 27024.7 KiB`；Vite 仍输出既有 `/assets/ui/...` runtime asset resolution warning 和 plugin timing warning；`git diff --check` 仍只输出仓库既有 CRLF warning。提交前只读复审 Goodall 已通过，未发现阻断问题；非阻断建议为后续若统一证据计数 helper，可让来函旁注数与实际清洗后渲染条目更严格一致，并可给旧案卷缺少 `eventArchiveView.counts.domain_consequence` 但仍有 `domainConsequenceView.recentConsequences` 的场景补读法 fallback。实现提交为 `4bf06317`；本次哈希回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档改动，跳过子代理复审。

2026-05-25：S89.28 已完成前端 JS 分包预算稳定。`vite.config.mjs` 在生产构建中用 `manualChunks()` 只拆分 `node_modules`：React/React Router 为 `vendor-react`，Zustand 为 `vendor-state`，Lucide 为 `vendor-icons`，其余 npm 依赖为 `vendor`，应用源码仍留在主 `index` chunk。`test/reactClientScaffold.test.js` 新增 S89.28 source canary，守住 vendor chunk 名、`node_modules` 匹配、`maxSingleJsBytes: 650_000` 预算口径，并禁止把分包误做成 `React.lazy` 或把预算数值塞进 Vite 配置。本步范围限 Vite 构建配置、source canary 和文档；不新增依赖、CSS 或素材，不改 React 页面运行时逻辑、玩家可见 UI、后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest。浏览器仍只消费安全 view、route/surface 状态和本地草稿，不请求诊断接口，不读取 raw 存档，不裁决资源、身份、交易、NPC 行动、经济、考试、官职、关系或 hidden 信息。当前已通过：`node --check vite.config.mjs`、`node --test test/reactClientScaffold.test.js`（63 tests）、`npm run typecheck:client`、`npm run test:client`（6 files / 134 tests；首次复现既有 Vitest fork worker 启动超时，重跑通过）、完整串行 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`（6 files / 134 tests）、`npm run qa:runtime-manifest`、`npm run build:client && npm run budget:client`、`npm run smoke:browser`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check` 和完整 `npm test`（1177 tests）。预算输出为 `JS 635.0 KiB / CSS 99.5 KiB / fonts 26288.4 KiB / client-assets 27022.9 KiB`，主应用 chunk 约 `358.1 KiB`、`vendor-react` 约 `272.2 KiB`。提交前只读复审 Chandrasekhar 已通过，未发现阻断问题；非阻断提醒为未来若新增或切回 `react-router-dom`，需同步 vendor 分组和 canary。实现提交为 `81d0ac0b`；本次哈希回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档改动，跳过子代理复审。

2026-05-25：S89.27 已完成前端 CSS 构建预算校准。`scripts/clientBuildBudget.js` 将 CSS 总预算从 `140000` bytes 调整为 `180000` bytes，将单个 CSS asset 上限从 `102400` bytes 调整为 `128000` bytes；这是针对当前构建 CSS `99.5 KiB` 只剩约 `513` bytes 余量的工程校准，不是放弃预算治理。后续若单个 CSS asset 超过约 `150 KiB`，应优先做 route/component CSS 拆分、删除无用选择器、合并重复规则或抽出共享样式，再评估是否继续提高硬门。本步不改 React 运行时行为、不新增样式、不新增依赖或素材，不改后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest。当前已通过：`node --check scripts/clientBuildBudget.js`、`npm run budget:client`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`；提交前只读复审已通过，未发现阻断问题。实现提交为 `31a1c97d`；本次哈希回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档改动，跳过子代理复审。

2026-05-25：S89.26 已完成人物交游候复笺与案头索引 polish。`PeoplePage` 顶部新增带 `data-polish-people-reader="s89-26-people-docket-reader"` 的“交游候复笺 / 人物案头索引”，把入谱人数、相识人物、当前人物页签、来函、后续簿、公开证据、关系网、交游议题、账解数量和本地候复稿有无整理为首屏读法；索引只显示草稿有无，不展示草稿正文、来处枚举、`draftContext` 或内部复核线索。本步零新增 CSS，复用 `sectionTitleRow`、`surfaceSafetyList`、`statusLine` 和既有人物页材质。`scripts/clientSmoke.js` 在桌面人物页检查 S89.26 marker、索引行数和候复边界文案；`test/reactClientScaffold.test.js` 新增 S89.26 source canary。实现提交为 `dd281b18`；本次哈希回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档改动，跳过子代理复审。

本步范围限 React `PeoplePage`、客户端 smoke/source canary、前端测试和文档；不新增依赖或素材，不改后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest。浏览器仍只消费安全 view、route/local 状态和本地草稿布尔状态，不请求诊断接口，不读取 raw 存档，不裁决资源、身份、交易、委派、NPC 行动、经济、关系、婚姻、弹劾、定罪、背叛或 hidden 信息。当前已通过：`node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`（62 tests）、focused `npx vitest --config vitest.config.mjs run client/src/__tests__/App.test.tsx -t "current people ledger|S88.8 economy trace in the people" --pool=vmThreads --fileParallelism=false --maxWorkers=1`（2 tests / 72 skipped）、`npm run test:client`（6 files / 134 tests）、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、`npm run smoke:browser`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`。最终预算输出为 `JS 634.2 KiB / CSS 99.5 KiB / fonts 26288.4 KiB / client-assets 27022.2 KiB`；首次 budget 因最大 JS chunk `635.3 KiB > 634.8 KiB` 失败，本轮已压缩 S89.26 helper 并复跑通过。`npm run smoke:browser` 通过并覆盖 desktop-people-assets、desktop-people-refresh、desktop-history-forward-people 和既有桌面/移动关键路由。开工只读子代理 Hypatia 确认本步适合做零 CSS 人物页读法切片，建议索引只消费安全 view 与本地草稿布尔状态、不要展示草稿正文或工程词；本轮按建议实施。提交前只读复审 Archimedes 已通过，无阻断问题；非阻断提示为来函/后续计数未像公开证据计数一样逐项 unsafe 过滤、账解计数字段集少于 `EconomyTraceSection`，但二者只暴露数量不暴露文本，且预算余量极窄，本轮接受该残余风险并建议后续若做预算专项时再统一计数 helper。

2026-05-25：S89.25 已完成覆盖层液态玻璃与共享纵深 polish。`SurfaceHost` 为印匣抽屉、普通弹窗、本地专题层和高清立绘查看器统一补 `data-polish-depth="s89-25-liquid-glass"`；`client/src/styles/global.css` 用共享 selector 增强覆盖层宣纸毛玻璃、半透明层、阴影纵深和内沿高光，并继续复用现有材质变量。本步同步折叠三列统计网格重复规则，保持 `global.css` 源码体积低于 129300 bytes。`scripts/clientSmoke.js` 在既有 S89.5 覆盖层 smoke 中检查抽屉、弹窗和专题层的 S89.25 纵深标记与 blur，并检查高清立绘查看器的 S89.25 纵深标记；`test/reactClientScaffold.test.js` 新增 S89.25 source canary。实现提交为 `bbc2c4e3`；本次哈希回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档改动，跳过子代理复审。

本步范围限 `SurfaceHost` 覆盖层标记、全局 CSS、browser smoke/source canary 和文档；不新增依赖或素材，不改后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest。浏览器仍只消费安全 view、route/surface 状态和本地草稿，不请求诊断接口，不读取 raw 存档，不裁决资源、身份、交易、NPC 行动、经济、关系、考试、官职或 hidden 信息。当前已通过：`node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`（61 tests）、`npm run test:client`（6 files / 134 tests）、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、`npm run smoke:browser`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`。当前预算输出为 `JS 631.1 KiB / CSS 99.5 KiB / fonts 26288.4 KiB / client-assets 27019.1 KiB`。`npm run smoke:browser` 通过并覆盖 desktop-home、desktop-mock-start、desktop-scholar-panel、desktop-inkbox-tabs、desktop-map-runtime、desktop-people-assets、desktop-inventory、desktop-archive-refresh、desktop-exam-fullscreen、desktop-ranking-fullscreen、desktop-topic-surfaces、desktop-court-refresh、desktop-settings-refresh、desktop-magistrate-panel、desktop-official-panel、desktop-minister-panel、desktop-general-panel、desktop-emperor-panel、mobile-* 和 browser-reduced-motion-home。开工只读子代理 Parfit 建议后续优先做人物页“交游候复笺 / 人物案头索引”零 CSS 切片；本轮主线先完成目标中明确要求的毛玻璃/液态玻璃覆盖层切片，人物页读法建议保留为下一候选。提交前只读复审 Pascal 已通过，无阻断问题；非阻断提示为高清立绘查看器只直接断言纵深标记、未单独断言 blur，但其复用 `.modalPanel[data-polish-depth="s89-25-liquid-glass"]`，browser smoke 已覆盖 marker 和 modal/surface blur，本轮接受该残余风险；CSS 源码余量仍紧，后续继续优先零 CSS 或先瘦身。

2026-05-25：S89.24 已完成前端 CSS 重复规则折叠与预算缓冲 polish。`client/src/styles/global.css` 合并静态确认重复的舆图右侧簿、四列统计网格和书生 compact 卡片基础声明：`.mapActionDeck` / `.mapNpcActivityDeck`、`.mapActionList` / `.mapNpcActivityList`、`.roleCycleMetrics` / `.mapHeroStats` / `.archiveStats` / `.npcFactGrid` / `.inventoryItemStats` 与 `.scholarPlanSummary div` / `.scholarPlanTimeline li` / `.scholarPanelCompactDl div` 共用同一底座。只读巡检确认 394 个 CSS class token 均有 React/runtime 源码命中，因此本步不删除 class；同时避开 `.portraitViewerCueGrid`，因为画像查看器依赖其在 `.portraitViewerTags` 之后覆盖 display。`test/reactClientScaffold.test.js` 新增 S89.24 source canary，守住合并 selector 和 `global.css` 源码体积低于 129300 bytes。实现提交为 `25a29f8c`；本次哈希回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档改动，跳过子代理复审。

本步范围限全局 CSS、source canary 和文档；不新增依赖或素材，不改 React 组件行为、后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest。浏览器仍只消费安全 view、route/surface 状态和本地草稿，不请求诊断接口，不读取 raw 存档，不裁决资源、身份、交易、NPC 行动、经济、关系、考试、官职或 hidden 信息。当前已通过：`node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`（60 tests）、完整串行 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`（6 files / 134 tests）、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、直接 `node scripts/clientSmoke.js`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`。当前预算输出为 `JS 631.0 KiB / CSS 99.4 KiB / fonts 26288.4 KiB / client-assets 27018.8 KiB`；直接 browser smoke 首次 240s 外层限时被截断，420s 重跑通过。开工只读子代理 Ramanujan 建议把本步收窄为 CSS 合并/去重复专项，不做废弃 selector 删除，并提醒不要动 `.portraitViewerCueGrid`；本轮按建议实施。提交前只读复审通过，无阻断问题；非阻断建议指出 source canary 依赖 exact formatting 且 CSS 预算仍需谨慎，本轮接受该残余风险。

2026-05-25：S89.23 已完成囊箧流转候批笺与移置候批读法 polish。`InventoryPage` 囊箧页新增带 `data-polish-inventory="s89-23-inventory-ledger-reader"` 与 `data-polish-inventory-boundary="s89-23-transfer-boundary"` 的“流转候批笺”，把资源、资产、物件、凭证数量、可流转物件数、当前移置选择和候批状态整理为玩家可见读法；未获案卷回批前，不写成已入账、已移置、已成交或已影响关系。提交前复审建议的 `item.unit` 污染边缘已修复，物品单位、资源单位和移置路径单位都走 `safeLabel()` 清洗。本步零新增 CSS，复用 `inventoryTransferPanel`、`surfaceSafetyList` 和 `statusLine`，最终预算为 `JS 631.0 KiB / CSS 99.8 KiB / fonts 26288.4 KiB / client-assets 27019.2 KiB`。实现提交为 `73903bff`；本次哈希回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档改动，跳过子代理复审。开工只读子代理 Darwin 建议选择囊箧账本/移置读法，避免 `SurfaceHost` 全局专题层风险，并强调当前 CSS 预算只剩约 182 bytes，需零 CSS；本轮按建议实现。初次 browser smoke 抓到“验收”属开发口径禁词，本轮已将玩家可见标题改为“流转候批笺”并复跑通过。

本步范围限 React `InventoryPage`、客户端 smoke/source canary、前端测试和文档；不新增依赖或素材，不改后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest。浏览器仍只消费安全囊箧、资源、资产、经济 trace 和 route-local 选择状态，不请求诊断接口，不读取 raw 存档，不裁决资源扣减、物品转移、交易、赠予、借用、NPC 行动、经济、关系、考试、官职或 hidden 信息。当前已通过：`node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`（59 tests）、focused `npx vitest --config vitest.config.mjs run client/src/__tests__/App.test.tsx -t "unsupported inventory|inventory economy trace|S89.23|inventory transfer selections" --pool=vmThreads --fileParallelism=false --maxWorkers=1`（3 tests）、补单位清洗后的 focused `npx vitest --config vitest.config.mjs run client/src/__tests__/App.test.tsx -t "inventory economy trace|S89.23" --pool=vmThreads --fileParallelism=false --maxWorkers=1`（1 test）、完整串行 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`（6 files / 134 tests）、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、直接 `node scripts/clientSmoke.js`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`。`npm run test:client` 两次复现既有 Vitest fork worker 启动超时：一次 5 files / 129 tests 已通过但 `qianqiuClient.test.ts` worker 未启动，一次 5 files / 131 tests 已通过但 `Portrait.test.tsx` worker 未启动；已用串行池完整复核同一客户端套件。提交前只读复审已通过，无阻断问题；实现提交为 `73903bff`，哈希回填提交为 `7ddd59d2`。

2026-05-25：S89.22 已完成主卷本旬行止笺与草稿状态读法 polish。`GamePage` 主卷右侧案头索引新增带 `data-polish-game="s89-22-main-ledger-reader"` 与 `data-polish-game-boundary="s89-22-main-ledger-boundary"` 的“本旬行止笺”，把已载公开卷宗数量、草稿状态、草稿来处和卷宗读法整理为玩家可见读法；草稿来处只显示“手写稿 / 案头摘录 / 舆图摘录 / 史册摘录 / 科举草稿”等中文标签，不展示内部 source enum、草稿全文或 `draftContext`。本步零新增 CSS，复用 `gameSideLedger`、`safeViewGrid`、`surfaceSafetyList` 和 `statusLine`，最终预算为 `JS 629.0 KiB / CSS 99.8 KiB / fonts 26288.4 KiB / client-assets 27017.3 KiB`。实现提交为 `1b085829`；本次哈希回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档改动，跳过子代理复审。

本步范围限 React `GamePage`、客户端 smoke/source canary、前端测试和文档；不新增依赖或素材，不改后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest。浏览器仍只消费安全 view、route 状态和本地草稿状态，不请求诊断接口，不读取 raw 存档，不裁决资源、关系、交易、NPC 行动、经济、官职、考试、地图行动、婚姻、弹劾、定罪、背叛或 hidden 信息。当前已通过：`node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`（58 tests）、focused `npx vitest --config vitest.config.mjs run client/src/__tests__/App.test.tsx -t "main game shell|scholar panel" --pool=vmThreads --fileParallelism=false --maxWorkers=1`、完整串行 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`（6 files / 134 tests）、`npm run test:client`（6 files / 134 tests）、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、直接 `node scripts/clientSmoke.js`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`。开工只读子代理建议将 S89.22 收窄为主卷 `GamePage` 的本旬行止笺，避免人物页 mutation 边界、保持零 CSS、不改后端/API/prompt/裁决；本轮按建议实现。提交前只读复审未发现阻断问题，非阻断建议已收紧 App/browser smoke 断言和 source canary。

2026-05-25：S89.21 已完成舆图局势读法与行动线索 polish。`MapPage` 右侧舆图簿新增带 `data-polish-map-situation="s89-21-situation-index"` 与 `data-polish-map-reading="s89-21-situation-reader"` 的“山河局势轴 / 本卷读法”，把公开图层显隐、最显著近事、人物视觉锚点、已裁决公开后果和可拟行动数整理为玩家可读读法；“据局势拟稿”只写本地 `map-runtime` 草稿，携带当前安全 event/npc refs 作为待服务器重建复核的 hint。样式复用 `.mapVisibleLayerDigest` 底座，只补局势轴局部排版和窄屏 header 换行兜底，最终预算为 `JS 627.8 KiB / CSS 99.8 KiB / fonts 26288.4 KiB / client-assets 27016.0 KiB`。实现提交为 `36c88f2b`；本次哈希回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档改动，跳过子代理复审。

本步范围限 React `MapPage`、少量 CSS、客户端 smoke/source canary、前端测试和文档；不新增依赖或素材，不改后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest。浏览器仍只消费安全 view、route/surface 状态和本地草稿，不裁决地图行动、资源、身份、交易、NPC 行动、经济、考试、官职、关系、婚姻、弹劾、定罪、背叛或 hidden 信息；坐标、layout、viewport、renderer 画面层级、visual-only effect 和 NPC visual-only anchor 不进入主卷裁决。当前已通过：`node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`（57 tests）、focused `npx vitest --config vitest.config.mjs run client/src/__tests__/App.test.tsx -t "map renderer" --pool=vmThreads --fileParallelism=false --maxWorkers=1`、完整串行 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`（6 files / 134 tests）、`npm run qa:runtime-manifest`、`npm run build:client && npm run budget:client`、直接 `node scripts/clientSmoke.js`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`。`npm run test:client` 复现既有 Vitest fork worker 启动超时：5 files / 129 tests 已通过，`qianqiuClient.test.ts` worker 未启动；已用串行池完整复核同一客户端套件。开工只读子代理建议把本步收窄为舆图右侧局势读法簿，避免后端/API/prompt/裁决改动和 raw/visual refs 进入 evidence；本轮按建议实现。提交前只读复审无阻断问题，非阻断建议指出后果-only 场景可能没有 draftContext evidence hint、局势轴 header 可加换行兜底，本轮已补 header wrap。

2026-05-25：S89.20 已完成前端 CSS 预算瘦身与材质变量清理。`client/src/styles/global.css` 将全局 `rgba(...)` 统一改为等价现代 `rgb(... / ...)` 写法，复用 `--qq-material-*` 材质变量补齐宣纸、折纸、破纸、朱痕、朱印、印匣纹理和墨线背景引用，删除唯一静态确认无 React 源码引用的 `.actionPanel`，并合并移动端重复的单列 grid、双列 grid 和竖排 flex 规则。`test/reactClientScaffold.test.js` 新增 S89.20 source canary，守住 CSS 源码体积、禁止 `rgba(` / `.actionPanel` 回归，并确认材质背景变量化。实现提交为 `ef41549d`；本次哈希回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档改动，跳过子代理复审。

本步范围限全局 CSS、source canary 和文档；不新增依赖或素材，不改 React 组件行为、后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest。浏览器仍只消费安全 view、route/surface 状态和本地草稿，不裁决资源、身份、交易、NPC 行动、经济、考试、官职、关系、婚姻、弹劾、定罪、背叛或 hidden 信息。当前已通过：`node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`（56 tests）、`npm run build:client && npm run budget:client`（最终 `JS 624.7 KiB / CSS 99.0 KiB / fonts 26288.4 KiB / client-assets 27012.1 KiB`）、`npm run test:client`（6 files / 134 tests）、`npm run qa:runtime-manifest`、直接 `node scripts/clientSmoke.js`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`。`npm run smoke:browser` 的组成步骤已逐项通过：runtime manifest、build、budget 和同一 `clientSmoke.js` 浏览器段。开工只读子代理 Heisenberg 确认当前 CSS class 静态扫描无明显大块废弃选择器，建议优先合并移动端重复规则、复用材质变量和守住 `.actionPanel` 回归；本轮按建议补第二轮移动端合并。提交前只读复审已通过，未发现阻断问题；非阻断建议指出 `form.actionPanel` 仍作为 smoke 负向查询存在，用于确认旧表单未残留，不影响删除 CSS 选择器。

2026-05-25：S89.19 已完成设置与断卷状态读法 polish。`SettingsPage` 带 `data-polish-settings-state="s89-19-settings-card-state"`、`s89-19-settings-directory-state` 与 `s89-19-settings-route-recovery`，在 `/game/:sessionId/settings` 四类案头工具中逐项说明“眼下可做 / 候复边界”，并在畸形案卷编号下固定提示印匣只打开本地显示偏好、全局推演设置和旧案入口，不读取主卷、不打开专题、不写行动草稿。`AiSettingsPanel` 带 `data-polish-ai-settings="s89-19-ai-state-ledger"` 与 `data-polish-ai-settings-ledger="s89-19-ai-state-ledger"`，新增“推演设置状态簿”，并清洗预设、分工 label/purpose/model、状态枚举和错误信息，污染 payload/API error 不回显 raw/provider/prompt/path/key/schema/manifest 等工程词。`ErrorPage` / `NotFoundPage` 带 `data-polish-route-state="s89-19-route-recovery"`，畸形主卷恢复页带 `data-polish-route-state="s89-19-game-route-recovery"`，只给安全归路，不显示底层诊断。本步零新增 CSS，复用 `.surfaceSafetyList`、`.statusLine`、`.settingsDirectoryCard`、`.aiSettingsMatrixStatus` 和既有案卷空态壳。实现提交为 `bf23677e`；本次哈希回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档改动，跳过子代理复审。

本步范围限 React `SettingsPage` / `AiSettingsPanel` / `ErrorPage` / `NotFoundPage` / `GamePage`、客户端 smoke/source canary、前端测试和文档；不新增依赖或素材，不改后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest。浏览器仍只打开本地印匣、既有全局 AI settings API 或本地显示偏好，不裁决资源、身份、交易、NPC 行动、经济、考试、官职、关系、婚姻、弹劾、定罪、背叛或 hidden 事实；畸形案卷不读取主卷接口、不打开专题层、不写行动草稿。当前已通过：`node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`（55 tests）、focused `npx vitest --config vitest.config.mjs run client/src/__tests__/App.test.tsx -t "settings route|global AI settings|S89.19|bad routes|game root recovery|malformed settings" --pool=vmThreads --fileParallelism=false --maxWorkers=1`（9 tests）、完整串行 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`（6 files / 134 tests）、`npm run test:client`（6 files / 134 tests）、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、直接 `node scripts/clientSmoke.js`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`。最终预算输出为 `JS 624.7 KiB / CSS 99.8 KiB / fonts 26288.4 KiB / client-assets 27013.0 KiB`。`npm run smoke:browser` 在 360s 外层限时内完成 runtime manifest、build 和 budget 后进入 browser smoke 被截断；已用直接 browser smoke 覆盖同一浏览器段。开工只读子代理建议将本步收窄为设置、推演矩阵与断卷恢复空态 polish，并指出 AI settings label/purpose/model/preset/error 污染风险；本轮已按建议实现。提交前只读复审首轮发现 active preset id 仍可能被污染 payload 原样回写，本轮已改为读取和保存都用安全 id 并对齐清洗后的预设列表，补污染 preset 保存测试和畸形 settings route 测试；复审子代理复核通过，无阻断问题。

2026-05-25：S89.18 已完成科举与皇榜仪式读法 polish。`ExamPage` 带 `data-polish-exam="s89-18-exam-ritual-ledger"` 与 `data-polish-exam-ledger="s89-18-exam-ritual"`，在 `/game/:sessionId/exam` 右栏新增“科举仪程”，按取题启封、场内推进、交卷候批和候榜回音说明当前案卷状态、草稿/交卷边界和候榜回音；`RankingPage` 带 `data-polish-ranking="s89-18-ranking-ceremony-ledger"` 与 `data-polish-ranking-ledger="s89-18-ranking-ceremony"`，在 `/game/:sessionId/ranking` 榜名详情新增“放榜仪程”，按张榜取材、我名、同年座师和授官过渡说明公开榜文读法。本步零新增 CSS，复用 `.examPreviewPanel`、`.surfaceSafetyList`、`.statusLine` 和既有科举/皇榜布局，最终预算为 `JS 621.2 KiB / CSS 99.8 KiB`。实现提交为 `060d0c8c`；本次哈希回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档改动，跳过子代理复审。

本步范围限 React `ExamPage` / `RankingPage`、客户端 smoke/source canary、前端测试和文档；不新增依赖或素材，不改后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest。浏览器仍只调用既有科举 API 或读取安全榜文 view，不裁决取题、评分、舞弊、放榜、晋级、授官、同年座师关系、官职任免或 hidden 事实；“拟行动”只写本地草稿，不调用 `/api/game/turn`。当前已通过：`node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`、focused `npx vitest --config vitest.config.mjs run client/src/__tests__/App.test.tsx -t "immersive exam page|ranking page" --pool=vmThreads --fileParallelism=false --maxWorkers=1`、完整串行 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、`npm run smoke:browser`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`。`npm run test:client` 当前无断言失败，但复现既有 Vitest fork worker 启动超时：5 files / 126 tests 已通过，`Portrait.test.tsx` worker 未启动；已用串行池完整复核同一客户端套件。提交前只读复审已通过，未发现阻断问题；非阻断建议指出 0 分评定边缘读法，本轮已改为按 `score !== null/undefined` 判断已有评定。

2026-05-25：S89.17 已完成朝议专题目录与官署案头索引 polish。`CourtPage` 带 `data-polish-court="s89-17-court-directory"`，把 `/game/:sessionId/court` 六个专题入口整理为“官署案头索引”，逐项显示卷宗取材、可拟草稿、候复边界和案卷未载不补造提示，同时保持每个专题唯一入口并继续打开既有 `SurfaceHost` 专题层。本步零新增 CSS，复用 `.courtSurfaceGroup`、`.peopleMeta`、`.surfaceSafetyList` 和 `.paperButton`，最终预算仍为 `JS 618.6 KiB / CSS 99.8 KiB`。实现提交为 `b9e23f11`；本次哈希回填仅修改本文与 [DEVELOPMENT_STEPS.md](DEVELOPMENT_STEPS.md)，属于低风险纯文档改动，跳过子代理复审。

本步范围限 React `CourtPage`、客户端 smoke/source canary、前端测试和文档；不新增依赖或素材，不改后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest；浏览器不递交回合、不裁决资源、身份、交易、NPC 行动、经济、考试、官职、任免、赏罚、定罪、战和、关系、婚姻、弹劾、背叛或隐藏信息。当前已通过：`node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`、focused `npx vitest --config vitest.config.mjs run client/src/__tests__/App.test.tsx -t "registry-backed local surfaces" --pool=vmThreads --fileParallelism=false --maxWorkers=1`、完整串行 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、`npm run smoke:browser`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`。`npm run test:client` 复现既有 Vitest fork worker 启动超时：5 files / 119 tests 已通过，`assetRegistry.test.ts` worker 未启动；已用串行池完整复核同一客户端套件。提交前只读复审已通过，未发现阻断问题；非阻断建议为后续可把 6 个 `data-court-surface` exact set 加入 App 测试。

2026-05-25：S89.16 已完成全局壳与基础控件交互反馈 polish。`AppShell` 移除 `topBarPolishStyle` / `inkboxButtonPolishStyle` 内联临时视觉样式，顶栏和右上角印匣入口改用 `s89-16-shell-controls` / `s89-16-inkbox-button` 标记；全局 CSS 统一承载顶栏绢帛材质、主导航 selected、印匣按钮光泽/朱痕/按压、纸按钮/纸链接 active/disabled 与低动效降级。本步同时删除确认未被 React 源码引用的旧 `.gameGrid`、旧书生练习面板和旧画像网格/卡片 CSS，使预算在首次失败后回落到 `JS 617.1 KiB / CSS 99.8 KiB`。

本步范围限 React `AppShell`、全局 CSS、客户端 smoke/source canary、前端测试和文档；不新增依赖或素材，不改后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest。浏览器仍只消费安全 view 和本地草稿，不裁决资源、身份、交易、NPC 行动、经济、考试、官职、关系、婚姻、弹劾、定罪、背叛或隐藏信息。当前已通过：`node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`、focused `npx vitest --config vitest.config.mjs run client/src/__tests__/App.test.tsx -t "route-derived UI page state|settings route as a directory" --pool=vmThreads --fileParallelism=false --maxWorkers=1`、完整串行 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`、`npm run test:client`、`npm run build:client`、`npm run budget:client`、直接 `node scripts/clientSmoke.js`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`。`npm run smoke:browser` 在 300s 外层限时内完成 runtime manifest、build 和 budget 后进入 browser smoke 被截断；直接 browser smoke 已通过。提交前只读复审已通过，未发现阻断问题；非阻断建议为后续可把 active 顶部导航 smoke 从条件式检查收紧为必须存在。实现提交 `ad2cbab4`；本次哈希回填为低风险纯文档改动，跳过子代理复审。

2026-05-25：S89.15 已完成来函与账解证据读法 polish。`NpcFollowUpEvidenceSection` 现在带 `s89-15-follow-up-reader` / `s89-15-follow-up-boundary` 标记并把 `human_debt_monthly`、`accepted_pending_server_resolution`、`integrity_watchlist` 等来函后续枚举转为中文读法；`EconomyTraceSection` 带 `s89-15-economy-reader` / `s89-15-economy-boundary` 标记并把 `trade_negotiation`、`under_review` 等账解标签玩家化，账本数值变化改为中文“至”；人物页交游议题同步把 `npc_relationship_action` 等来源标签转为“交游记录”。

本步范围限 React 前端 `NpcFollowUpEvidenceSection` / `EconomyTraceSection` / `PeoplePage`、`App.test.tsx`、`scripts/clientSmoke.js`、source canary 和文档；未新增 CSS、依赖或素材，不改后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest。来函 evidence、经济 trace 和人物交游议题补 POSIX/Windows 本地路径、`draftContext`、`schema`、`manifest`、`safe view`、`resolver`、`sourceRef`、`relatedRefs`、`scopeRefs` 等工程词清洗；浏览器仍只读服务器安全 view 并写本地草稿，不裁决资源、身份、交易、NPC 行动、经济、考试、官职、关系、婚姻、弹劾、定罪、背叛或隐藏信息。

当前已通过：`npm run typecheck:client`、`node --check scripts/clientSmoke.js`、`node --test test/reactClientScaffold.test.js`（52 tests）、focused `npx vitest --config vitest.config.mjs run client/src/__tests__/App.test.tsx -t "current people ledger|economy trace" --pool=vmThreads --fileParallelism=false --maxWorkers=1`、完整串行 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`（6 files / 129 tests）、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、`npm run smoke:browser`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`。最终预算输出 `JS 617.1 KiB / CSS 99.9 KiB`，仍需继续谨慎控制 CSS。`npm run test:client` 两次命中既有 Vitest fork worker 启动超时，5 files / 60 tests 已通过但 `App.test.tsx` worker 未启动；已用串行池完整复核同一客户端套件。提交前只读复审已通过，未发现阻断问题；复审建议的 raw risk tag 边缘情况已补中文映射并由同一子代理复核确认。实现提交 `99072f6e`；本次哈希回填为低风险纯文档改动，跳过子代理复审。

2026-05-25：S89.14 已完成玩家身份标签中文化与 CSS 预算缓冲 polish。`client/src/text/playerLabels.ts` 统一前端玩家身份标签，首页续局、主卷案头、人物页、旧案架、右上角印匣、人物档案专题层和高清立绘标题不再把 `scholar` / `official` / `general` 等 role 枚举作为玩家可见兜底；设置目录短章法标签复用 `.peopleMeta`，舆图图层摘要规则小幅合并。

本步范围限 React 前端文本 helper、`HomePage` / `GamePage` / `PeoplePage` / `SaveCaseList` / `SurfaceHost` 身份显示、`SettingsPage` 标签样式复用、客户端 smoke/source canary、前端测试和文档；不新增依赖或素材，不改后端 API/schema、AI 权限矩阵、prompt、provider facade、SQLite schema、存档格式、runtime manifest 或素材 manifest。浏览器不裁决资源、身份、交易、NPC 行动、经济、考试、官职或隐藏信息。

当前已通过：`npm run typecheck:client`、`node --check scripts/clientSmoke.js`、`node --test test/reactClientScaffold.test.js`、focused `npx vitest --config vitest.config.mjs run client/src/__tests__/App.test.tsx -t "opens the S75.4 inkbox tabs|keeps the settings route|falls back when the S75.6 continue summary|drops polluted S75.5 save metadata|keeps the current session pointer" --pool=vmThreads --fileParallelism=false --maxWorkers=1`、完整串行 `npx vitest --config vitest.config.mjs run --pool=vmThreads --fileParallelism=false --maxWorkers=1`、`npm run test:client`、`npm run qa:runtime-manifest`、`npm run build:client`、`npm run budget:client`、`npm run smoke:browser`、`npm run check:docs-governance` 和 `node --test test/documentationGovernance.test.js`。预算输出 `JS 614.1 KiB / CSS 99.9 KiB`，仍需继续谨慎控制 CSS；提交前只读复审已通过，未发现阻断问题。实现提交 `0739b7f9`；本次哈希回填为低风险纯文档改动，跳过子代理复审。

上一轮 S89.13 已完成右上角印匣与设置目录信息架构 polish，实现提交 `54a6186d`，哈希回填提交 `71f71e02`。右上角仍是唯一显眼印匣入口，`/game/:sessionId/settings` 继续只作目录页并带 `data-polish-settings="s89-13-settings-directory"`；印匣新增 `s89-13-inkbox-overview` 总览，显示偏好新增 `s89-13-display-panel` 四项读法，案卷摘要新增 `s89-13-safe-summary` 并把 `player-state` / `exam-submit` 等内部来源改写为玩家可读来处。

上一轮 S89.12 已完成舆图筛选专题层体验 polish，实现提交 `b658a1a3`，哈希回填提交 `c48fcd22`。`map-filter` 继续作为本地 `SurfaceHost` 专题层，不加入后端 topic surface；点击“筛舆图”后显示 `data-polish-map-filter="s89-12-surface-guide"` 与 `data-polish-map-surface="s89-12-filter-ledger"` 的只读舆图筛选说明，整理地点、驿路、近事、人物动向和后果追踪的卷上读法。

上一轮 S89.11 已完成舆图全关图层空态与筛选交互 polish，实现提交 `0285f36e`，哈希回填提交 `4bbb0f52`。舆图页带 `data-polish-map-empty="s89-11-layer-empty"` 与 `data-layer-visibility`，运行时桥带 `data-polish-map-empty="s89-11-runtime-empty"`；地点、驿路、近事三层全隐时显示“素绢空图”空态和“展开三层”恢复入口。

上一轮 S89.10 已完成史册信息密度与移动端长文本二轮 polish，实现提交 `283b2c0a`，哈希回填提交 `49ac138a`。史册页带 `data-polish-archive="s89-10-chronicle-density"`，公开追踪区带 `data-archive-layout="ledger-rail"` 与 `data-polish-archive-trace="s89-10-chronicle-density"`，从三列重排为“近次归档主列 + 证据侧栏”，侧栏用 `.archiveEvidenceStack` 叠放后果追踪与来函证据，移动端继续单列并由 browser smoke 守住无横向溢出。

上一轮 S89.9 已完成人物页与立绘查看器材质题签 polish，实现提交 `8a7654ee9c5b7daa1c6790859ee885795f95928c`，哈希回填提交 `62d2b311`。人物页外层、人物名册、人物详情、人物谱牒和人物卡带 `s89-9-portrait-material` 可检测标记，高清立绘查看器题签格带 `data-polish-cue="s89-9-portrait-cue-material"`；CSS 复用已审核 `paper-aged-silk-v1.webp` 和宣纸底色，补人物卡悬停浮起、题签格网格材质、移动端题签单列与低动效降级。

上一轮 S89.8 完成高清立绘查看器画中所见 polish，实现提交 `2476a12c4d77e598b65ba74931361edb362e2b6c`，哈希回填提交 `d55cd94e`。立绘查看器带 `data-polish-portrait="s89-8-life-scroll"`，在原有 `data-polish-profile="s89-6-portrait-life"` 公开说明区上，显示“画卷题签 / 仪态 / 衣饰 / 神采”和“画中所见 / 身世线索 / 眼下处境”三段；人物页传入查看器的安全 profile 也补案主经历线索与 NPC 公开近事。

## Next Recommended Step

若继续前端产品化打磨，下一候选为 CSS 物理拆文件第二阶段、S89 polish 阶段归档、更多 route 空态一致性 polish、舆图/囊箧/设置跨页读法继续深化，或小批量 CSS/JS 预算专项。

无论下一步是什么，都必须继续从安全 view 重建 evidence，保持 proposal-only、browser-draft-only 和服务器裁决，不让浏览器 task、地图 layout、visual-only effect、NPC anchor、runtime manifest 元数据、world entity impact/recent impact、交游 evidence、world thread 或 draftContext 变成真实任务队列、资源结算器、关系/婚姻/弹劾/定罪/背叛裁决器。
