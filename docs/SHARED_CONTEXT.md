# Shared AI Development Context

这是 Codex 当前使用的压缩交接板。它只保留接手下一步必须知道的状态、边界、验证入口和内容保护规则；阶段细节请追溯 `docs/DEVELOPMENT_STEPS.md` 顶部归档索引和各专题归档。

## Read First

每次开发会话开始前必须读取：

1. `AGENTS.md` 或 `CLAUDE.md`
2. `docs/SHARED_CONTEXT.md`
3. `docs/QIANQIU_DEVELOPMENT_BRIEF.md`
4. `docs/DEVELOPMENT_STEPS.md`

稳定治理锚点见 `docs/DEVELOPMENT_GOVERNANCE.md`。重写 brief、路线图或交接文档时不得删弱受保护规则，并运行 `npm run check:docs-governance`；完整测试中的 `test/documentationGovernance.test.js` 也会守门。

## Current Snapshot

- Product: browser + Node.js historical simulation text game **Qianqiu / 千秋**。
- Runtime target: `npm install && npm start`，默认 `AI_PROVIDER=mock`，打开 `http://localhost:3000` 即可本地游玩。
- Frontend: S74.1 起 React + TypeScript + Vite 构建产物接管默认 `/`；源码在 `client/`，生产构建在 `dist/client/`。React Router Data Mode 管理首页、主卷、舆图、人物、史册、科举、皇榜、朝议和设置等路由。旧 `public/index.html`、`public/app.js`、`public/styles.css`、`public/mapPanel.js` 只作迁移参考；`public/assets/`、`public/vendor/`、`public/mapRenderer.js` 继续提供已审核素材和 S72 地图 runtime。
- Backend: Node.js + Express，当前以 CommonJS JavaScript 为主；S86 起已建立渐进 TypeScript 检查地基，`npm run typecheck:server` 覆盖契约、API/view 类型、安全 projection、AI schema/provider facade、storage adapter 和核心 resolver 的首批边界，不做全仓重写；S87 已完成 route/API response shape 首轮覆盖，`src/contracts/serverContracts.ts` 固定 game/exam/AI/inventory/NPC/trade/delegation public response，`src/routes/routeResponses.js` 用局部 `@ts-check` helper 接入大型 route 并运行时拒绝 public `worldState` raw ledger key，仍不对大型 route whole-file `@ts-check`。AI provider 为 adapter-based Mock/OpenAI/DeepSeek/MiMo/MiMo+DeepSeek/Anthropic；真实 provider 只作为可选配置，不能成为启动门槛。
- Storage: 默认 JSON session files under `data/sessions/`；可选 `STORAGE_ADAPTER=sqlite` 使用本地 `schema_migrations`、`world_sessions`、audit tables、`geo_*`、`people_*`、`office_*`、`event_archive_index`、`prompt_retrieval_index` 和 `safe_search_index`。SQLite 派生行只从 `world_sessions.world_state_json` 单向修复，不是玩家 API、prompt 或服务器裁决的 raw truth source。
- Roadmap status: S49-S67 本地数据库与大世界内容、S68-S69 科举深化、S70 AI 编排、S71 数据库玩法化、S72 PixiJS 水墨地图、S73-S77 前端水墨重构、S78 官署专题玩法化、S79 前端修复与高清立绘使用、S80 服务端全局 AI 设置均已完成并归档或压缩记录。S81-S85 NPC、资产、储物、交易、委派、经济、NPC 主动性和礼法扩展位已完成首轮闭环并归档。S86 后端 TypeScript 渐进迁移首轮与 S87 route/API 响应类型覆盖已完成并归档。当前活动专项为 S88 全面系统打磨，规划见 `docs/QIANQIU_POLISHING_ROADMAP.md`；S88.1 AI remote helper/provider public-safe envelope、S88.2 SQLite derived row builder 类型边界和 S88.3 书生主线补强一轮均已完成。S88.4 入仕官员首轮官场体验已完成七个切片：`officialCareerView.firstMonthExperience` 从首月差事派生官署首月进度、回署回执、上官同僚反馈、考成信号、下一步草稿和月报摘录提示；`officialCareerView.courtEntry` / `courtEntries` 把首月回署材料整理为奏折队列、朝议筹议和长期考成 trace 的安全 evidence，供 `memorial-review`、`court-debate` 和 `topic_draft` 只读拟稿；普通回合提交首月回署奏折/朝议/考成追踪后，服务器写入 `officialCareer.courtEntryResolutions`，并通过 `officialCareerView.courtEntry.latestResolution`、事件档案和月报公开近次裁决；继续提交朝议/部院/御前/考成跟进后，服务器写入 `officialCareer.courtEntryFollowUps`，并通过 `latestFollowUp`、`official_court_follow_up`、月报和 `worldThreadView` 公开中间批复；本轮新增独立 `worldState.officialCourtResponses` 服务器账本和 `courtResponseView`，让皇帝/大臣/官员跨身份回应公开奏议，普通回合只写 `official_court_response` 中间态，事件档案、world thread、`memorial-review` / `court-debate` 和 React 皇帝/官员面板只读消费；第六切片新增 `worldState.officialCourtConsequences` 服务器账本和 `courtConsequenceView`，把公开奏议裁决、跟进与回应转为证据缺口、考成压力、风宪关注、功绩留痕或朝局余波等长期官场后果信号，同步 `official_court_consequence` 事件档案、world thread、官职月报、topic surface、React 官员“考成与弹劾”和皇帝“赏罚预留”，不直接任免、奖惩、处分、财赋动用、奏议终局或风宪定案。
- S88.4 第七切片新增皇帝/部院续办链路：`courtResponseView.chainItems` 会把上一轮 `official_court_response` 整理成下一轮可回应来源，`officialCourtResponses.responses` 记录 `chainId`、`previousResponseId`、`sourceResponseId`、`chainRound`、`chainStageLabel`、`nextHandlerRole` 和 `chainPath`；普通回合带“续办 / 承前 / 御前再摘 / 部院再覆 / 补据承批”等意图时，服务器只追加新的 `[奏议回应记录]` 中间态，事件档案、`worldThreadView`、topic surface、React 皇帝/官员面板和 `courtConsequenceView` 继续只读消费，不写任免、奖惩、处分、拨钱粮、奏议终局、风宪定案或 raw ledger。
- S88.5 已推进六身份循环矩阵与跨域入口：服务器派生 `roleCycleView` 把皇帝、大臣、将领、地方官、书生、入仕官员统一为当前身份循环摘要，只展开当前身份事务、风险、待办、AI read scope、工具权限、proposal 边界和服务器裁决说明；非当前身份只显示“待任后展开”占位，避免低权限身份读取其他身份 hidden 军务、任所、私档或 raw ledger。S88.5.2 让地方官循环只读接入 `marketPriceView` / `npcEconomyView`，让将领循环只读接入 `mapRuntimeView` / `eventArchiveView`，并只暴露 capped evidence refs 与 route/surface 入口，不暴露地图布局、坐标、raw ledger 或新写 API。S88.5.3 新增 `roleCycleDomainAdjudication` 普通回合接缝：地方官“处置市价/平粜稳价”会用玩家可见 `market` evidence 调用既有 `cityPolicyResolver`，将领“舆图军议/战事档案”后的侦察或调粮会调用既有 `militaryDiplomacyResolver`；人物月账入口保持只读，不即时改 NPC 资产、关系或交易账本。当前 S88.6 红队片进一步扩展高风险军务词门禁，`发兵/攻取/奇袭/mobilize/engage/assault/siege` 等混入侦察或调粮草稿时不会触发 role-cycle resolver，书生/大臣/皇帝等非当前领域身份的同类文本也不会写 city/military ledger；同一当前角色、意图和公开 evidence refs 在近 3 旬重复提交时返回 `duplicate_recent`，不再次扣资源、写 city/military ledger 或追加角色循环 attribute changes。React 六身份主面板共用 `RoleCycleSection` 展示“本旬身份循环 / 本旬事务 / 风险 / 可查入口 / 可拟草稿”，入口只做路由跳转或打开本地 surface，按钮只写本地行动草稿，不调用 turn API。
- S88.6 已推进公开领域后果追踪与红队：`domainConsequenceView` 只从已裁决的 `cityPolicyLedger`、`militaryDiplomacyLedger`、`judicialCaseLedger` 和安全 `npcEconomyView` 派生 `city_policy` / `military_diplomacy` / `judicial_case` / `npc_economy` 公开余波，不暴露 outcomeId、raw evidence refs、`stateDelta` / `playerDelta`、auditRecord、资源消耗、关系信号、raw ledger、SQLite 行、本地路径或 key；`recentConsequences` 已接入 resolver input、奏折/诏令/朝议/刑名/军议 topic evidence、`topic_draft`、`safeWorldSearch` 和 SQLite `safe_search_index`；React `DomainConsequenceSection` 已接入地方官、将领、官员/大臣、皇帝主卷，并进一步接入舆图页“舆图后果追踪”和史册页“史册后果追踪”，按钮只写本地行动草稿。红队已收紧旧存档污染、重复后果、inactive role、高风险军务绕过、普通回合重复触发、角色可见性、map runtime 后果 effect、high-volume / cross-view cap 和长链路回响：账本行必须是 `accepted` / `applied` / `recorded` 且已有应用旬标记才会进入公开 view，同一安全 `publicSourceId` 只保留较新/更完整后果，每账本 cap 前会按应用旬排序以兼容乱序旧存档；服务器按当前 `player.role` 裁剪后果类型，书生默认不见领域后果，地方官只见地方政策/刑名/NPC 经济，将领只见军务外交，官员/大臣/皇帝可见跨域后果，archive/resolver/topic/search/SQLite 都只消费裁剪后的 view；高量场景已回归 `domainConsequenceView`、事件档案、resolver input、topic surface、safe search 和 SQLite safe-search 同步只见 capped 后果，不复活被 source/global cap 或角色裁剪掉的来源；`mapRuntimeView` 会把当前角色可见的 domain consequence 派生为 visual-only `eventEffects`，只绑定已有可见 runtime ref，不进入 `mapContextView` prompt 摘要或服务器裁决事实。`privateResultRefs`、`hiddenDossier`、`sealedMapping`、`retrievalContext`、`actorMemoryLedger`、`sessionSummary`、`relationshipLedger`、`providerPayload`、`rawEvidence` 和已配置环境密钥片段都会被过滤；`domainConsequenceView.caps` / `trackingEntryPoints` 公开当前角色可见 cap 与 map/archive 入口，`safeWorldSearch` 在全局 1200 行上限下优先保留当前角色可见 domain consequence 搜索行。最新两片新增安全 `publicEchoRef` 和 topic draft -> turn canonical echo 审计链：月报、world thread 和 role-cycle 都按该公开回响键引用/去重；resolver input、event archive、world thread、topic surface 与 `topic_draft` 都保留同一安全 echo ref 并在 surface 端去重同一公开后果；浏览器写入草稿时只附带待校验 `draftContext`，`POST /api/game/turn` 会用当前服务器 topic surface 重新校验 evidence refs 后才把 canonical echo refs 写入 `roleCycleDomainAdjudication` 公开反馈、内部 audit 和最新 city/military ledger 记录，伪造 echo 不被采纳，新证据、新意图或冷却后的普通回合裁决仍不被误挡。
- S88.7 已启动 NPC 与关系深化：首片让 `npcActiveRequests` 在回应或逾期时生成 `npc_active_request_resolver` 安全 trace，公开 request type、回应动作、状态、处置分层、风险标签、安全 source refs、服务器边界和 public resolution ref，并写入请求 `auditRefs` 与 turn feedback 的 `outcome.resolutionTraces`；`npcRelationshipActions` 为论道、切磋、求爱和议婚生成 `npc_relationship_action_resolver` trace，并随 `npcInteractionLedger` 安全记录返回。第二片把这些安全 trace 接入长期回响：`eventArchiveView` 新增 `npc_active_request` 与 `npc_relationship_action` 公开条目，只消费 `npcActiveRequestView({ includeResolved: true })` 和 `npcInteractionView` safe projection；普通 turn 会把本旬 `npc_active_request_resolver` trace 通过 `actorMemoryLedger` 写成已知 NPC 的可见记忆，且必须先命中 canonical active request safe record，伪造可见/不可见 NPC refs 或同 ref 篡改 trace 字段都不会改变记忆类型、标签或摘要。第三片让 `/npc-interaction` 在记录论道、切磋、求爱和议婚后，从刚写入的安全 `npcInteractionRecords` 派生 `npc_relationship_action_trace` 可见记忆，并把 `actorMemory` 摘要、`actorMemoryView` 和 `eventArchiveView` 随响应返回；React store 会把这些安全投影合并进当前 session，人物页/史册页可立即刷新。第四片新增 `npcActiveRequestView.items[].responseOptions` 与 resolved `outcome.followUpView`：行贿、弹劾、背叛、引荐、请托、索债/人情债、献策、求助和议婚都有服务器配置的后续复核类型、公开摘要、下一步、证据 refs、风险标签和草稿按钮；事件档案与主动来函记忆会带上 follow-up 上下文，人物页来函 inbox 只对 `active` / `deferred` 来函显示草稿按钮，已裁决 follow-up 只展示后续说明和状态。第五片实现提交 `18e489e5`：`npcActiveRequestView.followUpTasks` 从已裁决安全 item 派生“来函后续簿”，把引荐、人情债、请托、行贿、弹劾、背叛、议婚等 follow-up 收束为公开 taskRoute、状态、草稿、证据 refs、风险标签和服务器边界；React 人物页只读展示并把“拟后续”写入本地行动草稿。第六片把后续簿接入普通回合窄裁决：玩家提交匹配 task 的“续办/后续/复核”文本时，服务器从安全 `followUpTasks` 重建 evidence，回写 canonical request 的 `outcome.followUpResolutions` 和 `followUpView.latestResolution`，turn feedback 返回 `npc_active_request_follow_up_resolver` 公开记录，仍只登记公开复核，不创建真实任务。第七片把 `followUpResolutions` 接入长期公开回响：事件档案沿用原 `npc_active_request` stable public ref 展示近次后续摘要，`actorMemoryLedger` 新增 canonical-only `npc_active_request_follow_up` 可见记忆，`worldThreadView` 把最新安全后续写入主动来函议题摘要和更新时间。第八片新增 `npcActiveRequestView.followUpEvidence`，只从安全 `followUpTasks`、`latestResolution` 和已清洗边界派生引荐拜会/同年师友、人情债月账、请托案牍、献策公开证据、廉政/风宪/背叛风险 watchlist 等 grouped `people` / `events` / `economy` 只读 evidence；resolver input、`memorial-review` / `court-debate` / `trial` / `npc-profile` topic surface、`safeWorldSearch` 和 SQLite `safe_search_index` 只读消费这些行。`worldThreadView` 消费新的 `npcActiveRequestView` 主动来函，`worldEntities` 可从 scheduled / resolver trace 派生公开实体压力；这些链路不读取 raw `npcActiveRequestLedger`、private signal、hidden dossier、SQLite 行、provider payload、prompt 或本地路径，也不直接扣资源、转物、写婚姻、定罪、成弹劾、处置 hidden 背叛事实或创建真实后续任务。
- S88.8 已启动资产、囊箧、交易、委派与经济解释性七片：第一片实现提交 `50b55538`，新增 `economyTraceView` 安全投影，只从现有安全 `resourceLedgerView`、`assetLedgerView`、`inventoryView`、`tradeLedgerView`、`delegatedTaskView`、`marketPriceView` 和 `npcEconomyView` 派生资源/资产/库存/交易/委派/市价/月账解释，使用 `sourceRefs` 而非 raw evidence 字段，公开 AI read scope、工具权限、proposal 边界和 server adjudication。`/api/game/start|state|player-state|turn|turn-stream`、囊箧读取和物品移置响应会返回该 view；React 囊箧页新增“账本为何变化”，共享 `EconomyTraceSection` 会丢弃污染行、守住 route session，不让旧案卷解释串页。第二片实现提交 `06c25f4c`：`/api/game/trade/:sessionId` 与 `/api/game/npc-command/:sessionId` 也返回即时 `economyTraceView`，React store 只在 session 匹配时合并交易/委派 trace，且 `currentSession` 只在自身 `sessionId` 匹配响应时更新，人物页新增“交易委派账本为何变化”并只展示交易、委派、人情债、市价和月账相关 trace。第三片实现提交 `62c9a260`：`economyTraceView.traceItems` 新增标准 evidence 字段并进入 resolver input 的 `economy` source collection、奏折/拟旨/朝议/军议/人物档案 topic surface、`safeWorldSearch` 和 SQLite `safe_search_index` 单向派生；topic allowlist 确保人物档案只读交易/人情债/关系月账等相关经济材料，堂审不旁路读取普通交易或市价 trace，高量安全检索会保护经济 trace 行不被噪声挤掉。第四片实现提交 `05fba067`：`GamePage` 安全视图索引新增“账解”，地方官主卷展示“钱粮与市价为何变化”，官员/大臣主卷展示“经济线索与官署材料”，均只展示市价、交易、委派、人情债、NPC 关系月账和普通月账解释，不展示纯资源快照、资产维护或囊箧保养；`SurfaceHost` 将 `economyTraceView` 数据来源摘要显示为“经济解释 1 条”，evidence meta 显示为“经济解释 · 月账”。第五片实现提交 `1becde0c`：经济解释推进 `topic_draft` 草稿链，provider context 为经济 evidence 增加“经济解释”来源、“月账”领域标签和“只作公开账解、仍由服务器裁决交易/资源/委派/人情债/关系变化”的边界，本地降级草稿会明示经济解释不视为交易成交或委派结算；前端回归覆盖勾选“经济解释”后 `/api/ai/topic-draft` 请求携带该 ref，并在“写入底部奏折”后保留为待服务器重校验的 `draftContext`；turn draft context 会重建当前 topic surface，只采纳 allowlist 内经济 refs，伪造 refs、伪造 canonical echo 和堂审 surface 的经济 refs 都被丢弃。第六片实现提交 `ecfdc3c0`：补普通 turn 与 SSE route 长链路回归，同一市价处置在已存在 city policy 基线后重复提交，带经济 evidence、伪造 economy ref 与伪造 canonical echo 的 `draftContext` 只会成为重校验后的安全 hint，不写 `topicDraftContext`、不接受伪造 echo、不再次写 city/military ledger，也不通过经济 evidence 结算交易、资源、委派、人情债或关系变化。第七片实现提交 `f533d1c1`：`test/streamingTurnRoute.test.js` 新增 true `provider.streamTurn()` 分支回归，先用同一 stub provider 的普通 JSON turn 建立市价裁决基线，再以可见 streamed narrative chunks 携带经济 `draftContext` 重复提交，确认 `streamTurn()` 路径同样丢弃伪造 economy ref/echo、不写 `topicDraftContext`、不再次结算经济账本；普通/SSE/true streaming 的保存态断言同步覆盖军务 ledger、资源账本、玩家金银、地方库银与经济关系信号 canary。所有按钮只写本地行动草稿；资源扣减、物品流转、交易成交、委派结果、人情债和关系变化仍由服务器 resolver/tick 裁决。Lagrange 提交前只读初审发现 transfer stale 响应跨案卷合并 P1 与公开 `SQLite` 实现名 P2；第一片已修复并补 stale transfer 回归与 SQLite canary。第二片 Dirac 提交前只读初审发现 route pointer 与旧 `currentSession` 分叉时可污染旧 payload 的 P1；已按 `currentSession.sessionId` 自身匹配修复并补回归，最终复审确认 P1 已关闭，未发现新的 P0/P1/P2。第三片 Meitner 提交前只读初审发现污染 id 走数字 fallback 时 `cleanId()` 可能抛 TypeError 的 P2；已强制 fallback 字符串化并补污染 `safe_search_index` id 回归，Meitner 最终复核确认 P2 关闭，未发现新的 P0/P1/P2。第四片 Bacon 已做只读前端/topic 接线梳理；已通过 `npm run typecheck:client`、`node --test --test-reporter=spec test/reactClientScaffold.test.js`（40 项）、focused `uiState` / `MemorialComposer` client tests（24 项）、focused `App` client tests（41 项）、完整 client Vitest（79 项）、`npm run build:client`、`AI_PROVIDER=mock npm run smoke:browser`、docs governance、documentation governance、完整 `npm test`（1115 项）和 `git diff --check`；Erdos 提交前只读初审发现 topic 数据来源摘要仍显示内部 `economyTraceView` 的 P2，已改为复用 `topicSourceLabel()` 并补 App/scaffold 回归，修复后重跑 `npm run typecheck:client`、`node --test --test-reporter=spec test/reactClientScaffold.test.js`、focused `App` client tests（41 项）和 `git diff --check` 通过。Erdos 最终只读复审确认 P2 关闭，未发现新的 P0/P1/P2；Erdos 复跑 client typecheck、scaffold 和 docs 守门通过，App focused 在其环境因缺少 `@rolldown/binding-linux-x64-gnu` optional native binding 未启动，主代理本地同一 App focused 41 项已通过。最终提交前再次通过 docs governance、documentation governance、完整 `npm test`（1115 项）和 `git diff --check`。第一片已通过语法检查、经济/turn/inventory focused Node tests、server/client typecheck、App/full client Vitest、client build、browser smoke、docs governance、documentation governance、完整 `npm test`（1108 项）和 `git diff --check`；Wegener 最终只读复审未发现 P0/P1/P2。第三片已通过语法检查、经济 trace / resolver input / topic surface / safe search focused Node tests（31 项）、SQLite safe-search focused tests（8 项）、`npm run typecheck:server`、docs governance、documentation governance、完整 `npm test`（1114 项）和 `git diff --check`。
- S88.9 已启动 React 前端操作与状态打磨第一片，实现提交 `1e1cb7a0`：`gameSessionState` 的 player-state、快捷建议、topic surface/draft、囊箧、NPC 名册和 NPC 详情读取都带前端请求序号与 `sessionId` / surface / NPC id guard；旧请求晚到会 reject，不写 store，也不让旧调用方继续执行本地副作用，错配最新回包进入错误态且不同步 UI safe payload。`uiState` route bridge 在切换到不同案卷时清空不匹配的 `currentPlayerPayload`，首页同案卷继续入口仍保留。`GamePage` 只从当前路由匹配的 `activeSession`、`activeLastTurn` 和 `activePlayerPayload` 渲染案主、身份、叙事、安全视图、角色面板和底部奏折；无匹配投影时显示候讯/占位，不展示旧案主、旧官职、旧叙事、旧错误或旧角色循环材料。`SurfaceHost` 也按当前 `currentSessionId` 过滤 `topicSurface` / `topicDraft`，案卷切换时清空或失效本地专题草稿、证据选择、草稿类型和 draft context，避免同 surface 旧案卷材料串入新案卷。React scaffold 静态断言已同步为 `activeSession` / `activePlayerPayload` 口径，并补 `topicSurface` / `topicDraft` 同案卷断言。Leibniz 只读 gap 扫描建议优先处理主卷 stale session / loading / empty 防线，本轮采纳并扩展到安全读取 store；Faraday 提交前只读初审发现 topic surface 同案卷切换 P1，已修复并补 App/scaffold 回归，最终只读复审确认 P1 关闭，未发现新的 P0/P1/P2。已通过 client typecheck、focused client tests（65 项）、React scaffold（40 项）、完整 client Vitest（84 项）、client build、Mock browser smoke、docs governance、documentation governance、完整 `npm test`（1126 项）和 `git diff --check`。
- S88.9 第二片本轮推进人物页/囊箧页 route-local 状态防线与印匣旧案刷新防线，实现提交 `960e0336`：`PeoplePage` 只按当前 `sessionId` 渲染 NPC 最新对话、交易、委派计划、礼法结果、NPC 详情、名册 fallback 记录、加载状态和错误提示，切换案卷时重置选中 NPC、页签、画像分页与对话/交易/委派/礼法草稿；旧案卷 mutation 完成后不再清空新案卷草稿。`InventoryPage` 将容器、物件、目标容器和移置提示绑定到当前案卷，切换案卷时清空本地选择；旧案卷物品移置请求晚到或回包案卷不匹配时不执行页面本地提示/选中副作用，避免旧提示、旧容器或 `未入容器` 空选中串入新案卷。`gameSessionState` 进一步给物品移置、NPC 对话、交易和委派 mutation 增加请求序号与当前案卷 guard，旧案卷请求在发起、成功或失败时都不得改当前路由 loading/error/最新结果；`loadSession()` / `loadNpcs()` 切案卷时会复位旧 NPC mutation loading。`SurfaceHost` 印匣旧案 tab 在 ready 列表缺少当前 runnable 案卷时自动刷新一次，避免本地存档列表 stale 时漏显当前案卷。新增 App 回归覆盖人物页旧案 NPC 结果/名册记录/错误过滤、旧案完成移置后导航到新案卷的囊箧本地状态重置，以及旧案列表首次缺当前案卷时自动刷新；新增 store 回归覆盖旧移置失败、旧 NPC mutation 失败和人物名册切案卷 loading 重置。代码提交 `960e0336` 已存在后，主代理补做 Helmholtz 只读复审；Helmholtz 未发现代码 P0/P1/P2，仅指出复审时序文档口径 P2，已由本次纯文档修正收口。该片仍不新增服务器写口，不改变资源、交易、委派、NPC 行动、经济、考试或官职裁决。
- S88.9 第三片本轮推进科举、皇榜和舆图 route-local 状态防线，实现提交 `9b32068a`：`gameSessionState` 的 `requestExamQuestion()`、`progressExam()` 和 `submitExam()` 现在共用前端考试 mutation 请求序号与当前案卷 guard；旧案卷考试请求晚到、失败或回包案卷错配时，不得覆盖当前 `activeExam`、`lastExamResult`、`currentSession`、全局 `status/error` 或 UI safe payload。`ExamPage` 切换案卷时重置试别、场内行动和文章草稿；`RankingPage` 切换案卷时清空本地榜名选中；`MapPage` 切换案卷时恢复地点/驿路/近事图层，继续只消费当前 `sessionId` 的安全 `mapRuntimeView` / `domainConsequenceView`。新增 store 回归覆盖旧案卷取题成功、推进失败和交卷成功晚到后不污染当前案卷或 UI 摘要；React scaffold 静态断言覆盖三页 route-local reset。本片仍只做前端安全消费与本地状态 guard，不新增后端 API，不改变考试晋级、放榜、地图事实、移动、资源、NPC 行动、经济或官职裁决。Gauss 提交前只读复审未发现 P0/P1/P2；残余非阻断风险是三页 route-local reset 主要由 store 回归和静态断言覆盖，后续可补 App 级路由跳转行为测试。
- S88.9 第四片本轮推进设置页全局 AI 请求 race/loading/empty 防线，实现提交 `ae5b64fd`：`gameSessionState` 为全局与兼容 session AI 设置读取增加 `aiSettingsReadRequestId`，为保存、preset 和任务路由更新增加 `aiSettingsWriteRequestId` / `aiSettingsActiveWriteRequestId`，并为 `testAiConnection()` 增加独立 `aiConnectionRequestId` / `aiConnectionStatus`；旧 GET/POST/试连响应晚到或错配时不覆盖 `aiSettings`、`aiConnection`、`settingsStatus`、`aiConnectionStatus` 或错误态，且稍后的 GET 不能抢占仍在途或刚完成的保存结果。兼容 session 设置回包必须是 global scope 或匹配请求案卷。`AiSettingsPanel` 现在保留未保存编辑，不让后台重载吞掉脏表单；设置页和印匣同时挂载时，较早面板会从 store 已采纳的最新安全 `aiSettings` 回填，避免空矩阵卡住；若本面板保存被另一面板或更新请求取代，会退出“保存中”、保留当前编辑并提示可重新保存。面板新增载入中、试连中、空任务矩阵、“未保存编辑已保留”和“保存请求已被新的设置请求取代”提示，按钮 loading/disabled 与设置读取/保存和试连状态分别对齐；AI 任务标题、说明和状态徽记增加溢出换行防线。Goodall 提交前只读复审发现双面板后发保存可让较早面板滞留“保存中”的 P1，已修复并补 App 回归；Goodall 最终复审确认 P1 关闭，未发现 P0/P1/P2，非阻塞缺口是双面板后发保存回归通过共享 store 触发而不是完整点击第二面板表单。本片仍只消费服务端全局 AI 设置安全响应，不保存 key、base URL、prompt、provider payload、本地路径或 raw audit，不改变 provider 路由策略、AI 权限、服务器裁决、考试/官职/资源/NPC/经济规则。
- S88.9 第五片本轮推进朝议/史册/舆图 route-bound surface 入口，实现提交 `2dccdae0`：`uiState.openSurfaceForSession(surface, sessionId)` 会先把 UI 当前案卷指针绑定到路由 `sessionId`，仅保留同案卷 `currentPlayerPayload`，并关闭抽屉、modal 和高清立绘查看器后再打开本地 surface；`CourtPage`、史册页“阅奏折”和舆图页“筛舆图”都改用当前 route `sessionId` 打开 `court-debate` / `memorial-review` / `map-filter`，避免旧 UI `currentSessionId` 滞留时加载另一案卷的 topic surface。新增 store 回归覆盖 route-bound surface 会清理旧安全摘要与 overlay；新增 App 级回归覆盖 stale UI 指针存在时，朝议、史册与舆图按钮只请求当前 route 案卷材料，不显示旧案题；React scaffold 静态守门同步覆盖 `openSurfaceForSession` 接线。Raman 提交前只读复审未发现 P0/P1/P2，非阻断建议是后续 S88.9 App 级路由 polish 可补焦点回收/overlay trigger 行为回归。该片不新增后端 API、不改变 topic surface 数据契约、AI 权限、服务器裁决、考试、官职、资源、NPC、交易、委派或经济规则；浏览器按钮仍只打开本地 surface 或写本地草稿。
- S88.9 第六片本轮推进 overlay 焦点与皇榜跳转可访问性打磨，实现提交 `ca472648`：`SurfaceHost` 为印匣抽屉、安全弹窗、本地专题层和高清立绘查看器增加统一 Tab 焦点循环，继续保留 Esc 关闭、焦点回收和 portrait-on-surface 返回逻辑；朝议/史册/舆图 route-bound surface 的 App 回归现在断言打开后焦点进入关闭按钮、Esc 后回到触发按钮。`RankingPage` 的“跳至我名”从纯锚点改为按钮，点击后滚动并把焦点移入 `tabIndex=-1` 的“榜名详情”区域，CSS 补程序聚焦轮廓和 `paperLink` button 基础样式。本片不新增后端 API、不改变 route view、AI 权限、topic surface、考试放榜、授官、资源、NPC、交易、委派或经济裁决；仍只消费安全 view、只写本地草稿。Socrates 只读扫描指出 overlay 缺焦点陷阱、史册/舆图缺焦点回收回归、皇榜跳转不聚焦详情；本轮已采纳。Confucius 提交前只读复审未发现 P0/P1/P2；非阻断残余缺口是抽屉、弹窗和高清立绘查看器的 Tab 循环主要由共享实现与静态守门覆盖，暂无逐一交互测试。
- S88.9 第七片实现提交 `71328cfa`：史册、皇榜和舆图移动端/长文本溢出打磨已完成，`global.css` 为公开后果追踪、来函 evidence、史册条目、皇榜榜单/详情和舆图控件补 `min-width: 0`、`overflow-wrap: anywhere`、移动端皇榜单列、史册 action auto-fit、舆图控件 auto-fit 和地图标签省略；`scripts/clientSmoke.js` 把 overflow 检查从按钮/链接扩展到史册、后果、来函、皇榜和舆图关键文本节点，并新增移动端史册路径 `s88-9-archive-mobile` 的 layout、横向溢出和 forbidden text 检查；`test/reactClientScaffold.test.js` 增加对应静态守门。本片不新增后端 API，不改变 route view、AI 权限、topic surface、考试放榜、地图事实、资源、NPC、交易、委派或经济裁决；浏览器仍只消费安全 view、只写本地草稿。
- S88.9 第八片低动效与空/错态 polish 实现提交 `65868232`：新增共享 `usePrefersReducedMotion()`，首页与舆图共用系统级低动效判断，舆图 runtime 现在只有在玩家应用偏好允许、舆图动效开启且浏览器未请求 reduced motion 时才传入 `mapMotionEnabled=true`；皇榜金粉与题名字样动画在应用低动效和浏览器 `prefers-reduced-motion: reduce` 下都会显式关闭，browser smoke 会进入皇榜预览页校验装饰动画静止。`AiSettingsPanel` 的空/载入/错误任务矩阵改为 `aiSettingsMatrixStatus` 安全状态块，移动端设置按钮和摘要控件全宽触控，不补造 provider、模型、工具权限或 key 状态。本片仍不新增后端 API，不改变服务端全局 AI 设置契约、route view、AI 权限、考试放榜、地图事实、资源、NPC、交易、委派或经济裁决；按钮仍只保存服务端允许的设置、写本地草稿或跳转。Arendt 提交前只读复审未发现代码或安全边界 P0/P1；唯一 P2 是复审状态与提交哈希占位需收口，已随实现提交前文档修正和本次低风险哈希回填收口。
- S88.9 第九片顶部主导航 active 精准化实现提交 `7223c036`：`AppShell` 顶部“主卷”链接现在只在 `/game/:sessionId` exact route 上高亮，进入 `/game/:sessionId/map|people|inventory|archive` 等子主路由时不再与当前页面双高亮；“舆图 / 人物 / 囊箧 / 史册”等主导航仍保持各自 route active。已补 App 回归和 React scaffold 静态守门，并已通过 client typecheck、focused App 回归、完整 client Vitest、client build、Mock browser smoke、docs governance、完整 `npm test` 与 `git diff --check`；Erdos 提交前只读复审未发现 P0/P1/P2。本片只调整 React Router `NavLink` active 展示和测试，不新增后端 API，不改变 href/session 绑定、route view、AI 权限、服务器裁决、考试、官职、资源、NPC、交易、委派或经济规则。残余非阻断风险：App 回归只实测 `/map`，`/people`、`/inventory`、`/archive` 依赖同一 React Router `NavLink` active 规则。
- S88.12 已启动 Mock/真实 provider 验收，实现提交 `53dc1f3d` 完成 no-key runtime fallback 首片：普通 JSON turn 与 SSE turn 在 `AI_PROVIDER=deepseek` 但无 `DEEPSEEK_API_KEY` 时，会通过 provider facade 回退到 Mock，仍保持本地可玩；经济 `draftContext` 会由服务器重建当前 topic surface 后重校验，不接受伪造 economy ref / canonical echo，不写 `topicDraftContext`，也不通过经济 evidence 结算交易、委派、人情债、金银或地方库银。第二片实现提交 `28141071`：`scripts/providerLongRun.js` 已在内存结算链中运行 NPC 经济 tick 并把 `economyTraceView` 纳入真实 provider 长跑验收，校验 trace schema、公开字段、`sourceRefs`、settlement 边界和 raw ledger/provider/key/path/`evidenceRefs` 防泄漏；`smoke:provider:long` / `smoke:provider:route` 在自动模式无 key 时 skip，但显式点名真实 provider 且缺 key 时必须失败，不能伪装成普通游玩 Mock fallback。`/api/ai/connection-test` 的缺 key 503 诊断语义保持不变；MiMo tool smoke 先处理无 key skip，再要求 `fetch`，确保 no-key 环境不会因缺少真实 provider 或 fetch 而失败。
- Current collaboration: 2026-05-14 起停止 Gemini CLI 协作。后续开发、素材生成/审核、验证、文档同步和 Git 提交由 Codex 负责；用户已授权本仓库使用 Codex 子代理，实施子代理不得提交，提交前复审子代理必须只读。
- Current local `.env`: 可能含用户 provider keys。`.env` 被 Git 忽略，不能打印、复制到文档或提交。

## Core Invariants

- 后续开发和维护不以“最小实现点”或“最小改动点”为目标；在安全边界、默认可运行、内容保护和可审查粒度不受损的前提下，优先交付完整、丰富、功能强大的游戏实现，并把必要的系统、交互、AI、数据、验证和文档一次设计到位。
- 复杂功能必须坚持前后端分离和大步骤拆分：后端/API/数据契约、AI 权限与服务器裁决、前端体验、验证与文档按可审查阶段分步交付；前端不得代替服务器裁决资源、身份、交易、NPC 行动、经济结果或隐藏信息。
- 后端契约、API/view 类型、安全 projection、AI schema/provider facade、storage adapter 和核心 resolver 新增或重构时，应优先使用 TypeScript 或纳入 TypeScript 检查；既有 JavaScript 允许渐进迁移，不得为语言迁移一次性重写大量稳定模块，TS 类型不能替代 Ajv 与服务器 runtime 校验。
- 后端 route/API response shape 新增或重构时，必须对齐 `src/contracts/serverContracts.ts` 或局部 JSDoc typedef，并运行 `npm run typecheck:server`；不得为了启用类型检查一次性 whole-file `@ts-check` 大型 route 文件，也不得放宽安全 projection、raw ledger 剥离、Ajv/runtime 校验或服务器裁决。
- 完整书生路径必须继续可用：`scholar -> child_exam -> provincial_exam -> metropolitan_exam -> palace_exam -> official`。
- AI 是《千秋》的核心世界引擎，不是可替换装饰。新增玩法、数据域、角色、官署、事件、面板或 prompt 检索时，必须定义 AI read scope、actor intelligence、tool permissions、proposal boundaries、server adjudication、audit records 和 Mock/no-key fallback。
- Provider 只能建议叙事、受限 `statePatch`、关系变化、评分 JSON、考试触发或身份受限领域工具 proposal；服务器拥有晋级、考试资格、作弊处罚、持久化、保护字段、官职任免、长期事件、时间推进、数据库写入和可见性过滤。
- AI JSON 必须先验证再应用；状态变化必须经过白名单、clamp 和 server-owned follow-up modules。
- 游戏规则、阈值、时间间隔、概率、UI caps、fixture 规模和 prompt budgets 等可调参数不得散落为 magic numbers；优先放入 `src/config/GameConfig.js` 或领域 `src/game/*Config.js`，写明单位、范围和默认意图。
- 每个 coherent change 必须更新本交接板和步骤台账，运行相关验证，并用 Git 提交。低风险纯文档改动可跳过子代理复审；涉及代码、测试、运行时、API/schema、提示词或验证工具时，提交前必须做只读子代理复审。
- 项目内协作文档、路线图、交接记录、领域注释和玩家可见文案优先使用中文；代码标识符、API/协议名、第三方术语和命令输出可保留英文。

## Content Protection

- 当前范围只考虑本地 JSON/SQLite；不规划远程存档、账号体系、多人同步、云冲突解决或托管数据库。
- AI 可以通过身份受限领域工具提交 proposal / request-adjudication，但不能执行 SQL，不能直接写 canonical 状态、`geo_*`、`people_*`、`office_*`、`event_archive_index`、`prompt_retrieval_index`、`event_log` 或 `ai_change_proposals`，工具调用也不能被当作已经发生的世界事实。
- 浏览器和 prompt 只读服务器生成的安全 view / capped retrieval summary，不读 raw SQLite table、raw audit、provider proposal、完整 prompt、本地路径、密钥、hidden notes 或 hidden intent。游戏/考试 route response 内的兼容 `worldState` 也必须剥离 raw `actorMemoryLedger` 与 `sessionSummary`。
- hidden NPC 私档、资产真数、未公开关系、未公开任所、密档事件链和隐藏情报真值不得回填当前 raw route `worldState`。若后续保存完整 hidden 私档，先设计 API redaction 与角色视野分层。
- 玩家可见 projection 边界包括 `informationPanelPageView`、`safeWorldSearchView`、`actorMemoryView`、`sessionSummaryView`、`mapContextView`、`mapRuntimeView`、`aiControlAuditView` 和 `server_player_visible_state_projection`；它们只暴露清洗后的公开摘要、snippet、ref、confidence、visibility、显示布局或 bounded audit summary。`mapRuntimeView` 坐标只供浏览器地图 runtime 使用，不进入 prompt context、AI 工具或服务器 resolver 的事实依据。
- 本机开发诊断 `GET /api/dev/session-diagnostics/:sessionId` 默认关闭，production 强制关闭，只在 `ENABLE_DEV_DIAGNOSTICS=true`、远端地址为 loopback 且 Origin 为空或本机 loopback Origin 通过时返回安全统计；不返回 raw state、raw audit、provider payload、prompt、本地路径、key、SQLite 原始行或 hidden ledger。
- 前端重构后的浏览器只能组合安全 route view、redacted player state、AI 设置 view、地图 runtime view、考试安全快照、已审核 `portraitRef` 和 runtime manifest。不得读取 raw audit、provider proposal、完整 prompt、本地路径、key、hidden notes 或 hidden intent；不得显示未审核素材、硬编码本地路径或一次性加载全量立绘池。
- S73-S79 立绘资产要求成年、端庄、身份明确、高颜值但不露骨；女性角色可用服饰剪裁、腰封、衣料层次和站姿体现优雅成熟女性身形比例，但不得性化未成年人、幼态化、挑逗或过度暴露。重要 NPC 专属池使用 `signature_npc_pool`，不得被随机通用头像系统抽取。

## Implemented Surface

- Core APIs: `GET /api/health`、`POST /api/game/start`、`GET /api/game/saves`、`GET /api/game/player-state/:sessionId`、兼容 `GET /api/game/state/:sessionId`、`GET /api/game/search/:sessionId`、`GET /api/game/topic-surface/:sessionId/:surfaceId`、`GET /api/game/inventory/:sessionId`、`POST /api/game/inventory-transfer/:sessionId`、`GET /api/game/npcs/:sessionId`、`GET /api/game/npc/:sessionId/:npcId`、`POST /api/game/npc-interaction/:sessionId`、`POST /api/game/trade/:sessionId`、`POST /api/game/npc-command/:sessionId`、`POST /api/game/turn`、`POST /api/exam/question|progress|submit`。S85 后 `npc-interaction` 还承载论道、切磋、求爱和婚姻的服务器 eligibility/outcome 扩展位。
- AI APIs: `POST /api/ai/connection-test`、`GET/POST /api/ai/settings/global`、兼容 `GET/POST /api/ai/settings/:sessionId`、`POST /api/ai/quick-actions/:sessionId`、`POST /api/ai/topic-draft/:sessionId`。AI 设置矩阵当前覆盖 18 类任务，新增 `background_claim_parser`、`npc_dialogue`、`npc_private_planner`、`trade_negotiator`、`delegated_task_planner`、`delegated_task_reporter`、`inventory_effect_explainer`。
- Main code areas: AI adapters/prompts/schemas/tools in `src/ai/`; server-owned game rules, exams, official career, resolver input, redacted state, memory, settings, audit and topic surfaces in `src/game/`; storage adapters and migrations in `src/storage/`; React app in `client/`; reviewed visual assets and runtime manifests in `public/assets/`.
- Recent validation baseline: S88.4 朝议/部院/御前跟进切片已通过语法检查、official/game-turn focused tests（24 项）、monthly/event/world-thread focused tests（23 项）、topic focused tests（17 项）、React scaffold tests（37 项）、server/client typecheck、React App focused tests（36 项）、全量 Vitest（68 项）、client build、`npm run smoke:exam-s69`、`AI_PROVIDER=mock npm run smoke:browser`、docs governance、documentation governance、完整 `npm test`（1015 项）和 `git diff --check`；Dalton 提交前只读初审发现的 `bureau-reply` generic action 重复推进 P1 与 public follow-up 权限文本“数据库查询”P2 已修复，最终复核确认 P1/P2 已关闭，未发现新的 P0/P1/P2，实现提交 `2eabfcb2`。S88.4 首月回署草稿服务器裁决闭环已通过语法检查、official/game-turn/monthly focused Node tests、topic/event archive focused tests、server/client typecheck、React App focused tests（36 项）、client build、全量 Vitest（68 项）、`npm run smoke:exam-s69`、`AI_PROVIDER=mock npm run smoke:browser`、docs governance、documentation governance、完整 `npm test`（1013 项）和 `git diff --check`；Chandrasekhar 初审 P2 修复后，Gauss 最终只读复核未发现新的 P0/P1/P2，实现提交 `8b678834`。S88.4 奏折朝议入口与长期考成追踪切片已通过语法检查、official/topic-draft/topic-surface/monthly focused Node tests、server/client typecheck、React App focused tests（36 项）、React scaffold tests（37 项）、全量 Vitest（68 项）、client build、`npm run smoke:exam-s69`、`AI_PROVIDER=mock npm run smoke:browser`、docs governance、documentation governance、完整 `npm test`（1009 项）和 `git diff --check`；Chandrasekhar 提交前只读复审发现的裸 SQLite/SQL/rawSql 污染与官场考成/弹劾成案话术两个 P2 已修复，最终未发现新的 P0/P1/P2；实现提交 `345d134d`。S88.4 官署首月体验切片已通过语法检查、official/monthly/appointment focused Node tests（P1 修复后 26 项）、server/client typecheck、React App focused tests（36 项）、React scaffold tests（37 项）、全量 Vitest（68 项）、client build、`npm run smoke:exam-s69`、`AI_PROVIDER=mock npm run smoke:browser`、docs governance、完整 `npm test`（1006 项）和 `git diff --check`；Hypatia 提交前只读复审发现的非翰林首月差事草稿无法推进 P1 已修复，复核未发现新的 P0/P1/P2。S88.3 首月差事切片已通过 appointment/official/exam focused tests、`npm run smoke:exam-s69`、`npm run typecheck:server`、docs governance、完整 `npm test`（993 项）和 `git diff --check`；Beauvoir 提交前只读复审发现的兼容 `worldState` 嵌套 `hiddenNotes` P2 已修复，最终未发现 P0/P1/P2。S88.3 备考压力与入场反馈切片已通过语法检查、study/exam focused tests（24 项）、server/client typecheck、client App focused test、全量 Vitest（68 项）、client build、browser smoke、`npm run smoke:exam-s69`、docs governance、完整 `npm test`（997 项）和 `git diff --check`；Halley、Carson、Copernicus、Einstein 提交前只读复审发现的 readiness 缺口、public `entryPreparation` 投影、`studyProfileView`/`examProcedureView` 清洗、嵌套/根层 `examCalendar` 污染 P2 已修复，最终复核未发现新的 P0/P1/P2。S88.3 阅卷放榜同年座师过渡切片当前已通过语法检查、exam aftermath/honors/network focused tests（6 项）、server/client typecheck、全量 Vitest（68 项）、client build、`npm run smoke:exam-s69`、`AI_PROVIDER=mock npm run smoke:browser`、docs governance、React 脚手架测试（37 项）、完整 `npm test`（1000 项）和 `git diff --check`；Euclid 初审 P2 修复后已重跑语法检查、focused exam aftermath/honors/network tests（6 项）、`npm run typecheck:server`、docs governance、S69 smoke、完整 `npm test`（1000 项）和 `git diff --check`，Euclid 复核未发现新的 P0/P1/P2。S88.3 读书计划深化切片已通过语法检查、study/exam focused tests（21 项）、server/client typecheck、React App focused tests（36 项）、React scaffold tests（37 项）、全量 Vitest（68 项）、client build、`AI_PROVIDER=mock npm run smoke:browser`、`npm run smoke:exam-s69`、docs governance、完整 `npm test`（1000 项）和 `git diff --check`；Sartre/Jason 的只读接线建议已采纳，Sagan 提交前只读初审发现 2 个 P2（复审状态文档待回填、未跟踪 `npm-start*.log` 需排除提交）已修复，Sagan 复核未发现新的 P0/P1/P2。S88.3 考试入场后反馈切片已通过语法检查、exam focused tests、server/client typecheck、React App focused tests、React scaffold tests、全量 Vitest（68 项）、client build、`npm run smoke:exam-s69`、`AI_PROVIDER=mock npm run smoke:browser`、docs governance、完整 `npm test`（1001 项）和 `git diff --check`；Descartes 提交前只读复审发现的 sceneTime 直接投影白名单与旧 phaseFeedback 伪反馈 P2 已修复，最终复核未发现新的 P0/P1/P2。S88.2 完整基线为 `npm run typecheck:server`、`npm run build:server:probe`、SQLite focused tests（70 项）、docs governance、完整 `npm test`（991 项）和 `git diff --check`。S81-S85 完整基线另见 `docs/DEVELOPMENT_STEPS.md` 与 `docs/NPC_INVENTORY_SYSTEM_ARCHIVE.md`。

- Current S88.4 cross-role validation baseline: 跨身份奏议回应切片已通过语法检查、`test/officialCourtResponse.test.js`、topic/worldThread/official focused tests、event/topicDraft focused tests、role-world/world-thread route tests、React scaffold tests（37 项）、server/client typecheck、React App focused tests（36 项）、client build、全量 Vitest（68 项）、`npm run smoke:exam-s69`、`AI_PROVIDER=mock npm run smoke:browser`、docs governance、documentation governance、完整 `npm test`（1020 项）和 `git diff --check`；diff check 退出码 0，仅打印未改动归档/QA 文件既有 CRLF 提示。自检已补 `official_court_response` 事件/议程 read scope，并避免裸“部院”误判为奏议回应导致大臣/官员 generic 身份联动被跳过；Kepler 提交前只读初审发现终局裁决文案过滤偏窄 P2，已扩展 sanitizer 并新增准奏/照准/题准/奉旨准行/革职/拨给钱粮回归，Kepler 复核确认 P2 已关闭，未发现新的 P0/P1/P2。实现提交 `37a0c4d5`。

- Current S88.4 consequence validation baseline: 长期官场后果信号切片已通过语法检查、official/court-response/event/world-thread/monthly focused Node tests（32 项）、React scaffold tests（37 项）、server/client typecheck、全量 Vitest（68 项）、client build、`npm run smoke:exam-s69`、`AI_PROVIDER=mock npm run smoke:browser`、docs governance、documentation governance、完整 `npm test`（1024 项）和 `git diff --check`；diff check 退出码 0，仅打印未改动归档/QA 文件既有 CRLF 提示。Kepler/Ohm 的只读接线建议已采纳；Kepler 提交前只读复审未发现 P0/P1/P2，建议后续补连续月末不重复同一 source 与旧存档 raw consequence 污染全链路过滤回归。

- Current S88.4 continuation validation baseline: 皇帝/部院续办链路切片已通过语法检查、official-court-response/world-thread/consequence/event/monthly focused Node tests（35 项）、React scaffold tests（37 项）、server/client typecheck、全量 Vitest（68 项）、client build、`npm run smoke:exam-s69`、`AI_PROVIDER=mock npm run smoke:browser`、docs governance、documentation governance、完整 `npm test`（1027 项）和 `git diff --check`；diff check 退出码 0，仅打印未改动归档/QA 文件既有 CRLF 提示。Kepler 提交前只读初审发现 public evidence 暴露 raw `officialCourtResponses.response:*` namespace 与 `nextHandlerRole` 只展示不裁决两个 P2；主代理已改用 `courtResponseView.recentResponse:*` public ref、按 `nextHandlerRole === responseRole` 限制可提交续办链路并补回归，Kepler 复核确认 P2 已关闭，未发现新的 P0/P1/P2。实现提交 `fb2be42d`。

- Current S88.5.3 validation baseline: 角色循环入口后端接缝当前已通过 `node --check src/game/roleCycleDomainAdjudication.js`、`node --check src/routes/game.js`、`node --check src/game/clientWorldState.js`、`node --check src/game/audit.js`、focused Node tests `test/roleCycleDomainAdjudication.test.js` / `test/gameTurnRoleCycleConsequences.test.js` / `test/worldGeography.test.js` / `test/mapVisibility.test.js`（23 项）、redaction/economy focused tests `test/routeResponseContracts.test.js` / `test/cityPolicyHiddenRedaction.test.js` / `test/militaryDiplomacyRedaction.test.js` / `test/npcEconomy.test.js` / `test/gameTurnNpcEconomy.test.js`（15 项）、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`npm run typecheck:server`、完整 `npm test`（1045 项）和 `git diff --check`。Pauli 提交前只读初审指出只读入口泛触发 resolver ledger 与验证状态文字陈旧两个 P2，复审又指出 read-only cue + action noun 仍可能写 ledger；本轮已收窄 classifier、加入只读优先 suppression 并补只读入口回归；Pauli 最终复核确认两个 P2 均关闭且无 P0/P1/P2。实现提交 `b3a84bde`。

- Current S88.4 closeout update: 上一条首月回署草稿切片的完整 smoke/governance/完整测试/diff check 已补跑通过，包括全量 Vitest（68 项）、`npm run smoke:exam-s69`、`AI_PROVIDER=mock npm run smoke:browser`、docs governance、documentation governance、完整 `npm test`（1013 项）和 `git diff --check`；Chandrasekhar 初审 P2 修复后，Gauss 最终只读复核未发现新的 P0/P1/P2。

## Archives And Contracts

优先追溯入口：

- `docs/DEVELOPMENT_GOVERNANCE.md`
- `docs/QIANQIU_DEVELOPMENT_BRIEF.md`
- `docs/DEVELOPMENT_STEPS.md`
- `docs/NPC_INVENTORY_SYSTEM_ROADMAP.md`
- `docs/NPC_INVENTORY_SYSTEM_CONTRACT.md`
- `docs/NPC_INVENTORY_SYSTEM_ARCHIVE.md`
- `docs/QIANQIU_POLISHING_ROADMAP.md`
- `docs/TYPESCRIPT_BACKEND_MIGRATION_ROADMAP.md`
- `docs/TYPESCRIPT_BACKEND_MIGRATION_ARCHIVE.md`
- `docs/TYPESCRIPT_ROUTE_RESPONSE_COVERAGE_ROADMAP.md`
- `docs/TYPESCRIPT_ROUTE_RESPONSE_COVERAGE_ARCHIVE.md`
- `docs/ACTIVITY_LEDGER_COMPLETED_ARCHIVE.md`
- `docs/ARCHITECTURE.md`
- `docs/AI_CONTROL_AUDIT_MATRIX.md`
- `docs/DEPENDENCY_PLUGIN_GOVERNANCE.md`
- `docs/LOCAL_DATABASE_AND_WORLD_CONTENT_ARCHIVE.md`
- `docs/IMPERIAL_EXAM_DEEPENING_ARCHIVE.md`
- `docs/AI_ORCHESTRATION_ARCHIVE.md`
- `docs/DATABASE_GAMEPLAY_RESOLVER_ARCHIVE.md`
- `docs/PIXIJS_INK_MAP_ARCHIVE.md`
- `docs/FRONTEND_INK_REDESIGN_ARCHIVE.md`
- `docs/FRONTEND_VISUAL_ASSET_GUIDE.md`
- `docs/FRONTEND_ASSET_LEDGER.md`
- `docs/MAP_ASSET_LEDGER.md`

## Current Work Note

- 2026-05-22：推进 S88.7 主动来函后续 evidence 前端体验片，实现提交 `fa105ec5`。新增 React 共享 `NpcFollowUpEvidenceSection`，人物页在来函 inbox / 后续簿之后展示 `npcActiveRequestView.followUpEvidence.people/events/economy` 的引荐拜会、人情债月账、请托案牍和廉政/风宪/背叛风险 watchlist，史册页在公开归档与领域后果旁展示“来函证据追踪”；按钮只写 `role-surface` / `archive-view` 本地行动草稿，不调用 `/api/game/turn`，不结算资源、人情债、婚姻、弹劾、定罪、背叛、交易、NPC 行动或 hidden facts。`SurfaceHost` 会把 `npcActiveRequestView` 材料标成“来函后续”，把 evidence checkbox 标成“来函证据 · 人物/月账/案牍”；`surfaceRegistry` 的奏折、人物、朝议、堂审数据来源同步写入 `npcActiveRequestView`。首页旧案卷与继续本局摘要的安全过滤扩展到 `S7x`、`smoke`、`placeholder`、`debug`、`验收`、`开发注释`、`实现说明` 等开发文案，避免本地历史开发存档污染玩家首页。James 提交前只读初审发现两个 P2：人物页可能渲染非当前 route session 的主动来函 evidence，以及完全污染的 evidence 行会 fallback 成可点击卡片；本轮已改为只读 `activeSession` 且整行丢弃核心污染 evidence，并补 stale session / polluted row 回归。已通过 `npm run typecheck:client`、focused App Vitest（38 项）、完整 client Vitest（73 项）、`AI_PROVIDER=mock npm run smoke:browser`、docs governance、documentation governance、完整 `npm test`（1107 项）和 `git diff --check`；James 最终只读复核确认两个 P2 已关闭，未发现新的 P0/P1/P2。
- 2026-05-22：`fa105ec5` 提交哈希回填为低风险纯文档同步，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
- 2026-05-22：推进 S88.7 主动来函后续领域 evidence 切片，实现提交 `73d6540d`。`npcActiveRequestsConfig` 新增 `followUpEvidence` schema/config 和 cap，`buildNpcActiveRequestView()` 从安全 `followUpTasks` 派生 `followUpEvidence.items/people/events/economy`，把引荐拜会、人情债月账、请托案牍、献策公开证据、廉政/风宪/背叛风险 watchlist 整理为 read-only、proposal-only、server-owned 材料。`resolverInputConfig` / `resolverInputContext` 接入 `npcActiveRequestView.followUpEvidence` 的 people/events/economy source collections，`topicSurfaceView` 让奏折、朝议、堂审和人物档案 surface 按 `topicSurfaceIds` allowlist 只读消费，`safeWorldSearch` 与 SQLite `safe_search_index` 同步索引这些公开行；客户端 API 类型已补 `NpcActiveRequestFollowUpEvidenceView`。边界仍是不创建真实任务、不结算资源/婚姻/弹劾/定罪/背叛/人情债、不采纳浏览器草稿为授权、不读取 raw ledger 或 provider/prompt/SQLite/path/key。已通过语法检查、S88.7 focused tests、server/client typecheck、docs governance、documentation governance、完整 `npm test`（1107 项）和 `git diff --check`；Poincare 提交前只读初审发现 topic surface 未执行 `topicSurfaceIds` 的 P2，主代理已在 resolver input 保留 surface ids、topic surface 按 surface 过滤并补回归，Poincare 复核确认 P2 已关闭，未发现新的 P0/P1/P2。非阻断残余风险：`sourceViews` 统计仍来自原始 resolver buckets，当前不会扩大 evidence 展示或裁决。
- 2026-05-22：`73d6540d` 提交哈希回填为低风险纯文档同步，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
- 2026-05-22：推进 S88.7 主动来函后续公开回响切片，实现提交 `c08eb0fd`。`eventArchiveView` 的 `npc_active_request` 条目现在沿用原 resolver public ref，并从 safe view / canonical `followUpResolutions` 展示近次后续 public summary、下一步和风险标签；`actorMemoryLedger` 新增 `npc_active_request_follow_up` 来源，只接受本旬 `npc_active_request_follow_up_resolver` 后续记录且必须命中 safe view、canonical resolutionId/ref 和 `serverOwnsFollowUp` 边界；`worldThreadView` 的 `active_npc_request` 议题会引用最新安全后续摘要与 `createdTurn`。本轮同步扩展事件档案和记忆清洗别名，覆盖 `provider_payload`、`private_signal_tags`、`hidden_dossier`、`safe_search_index`、`true_assets`、`secret_relationships`、`unrevealed_tasks`、`state_patch` 等旧存档污染写法；边界仍是公开回响，不创建真实任务、资源、婚姻、弹劾、定罪、背叛或 hidden truth 结果。已通过语法检查、focused event archive / actor memory / world thread / NPC route / active request tests、`npm run typecheck:server`、docs governance、documentation governance、完整 `npm test`（1103 项）和 `git diff --check`；Franklin 提交前只读复审未发现 P0/P1/P2，非阻塞残余风险是 `worldThreads` 安全性依赖继续只消费 `npcActiveRequestView` safe projection。
- 2026-05-22：`c08eb0fd` 提交哈希回填为低风险纯文档同步，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
- 2026-05-21：推进 S88.7 主动来函 follow-up 安全 view 切片，实现提交 `e5ab3417`。`npcActiveRequestsConfig` 新增 response/follow-up 配置，`npcActiveRequestView.items[].responseOptions` 为每类来函提供服务器选定的前三个回应草稿；resolved `outcome.followUpView` 公开 follow-up kind、摘要、下一步、证据 refs、风险标签和边界，覆盖行贿、弹劾、背叛、引荐、请托、索债/人情债、献策、求助和议婚。`eventArchiveView` 的 `npc_active_request` 条目和 `actorMemoryLedger` 的 `npc_active_request_trace` 记忆会带上 follow-up 上下文；主动来函记忆派生已要求 trace ref 命中 canonical active request safe record，同 ref 篡改字段只使用服务器记录里的 resolver trace；React 人物页来函 inbox 只对 `active` / `deferred` 来函按服务器 response options 写本地行动草稿，已裁决 follow-up 只显示后续说明。已通过语法检查、`test/npcActiveRequests.test.js`、`test/eventArchive.test.js`、`test/actorMemoryLedger.test.js`、NPC route/safety/event focused tests、world thread/entity focused tests、server/client typecheck、React App focused test、受控 worker client focused tests、完整 client Vitest（72 项）、client build、docs governance、documentation governance、`git diff --check` 和完整 `npm test`（1098 项）；未加受控 worker 的 focused client Vitest 曾出现 fork worker 启动超时，随后同目标受控 worker 重跑通过，240 秒 `npm test` 也曾超时截断，随后 720 秒重跑完整通过。Franklin 提交前只读初审发现主动来函记忆仍可被非 canonical trace 影响的 P2；本轮已修复并新增 forged visible 与同 ref 篡改回归。Franklin 复审又发现已裁决 follow-up 仍显示回应按钮的 P2；本轮已限制 `active` / `deferred` 才返回/渲染回应选项并补 view 回归。Franklin 最终复核确认两个 P2 均关闭，未发现新的 P0/P1/P2；残余风险仅是 `followUpView.recommendedResponseOptions` 仍作为 draft-only 上下文保留，当前 PeoplePage 不渲染。
- 2026-05-21：`e5ab3417` 提交哈希回填为低风险纯文档同步，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
- 2026-05-22：推进 S88.7 主动来函后续簿安全投影切片，实现提交 `18e489e5`。`npcActiveRequestsConfig` 新增 follow-up task schema、taskRoute/taskRouteLabel/taskDraftTemplate 与 `maxFollowUpTasks`；`buildNpcActiveRequestView()` 现在从已裁决安全 item 派生 `followUpTasks`，只收录 `under_review`、`reported`、`converted_to_risk` 和 `accepted_pending_server_resolution`，不把 `active`、`deferred`、`refused` 或 `expired` 变成后续任务。每条任务公开 task route、状态、标题、摘要、下一步、draftText、NPC 摘要、证据 refs、风险标签和 proposal-only/server-owned 边界；React 人物页新增“来函后续簿”，按钮只写本地草稿。已通过语法检查、`test/npcActiveRequests.test.js`、event archive / actor memory / world threads focused tests、server/client typecheck、完整 client Vitest（72 项）、client build、docs governance、documentation governance、`git diff --check` 和完整 `npm test`（1098 项）。Franklin 提交前只读复审未发现 P0/P1/P2；残余非阻塞缺口是暂无专门 React click 测试断言“拟后续”只写草稿且不调用 API，`active` / `expired` 不入后续簿主要由实现过滤与复审覆盖。
- 2026-05-22：`18e489e5` 提交哈希回填为低风险纯文档同步，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
- 2026-05-22：推进 S88.7 主动来函后续簿服务器登记切片，实现提交 `a16e34df`。`npcActiveRequestsConfig` 新增后续复核 resolution schema、每类来函 resolution 状态/摘要/下一步和匹配关键词；`runNpcActiveRequestStep()` 现在先用安全 `npcActiveRequestView.followUpTasks` 识别“续办/后续/复核”输入，再回写 canonical request 的 `outcome.followUpResolutions`、`followUpView.latestResolution`、`auditRefs` 和 `[NPC 后续]` 事件。`npcActiveRequests.outcome.followUpResolved` / `followUpResolutions` 随普通回合返回，`npcActiveRequestView.followUpTasks[].latestResolution` 与人物页后续簿展示近次服务器登记；污染输入不会生成 resolution。边界：该 resolver 只登记公开复核记录和证据 refs，不扣资源、不转物、不写配偶/婚姻、不成弹劾/处分/定罪、不处置背叛 hidden truth、不创建真实任务队列。已通过语法检查、focused NPC/route tests、server/client typecheck、完整 client Vitest（72 项）、client build、docs governance、documentation governance、`git diff --check` 和完整 `npm test`（1101 项通过）。Franklin 提交前只读复审先发现失败 follow-up attempt 可经“复核/核验”或“登记+线索”泛词回落/误劫持普通 active 来函的 P2；本轮已统一 follow-up cue 与 task-specific signal、过滤通用动词和弱任务词，并补污染 `复核引荐`、普通 `先查证来意`、普通 `先登记线索，再查证来意` 回归。Franklin 最终复核确认 P2 均关闭，未发现新的 P0/P1/P2；残余风险是后续新增 `taskIntentKeywords` 时仍需避免把泛化词重新放入 task-specific signal。
- 2026-05-22：`a16e34df` 提交哈希回填为低风险纯文档同步，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
- 2026-05-21：推进 S88.7 `/npc-interaction` 即时记忆与前端刷新切片，实现提交 `bec67aab`。`actorMemoryLedger` 新增 `npc_relationship_action_trace` 来源，只从本次 `recordNpcInteraction()` 写入的安全记录派生论道、切磋、求爱和议婚记忆；孤立 trace 或伪造不可见 NPC 记录会因 `requireKnownActor` 被拒，且同 ID/ref/npcId 的替换对象也只能使用 canonical `npcInteractionLedger.records` 内容派生记忆。`POST /api/game/npc-interaction/:sessionId` 在关系行动裁决后调用 `applyTurnActorMemoryUpdates()`，响应新增安全 `actorMemory` 摘要、`actorMemoryView` 和 `eventArchiveView`，仍不返回 raw ledger 或 worldState；`client/src/state/gameSessionState.ts` 会把这些投影合并进当前 session，并按 session/NPC 守住 stale payload 不污染另一案卷的 `npcDetail` / `npcRoster` / `lastNpcInteraction`。已新增 actor memory、NPC 路由和 UI store 回归测试；语法检查、actor memory/route/NPC/event focused tests、完整 client Vitest（72 项）、server/client typecheck、docs governance、documentation governance、完整 `npm test`（1095 项）和 `git diff --check` 已通过。Franklin 提交前只读复审先发现 stale session 与关系行动记忆 provenance 两个 P2，复核又指出同 ref 替换内容残口；本轮已改为 canonical ledger record 派生并补回归，Franklin 最终复核确认所有 P2 关闭，未发现新的 P0/P1/P2。
- 2026-05-21：`bec67aab` 提交哈希回填为低风险纯文档状态更新，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
- 2026-05-21：推进 S88.6 topic draft 与下一轮服务器裁决 canonical echo 审计链，实现提交 `9a7a934b`。新增 `domainConsequenceEchoRefs` 公共清洗工具；`eventArchiveView`、`resolverInputContext`、`worldThreadView` source collection、`topicSurfaceView` 和 `topicDrafts` 现在都保留安全 `canonicalEchoRefs`，同一公开领域后果经 direct domain consequence、事件档案或 world thread 进入同一 topic surface 时按 canonical echo 去重并优先保留 direct evidence。`topic_draft` provider context 与响应会带从服务器 evidence 派生的 `canonicalEchoRefs`；React surface 写入底部草稿时附带 `draftContext`，主卷提交后 `/api/game/turn` 只把它当审计 hint，并用当前服务器 `buildTopicSurfaceView()` 重新校验 `surfaceId` / `evidenceRefs` 后反推 echo refs，伪造 echo 会被忽略。`roleCycleDomainAdjudication` 接收已校验的 draft context 后，把 echo 审计链写入公开 outcome、内部 auditRecord 和最新 accepted city/military ledger record；近 3 旬重复触发仍按 actor/intent/evidence refs 拦截并可回显同一安全 echo，但新 evidence、新 intent 或冷却后的普通回合仍可重新裁决。Poincare 提交前只读初审发现 3 个 P2：accepted public outcome 回传 `outcomeId`、手动编辑奏折保留 stale `draftContext`、交接验证状态未回填；本轮已移除 public `outcomeId`、让手动改写清除 draftContext、补 accepted payload / UI store 回归，并重跑修复后的语法、focused tests、server/client typecheck、docs governance、client Vitest（70 项）、client build、完整 `npm test`（1083 项）和 `git diff --check`；Poincare 最终复审确认三项 P2 均已关闭，未发现新的 P0/P1/P2。
- 2026-05-21：`9a7a934b` 提交哈希回填为低风险纯文档状态更新，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
- 2026-05-21：推进 S88.7 NPC/关系 trace 长期回响片，实现提交 `ad44a420`。`eventArchiveView` 新增 `npc_active_request` 与 `npc_relationship_action` 来源，只从 `npcActiveRequestView({ includeResolved: true })` 和 `npcInteractionView` 收录已清洗 resolver trace；普通 turn 现在把本旬 `npc_active_request_resolver` trace 传给 `actorMemoryLedger`，为已知 NPC 写入 `npc_active_request_trace` 可见记忆，并拒绝伪造/不可见 NPC refs。`actorMemoryView` 的可见 actor label map 纳入公开 `npcRosterView`，使 roster NPC 的来函记忆能显示，但 provider 仍不能写 private/hidden memory 或未知 actor。验证已通过语法检查、actor memory / event archive focused tests、NPC/relationship/safety focused tests（14 项）、route/NPC focused tests（11 项）、world thread/entity focused tests（16 项）、server/client typecheck、docs governance、documentation governance、完整 `npm test`（1091 项）和 `git diff --check`；Carver 提交前只读复审未发现 P0/P1/P2，建议后续若复用该 helper 可考虑要求 trace 命中当前 safe request record 后再写泛化记忆。
- 2026-05-21：`ad44a420` 提交哈希回填为低风险纯文档状态更新，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
- 2026-05-21：推进 S88.7 NPC/关系 resolver trace 首片，实现提交 `875a0147`。主动来函回应/逾期写入 `npc_active_request_resolver` trace 和 turn `outcome.resolutionTraces`，论道/切磋/求爱/议婚写入 `npc_relationship_action_resolver` trace 并随 `npcInteractionView` 返回；`worldThreadView` 从安全 `npcActiveRequestView` 生成新主动来函议题，`worldEntities` 从 scheduled / resolver trace 派生有限公开压力。新增清洗回归覆盖旧账身份字段、route view、interaction trace、ignored client key、safe-search 表名和 camelCase/snake_case unsafe aliases；仍不直接扣资源、转物、写婚姻、成弹劾/定罪、处置背叛或公开 hidden 私档。验证已通过语法检查、focused NPC/relationship/safety tests（14 项）、route/world focused tests（19 项）、server/client typecheck、docs governance、documentation governance、完整 `npm test`（1088 项）和 `git diff --check`；Poincare 提交前只读复审发现的 P2 均已修复，最终未发现 P0/P1/P2。
- 2026-05-21：推进 S88.6 长链路后果回响与公开 echo ref，实现提交 `12fb20bf`。`domainConsequenceView.recentConsequences` 新增安全 `publicEchoRef`，由内部去重键派生，不暴露 `publicSourceId`、outcomeId、raw evidence、delta、audit 或 ledger path；`playerMonthlyBriefing` 会用已归档月报的 domain source refs 跳过已入报后果，避免连续月报重复把同一后果放入 sourceRefs/actionItems/riskItems，空后果行不再生成 fallback 伪 sourceRef，且新格式只按 stable echo/id/source 去重，不用标题误挡同题新后果；`worldThreads` 的 domain consequence thread id/sourceId 改用 `publicEchoRef`，并仅对 domain consequence 按来源回响去重 `recentResolved`，角色可见性反复切换时不会重复归档；`roleCycleView` 为地方官、官员、大臣、将领和皇帝新增只读 `domainConsequenceView` 复核事务/入口/草稿，大臣和皇帝会过滤同一后果经 world thread 间接重复进入的条目，复核草稿不再嵌入后果标题或稳价/调粮等执行词，地方官与将领的通用处置/军务 action 也排除 domain item。McClintock 只读探查指出连续月报重复、resolved 重复和 role-cycle 间接/重复消费缺口，本轮采纳；Maxwell 提交前只读初审发现 role-cycle 草稿误触发写裁决与月报标题级误去重两个 P1，复审又发现将领通用军务 action 仍会消费 domain item 的 P1；主代理已修复并补 classifier/同题后果/全量 domain action 回归，Maxwell 最终复核未发现 P0/P1/P2。当前已通过语法检查、focused domain/monthly/world-thread/role-cycle/domain-adjudication tests（51 项）、server/client typecheck、docs governance、documentation governance、完整 `npm test`（1079 项）和 `git diff --check`；本哈希回填为低风险纯文档状态更新，按项目规则跳过额外子代理复审。
- 2026-05-21：推进 S88.6 普通回合重复触发与跨视图 cap 压力红队，实现提交 `428a26bb`。`roleCycleDomainAdjudication` 新增近 3 旬重复裁决拦截：同一当前角色、resolver 意图和清洗后的公开 evidence refs 已在 `cityPolicyLedger` / `militaryDiplomacyLedger` 有 accepted 记录时，普通回合再次提交只返回 `duplicate_recent` 反馈，不重复调用 city/military apply、不追加 ledger、不扣钱粮或军需，也不产生角色循环 attribute changes；重复反馈只暴露安全 resolver、intent、label、公开 evidence refs 计数和冷却说明，不回显 raw input、outcomeId、state/player delta、auditRecord、raw ledger、hidden evidence、SQLite 行、provider payload、prompt、本地路径或 key。重复匹配要求 ledger row 带显式 `actorRef.actorId`，旧存档缺 actor 身份的 accepted 行不会挡住当前玩家首次处置。跨视图 cap 回归新增 official 高量后果与 general 角色裁剪高量场景，确认 `domainConsequenceView` 的 source/global cap 会一致传入事件档案、resolver input、topic surface、safe search 与 SQLite safe-search，且被 cap 掉的 city/older rows 或低权限不可见类型不会在下游复活；`resolverInputContext` 在同领域/同置信度下按 source collection 顺序稳定排序，确保 direct `domainConsequenceView` evidence 不被 `eventArchiveView` 中转条目挤出 events cap。Raman 只读探查确认重复触发缺口与 cap 组合缺口；Banach 提交前只读初审发现 legacy actor wildcard 与 direct consequence 空数组断言两个 P2，主代理已修复并重跑 focused/full 验证，Banach 最终复核未发现 P0/P1/P2。
- 2026-05-21：`428a26bb` 提交哈希回填为低风险纯文档记录，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
- 2026-05-21：推进 S88.6 角色可见性与舆图运行时后果 effect 红队，实现提交 `7ee4ae6f`。`domainConsequenceView` 现在在服务端按当前 `player.role` 裁剪 `recentConsequences`、`nextActions`、`counts` 和 cap 语义：书生默认不接收领域后果，地方官只接地方政策/刑名/NPC 经济，将领只接军务外交，官员/大臣/皇帝接跨域后果；`caps.publicCandidates` / `roleEligibleCandidates` 只统计当前角色可见候选，不向低权限身份泄露被过滤后果数量。事件档案、resolver input、topic surface、`topic_draft`、safe search 和 SQLite safe-search 均通过裁剪后的 view 消费，不能从全量后果旁路复活。`mapRuntimeView` 新增 visual-only domain consequence `eventEffects`，只把可见后果绑定到已有可渲染 runtime refs（如军务->边面/军报、地方政策->市况/辖区、刑名->案牍），sourceRefs 只用 `domainConsequenceView:<safe-id>`，route 共用同一份 `domainConsequenceView` 构建 map runtime，避免页面 view 与舆图 effect 不一致。Ptolemy 提交前只读复审未发现 P0/P1/P2，并确认低权限旁路、cap 泄漏、runtime effect 扩散和 route/exam 不一致风险均已处理。当前已通过语法检查、domain/map runtime/resolver/topic/archive/search/SQLite/map route/topic-draft focused tests、server/client typecheck、docs governance、documentation governance、完整 `npm test`（1066 项）、受控完整 client Vitest（69 项）、client build、`AI_PROVIDER=mock npm run smoke:browser` 和 `git diff --check`；默认 `npm run test:client` 首轮因 fork worker 启动超时失败，随后以 `--pool=vmForks --maxWorkers=2` 重跑通过，记录为环境并发型失败而非断言失败。
- 2026-05-21：`7ee4ae6f` 提交哈希回填为低风险纯文档记录，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
- 2026-05-21：推进 S88.6 地图/史册后果追踪入口与 high-volume cap 可用性红队，实现提交 `eee0185a`。`domainConsequenceView` 现在先筛选 accepted/applied/recorded 且已应用的公开行，再按应用旬排序并套每账本 source cap，避免旧存档末尾的 pending/rejected/污染行或乱序行把较早有效/较新有效后果挤出；view 新增 `caps.publicCandidates` / `visibleConsequences` / `capped` 和 `trackingEntryPoints`，公开“入舆图追踪 / 入史册归档”入口。`safeWorldSearch` 在全局行数 cap 下保留 `domainConsequenceView.recentConsequences` 行，防止地理/人物/report 噪声把后果搜索结果挤掉。React 舆图页新增“舆图后果追踪”，史册页改为读取安全 `eventArchiveView` 与 `domainConsequenceView` 的正式页面；两处按钮只写 `map-runtime` / `archive-view` 草稿，不调用 turn API、不裁决军务/刑名/财政/NPC 经济。Kant 只读探查建议已采纳；Poincare 首轮只读复审未发现 P0/P1/P2，低风险建议已补前端 deny list、`caps` 类型和乱序旧账回归，最终复审仍未发现 P0/P1/P2，仅建议后续清理 `npcEconomyConsequence()` 重复空数组判断并可进一步收紧乱序 cap 断言。当前已通过语法检查、domain/search/SQLite/event/topic focused tests、server/client typecheck、focused React App test（37 项）、完整 client Vitest（69 项）、client build、`AI_PROVIDER=mock npm run smoke:browser`、docs governance、documentation governance、完整 `npm test`（1062 项）和 `git diff --check`；本哈希回填为低风险纯文档状态更新，按项目规则跳过额外子代理复审。
- 2026-05-21：完成 S88.6 前端可见领域后果追踪片，实现提交 `0b863158`。新增 React `DomainConsequenceSection`，只消费安全 `domainConsequenceView.recentConsequences` / `nextActions`，二次过滤 provider/raw/prompt/path/key/hidden/SQLite、`stateDelta`、`playerDelta`、`evidenceRefs`、`outcomeId`、`auditRecord`、raw ledger 名称、safe-search 表名和本地路径形态文本；地方官面板展示 `city_policy` / `judicial_case` / `npc_economy` 后果，将领面板展示 `military_diplomacy` 后果，官员/大臣与皇帝面板展示跨域后果/天下余波，`SafePlayerPayload.routeViews` 和主卷安全视图索引新增 `hasDomainConsequenceView` / “后果”。按钮只写 `role-surface` 行动草稿，不提交 `/api/game/turn`，不新增后端写 API、resolver 或持久账本。Cicero 只读探查建议采用。Ampere 提交前只读初审发现无 `sourceTypes` 面板会展示未绑定可见后果 action 的 P2；本轮已改为显式 action 必须匹配当前可见 item，并补官员/皇帝 orphan action 回归，Ampere 复审确认 P2 关闭且未发现 P0/P1/P2。当前已通过 `npm run typecheck:client`、`node --test --test-reporter=spec test/reactClientScaffold.test.js`（39 项）、focused React/UI state/composer Vitest（54 项）、`AI_PROVIDER=mock npm run smoke:browser`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、完整 `npm test`（1052 项）和 `git diff --check`；本哈希回填为低风险纯文档状态更新，按项目规则跳过额外子代理复审。
- 2026-05-21：推进 S88.6 inactive role 与高风险军务绕过红队，实现提交 `563c911e`。`roleCycleDomainAdjudication` 的高风险军务词门禁扩展到发兵、动兵、调兵、攻取、攻伐、突袭、奇袭、强攻、围城、合围、鏖战、接战、交战、动员、请战、扣使、扣留、mobilize、engage、assault、siege 等形态；将领草稿若把这些词混入“军议/战事档案/侦察/调粮”句，不会走低风险 scout/resupply 接缝。新增当前身份 gate：只有 `player.role === "magistrate"` 才能走市价接缝，只有 `player.role === "general"` 才能走军议接缝；`official` 即使因官衔被 actorProfile 推断为知县或将领，也不会静默借用地方官/将领 role-cycle resolver，后续若支持入仕地方官或武职官员同类处置，应新增 official 专属 resolver 或显式服务器裁决身份。当前已通过 role-cycle focused tests、普通回合 focused tests、军务 resolver/权限 focused tests、`npm run typecheck:server`、docs governance、documentation governance、完整 `npm test`（1058 项）和 `git diff --check`；Kierkegaard 只读探查与提交前复审建议已采纳，最终未发现 P0/P1，文档 gate 描述与复审状态 P2 已修复。本哈希回填为低风险纯文档状态更新，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
- 2026-05-21：推进 S88.6 旧存档污染与重复后果去重红队，实现提交 `620d2d1f`。`domainConsequenceView` 现在只接受 `accepted` / `applied` / `recorded` 且带 `appliedAtTurn` / `generatedAtTurn` / `lastTickTurn` 的公开记录，防止 `pending`、`rejected` 或仅拟议行被旧存档重放为公开后果；若 resolver 提供安全 `publicSourceId`，view 用内部指纹去重并保留较新、指标更完整的公开后果，不把 `publicSourceId` 原值放进 JSON。文本清洗新增环境变量密钥片段检测，并覆盖 `privateResultRefs`、`hiddenDossier`、`sealedMapping`、`retrievalContext`、`actorMemoryLedger`、`sessionSummary`、`relationshipLedger`、`providerPayload`、`rawEvidence` 等旧别名污染。Zeno 只读探查建议已采纳；提交前只读复审未发现 P0/P1/P2，并额外确认 `publicSourceId` 原值未进入 `domainConsequenceView` JSON。当前已通过语法检查、domain/topic/resolver/search/SQLite/topic-draft/route-redaction focused tests、`npm run typecheck:server`、docs governance、documentation governance、完整 `npm test`（1054 项）和 `git diff --check`；本哈希回填为低风险纯文档状态更新，按项目规则跳过额外子代理复审。
- 2026-05-21：完成 S88.6 topic/source evidence 与安全检索接线，实现提交 `d5f1055b`。本轮把安全 `domainConsequenceView.recentConsequences` 加入 resolver input 的 `events` source collection，并接到 `memorial-review`、`edict-draft`、`court-debate`、`trial`、`war-council` topic surface；`topic_draft` 可引用公开 consequence evidence 拟稿，但只读 safe refs，不执行 resolver、不推进时间、不写 canonical state。`safeWorldSearch` 现在可从同一安全 view 生成 `domainConsequenceView.recentConsequences` 搜索行，SQLite `safe_search_index` 仍通过既有 `buildSafeSearchRows()` 单向同步，不新增 persistent consequence ledger 或新 SQLite 表。raw `cityPolicyLedger` / `militaryDiplomacyLedger` / `judicialCaseLedger` / `npcEconomyLedger`、resolver outcomeId、`stateDelta` / `playerDelta`、资源消耗、关系信号、auditRecord、raw SQL/table/path/key 等内部字段继续被过滤；topic surface / topic draft 保留公开字段名 `evidenceRefs`，但不得接收 raw evidence ref 值。Gibbs 只读探查建议已采纳；Lovelace 提交前只读复审未发现 P0/P1/P2。当前已通过语法检查、focused domain/topic/search/SQLite tests、`npm run typecheck:server`、docs governance、documentation governance、完整 `npm test`（1051 项）和 `git diff --check`。
- 2026-05-21：`d5f1055b` 提交哈希回填为低风险纯文档记录，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
- 2026-05-21：完成 S88.6 公开领域后果 refs 首片，实现提交 `6943c7de`。新增 `src/game/domainConsequenceTrace.js` / `domainConsequenceView`，把服务器已裁决的 `cityPolicyLedger`、`militaryDiplomacyLedger`、`judicialCaseLedger` 和安全 NPC 经济月账整理为公开后果追踪 refs；public sourceId 用稳定哈希后缀，不复用可能含 evidence ref 的 resolver outcomeId，也不信任 caller-supplied `publicSourceId`，view 不暴露 `stateDelta` / `playerDelta`、资源消耗、关系信号、evidence refs、auditRecord、raw ledger 名称、本地路径、key 或数据库内部行。`eventArchiveView` 已新增 `domain_consequence` 条目，`worldThreadView` 新增领域后果议题，`playerMonthlyBriefing` 纳入近次领域后果 sourceRefs、职责摘要、行动建议和风险提示；game/exam route contract 返回 `domainConsequenceView`。`judicialCaseLedger` 已补入 `RAW_LEDGER_KEYS`、`buildClientWorldState()` 和 redacted state forbidden keys，防止刑名账本从兼容 public `worldState` 外泄。Kuhn/Aristotle 的只读接线建议已采纳；Lovelace 提交前只读复审发现的 public `affectedMetrics`、内部 token 过滤和 `publicSourceId` 覆盖风险已修复并复核关闭，最终未发现 P0/P1/P2。当前已通过语法检查、focused tests、server/client typecheck、docs governance、documentation governance、完整 `npm test`（1048 项）和 `git diff --check`（退出码 0；仅打印未改动归档/QA 文件既有 CRLF 提示）。
- 2026-05-21：`6943c7de` 提交哈希回填为低风险纯文档记录，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
- 2026-05-21：推进 S88.5.2 跨域入口与证据 refs，实现提交 `871c3a47`。`roleCycleView` 已补齐 `marketPriceView`、`npcEconomyView`、`mapRuntimeView`、`eventArchiveView` builders，并为当前身份循环增加 capped `entryPoints`、`evidenceRefs` 和 prompt compact summary；地方官循环可只读指向市价、人物月账、升堂审案，将领循环可只读指向舆图、军议和战事档案。前端 `RoleCycleSection` 增加“证据来源”和“可查入口”，入口只走 route allowlist 或打开本地 surface，不调用 `/api/game/turn`，草稿按钮仍只写本地行动草稿。地图 runtime evidence 只保留 ref/label/summary/route，不暴露 `layout`、`layoutPath`、`mapBounds`、`assetSetId`、坐标或 raw map projection。Gibbs 已只读梳理后端 source view 缺口，Cicero 已只读梳理前端入口接线；两者建议已采纳。已通过 role cycle focused tests（9 项）、React scaffold（38 项）、browser smoke script（44 项）、client Vitest（68 项）、server/client typecheck、client build、docs governance、`npm run smoke:exam-s69`、`AI_PROVIDER=mock npm run smoke:browser`、完整 `npm test`（1037 项）和 `git diff --check`（退出码 0；仅打印未改动归档/QA 文件既有 CRLF 提示）；Peirce 提交前只读初审未发现 P0/P1/P2，仅指出入口可见重复的低风险 polish，本轮已补 dedupe 与回归并重跑 focused role cycle tests、server/client typecheck、browser smoke 和完整 `npm test`。Peirce 最终只读复审确认无 P0/P1/P2 且无残留低风险 gap。`npm run smoke` 当前不是 `package.json` 脚本，验证以现有 `smoke:*` 脚本为准。
- 2026-05-21：推进 S88.5.3 角色循环入口后端接缝，实现提交 `b3a84bde`。新增 `src/game/roleCycleDomainAdjudication.js`，在普通回合 `runRoleWorldCouplingStep()` 后、world tick 前识别低风险身份循环后续：地方官“处置市价/平粜稳价”只从当前 actor 可见 `market` evidence 取 refs 并调用 `resolveAndApplyCityPolicy()`；将领“舆图军议/战事档案”后的明确侦察或调粮只从可见 `military/geography/intel/market` evidence 取 refs 并调用 `resolveAndApplyMilitaryDiplomacy()`；`查市价`、`查看平粜旧案`、`复核稳价记录`、`据舆图开军议`、`开军议查看战事档案`、`开军议查看补给记录`、`翻看侦察案卷` 等只读入口不写 resolver ledger，含会战/夜袭/出击/进兵/宣战等高风险词的输入也不走该接缝，仍由既有军务/身份联动边界处理；人物月账入口返回 `read_only` 反馈，不即时写 NPC 资产、关系、交易或经济账本。`/api/game/turn` 和 SSE preview 新增 `roleCycleDomainAdjudication` 反馈，`src/contracts/serverContracts.ts` 与 `src/routes/routeResponses.js` 把 `cityPolicyLedger` / `militaryDiplomacyLedger` 纳入 raw ledger 剥离；`buildClientWorldState()` 现在用 `worldGeographyView` 替换兼容 `worldState.worldGeography`，避免 hidden route/name 回流 public payload。Dewey 只读梳理确认应复用 city/military resolver 而非新增 ledger/API；Pauli 初审/复审 P2 已按建议修复，最终复核确认无 P0/P1/P2。
- 2026-05-21：推进 S88.5.1 六身份循环矩阵首片，实现提交 `8d24534e`。新增 `src/game/roleCycleConfig.js` / `src/game/roleCycleView.js`，由服务器从各身份现有安全 view 即时派生 `roleCycleView`，并通过 game/exam route、server/client contract、resolver input 和 prompt context 暴露 capped 当前身份摘要；不新增 persistent ledger，不暴露 raw ledger、provider payload、完整 prompt、本地路径、key、SQLite 物理表行、hidden notes 或其他身份 hidden 信息。React 新增 `RoleCycleSection` 并接入书生、地方官、官员/大臣、将领和皇帝面板，统一显示本旬事务、风险与草稿按钮；按钮只写本地行动草稿。`docs/AI_CONTROL_AUDIT_MATRIX.md` 已补六身份循环矩阵 AI 权限行。Sartre/Jason 的只读接线建议已采纳；完整验证已通过，Mencius 提交前只读初审仅发现文档验证状态待回填 P2，修正后复核确认 P2 关闭且未发现新的 P0/P1/P2。
- 2026-05-21：`8d24534e` 提交哈希回填为低风险纯文档记录，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
- 2026-05-20：完成 S88.0-S88.1 全面系统打磨规划与 AI provider 安全 envelope，实现提交 `5c0665ee`。新增 `docs/QIANQIU_POLISHING_ROADMAP.md`，把用户要求的完整书生主线、多身份玩法、官场世界系统、AI 世界引擎、NPC/关系、资产经济、React 前端、PixiJS 地图、视觉立绘、后端类型安全和 Mock/真实 provider 验收拆为 S88.1-S88.12。S88.1 新增 `src/ai/providerSafety.js`，统一 provider 错误/诊断文本脱敏，`src/ai/index.js` 的 fallback warning 不再回显 raw provider payload、request/response body、base URL、key 或本地路径；`src/ai/diagnostics.js` 的 connection-test 叙事 preview 与错误摘要也走同一安全清洗。`src/ai/providers/remoteHelpers.js` 已进入 `npm run typecheck:server`，新增 AI prompt task / remote requester JSDoc；`src/contracts/serverContracts.ts` 增加 `AiRemoteTaskEnvelope`、`AiPromptTaskEnvelope`、requester 类型和 public AI forbidden fields，`src/routes/routeResponses.js` 会运行时拒绝 AI connection public envelope 携带 forbidden fields。`aiControlAudit` 与 `redactedState` 对敏感本地路径先按原始文本判定并丢弃，避免脱敏占位进入玩家/诊断安全出口。已通过 focused tests、`npm run typecheck:server`、`npm run build:server:probe`、docs governance、完整 `npm test`（991 项）和 `git diff --check`；Confucius 提交前只读复审发现的 provider body 脱敏与嵌套 forbidden field P1/P2 已修复，最终未发现 P0/P1/P2。
- 2026-05-20：完成 S88.2 SQLite derived row builder 类型边界，实现提交 `c4a97021`。`src/contracts/serverContracts.ts` 已固定 `SqliteWorldSessionRow`、`SqlitePromptRetrievalRow`、`SqliteSafeSearchIndexRow`、`SqliteSafeSearchFtsRow`、prompt/safe repair status、derived drift 和 safe diagnostics 类型；`src/storage/sqliteSessionAdapter.js`、`sqlitePromptRetrievalTables.js`、`sqliteSafeSearchTables.js` 和 `sqliteMaintenance.js` 已补局部 JSDoc/`@ts-check` 并进入 `npm run typecheck:server`。safe-search source literal 已对齐 `server_visible_safe_search_projection`。事实源边界不变：SQLite 派生表仍只从 `world_sessions.world_state_json -> server safe views -> derived rows` 单向修复，不让 AI、浏览器、prompt 或 resolver 把 raw SQLite 行当作 canonical truth。已通过 server typecheck/probe、SQLite focused tests（70 项）、docs governance、完整 `npm test`（991 项）和 `git diff --check`；Noether 只读探查建议已采纳，Mendel 提交前只读复审未发现 P0/P1/P2。
- 2026-05-20：推进 S88.3 书生主线首月差事切片，实现提交 `2568db51`。`src/game/appointmentTracksConfig.js` 新增按授官轨迹区分的一甲翰林、庶吉士、部曹、外放和候缺首月差事配置；`src/game/appointmentTracks.js` 在殿试 `resolveInitialAppointmentTrack()` 写初授履历时，同步给 `officialCareer.assignments` seed 一条一月三旬期限的服务器拥有任务，并让 `officialCareerView` / prompt summary 可见。`src/game/clientWorldState.js` 与 `src/routes/routeResponses.js` 同步递归剥离或拒绝兼容 `worldState` 内的嵌套 hidden/raw/provider/prompt/statePatch 字段，避免首月差事 raw 存档中的 `hiddenNotes` 字段名进入 public response。`npm run smoke:exam-s69` 现在断言殿试后 `officialCareerView` 和最终 JSON session 都有首月差事。已通过 focused tests、exam smoke、`npm run typecheck:server`、docs governance、完整 `npm test`（993 项）和 `git diff --check`；Beauvoir 提交前只读复审发现的兼容 `worldState` 嵌套 `hiddenNotes` P2 已修复，最终未发现 P0/P1/P2。
- 2026-05-20：推进 S88.3 备考压力与入场反馈切片，实现提交 `49459c98`。新增 `src/game/examPreparationConfig.js`，把备考压力分段、权重、公开文本 caps 和权限边界文案放入领域配置；`src/game/examTravel.js` 按盘费缺口、路途、保结、真实准考缺口、学业维度、体力心态和读书计划派生 `entryPreparation.preparationPressure` 与 `entryFeedback`，并提供 public `entryPreparation` sanitizer；`src/game/examProcedure.js` 把压力等级、入场搜检/号舍反馈和高压 incident 纳入安全 `examProcedureView` 与 prompt summary；`src/game/studyProfile.js` 暴露 `studyProfileView.examPreparation`，React `ScholarPanel` 与 `ExamPage` 只读显示压力、原因、建议和流程事件，按钮只写行动草稿。`src/game/clientWorldState.js` 还会清洗兼容 `worldState` 内 active exam / exam history 的入场准备快照和根层/嵌套 `examCalendar`。AI/provider/前端不能写压力分数、搜检结果、号舍状态、晋级、榜单、授官或 canonical state。已通过语法检查、`node --test test/studyProfile.test.js test/examTravel.test.js test/examProcedure.test.js`（24 项）、`npm run typecheck:server`、`npm run typecheck:client`、client focused/all Vitest、`npm run build:client`、`AI_PROVIDER=mock npm run smoke:browser`、`npm run smoke:exam-s69`、docs governance、完整 `npm test`（997 项）和 `git diff --check`；Halley、Carson、Copernicus、Einstein 初审/复审 P2 均已修复，最终复核未发现新的 P0/P1/P2。
- 2026-05-20：推进 S88.3 阅卷放榜同年座师过渡切片，实现提交 `66153cb7`。新增 `src/game/examAftermathConfig.js` / `src/game/examAftermath.js`，从服务器定榜、`examHonor`、`examNetwork`、`appointmentTrack`、成绩和首月差事生成清洗后的 `examAftermathView`，并写入 `examHistory.examAftermath`；`src/routes/exam.js`、`src/routes/game.js`、`src/contracts/serverContracts.ts` 和前端 API 类型已纳入该安全 view。React `RankingPage` 新增“同年座师”区块，只展示公开同年、座师/考官、摘要和下一步建议，“拟行动”只写行动草稿，不调用 turn API；`scripts/mockImperialExamAcceptance.js` 与 `scripts/clientSmoke.js` 已增加放榜过渡断言。边界：AI 与前端不能补名次、造关系、定官职或写 hidden 私档，兼容 `worldState` 历史快照也走同一 sanitizer。当前已通过语法检查、exam aftermath/honors/network focused tests（6 项）、server/client typecheck、全量 Vitest（68 项）、client build、`npm run smoke:exam-s69`、`AI_PROVIDER=mock npm run smoke:browser`、docs governance、React 脚手架测试（37 项）、完整 `npm test`（1000 项）和 `git diff --check`；Euclid 初审 P2 修复后已重跑语法检查、focused exam aftermath/honors/network tests（6 项）、`npm run typecheck:server`、docs governance、S69 smoke、完整 `npm test`（1000 项）和 `git diff --check`。Epicurus 与 Newton 的只读接线建议已采纳；Euclid 发现的普通 `provider` / `provider payload` / `statePatch` 文本污染 P2 已用 aftermath 专用 unsafe pattern 和回归测试修复，复核未发现新的 P0/P1/P2。
- 2026-05-20：推进 S88.3 读书计划深化切片，实现提交 `10e4e79f`。`src/game/studyProfileConfig.js` 新增读书计划 schema、三旬复盘周期、强度档位、目标增益、晨午暮日课和复盘模板；`src/game/studyProfile.js` 把 `studyProfileView.nextPlan` 规范化为 `planningWindow`、`intensity`、`dailyRhythm`、`checkpoints`、`riskNotes`、`nextActions` 和 `authorityBoundary`，并同步进入 prompt summary。React `ScholarPanel` 显示三旬窗口、补弱强度、当前/目标分、晨午暮日课、复盘节点、风险和权限边界，新增“执行首课”按钮但只写行动草稿；`scripts/clientSmoke.js`、App fixture 和 React scaffold 均增加深度计划断言。边界：读书计划仍由服务器按学业画像、师友关系和科场记录生成，AI 老师只能建议，不能写属性、保结、科名、榜次、官职或 canonical state；后端 sanitizer 会拒绝 provider/statePatch/worldState/raw/prompt/key/path/SQLite 形态污染，并在整组节点被清空时回落默认日课。已通过语法检查、study/exam focused tests（21 项）、server/client typecheck、React App focused tests（36 项）、React scaffold tests（37 项）、全量 Vitest（68 项）、client build、browser smoke、S69 完整路径 smoke、docs governance、完整 `npm test`（1000 项）和 `git diff --check`；Sagan 提交前只读初审发现复审状态文档未回填与未跟踪 `npm-start*.log` 需排除两个 P2，本轮已回填文档并排除日志，Sagan 复核未发现新的 P0/P1/P2。
- 2026-05-20：完成 S88.3 考试入场后反馈切片，实现提交 `6972b923`。`src/game/examProcedureConfig.js` 新增入场后反馈模板、风险 note cap 和权限边界；`src/game/examProcedure.js` 随 `initialize/advance/complete` 派生 `examProcedureView.phaseFeedback`，包含阶段摘要、压力标签、公开行动回声、风险提示和下一步草稿建议，并进入 prompt capped summary。`src/game/examSceneTime.js`、`src/routes/exam.js` 与 `src/game/clientWorldState.js` 清洗直接 payload、`examScene`、兼容 `worldState.activeExam` 和考试历史里的科场局部时间 / 流程快照，旧 `procedure.phaseFeedback` 的 raw/provider/prompt/path/key 污染回落服务器默认文本，且 clean-looking 伪摘要、伪风险、伪行动和伪 phase 不再被信任。React `ExamPage` 新增“入场后反馈”展示和“拟行动”按钮，按钮只写 `useUiStateStore` 行动草稿，不推进考试、不交卷、不调用普通回合。边界：AI、前端和 provider 不能写分数、处罚、榜次、晋级、弥封映射、官职或场内 canonical 结果。Descartes/Curie 的只读接线建议已采纳；Descartes 提交前复审发现的 P2 已修复，最终未发现新的 P0/P1/P2。
- 2026-05-20：推进 S88.4 官署首月体验切片，实现提交 `bafbdce1`。新增 `src/game/officialFirstMonthConfig.js` / `src/game/officialFirstMonth.js`，从首月 `officialCareer.assignments` 派生 `officialCareerView.firstMonthExperience`，公开官署首月进度、风险、限期、上官同僚反馈、回署回执、考成信号、下一步草稿建议和月报摘录提示；普通回合推进首月差事时追加 `[官署回执]`，`summarizeOfficialCareerForPrompt()` 只读取 capped 摘要。`src/game/playerMonthlyBriefing.js` 在月末“本职差事 / 上官同僚 / 下月可行”优先摘录首月体验；`client/src/components/OfficialMinisterPanel.tsx` 新增“官署首月”区块，只读 `officialCareerView.firstMonthExperience` 与 `playerMonthlyBriefingView`，按钮只写行动草稿。`officialCareer` 公开文本清洗已收紧，拒绝 provider/proposal/prompt/raw/statePatch/worldState/key/path/SQLite 形态污染；首月草稿分类已覆盖馆课/清册/观政日课/册籍民情等所有授官模板，避免非翰林首月差事不能推进。Hypatia 只读后端探索建议已采纳；提交前只读复审发现的非翰林首月草稿无法推进 P1 已修复，复核未发现新的 P0/P1/P2。提交哈希回填为低风险纯文档记录，按项目规则跳过额外子代理复审。
- 2026-05-20：推进 S88.4 奏折朝议入口与长期考成追踪切片，实现提交 `345d134d`。新增 `src/game/officialCourtEntryConfig.js` / `src/game/officialCourtEntry.js`，从首月 `firstMonthExperience`、回署回执和 `assessmentDossier` 派生 `officialCareerView.courtEntry` / `courtEntries`，整理奏折队列、朝议筹议、长期考成 trace、上官同僚后续回响和 draft-only 下一步。`topicSurfaceView` 的 `memorial-review`、`edict-draft`、`court-debate` 可读取 `officialCareerView.courtEntries`，并新增首月回署/首月筹议草稿模板；`topic_draft` 可引用这些 evidence 拟稿，但仍只读安全 view、不提交回合、不推进时间、不执行 resolver、不写 canonical state。React `OfficialMinisterPanel` 新增“奏折朝议入口”，显示首月材料、surface 流向、考成 trace、功绩/风险、后续回响和“入奏折队列 / 付朝议筹议”草稿按钮；`scripts/clientSmoke.js` 已同步新标题断言。Chandrasekhar 只读接线建议已采纳；提交前只读复审发现裸 SQLite/SQL/rawSql 污染与官场考成/弹劾成案话术两个 P2，主代理已补强首月文本清洗、topic draft 结果宣称拦截和回归测试，复核未发现新的 P0/P1/P2。
- 2026-05-20：推进 S88.4 首月回署草稿服务器裁决闭环，实现提交 `8b678834`。`src/game/officialCourtEntryConfig.js` / `src/game/officialCourtEntry.js` 新增裁决状态、记录归一化和普通回合提交识别；`src/game/officialCareer.js` 新增 `courtEntryResolutions`，在 `runOfficialCareerStep()` 里把“入奏折队列 / 付朝议筹议 / 续记考成”裁为准入复核、转部核议、留中补查、驳回补据或续入考成，并投影到 `officialCareerView.courtEntry.latestResolution`、prompt summary、事件档案 `official_court_entry` 和官职月报。React 官员面板只读显示近次裁决，按钮仍只写行动草稿。边界：`topic_draft` / surface 仍不写状态，裁决只做公开记录和微量进度/考成影响，不直接任免、奖惩、处分或成弹劾。Newton 只读探查建议已采纳。
- 2026-05-20：S88.4 首月回署草稿服务器裁决闭环复审更新：Chandrasekhar 提交前只读初审发现 `续记考成` 后端 `assessment-trace` 分支没有前端按钮、action metadata 仍指向 `memorial-review` 且测试未覆盖的 P2；本轮已改为显示第三个草稿按钮、`track-assessment.targetSurfaceId = assessment-trace`，并覆盖所有授官首月模板的 `track-assessment` 裁决回归。Gauss 最终只读复核确认该 P2 已关闭，未发现新的 P0/P1/P2。
- 2026-05-20：`8b678834` 提交哈希回填为低风险纯文档记录，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
- 2026-05-20：推进 S88.4 朝议/部院/御前跟进切片，实现提交 `2eabfcb2`。`src/game/officialCourtEntryConfig.js` / `src/game/officialCourtEntry.js` 新增 `courtEntryFollowUps` schema、stage/status、AI read scope、工具权限、server adjudication 边界、跟进归一化、参与 actor 摘要、场景 preview 和 draft-only next actions；`src/game/officialCareer.js` 在 `runOfficialCareerStep()` 中优先识别“朝议跟进 / 部院覆奏 / 御前摘报 / 考成观察”，写入 `officialCareer.courtEntryFollowUps`、`latestFollowUp`、受限差事 progress/risk 和考成 notes。`eventArchive` 新增 `official_court_follow_up`，`playerMonthlyBriefing`、`resolverInputConfig` 和 `worldThreads` 同步月报、topic evidence 与 world thread 线索；`src/contracts/serverContracts.ts`、`client/src/api/types.ts` 和 React `OfficialMinisterPanel` 已显示跟进 stage/status、参与者和草稿按钮。边界：跟进只记录朝议/部院/御前中间反馈和微量进度/考成影响，不直接任免、奖惩、处分、采纳奏折或成弹劾；Helmholtz、Ohm 只读接线建议已采纳。Dalton 初审发现的 P1/P2 已修复：follow-up 先于 generic action 分类，`bureau-reply` 不再重复生成官场差遣/官署回执；公开工具权限文本不再携带数据库查询/写库话术。已通过语法检查、focused Node tests、server/client typecheck、React App/scaffold tests、全量 Vitest、client build、S69 smoke、browser smoke、docs governance、完整 `npm test`（1015 项）和 `git diff --check`；Dalton 最终只读复核确认 P1/P2 已关闭，未发现新的 P0/P1/P2。
- 2026-05-20：推进 S88.4 跨身份奏议回应切片，实现提交 `37a0c4d5`。新增 `src/game/officialCourtResponseConfig.js` / `src/game/officialCourtResponse.js`，以独立 server-owned `worldState.officialCourtResponses` 记录皇帝、大臣、官员或有司对公开奏议的朱批留览、票拟覆奏、补据、朝议回应和考成观察；兼容 `worldState` 会剥离该 raw ledger，只通过 `courtResponseView` 暴露安全 docket、近次回应、AI read scope、工具权限、proposal 边界和 server adjudication。普通回合提交跨身份回应时只写 `[奏议回应记录]` 与 `official_court_response` 事件档案/world thread 中间态，不写任免、奖惩、处分、拨钱粮、采纳奏折或弹劾终局；`roleWorldCoupling` 会避开朱批/覆奏/补据类输入，避免误触皇帝任免或大臣弹劾联动。`topicSurfaceView` / resolver input 新增 `courtResponseView` evidence，React `EmperorPanel` 新增“奏议回应”，`OfficialMinisterPanel` 在奏折朝议入口显示跨身份回应 docket，按钮继续只写本地行动草稿。
- 2026-05-20：`37a0c4d5` 提交哈希回填为低风险纯文档记录，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
- 2026-05-21：推进 S88.4 长期官场后果信号切片，实现提交 `09f9f83b`。新增 `src/game/officialCourtConsequencesConfig.js` / `src/game/officialCourtConsequences.js`，以独立 server-owned `worldState.officialCourtConsequences` 记录公开奏议裁决、朝议/部院/御前跟进和跨身份回应之后的证据缺口、考成压力、风宪关注、功绩留痕或朝局余波；兼容 `worldState` 会剥离该 raw ledger，只通过 `courtConsequenceView` 暴露安全来源、近次信号、AI read scope、工具权限、proposal 边界和 server adjudication。普通回合/月末只写 `[官场后果信号]` 与 `official_court_consequence` 中间态，可追加 bounded career dossier note 或 `risk_watch` 观察，不写官缺调整、赏罚处分终局、财赋动用、奏议终局或风宪定案。`eventArchive`、`worldThreads`、`playerMonthlyBriefing`、`topicSurfaceView` 和 resolver input 已接入 `courtConsequenceView` evidence；React `OfficialMinisterPanel` 在“考成与弹劾”显示长期后果，`EmperorPanel` 在“赏罚预留”显示长期余波，按钮继续只写本地行动草稿。Kepler/Ohm 的只读接线建议已采纳；Kepler 提交前只读复审未发现 P0/P1/P2，建议后续补连续月末不重复同一 source 与旧存档 raw consequence 污染全链路过滤回归。
- 2026-05-21：`09f9f83b` 提交哈希回填为低风险纯文档记录，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
- 2026-05-21：推进 S88.4 皇帝/部院续办链路切片，实现提交 `fb2be42d`。`src/game/officialCourtResponseConfig.js` / `src/game/officialCourtResponse.js` 升级为 v2，上一轮 `official_court_response` 现在可作为 `courtResponseView.chainItems` 暴露给御前再摘、部院再覆、补据承批和考成续记；普通回合只写新的受限 `officialCourtResponses.responses` 行，记录 `previousResponseId`、`sourceResponseId`、`chainRound`、`nextHandlerRole` 和 `chainPath`，兼容 public `worldState` 仍剥离 raw ledger。`resolverInputConfig`、`worldThreads`、`src/contracts/serverContracts.ts`、`client/src/api/types.ts`、React `EmperorPanel` / `OfficialMinisterPanel` 和 focused tests 已同步；边界仍是不直接任免、奖惩、处分、拨款、采纳奏折、弹劾成案或泄露 hidden/raw/provider/prompt/path/key。Kepler 初审发现的 raw namespace 与续办角色裁决两个 P2 已修复并复核关闭，未发现新的 P0/P1/P2。
- 2026-05-21：`fb2be42d` 提交哈希回填为低风险纯文档记录，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
- 2026-05-20：完成 S87.1-S87.7 后端 route/API 响应类型覆盖。`src/contracts/serverContracts.ts` 已扩展 common route envelope、`CommonTurnViews`、game start/saves/state/player-state/turn/SSE、inventory/NPC/trade/delegation、exam question/progress/submit、AI settings/quick-actions/topic-draft/connection-test 等 public response contract；`src/contracts/serverContracts.typecheck.ts` 已断言 start/state/player-state/turn/exam response 的 `worldState` 拒绝 raw ledger key。新增 `src/routes/routeResponses.js` 局部 `@ts-check` response helper，接入 `src/routes/game.js`、`src/routes/exam.js` 和 `src/routes/ai.js`，并在运行时拒绝 public `worldState` 出现 raw ledger key；大型 route 文件仍未 whole-file `@ts-check`，CommonJS 运行方式不变。新增 `test/routeResponseContracts.test.js` 并补强兼容 state route raw ledger 剥离断言。完成归档见 `docs/TYPESCRIPT_ROUTE_RESPONSE_COVERAGE_ARCHIVE.md`。Einstein 提交前只读复审未发现 P0/P1，指出 1 个 P2（saves 未接 response helper）和 1 个类型负例缺口，均已修复。已通过 syntax checks、`npm run typecheck:server`、game/exam/AI focused route tests、docs governance、完整 `npm test` 和 `git diff --check`。
- 2026-05-20：按用户要求新增 S87 后端 route/API 响应类型覆盖规划。`docs/TYPESCRIPT_ROUTE_RESPONSE_COVERAGE_ROADMAP.md` 已把下一轮拆为 S87.1-S87.7：route response contract、`buildCommonTurnViews` 局部类型边界、start/state/player-state/turn、inventory/NPC/trade/delegation、exam route、AI route 和验收归档。开发规范已明确：后端 route/API response shape 新增或重构时必须对齐 `src/contracts/serverContracts.ts` 或局部 JSDoc typedef，并运行 `npm run typecheck:server`；不得为了启用类型检查一次性 whole-file `@ts-check` 大型 route 文件，也不得放宽安全 projection、raw ledger 剥离、Ajv/runtime 校验或服务器裁决。本轮同步 `docs/DEVELOPMENT_GOVERNANCE.md` 受保护段落、台账、brief、README、AGENTS/CLAUDE 和检查脚本；因修改治理检查脚本，按非纯低风险文档处理。已通过 `npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`npm run typecheck:server`、`npm test`（983 项）和 `git diff --check`。Lagrange 提交前只读复审未发现 P0/P1；发现并已修复 2 个 P2：补 `scripts/checkGovernanceDocs.js` 的 `npm run typecheck:server` 守门 needle，并清理共享上下文/台账复审待回填占位。Lagrange 复核确认 2 个 P2 均已解决，未发现新的 P0/P1/P2。
- 2026-05-20：完成 S86.1-S86.7 后端 TypeScript 渐进迁移首轮。新增 `tsconfig.server-check.json`、`tsconfig.server-probe.json`、`npm run typecheck:server`、`npm run build:server:probe`、`src/contracts/serverContracts.ts`、`src/contracts/runtimeGuards.ts` 和 `docs/TYPESCRIPT_BACKEND_MIGRATION_ARCHIVE.md`；`.tmp/` 已加入 Git 忽略。`clientWorldState`、`redactedState`、`stateRules`、AI provider facade/route policy、session record、session store、JSON adapter 和 SQLite adapter 已选择性 `@ts-check`。contract 固化 raw ledger 剥离、player-state envelope、安全 route views、AI task/provider/tool envelope、session/storage adapter 和 SQLite row 形状，并用 `src/contracts/serverContracts.typecheck.ts` 断言 public route worldState 拒绝 raw ledger。生产仍直接运行 CommonJS `server.js`，`.ts` 试点只用于 type-only contract、纯 guard/helper 和 probe 编译，不改变 `npm start`；Rust 继续只作为未来有性能证据后的可选 CLI/WASM/离线工具评估。已通过 `npm run typecheck:server`、`npm run build:server:probe`、redaction/stateRules focused tests、AI schema/route/tool focused tests、storage focused tests、docs governance、documentation governance、`npm test`（983 项）和本轮改动路径 `git diff --check`。提交前只读复审 Sartre/Bohr 的 P2 已全部修复，最终未发现 P0/P1/P2；实现提交 `bedea92a`。
- 2026-05-20：按用户要求新增 S86 后端 TypeScript 渐进迁移与 Rust 使用边界规划。`docs/TYPESCRIPT_BACKEND_MIGRATION_ROADMAP.md` 已记录当前判断、总原则、非目标、S86.1-S86.7 小步骤、验证口径、Rust 候选和回滚策略；`docs/DEVELOPMENT_STEPS.md` 已恢复活动专项表，当前只列 S86 TODO 小步骤。开发治理已写入“新增或重构后端契约、API/view 类型、安全 projection、AI schema/provider facade、storage adapter 和核心 resolver 时优先使用 TypeScript 或纳入 TypeScript 检查；TS 不能替代 Ajv/runtime 校验”；已同步 brief、README、AGENTS/CLAUDE 和检查脚本。本轮修改治理检查脚本，按非纯低风险文档处理。验证已通过：`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`npm test`（983 项）、`git diff --check`。提交前只读复审 Dalton 未发现 P0/P1；唯一 P2 是本地未跟踪 `npm-start.log` / `npm-start.err.log` 不属于本轮范围，提交时需排除。
- 2026-05-20：用户指出活动路线图仍保留已完成 DONE 项，已修正 `docs/DEVELOPMENT_STEPS.md` 第 4 节。活动路线图总览现在只说明当前没有 `TODO` / `IN_PROGRESS` 专项、S81-S85 已归档、下一步由用户或新路线图指定；已完成步骤只留在归档索引、近期进度记录和专题归档，不再以 DONE 长表占据活动路线图。本轮为低风险纯文档维护，不改代码、API/schema、AI 权限、素材 manifest、存档格式或运行时行为；按项目规则跳过提交前子代理复审。验证：`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check`。
- 2026-05-20：按用户要求再压缩已完成活动台账归档。`docs/ACTIVITY_LEDGER_COMPLETED_ARCHIVE.md` 已从旧活动台账复制件改为压缩索引，只保留完成阶段入口、S73-S78 关键锚点、验证锚点、稳定边界和后续维护规则；`docs/DEVELOPMENT_STEPS.md` 顶部追溯口径已改为“压缩索引”，并新增 `DOCS-LEDGER-ARCHIVE-COMPRESSION` 完成项和近期记录。本轮为低风险纯文档维护，不改代码、API/schema、AI 权限、素材 manifest、存档格式或运行时行为；按项目规则跳过提交前子代理复审。验证：`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check`。
- 2026-05-20：S85.3-S85.6 NPC 主动性、礼法扩展位、总验收与归档已完成实现、验证、文档同步和提交前只读复审，实现提交 `f984ebc5`。新增 `src/game/npcActiveRequestsConfig.js` / `npcActiveRequests.js`，普通回合在经济 tick 后运行 NPC 私人 planner 与主动请求 step，支持求助、索债、献策、请托、行贿、弹劾、引荐、求婚和背叛；玩家回应只进入服务器裁决，不即时写资源、婚姻、弹劾、背叛或隐藏任务结果。新增 `src/game/npcRelationshipActionsConfig.js` / `npcRelationshipActions.js`，为论道、切磋、求爱和婚姻提供 schema、NPC eligibility、服务器 outcome 与伪造客户端结果字段忽略；人物页新增主动来函 inbox 和“礼法”tab，主卷安全视图索引扩展到 `10 / 10`。SQLite 新增 `npc_active_requests` 安全派生表。Poincare 只读复审发现 NPC 主动来函公开 view 回显 `privateSignalTags` 值的 P1，已修复为私档软信号仅作内部选择，不进入公开 intent/risk/evidence；Poincare 复核发现旧存档污染 `evidenceRefs`、`outcome` 和 `recentEvents` 的 P2，已补 view 层过滤与 SQLite 派生表回归测试；最终复审未发现 P0/P1/P2。
- 2026-05-20：S85.1-S85.2 已完成首轮实现、最终复审和验证，实现提交 `6ff7b842`。新增 `src/game/npcEconomyConfig.js` 与 `src/game/npcEconomy.js`，在普通回合/跳时共享的世界旬/月 tick 后运行 NPC 经济：旬更刷新 `marketPriceLedger`，月末结算资产维护与收益、库存损耗、委派到期回禀、逾期交易承诺、人情债和 NPC 关系记忆；考试入场/场内场景只返回安全 projection，不推进全局经济。`marketPriceView` / `npcEconomyView` 已进入 turn、SSE、player-state 和县令主卷，兼容 `worldState` 与 redacted player API 剥离 raw `marketPriceLedger` / `npcEconomyLedger`。所有委派创建已拒绝超过地方库银的预算，月结旧任务也按服务器确认的有效预算参与成功率和扣款；NPC 经济反馈只返回公开 `economy.*` 变化分类，不暴露内部 ledger path。Lovelace 与 Meitner 已分别做只读接线/链路探查；Zeno 只读复审发现的预算越权 P1 已修复并补测；Parfit 只读复审发现的考试入场态、泛委派预算和反馈 path 三个 P2 已修复并补测；最终只读复审 Franklin 未发现 P0/P1/P2。
- 2026-05-20：推进完成 S81-S84 NPC、资产、储物、交易与委派首轮闭环，实现提交 `4c007f00`。新增 S81-S84 执行契约、资产/资源/背包/NPC/交互/交易/委派/开局背景裁决领域模块，`POST /api/game/start` 接入 `background_claim_parser` 并由服务器兑现或折算宣称；新增 inventory、NPC list/detail、NPC interaction、trade、npc-command 安全 API；SQLite 新增安全派生表并接入 adapter/maintenance；React 新增“囊箧” route、人物 NPC 工作台、对话/交易/委派 tabs 与主卷开局裁决摘要。交易 API 现在把 AI `accepted` 视为议价文本，不直接写银钱/物品；NPC/交易/委派 AI 文本在即时响应前也会被服务端清洗和拒绝；委派创建响应只返回安全 task view，SQLite legacy 交易派生行也补了 actor fallback。Bacon、Linnaeus、Pasteur、Turing 分别完成小步 patches，均未提交；主代理整合验证和文档同步。第一次只读复审 Epicurus 发现的交易结算和 AI 文本泄漏 P1 已修复并补测，第二次复审 Hegel 未发现 P0/P1，指出的非阻断项已收口；最终只读复审 Russell 未发现 P0/P1，记录 S85 可继续收紧全局错误脱敏和畸形 legacy trade actor fallback。
- 2026-05-20：按用户追问补充 S81-S85 全量小步骤台账，已把 S81.1-S81.5、S82.1-S82.5、S83.1-S83.5、S84.1-S84.8、S85.1-S85.6 全部写入 `docs/DEVELOPMENT_STEPS.md`。用户明确本轮不用子代理审核；本轮为低风险文档台账补充，跳过子代理复审，已通过 docs governance、documentation governance 和 diff check。
- 2026-05-20：按用户要求新增 S81-S85 NPC、资产与储物系统规划，详见 `docs/NPC_INVENTORY_SYSTEM_ROADMAP.md`；活动台账已加入 S81-S85 TODO，开发治理新增“前后端分离和大步骤拆分”受保护规则，并同步 `scripts/checkGovernanceDocs.js`、brief 与 `AGENTS.md`。本轮为规划/治理改动，需通过 docs governance、documentation governance、diff check，并因修改检查脚本执行只读复审。
- 2026-05-20：按用户要求压缩 `docs/SHARED_CONTEXT.md` 和 `docs/DEVELOPMENT_STEPS.md`，只保留接手下一步必要的状态、边界、验证入口和归档索引；新增“不要最小实现点/最小改动点，追求完整丰富实现”的开发规范，并写入 `docs/DEVELOPMENT_GOVERNANCE.md`、本文件、活动台账、brief 与 `AGENTS.md`。已通过 `npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`npm test`（940 项）和 `git diff --check`；提交前只读复审 Anscombe 未发现 P0/P1/P2。

## Next Recommended Step

S88.9 第九片顶部主导航 active 精准化已完成实现、完整本地验证、提交前只读复审和提交，代码提交 `7223c036`，本次哈希回填为低风险纯文档记录。下一步可继续 S88.9 补更多 App 级路由跳转细节、剩余窄屏操作效率和其他页面 empty/error 文案；也可转回 S88.12 真实 keyed provider 长跑证据、S88.7 实体压力/关系网影响片。无论选择哪条线，都必须仍从安全 view 重建 evidence，保持 proposal-only、browser-draft-only 和服务器裁决，不让浏览器 task 或 evidence 变成真实任务队列、资源结算器、婚姻/弹劾/定罪/背叛裁决器。继续保持 NPC 私人目标、`privateSignalTags`、资产真数、未公开关系和 hidden ledger 只在服务器内部使用；浏览器按钮只写行动草稿或提交受限意图，不能裁决资源、婚姻、交易、NPC 行动、关系真相或隐藏信息。保持 `scholar -> child_exam -> provincial_exam -> metropolitan_exam -> palace_exam -> official` 完整路径，由服务器继续裁决考试晋级、榜单、授官、读书属性、备考压力、同年座师关系、科场入场后反馈、官署首月差事、回署回执、奏折/朝议裁决、朝议/部院/御前跟进、跨身份回应、续办链路、长期后果信号、领域后果、六身份事务结果、考成和弹劾风险；验证入口优先使用 client focused/full tests、browser smoke、provider smoke/long-run scripts、`npm run smoke:exam-s69`、身份/官场/topic/search/NPC/经济 focused tests。

2026-05-22 哈希回填说明：`7223c036` 提交哈希回填为低风险纯文档记录，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。

2026-05-22 哈希回填说明：`71328cfa` 提交哈希回填为低风险纯文档记录，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。

2026-05-22 哈希回填说明：`ca472648` 提交哈希回填为低风险纯文档记录，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。

2026-05-22 哈希回填说明：`2dccdae0` 提交哈希回填为低风险纯文档记录，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。

2026-05-22 哈希回填说明：`9b32068a` 提交哈希回填为低风险纯文档记录，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。

2026-05-22 哈希回填说明：`960e0336` 提交哈希回填为低风险纯文档记录，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。

2026-05-22 复审时序文档修正：S88.9 第二片代码提交 `960e0336` 已存在后，主代理补做 Helmholtz 只读复审；Helmholtz 未发现代码 P0/P1/P2，仅指出复审时序文档口径 P2，已由本次低风险纯文档修正收口。本次修正不改代码、API/schema、运行时行为、提示词或验证工具，按项目规则跳过额外子代理复审。

2026-05-22 哈希回填说明：`1e1cb7a0` 提交哈希回填为低风险纯文档记录，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。

2026-05-22 哈希回填说明：`50b55538` 提交哈希回填为低风险纯文档记录，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
2026-05-22 哈希回填说明：`06c25f4c` 提交哈希回填为低风险纯文档记录，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
2026-05-22 哈希回填说明：`62c9a260` 提交哈希回填为低风险纯文档记录，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
2026-05-22 哈希回填说明：`05fba067` 提交哈希回填为低风险纯文档记录，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
2026-05-22 哈希回填说明：`1becde0c` 提交哈希回填为低风险纯文档记录，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
2026-05-22 哈希回填说明：`ecfdc3c0` 提交哈希回填为低风险纯文档记录，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
2026-05-22 哈希回填说明：`f533d1c1` 提交哈希回填为低风险纯文档记录，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
2026-05-22 哈希回填说明：`53dc1f3d` 提交哈希回填为低风险纯文档记录，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
2026-05-22 哈希回填说明：`28141071` 提交哈希回填为低风险纯文档记录，不改代码、API/schema、运行时行为、提示词或验证工具；按项目规则跳过额外子代理复审。
