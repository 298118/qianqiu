# Shared AI Development Context

这是 Codex 当前使用的精简交接板。详细历史请看 `docs/DEVELOPMENT_STEPS.md`、各阶段归档和专题契约文档；本文件只保留接手下一步所必需的状态、规则和内容保护边界。

## Read First

每次开发会话开始前必须读取：

1. `AGENTS.md` 或 `CLAUDE.md`
2. `docs/SHARED_CONTEXT.md`
3. `docs/QIANQIU_DEVELOPMENT_BRIEF.md`
4. `docs/DEVELOPMENT_STEPS.md`

稳定治理锚点见 `docs/DEVELOPMENT_GOVERNANCE.md`。重写路线图、交接文档或 brief 时不得删弱其中的必守规则，并运行 `npm run check:docs-governance`。

## Current Snapshot

- Product: browser + Node.js historical simulation text game **Qianqiu / 千秋**。
- Runtime target: `npm install && npm start`，然后打开 `http://localhost:3000`。
- Frontend: 默认发布入口已由 S74.1 React + TypeScript + Vite 构建产物接管；源码位于 `client/`，生产构建输出为 `dist/client/`，React Router Data Mode 管理 `/`、`/game/:sessionId`、`/game/:sessionId/map|people|archive|exam|ranking|court|settings`。S74.7 起真实案卷内顶部主卷/舆图/人物/史册入口绑定当前安全 `sessionId`，`npm run smoke:browser` 是 React 默认入口 smoke，且无 `--url` 时强制 Mock。旧 `public/index.html`、`public/app.js`、`public/styles.css` 和 `public/mapPanel.js` 暂留作迁移参考，不再是交付入口；`public/assets/`、`public/vendor/` 和 `public/mapRenderer.js` 继续提供已审核素材与 S72 地图静态资源。Backend: Node.js + Express，plain JavaScript。
- AI providers: adapter-based Mock/OpenAI/DeepSeek/MiMo/MiMo+DeepSeek/Anthropic。`AI_PROVIDER=mock` 仍是默认可玩模式，即使本机存在真实 provider key 也不会自动改走真实模型；`mimo-deepseek` 下 narrator/科举出题仍优先 MiMo，domain_specialist/科举评卷和 critic/safety 在 DeepSeek key 可用时走 DeepSeek。
- Storage: 默认 JSON session files under `data/sessions/`；可选 `STORAGE_ADAPTER=sqlite` 使用本地 `schema_migrations`、`world_sessions`、audit tables、`geo_*`、`people_*`、`office_*`、`event_archive_index`、`prompt_retrieval_index` 和 `safe_search_index`。SQLite 派生行只从 `world_sessions.world_state_json` 单向修复；raw business/audit rows 不是 route、prompt、browser、搜索或服务器裁决 truth source。
- S67 scale acceptance: `rawFixtureGenerationMs` 只表示 large fixture 原始数据生成，`fixtureBuildMs` 是外层构建总耗时报告字段；完整 `npm test` 并发下的资源波动不再用旧 `fixtureGenerationMs` 名义硬失败。数量、防泄漏、分页 cap、prompt budget、SQLite read-repair parity、行为级耗时和 heap guard 仍是硬门。
- Roadmap status: S49-S67 本地数据库与大世界内容、S68-S69 科举深化、S70 AI 编排、S71 数据库玩法化和 S72 PixiJS 水墨地图均已完成并归档。当前活动路线进入 S73-S77 前端水墨重构，任务书见 `docs/FRONTEND_INK_REDESIGN_ROADMAP.md`；S73 完成前端素材、manifest、QA 与全量/补充立绘池；S74 完成 React/Vite/React Router 默认入口、安全 API client、Zustand 状态层、shell/surface registry、manifest 资产层、React 舆图桥和默认入口 smoke；S75 完成画卷首页、开局表单、朱印反馈、右上角印匣、案卷式存档/读档、返回首页/继续本局、显示偏好、底部奏折和快捷建议 AI 接入/验收；S76.1-S76.12 已完成主游戏壳、六类身份面板、科举/放榜全屏、独立舆图、人物谱牒、专题 surface 扩展位和 S76 总验收；S78 已把奏折队列、拟圣旨、朝议、堂审、军议和人物档案升级为真实安全专题玩法，新增只读 `topicSurfaceView`、`GET /api/game/topic-surface/:sessionId/:surfaceId`、只读 `POST /api/ai/topic-draft/:sessionId`、`topic_draft` AI task、Mock/provider 草稿、前端三栏材料/筹议/草稿闭环。S77.1 默认入口确认、S77.2 浏览器 smoke 扩展、S77.3 视觉像素检查、S77.4 安全污染防线和 S77.5 性能资源预算已完成；下一步进入 S77.6 可访问性，随后继续归档验收。S72 归档见 `docs/PIXIJS_INK_MAP_ARCHIVE.md`。
- Current collaboration: 2026-05-14 起停止与 Gemini CLI 协作，后续开发全部由 Codex 承担。Codex 负责后端、前端、AI/server 权限、素材生成与台账、视觉审核、最终文档同步、验证和 Git 提交；S73-S77 的 AI 生图统一由 Codex 使用 `gpt-image-2` 完成，AI 生成素材和第三方入库候选素材都必须由 Codex 做视觉审核，确认游戏基调、历史/水墨适配、可读性和同批一致性。
- Current local `.env`: 可能含用户提供的 provider keys。`.env` 被 Git 忽略，不能打印或提交。

## Core Invariants

- 完整书生路径必须继续可用：`scholar -> child_exam -> provincial_exam -> metropolitan_exam -> palace_exam -> official`。
- AI 是《千秋》的核心世界引擎，不是可替换装饰。新增玩法、数据域、角色、官署、事件、面板或 prompt 检索时，必须设计 AI read scope、actor intelligence、tool permissions、proposal boundaries、server adjudication、audit records 和 Mock/no-key fallback。
- Provider 只能建议叙事、受限 `statePatch`、关系变化、评分 JSON、考试触发或身份受限领域工具 proposal；服务器拥有晋级、考试资格、作弊处罚、持久化、保护字段、官职任免、长期事件、时间推进、数据库写入和可见性过滤。
- AI JSON 必须先验证再应用；状态变化必须经过白名单、clamp 和 server-owned follow-up modules。
- 游戏规则、阈值、时间间隔、概率、UI caps、fixture 规模和 prompt budgets 等可调参数不得散落为 magic numbers；优先放入 `src/config/GameConfig.js` 或领域 `src/game/*Config.js`，写明单位、范围和默认意图。
- `GET /api/game/saves` 只返回脱敏 metadata；普通浏览器读档优先通过 `GET /api/game/player-state/:sessionId` 读取 redacted player state 和清洗后的 route views，`GET /api/game/state/:sessionId` 仅作为短期开发兼容快照。
- 本地可玩性不能依赖真实模型 key；有 key 的 provider 检查必须在缺 key 时受控 skip 或 fail。
- 每个 coherent change 必须更新本交接板和步骤台账，运行相关验证，并用 Git 提交。
- 项目内协作文档、路线图、交接记录、领域注释和玩家可见文案优先使用中文；代码标识符、API/协议名、第三方术语和命令输出可保留英文。

## Content Protection

- 当前范围只考虑本地 JSON/SQLite；不规划远程存档、账号体系、多人同步、云冲突解决或托管数据库。
- AI 可以通过身份受限领域工具提交 proposal / request-adjudication，但不能执行 SQL，不能直接写 canonical 状态、`geo_*`、`people_*`、`office_*`、`event_archive_index`、`prompt_retrieval_index`、`event_log` 或 `ai_change_proposals`，工具调用也不能被当作已经发生的世界事实。
- 浏览器和 prompt 只读服务器生成的 view / capped retrieval summary，不读 raw SQLite table、raw audit、provider proposal、完整 prompt、本地路径、密钥、hidden notes 或 hidden intent。S70.12 起游戏/考试 route response 内的兼容 `worldState` 也会剥离 raw `actorMemoryLedger` 与 `sessionSummary`。
- hidden NPC 私档、资产真数、未公开关系、未公开任所、密档事件链和隐藏情报真值不得回填当前 raw route `worldState`。如果后续要保存完整 hidden 私档，先设计 API redaction 与角色视野分层。
- 玩家可见 projection 边界：`informationPanelPageView`、`safeWorldSearchView`、`actorMemoryView`、`sessionSummaryView`、`mapContextView`、`mapRuntimeView`、`aiControlAuditView` 和 `server_player_visible_state_projection` 都只暴露清洗后的公开摘要、snippet、ref、confidence、visibility、显示布局或 bounded audit summary；不返回内部行、审计原文、provider 输出、完整 prompt、本地路径、key、hidden notes 或 hidden intent。`mapRuntimeView` 的显示坐标只供浏览器地图 runtime 使用，不进入 prompt context、AI 工具或服务器 resolver 的事实依据。
- 本机开发诊断 `GET /api/dev/session-diagnostics/:sessionId` 默认关闭，production 强制关闭，只在 `ENABLE_DEV_DIAGNOSTICS=true`、远端地址为 loopback 且 Origin 为空或本机 loopback Origin 通过时返回安全统计；不返回 raw state、raw audit、provider payload、prompt、本地路径、key、SQLite 原始行或 hidden ledger。
- S70/S71 领域工具与 resolver 边界：`cityPolicyResolver`、`judicialCaseResolver`、`militaryDiplomacyResolver`、`worldPressureEventGenerator`、`sceneRuntime`、NPC 记忆和 AI 调动审计均由服务器拥有裁决与 apply；`server.*` 只能是内部 resolver/audit label，不能进入模型可见工具列表或 provider schema。
- S68-S69 科举深化继续保持 proposal-only：老师、保人、同年、考官、吏部和皇帝只能提交题目、点评、事件、批语、复核疑点或授官 proposal；服务器仍拥有资格、舞弊、榜单、名次、授官、任免和持久化裁决。外层 API 保持 `child_exam -> provincial_exam -> metropolitan_exam -> palace_exam`，浏览器/prompt 只读 `studyProfileView`、`examProcedureView`、`examinerPanelView`、`examHonorView`、`examNetwork`、`appointmentTrackView` 和考试历史安全快照。
- S73-S77 前端重构只能组合安全 route view、redacted player state、AI 设置 view、地图 runtime view 和考试安全快照；新增身份/场景/NPC/设置界面不得读取 raw audit、provider proposal、完整 prompt、本地路径、key、hidden notes 或 hidden intent。S73.10 必须在 S73 内完成 300-400 张全量玩家/NPC 立绘池的生成、缩略图、压缩、视觉/安全审核、manifest 入库和台账登记；S74-S77 的人物显示只能通过已审核 `portraitRef`、缩略图和 fallback 使用，不得硬编码路径、不得显示未审核素材、不得一次性加载全量池。玩家与 NPC 立绘要求成年、端庄、身份明确、高颜值但不露骨；女性角色可用服饰剪裁、腰封、衣料层次和站姿体现优雅成熟女性身形比例，但不得性化未成年人、不得幼态化、不得挑逗或过度暴露。重要 NPC 专属池使用 `signature_npc_pool`，不得被随机通用头像系统抽取；未进入公开剧情视野的人物只可暴露安全 `portraitRef`。

## Subagent Discipline

- 用户已授权 Codex 在本仓库使用子代理，除非后续指令撤销，否则视为长期项目上下文。Gemini CLI 协作已停止，后续实施、复审、验证和提交均由 Codex 及 Codex 子代理完成。
- 实施子代理只能做 scoped patches 和 focused verification；不得运行 `git add`、`git commit`、`git push` 或创建 PR。
- 主代理负责整合、最终验证、共享文档同步和唯一 coherent commit。
- 包含代码、测试、运行时行为、API/schema、提示词或验证工具变化的提交，暂存和提交前必须至少委派一个只读子代理审查最终 diff 与验证证据。
- 低风险纯文档改动可跳过子代理复审；路线图重写或内容安全边界变更即使是文档，也应做只读复审。

## Implemented Surface

API:

- `GET /api/health`
- `POST /api/game/start`
- `GET /api/game/saves`
- `GET /api/game/player-state/:sessionId`
- `GET /api/game/state/:sessionId`
- `GET /api/game/search/:sessionId`
- `GET /api/game/topic-surface/:sessionId/:surfaceId`（只读官署专题安全投影）
- `POST /api/game/turn`
- `POST /api/ai/connection-test`
- `GET /api/ai/settings/:sessionId`
- `POST /api/ai/settings/:sessionId`
- `POST /api/ai/quick-actions/:sessionId`（只读快捷行动草稿建议）
- `POST /api/ai/topic-draft/:sessionId`（只读专题 surface AI/Mock 草稿）
- `GET /api/dev/session-diagnostics/:sessionId`（默认关闭，仅本机开发诊断）
- `POST /api/exam/question`
- `POST /api/exam/progress`
- `POST /api/exam/submit`

Important module areas:

- AI adapters/prompts/schemas/tool contracts、actor 权限、快捷建议、专题拟稿、内部工具运行时、NPC mind、制度场景、事件/领域/地图工具：`src/ai/`、`src/game/aiActorProfiles.js`、`src/game/quickActionSuggestions.js`、`src/game/topicSurfaceView.js`、`src/game/topicDrafts.js`、`src/ai/gameAiToolRunner.js`、`src/game/npcMind.js`、`src/game/institutionScenes.js`、`src/game/domainToolResolvers.js`。
- 财政/城市政策、刑名案件、军务/外交、压力事件、scene runtime、NPC 记忆、AI 设置和 AI 调动审计：`src/game/cityPolicyResolver.js`、`src/game/judicialCaseResolver.js`、`src/game/militaryDiplomacyResolver.js`、`src/game/worldPressureEventGenerator.js`、`src/game/sceneRuntime.js`、`src/game/actorMemoryLedger.js`、`src/game/aiSettings.js`、`src/game/aiControlAudit.js`。
- State rules、time、exams、promotions、exam procedure/review/honors/networks/appointment tracks、study profile、official career、relationships、long-term events、world entities/threads：`src/game/`。
- 地图接口与运行时投影：`src/game/mapContext.js`、`src/game/mapRuntimeConfig.js`、`src/game/mapVisualLayoutSeed.js`、`src/game/mapRuntimeView.js`、`src/game/mapToolResolvers.js`。
- Redacted player state、本机开发诊断、安全本地搜索、事件档案、prompt retrieval、SQLite migration/maintenance 与 storage adapters：`src/game/redactedState.js`、`src/routes/dev.js`、`src/game/safeWorldSearch.js`、`src/game/eventArchive.js`、`src/storage/`。
- Browser app: React/Vite 源码在 `client/`，生产构建输出 `dist/client/`；S72/S73 静态素材与地图 runtime 在 `public/assets/`、`public/vendor/` 和 `public/mapRenderer.js`。旧 `public/index.html`、`public/app.js`、`public/styles.css`、`public/mapPanel.js` 仅作迁移参考。

## Archives And Contracts

各阶段规划源头不在本文件展开，追溯路线图源头时优先查看 `docs/DEVELOPMENT_STEPS.md` 顶部归档索引。

- `docs/DEVELOPMENT_GOVERNANCE.md`
- `docs/QIANQIU_DEVELOPMENT_BRIEF.md`
- `docs/DEVELOPMENT_STEPS.md`
- `docs/ARCHITECTURE.md`
- `docs/AI_CONTROL_AUDIT_MATRIX.md`
- `docs/DEPENDENCY_PLUGIN_GOVERNANCE.md`
- `docs/LOCAL_DATABASE_AND_WORLD_CONTENT_ARCHIVE.md`
- `docs/IMPERIAL_EXAM_DEEPENING_ARCHIVE.md`
- `docs/IMPERIAL_EXAM_SYSTEM_CONTRACT.md`
- `docs/AI_PROMPT_ENGINEERING_CONTRACT.md`
- `docs/AI_TOOL_PROTOCOL_CONTRACT.md`
- `docs/AI_ORCHESTRATION_ARCHIVE.md`
- `docs/DATABASE_RESOLVER_INPUT_CONTRACT.md`
- `docs/DATABASE_GAMEPLAY_RESOLVER_ARCHIVE.md`
- `docs/DATABASE_GAMEPLAY_RESOLVER_ROADMAP.md`
- `docs/PIXIJS_INK_MAP_ARCHIVE.md`
- `docs/PIXIJS_INK_MAP_ROADMAP.md`
- `docs/FRONTEND_INK_REDESIGN_ROADMAP.md`
- `docs/FRONTEND_VISUAL_ASSET_GUIDE.md`
- `docs/FRONTEND_ASSET_LEDGER.md`
- `docs/MAP_ASSET_GUIDE.md`
- `docs/MAP_ASSET_LEDGER.md`

## Current Work Note

- 2026-05-19：执行 S77.5 性能与资源预算。新增 `scripts/frontendRuntimeManifest.js` 与 `public/assets/ui/ink-ui-runtime-manifest.json`，React `assetRegistry` 改读精简 runtime manifest（约 0.69MB，对比完整素材 manifest 约 2.4MB），浏览器投影只保留路径、usage、fallback、review status、portraitRef、懒加载和必要运行时字段，不包含审核说明、本地母版路径、性能报告或生成记录；完整 `ink-ui-manifest.json` 仍是素材生产/QA 源头。新增 `scripts/clientBuildBudget.js`、`budget:client`，`npm run smoke:browser` 现在先检查 runtime manifest 新鲜度，再在 Vite build 后检查 JS/CSS/字体/client-assets 预算，并阻止 `public/assets/ui` 大图、S72 地图运行时或全量立绘被打入 `dist/client/client-assets`。`scripts/clientSmoke.js` 新增真实浏览器 resource timing 预算：首页不得请求完整 manifest 或地图运行时，舆图页必须按需加载 Pixi/mapRenderer，人物页等可见立绘稳定加载后再计量且不得越过当前页公开人物范围。按用户最新偏好，人物立绘以清晰为先，当前可见人物和首页案主候选使用已审核主图；预算统计覆盖 `/assets/ui/portraits/` 下所有非 placeholder 主图，限制首页最多 6 张、人物页最多 8 张，以及不得一次性请求全量 S73.10 主图。本轮不新增后端 API、provider schema、SQLite schema、存档格式、AI 权限、服务器 resolver 或 canonical state 写入。Anscombe 只读勘察 S77.5 差距，未改文件、未运行 Git 操作；Beauvoir 提交前只读复审未发现 P0/P1，提出的 P2 已修正。验证已通过 `node --check scripts/clientSmoke.js`、`node --check scripts/frontendRuntimeManifest.js`、`node --check scripts/clientBuildBudget.js`、`node scripts/frontendRuntimeManifest.js`、`node --test test/reactClientScaffold.test.js`（34 项）、`node --test test/browserSmokeScript.test.js`（44 项）、`npm run typecheck:client`、`npm run test:client`（65 项）、`node --test test/frontendInkAssetsManifest.test.js`（16 项）、`npm run smoke:browser -- --client react --screenshots artifacts/s77-5-resource-budget`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check`（仅 CRLF/LF 提示）和完整 `npm test`（932 项通过、0 失败）；Vite 构建仍保留既有 `/assets/ui/...` 运行时资源解析提示，不是新失败。下一步是 S77.6 可访问性。
- 2026-05-19：执行 S77.4 安全污染防线。`scripts/clientSmoke.js` 新增统一安全污染扫描 helper，React smoke 会扫描 DOM 文本、表单值、关键 `data-*` 属性、`localStorage`、`sessionStorage`、运行时 manifest 安全字段和截图产物名，阻断 key、本地路径、Unix/macOS/Windows 本地路径形状、raw prompt/provider、hidden/raw、`/api/game/state` 与 `/api/dev` 词样；manifest 扫描只检查浏览器 runtime 使用的路径、id、usage、fallback、review status、lazy-load 等字段，审核说明不作为 runtime 数据源。`assetRegistry` 现在拒绝 active manifest 暴露 `source.localHighResSourcePath`。科举和皇榜安全边界文案改为玩家语气的“已公开考试快照/榜文”，不再在 DOM 展示弥封身份映射、考官隐藏意图或模型原始提案等内部词样，考试/放榜交互态和移动端截图前也会统一调用 S77.4 页面扫描。本轮不新增后端 API、provider schema、SQLite schema、存档格式、AI 权限、服务器 resolver 或 canonical state 写入；普通读档仍只走 `GET /api/game/player-state/:sessionId`，地图仍只读 `mapRuntimeView`，考试/放榜仍由服务器裁决。low 子代理完成只读缺口扫描，未改文件、未运行 Git 操作；提交前只读复审发现的 Unix/macOS 本地路径漏扫、考试/放榜交互态未统一调用 S77.4 扫描和尾部下一步口径滞后均已修正。验证已通过 `node --check scripts/clientSmoke.js`、`node --test test/reactClientScaffold.test.js`（32 项）、`node --test test/browserSmokeScript.test.js`（44 项）、`npm run typecheck:client`、`npm run test:client`（64 项）、`npm run build:client`、`npm run smoke:browser -- --client react --screenshots artifacts/s77-4-security-pollution`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check`（仅 CRLF/LF 提示）和完整 `npm test`（930 项通过、0 失败）；首次 `npm run test:client -- --runInBand` 因 Vitest 不支持该参数失败，随后按项目脚本原样重跑通过；一次提交前 browser smoke 曾因 Unix 路径正则误扫 `/assets/ui/home/...` 失败，已改为只匹配路径边界后的本地路径并重跑通过。下一步是 S77.5 性能资源预算。
- 2026-05-19：执行 S77.3 视觉与像素检查。`scripts/clientSmoke.js` 新增 S77.3 视觉断言：已审核首页/考试/放榜背景资源加载与像素非空、PixiJS 舆图 canvas 像素采样、人物谱牒缩略图资源可取与布局、可见文本/控件粗重叠扫描、玩家可见开发口径词扫描；地图标签、顶栏、固定底部奏折、tooltip/overlay 等刻意布局从通用重叠扫描排除，避免把舆图标注拥挤当成主流程重叠。快捷建议点击改为按当前可见 slip 重取，避免 Mock 建议刷新时等待旧文案。人工复看初版截图发现 Linux/WSL 缺 CJK 系统字体导致中文方框字，本轮新增 `@fontsource/noto-serif-sc@^5.2.9` runtime 依赖（OFL-1.1），React 入口只导入简体中文 400/700/900 三个字重，替代对系统中文字体的隐式依赖；玩家可见安全说明改为“内部推演细节/连接凭据/私密材料”等产品语气，不再展示“完整提示词/本地路径/密钥/内部审计原文”等敏感词样。low 子代理完成视觉复核口径发散，medium 子代理完成自动化缺口勘察，均只读、未改文件、未运行 Git 操作；提交前只读复审按最终 diff 和验证证据执行，未发现 P0/P1/P2。本轮不新增后端 API、provider schema、SQLite schema、存档格式、AI 权限、服务器 resolver 或 canonical state 写入。验证已通过 `npm install`、`node --check scripts/clientSmoke.js`、`node --test test/reactClientScaffold.test.js`（31 项）、`node --test test/browserSmokeScript.test.js`（44 项）、`npm run typecheck:client`、`npm run test:client`（63 项）、`npm run build:client`、`npm run smoke:browser -- --client react --screenshots artifacts/s77-3-visual-pixel`、`npm audit --omit=dev`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check` 和完整 `npm test`（929 项通过、0 失败）；首次 `npm run test:client` 曾因本机 rolldown npm optional binding 缺失启动失败，已运行 `npm install` 修复本地依赖后通过。最终 S77.3 截图集人工复看：首页、主卷、舆图、人物、考试、放榜、专题 surface、印匣、六类身份面板和移动端截图中文正常渲染，未见明显文本重叠、现代元素、水印、乱码、露骨或幼态问题。
- 2026-05-19：执行 S76.12 S76 验收。`scripts/clientSmoke.js` 已补强为 S76 总验收入口：大臣身份与入仕官员分开直启验证，六类身份代表草稿行动继续覆盖；奏折队列、拟圣旨、朝议、堂审、军议、人物档案六类专题 surface 会逐项打开检查材料/筹议/草稿三栏和服务器裁决边界，朝议专题保留一次 AI/Mock 拟稿与写入底部奏折闭环；快捷建议按钮点击加入小范围重试，避免 Mock 建议刷新时 DOM 替换造成误报。公开 `ink-ui-manifest.json` 顶层说明改为普通中文安全描述，不在浏览器可读 manifest 里保留敏感词样，并刷新 `asset-qa-report-v1.json` 与 `portrait-compression-qa-v1.json` 的 manifest hash。本轮不新增后端 API、provider schema、SQLite schema、存档格式、AI 权限、服务器 resolver 或 canonical state 写入；完整 `scholar -> child_exam -> provincial_exam -> metropolitan_exam -> palace_exam -> official` 晋级链仍由 `npm run smoke:exam-s69` 验证。只读勘察子代理确认 S76.12 覆盖口径，未改文件、未运行 Git 操作。验证已通过 `node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`npm run test:client`、`node --test test/reactClientScaffold.test.js`、`node --test test/browserSmokeScript.test.js`、`npm run build:client`、`npm run smoke:browser -- --client react --screenshots artifacts/s76-frontend-ink`、`npm run smoke:exam-s69`、`npm run smoke:dual-mode -- --storage-only`、`npm run qa:frontend-assets`、`npm run qa:portrait-compression`、`node --test test/frontendInkAssetsManifest.test.js`、结构化污染扫描、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check`（仅 CRLF/LF 提示）和完整 `npm test`（925 项通过、0 失败）；初次 browser smoke 曾暴露快捷建议按钮 DOM 替换竞态和堂审专题裁决边界断言过窄，已修复并重跑通过。
- 2026-05-19：执行 S77.1 默认入口确认。新增 `scripts/ensureClientBuild.js`，`package.json` 的 `prestart` 现在先检查 `dist/client/index.html` 是否缺失或早于 `client/`、Vite/TS 配置与 package 文件；缺失/陈旧时自动运行 `npm run build:client`，已最新时跳过，保证 README 的 `npm install && npm start` 不因缺 React 构建产物坏掉。`test/reactClientScaffold.test.js` 已显式覆盖 `/game/:sessionId`、`/game/:sessionId/map|people|archive|exam|ranking|court|settings` 的 React Router HTML fallback，并确认产品内没有 `public/legacy.html`、`public/ink-client` 或根级 `ink-client` 旧双入口文件。本轮不新增 API、provider schema、SQLite schema、存档格式、AI 权限、服务器 resolver 或 canonical state 写入；旧 `public/index.html`、`public/app.js`、`public/styles.css` 仍仅作迁移参考。只读勘察子代理确认缺口，未改文件、未运行 Git 操作；提交前只读复审未发现 P0/P1，指出的 P2/P3 文档收口滞后已修正。验证已通过 `node --check scripts/ensureClientBuild.js`、`node --check scripts/clientSmoke.js`、`node --test test/reactClientScaffold.test.js`（29 项）、`node --test test/browserSmokeScript.test.js`（44 项）、`node scripts/ensureClientBuild.js`、`npm run typecheck:client`、`npm run test:client`（63 项）、`npm run build:client`、`npm run smoke:browser -- --client react --screenshots artifacts/s77-1-default-entry`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check`（仅 CRLF/LF 提示）和完整 `npm test`（927 项通过、0 失败）。
- 2026-05-19：执行 S77.2 浏览器 smoke 扩展。`scripts/clientSmoke.js` 新增 `assertHistoryBackForward()` 与 `assertMapResourceFailureFallback()`：React browser smoke 现在从舆图页进入人物页后执行 `page.goBack()` / `page.goForward()`，断言当前 session 舆图/人物路径、React entry、ready selector、无横向溢出和 unsafe API 防线；另开临时页面拦截 `/vendor/pixi.min.js` 与 `/mapRenderer.js`，验证舆图运行时资源失败时显示安全中文 fallback、不渲染坏 canvas、不泄漏 hidden/raw/provider/path/key 形状、不触碰 `/api/game/state/*` 或 `/api/dev/*`。`test/reactClientScaffold.test.js` 新增 S77.2 源码守护测试。low 子代理已做覆盖差距勘察，medium 子代理已做前端体验/资源 fallback/history/reduced-motion 建议，均只读、未改文件、未运行 Git 操作；浏览器级 `prefers-reduced-motion` 深测仍留给 S77.6 可访问性步骤。本轮不新增后端 API、provider schema、SQLite schema、存档格式、AI 权限、服务器 resolver 或 canonical state 写入。验证已通过 `node --check scripts/clientSmoke.js`、`node --test test/reactClientScaffold.test.js`（30 项）、`node --test test/browserSmokeScript.test.js`（44 项）、`npm run typecheck:client`、`npm run test:client`（63 项）、`npm run build:client`、`npm run smoke:browser -- --client react --screenshots artifacts/s77-2-browser-smoke`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check`（仅 CRLF/LF 提示）和完整 `npm test`（928 项通过、0 失败）；Vite 构建仍保留既有 `/assets/ui/...` 运行时资源解析提示，不是新失败。提交前只读最终 diff 复审未发现 P0/P1/P2，提出的 P3 文档收口建议已修正。
- 2026-05-19：执行 S78 官署专题玩法化。新增统一 `topicSurfaceView` 安全投影和六类专题模板：奏折队列、拟圣旨、朝议、堂审、军议、人物档案；后端新增只读 `GET /api/game/topic-surface/:sessionId/:surfaceId`，只从当前 session 派生玩家可见材料、evidence refs、draft slots、scene preview 和公开结果摘要，不写 session、不调用 resolver、不推进时间。新增 `topic_draft` AI task、`topicDraft` schema/prompt pack、Mock/provider/MiMo+DeepSeek 接线和只读 `POST /api/ai/topic-draft/:sessionId`；请求只接收 surfaceId、draftKind、公开 evidence refs 和短按语，服务端校验 evidence 白名单、草稿文体、越权结果声称、hidden/raw/provider/path/key/prompt/server.* 污染，provider 失败、坏 JSON、伪造 evidence 或已裁决语气均降级本地规则草稿。React `SurfaceHost` 将六类专题升级为三栏工作台：材料、筹议、草稿；玩家可勾选证据、选草稿类型、让 AI/Mock 拟稿、编辑正文并写入底部奏折，真正后果仍走普通 `/api/game/turn`。本轮不新增远程存档、账号、多人与云数据库，不扩大 canonical state 写入边界。提交前只读复审发现 P2 裸 `provider/prompt` 草稿污染词缺口和 P3 军议/拟旨地图来源名不一致，已补裸词拦截、`mapContextView` 来源对齐、按专题来源优先排序、裸词降级测试和军议舆图证据测试。验证已通过聚焦 `node --check`、`node --test test/topicSurfaceView.test.js test/topicDraftRoute.test.js test/modelRoutePolicy.test.js test/prompts.test.js test/aiSettings.test.js test/aiSettingsRoute.test.js test/aiSchemas.test.js`（74 项）、`npm run typecheck:client`、`npm run test:client`（63 项）、`node --test test/reactClientScaffold.test.js`（27 项）、`node --test test/browserSmokeScript.test.js`（44 项）、`npm run build:client`、完整 `npm test`（925 项通过）、`npm run smoke:browser -- --client react --screenshots artifacts/s78-topic-surfaces`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`（仅 CRLF/LF 提示）。本轮初次 `npm run smoke:browser` 因脚本仍要求旧“不可调用 resolver”字样失败，已改为接受 S78 服务器裁决边界并重跑通过。
- 2026-05-19：执行 S76.11 专题 surface 扩展位。`client/src/state/uiState.ts` 已扩展 `LocalSurface`，`client/src/surfaces/surfaceRegistry.tsx` 已补齐奏折队列、拟圣旨、朝议、堂审、军议、人物档案和舆图筛选的安全数据来源、占位状态、裁决边界与固定草稿模板；`client/src/components/SurfaceHost.tsx` 统一展示这些边界并继续只写 `actionDraft`；`client/src/pages/CourtPage.tsx` 升级为“朝议与官署”专题入口页，按御案/台阁、官署/军帐、公开人物分组打开 registry-backed surface。`scripts/clientSmoke.js` 新增 `assertTopicSurfaces`，验证 `/court` 六类入口、朝议专题弹层、草稿写入以及不触发普通回合/unsafe API；`client/src/__tests__/App.test.tsx` 与 `test/reactClientScaffold.test.js` 已补 S76.11 运行时/静态守卫。本轮不新增后端 API、provider schema、SQLite schema、存档格式、AI 权限、服务器 resolver 或 canonical state 写入；无安全 projection 时只显示占位，不伪造奏折队列、诏令生效、朝议发言、判词、敌情真值、兵力实数或人物 hidden 真值。按用户要求委派 low 创意发散子代理和 medium 只读勘察子代理；二者均未改文件、未运行 Git 操作。验证已通过 `node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`（27 项）、`npm run test:client`（62 项）、`npm run build:client`、`npm run smoke:browser -- --client react --screenshots artifacts/s76-11-topic-surfaces`（含 `desktop-topic-surfaces`）、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check`（仅 CRLF/LF 提示）和完整 `npm test`（907 项通过、0 失败）；本轮初次 `npm run test:client` 因本地 `node_modules` 缺少 npm optional dependency `@rolldown/binding-linux-x64-gnu` 启动失败，已运行 `npm install` 修复后重跑通过。Vite 构建仍保留既有 `/assets/ui/...` 运行时资源解析提示，不是新失败。下一步是 S76.12 S76 验收。
- 2026-05-19：执行 S76.10 NPC 与玩家立绘接线。`client/src/pages/HomePage.tsx` 新增案主立绘选择，候选来自已审核 manifest runtime 池，优先按玩家身份阶段读取 `portrait_pool_player_s73_10`，女性候选沿用 registry 的高清重制优先排序，无重制时使用已审核原图；`POST /api/game/start` 可接收安全 `portraitRef`，`src/game/initialState.js` 只保存符合 `portrait-...` id 且不含本地路径、远程 URL、key/path/raw/provider/prompt/hidden 等污染片段的值。`client/src/pages/PeoplePage.tsx` 已从全量立绘素材目录改为当前案卷人物谱牒，只读 `worldState.player`、`worldPeopleView.npcs` 和 `worldPeopleView.relationships`，每页最多 8 个公开人物，总量最多保留案主加 79 名公开 NPC；玩家优先使用已选立绘，NPC 只从通用 NPC 池按公开身份/性别匹配，不随机抽取重要 NPC 专属池。`src/game/redactedState.js` 玩家白名单新增清洗后的 `portraitRef`，并会把旧档/污染档中的不安全 ref 清为 `null`；`src/game/worldPeopleSchemas.js` 可见 NPC view 可投影安全 `portraitRef`；前端只把它当 opaque id 交给 registry 命中，不拼路径、不显示未审核素材、不暴露全量素材池数量，无效或缺失 ref 使用纸底 fallback。按用户要求委派 low 创意发散子代理和 medium 只读勘察子代理；子代理均未改文件、未运行 Git 操作。验证已通过 `node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js test/gameStartRole.test.js test/redactedState.test.js test/worldPeopleSchemas.test.js`（46 项）、`npm run test:client`（62 项）、`node --test test/browserSmokeScript.test.js`（44 项）、`npm run build:client`、`npm run smoke:browser -- --client react --screenshots artifacts/s76-10-people-portraits`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check`（仅 CRLF/LF 提示）和完整 `npm test`（906 项通过、0 失败）；提交前只读复审先发现旧档污染 `portraitRef` 清洗和人物谱牒总量上限缺口，均已修复并补测试，最终复审未发现 P0/P1/P2/P3。下一步是 S76.11 专题 surface 扩展位。
- 2026-05-19：执行 S76.9 独立舆图页与地图重构。`/game/:sessionId/map` 现在直接渲染“山河舆图”独立页面，不再先铺主卷案台；`client/src/pages/MapPage.tsx` 展示大画布、地点/驿路/近事统计与图层筛选、公开近事局势簿摘录、入局势簿/回主卷跳转、公开近事“据此拟稿”和舆图安全边界。`client/src/components/InkMapRuntimeBridge.tsx` 继续复用 S72 `public/mapRenderer.js` / PixiJS runtime，并新增前端显示层过滤、tooltip 关闭按钮、标签/tooltip/草稿文本安全清洗、重复 label key 防护和资源失败 fallback 文案。页面只读当前路由 `sessionId` 对应的 `mapRuntimeView` 安全投影；不新增后端 API、provider schema、SQLite schema、存档格式、地图素材 manifest、AI 权限、服务器 resolver 或 canonical state 写入，不访问 raw state/dev diagnostics/provider payload/完整 prompt/本地路径/key/hidden 内容，不把地图坐标或 `layoutPath` 写入 prompt、AI 工具、服务器 resolver、行动事实、URL 或本地存储；图层筛选只影响浏览器显示，舆图按钮只写行动草稿或导航，移动、查案、调兵、财政、外交、任免和持久化继续由主卷普通回合提交后服务器裁决。按用户要求委派 low 创意发散子代理和 medium 只读勘察子代理；子代理均未改文件、未运行 Git 操作。验证已通过 `node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`（25 项）、`npm run test:client`（60 项）、`node --test test/browserSmokeScript.test.js`（44 项）、`npm run build:client`、`npm run smoke:browser -- --client react --screenshots artifacts/s76-9-map-fullscreen`（含 `desktop-map-runtime` / `desktop-map-refresh` / `mobile-map-fullscreen`）、`npm run smoke:exam-s69`、`npm run smoke:dual-mode -- --storage-only`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check`（仅 CRLF/LF 提示）和完整 `npm test`（903 项通过、0 失败）；已人工复看桌面与移动端截图。Vite 构建仍保留既有 `/assets/ui/...` 运行时资源解析提示，不是新失败。下一步是 S76.10 NPC 与玩家立绘接线。
- 2026-05-19：执行 S76.8 放榜全屏。`client/src/pages/RankingPage.tsx` 现在渲染贡院外皇榜全屏界面，通过 manifest registry 读取已审核 `ranking_page` 放榜场景、`imperial_notice` 皇榜纸底和 `red_ink_smudge` 朱砂墨迹素材，展示三鼎甲朱砂章、服务器定榜名单、玩家高亮、榜名详情、考官公开评语、评分维度、防作弊检测、授官提示和皇榜安全边界。页面只读当前路由 `sessionId` 对应的 `lastExamResult.ranking`、考试历史 ranking、`examHonorView`、`examinerPanelView` 和 `appointmentTrackView` 安全投影；不新增后端 API、provider schema、SQLite schema、存档格式、AI 权限或 canonical state 写入，不访问 raw state/dev diagnostics，不在前端排序、补分、补榜、判防弊、判晋级或推断授官，不显示 provider/raw/prompt/path/key/hidden 污染文本、弥封身份映射、未采纳评语或模型原始提案。按用户要求委派 low 创意发散子代理和 medium 只读勘察子代理；子代理均未改文件、未运行 Git 操作。提交前只读复审发现的无 ranking 前端补榜、无防弊结果默认“通过”、缺 place 从 index 推断、评分 key 偏离真实字段和非殿试三鼎甲视觉风险均已修复；最终复审无阻塞项。验证已通过 `node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`（24 项）、`npm run test:client`（60 项）、`node --test test/browserSmokeScript.test.js`（44 项）、`npm run build:client`、`npm run smoke:browser -- --client react --screenshots artifacts/s76-8-ranking-fullscreen`（含 `desktop-ranking-fullscreen` / `mobile-ranking-fullscreen`）、`npm run smoke:exam-s69`、`npm run smoke:dual-mode -- --storage-only`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check`（仅 CRLF/LF 提示）和完整 `npm test`（902 项通过、0 失败）。下一步是 S76.9 独立舆图页与地图重构。
- 2026-05-18：执行 S76.7 科举考试全屏。`client/src/pages/ExamPage.tsx` 现在渲染贡院/殿试全屏考试界面，按考试阶段读取已审核 `exam_page` 场景素材，并使用已审核试卷界栏、朱印按钮和纸张材质；页面展示顶部试别/阶段/局部时间、中央试卷、考题、写作区、字数/草稿状态、场内行动、考场记录、虚拟考生安全占位、近次交卷反馈和服务器裁决边界。新增安全 route view 类型预留 `examProcedureView`、`examinerPanelView`、`examRivalView`、`examHonorView`，但本轮不新增后端 API、provider schema、SQLite schema、存档格式或 AI 权限；取题、推进考场和交卷仍走既有考试 API，不提交普通回合，不直接评分、判舞弊、放榜、晋级、授官或写 canonical state。按用户要求委派 low/medium 子代理做创意发散、代码勘查和小块实现；实施子代理只改 `ExamPage.tsx`、未执行 Git 操作，主代理已审阅整合、补类型、样式、smoke 和测试。提交前只读复审未发现 P0/P1；P2 最近交卷考试名未过滤和 P3 移动端科举 smoke 缺口均已修复，错误文本也已走本页安全过滤。验证已通过 `npm run typecheck:client`、`node --check scripts/clientSmoke.js`、`node --test test/reactClientScaffold.test.js`（23 项）、`npm run test:client`（58 项）、`npm run build:client`、`npm run smoke:browser -- --client react --screenshots artifacts/s76-7-exam-fullscreen`（含 `mobile-exam-fullscreen`）、`npm run smoke:exam-s69`、`npm run smoke:dual-mode -- --storage-only`、`node --test test/browserSmokeScript.test.js`（44 项）、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check`（仅 CRLF/LF 提示）和完整 `npm test`（901 项通过、0 失败）。下一步是 S76.8 放榜全屏。
- 2026-05-18：执行 S76.6 皇帝面板。新增 `client/src/components/EmperorPanel.tsx`，`client/src/pages/GamePage.tsx` 在 `player.role === "emperor"` 时接入已审核 `role_background` 御案素材、安全 `officialPostingsView`、`eventArchiveView`、`actorMemoryView`、`aiControlAuditView`、`worldEntityView`、`worldThreadView`、`mapRuntimeView` 和玩家公开摘要；面板展示御案朝仪、奏折队列、朱批拟稿、圣旨草稿、朝议、任免候选、赏罚预留和御案边界。所有专题按钮只写入底部 `MemorialComposer` 行动草稿，不提交回合、不调用 resolver、不推进时间、不直接让朱批成案、圣旨生效、任免、赏罚、处分或写 canonical state；皇帝行动仍走普通自然语言回合并由服务器裁决。本轮不新增后端 API、provider schema、SQLite schema、存档格式或 AI 权限。按用户要求委派 medium 实施子代理处理组件小块；该子代理只新增 `EmperorPanel.tsx`、未执行 Git 操作，主代理已审阅、接线、补样式和测试。验证已通过 `node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`、`npm run test:client`、`node --test test/browserSmokeScript.test.js`、`npm run build:client`、`npm run smoke:browser -- --client react --screenshots artifacts/s76-6-emperor-panel`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check`（仅 CRLF/LF 提示）和完整 `npm test`（900 项通过、0 失败）。下一步是 S76.7 科举考试全屏。
- 2026-05-18：执行 S76.5 将领面板。新增 `client/src/components/GeneralPanel.tsx`，`client/src/pages/GamePage.tsx` 在 `player.role === "general"` 时接入已审核 `role_background` 边塞军帐素材、安全 `militaryDiplomacyView`、`officialPostingsView`、`mapRuntimeView`、`eventArchiveView`、`actorMemoryView` 和玩家公开摘要；面板展示军帐总览、粮饷与军心、斥候与情报、边患与舆图、战报与边议和军令边界。所有专题按钮只写入底部 `MemorialComposer` 行动草稿，不提交回合、不调用 resolver、不推进时间、不直接结算战役、调兵遣将、外交和战、统帅任免、粮饷拨付、赏罚或写 canonical state；军令仍走普通自然语言行动或未来服务器 `militaryDiplomacyResolver` 裁决。本轮不新增后端 API、provider schema、SQLite schema、存档格式或 AI 权限。按用户要求委派 medium 实施子代理处理组件小块；该子代理只新增 `GeneralPanel.tsx`、未执行 Git 操作，主代理已审阅、调整安全投影字段、接线、补样式和测试。验证已通过 `node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`、`npm run test:client`、`node --test test/browserSmokeScript.test.js`、`npm run build:client`、`npm run smoke:browser -- --client react --screenshots artifacts/s76-5-general-panel`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check`（仅 CRLF/LF 提示）和完整 `npm test`（899 项通过、0 失败）。下一步是 S76.6 皇帝面板。
- 2026-05-18：执行 S76.4 入仕官员/大臣面板。新增 `client/src/components/OfficialMinisterPanel.tsx`，`client/src/pages/GamePage.tsx` 在 `player.role === "official"` 或 `player.role === "minister"` 时接入已审核 `role_background` 部院/大臣素材、安全 `officialCareerView`、`appointmentTrackView`、`officialPostingsView`、`actorMemoryView`、`aiControlAuditView` 和玩家公开摘要；面板展示官职履历、部院公文、同年座师、人脉记忆、派系/朝局风险、考成、弹劾和奏疏入口。所有专题按钮只写入底部 `MemorialComposer` 行动草稿，不提交回合、不调用 resolver、不推进时间、不直接任免、奖惩、处分、弹劾成案、不改写考成、不写 canonical state；官缺、升降、铨选、考成、处分、派系影响和持久化仍由服务器裁决。本轮不新增后端 API、provider schema、SQLite schema、存档格式或 AI 权限。按用户要求委派 medium 实施子代理处理组件小块；该子代理只新增 `OfficialMinisterPanel.tsx`、未执行 Git 操作，主代理已审阅、接线、补样式和测试。验证已通过 `node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`、`npm run test:client`、`node --test test/browserSmokeScript.test.js`、`npm run build:client`、`npm run smoke:browser -- --client react --screenshots artifacts/s76-4-official-minister-panel`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check`（仅 CRLF/LF 提示）和完整 `npm test`（898 项通过、0 失败）。下一步是 S76.5 将领面板。
- 2026-05-18：执行 S76.3 地方官面板。新增 `client/src/components/MagistratePanel.tsx`，`client/src/pages/GamePage.tsx` 在 `player.role === "magistrate"` 时接入已审核 `role_background` 县衙素材、安全 `localAffairsDocketView`、`officialPostingsView`、`economicFiscalView` 和玩家公开摘要；面板展示案牍总览、公堂词讼、钱粮仓储、水利盗警、士绅乡约、考成/风评/名望和裁决边界。所有专题按钮只写入底部 `MemorialComposer` 行动草稿，不提交回合、不调用 resolver、不结案、不征税/开仓、不修堤结算、不缉捕归案、不任免、不推进时间、不写 canonical state；审案、钱粮、水利、盗匪、士绅、考成和持久化仍由服务器裁决。本轮不新增后端 API、provider schema、SQLite schema、存档格式或 AI 权限。按用户要求委派 medium 只读子代理做地方官面板信息架构/文案发散，主代理实现并整合；提交前只读复审未发现 P0/P1，P2 文档验证占位已修正，P3 截图覆盖为既有固定 composer 表现、无阻塞。验证已通过 `node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`、`npm run test:client`、`node --test test/browserSmokeScript.test.js`、`npm run build:client`、`npm run smoke:browser -- --client react --screenshots artifacts/s76-3-magistrate-panel`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check`（仅 CRLF/LF 提示）和完整 `npm test`（897 项通过、0 失败）。下一步是 S76.4 入仕官员/大臣面板。
- 2026-05-18：执行 S76.1 主游戏壳。`client/src/pages/GamePage.tsx` 现在将主卷整理为案卷状态条、按玩家公开身份选择的场景插画带、叙事卷、案头安全投影索引、舆图/人物/史册/科举/皇榜/朝议/印匣功能页签和固定底部 `MemorialComposer`；场景带优先通过 `assetRegistry` 读取 manifest 中 runtime 可用、`usage="game_main"` 的已审核场景图，失败时只回落到既有已审核 `/assets/ui/scenes/` 项目路径。主卷只消费安全 `player-state` / turn payload、UI store 安全摘要和 route view flags；污染的案主、身份和叙事文本会回落为中文 fallback，不把 raw state、开发诊断、provider 原文、完整 prompt、本地路径、key、hidden notes 或 hidden intent 写入 DOM/URL/本地存储。功能页签只导航，快捷建议仍只写草稿，普通行动、移动、任免、审案、行军、考试和时间推进继续由服务器裁决；自动刷新快捷建议不再携带草稿预览，草稿预览只在玩家显式刷新时发送，避免逐字触发 provider 建议。本轮不新增后端 API、provider schema、SQLite schema、存档格式、AI 权限、科举晋级或官职任免规则。按用户要求已委派 medium 子代理做前端信息架构发散；提交前只读复审未发现 P0/P1，发现的 P2 已修正并补测试。验证已通过 `npm run typecheck:client`、`npm run test:client`、`node --test test/reactClientScaffold.test.js test/browserSmokeScript.test.js`、`npm run build:client`、`npm run smoke:browser -- --client react --screenshots artifacts/s76-1-game-shell`、docs governance、documentation governance、`git diff --check` 和完整 `npm test`（895 项通过、0 失败）。下一步是 S76.2 书生面板。
- 2026-05-18：执行 S76.2 书生面板。`client/src/components/ScholarPanel.tsx` 已新增书生专题面板，`client/src/pages/GamePage.tsx` 在 `player.role === "scholar"` 时接入已审核 `role_background` 书斋素材、安全 `studyProfileView`、`examCalendarView` 和玩家公开摘要；面板展示读书簿七项、老师点评、师友与保结前置、科期、误期/近科记录、文章练习、荐书、近课和科举/皇榜入口。所有专题按钮只写入底部 `MemorialComposer` 行动草稿，不取题、不交卷、不推进时间、不调用 resolver、不改榜、不晋级、不授官；完整书生路径仍由既有考试与普通回合服务器裁决。本轮不新增后端 API、provider schema、SQLite schema、存档格式或 AI 权限。按用户要求曾委派 medium 实施子代理处理组件小块；该子代理写入 `ScholarPanel.tsx` 后因并发写入风险被主代理关闭，未执行 Git 操作，主代理已审阅整合其产出。验证已通过 `node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`node --test test/reactClientScaffold.test.js`、`npm run test:client`、`node --test test/browserSmokeScript.test.js`、`npm run build:client`、`npm run smoke:browser -- --client react --screenshots artifacts/s76-2-scholar-panel`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check` 和完整 `npm test`（896 项通过、0 失败）；提交前只读复审未发现 P0/P1/P2/P3。下一步是 S76.3 地方官面板。
- 2026-05-18：按用户要求压缩 `docs/DEVELOPMENT_STEPS.md` 的“5. 进度记录”。本轮只改活动台账的历史流水呈现，把 S73-S75 保留为关键里程碑、最新 S75.10 验收口径和 S76.1 下一步；不改运行时代码、API/schema、provider schema、SQLite schema、存档格式、AI 权限、素材 manifest 或前端行为。旧逐日细节不再在活动台账展开，需要追溯时看 Git history、专题归档和本交接板历史记录。低风险纯文档压缩，跳过提交前子代理复审。
- 2026-05-18：执行 S75.10 S75 浏览器验收。`scripts/clientSmoke.js` 现兼容 `npm run smoke:browser -- --client react`，非 React 目标会明确失败；真实浏览器 smoke 从 `/` Mock 开局后通过右上角印匣里的“返回首页”验证返回首页/继续本局，遍历桌面端 `AI 设置`、`显示`、`安全`、`旧案` 四个 tab，断言 `aria-selected`、快捷建议工具预算固定为 0、关闭后焦点回收、hidden/raw/key/path/provider payload 防线，并在“旧案”tab 按当前案卷短码点击“载入”，监听 `GET /api/game/player-state/:sessionId`，继续由全局拦截确认不触碰 `/api/game/state/*` 或 `/api/dev/*`。移动端 390x844 也会打开印匣并遍历四 tab，检查抽屉在视口内、无横向溢出和无敏感泄漏；快捷建议点击后改为等待草稿实际进入 textarea 再 Enter 呈上，避免 React 状态同步假阴性。截图证据目录为 `artifacts/s75-10-browser-acceptance`。本轮不改后端 API/schema、provider schema、SQLite schema、存档格式、AI 权限、科举晋级、官职裁决或服务器 canonical state；`surface` 来源仍是 helper/扩展路径，真实专题 composer 留给 S76.11；浏览器级 `prefers-reduced-motion` context 深测留给 S77.6。验证已通过 `node --check scripts/clientSmoke.js`、`node --test test/reactClientScaffold.test.js test/browserSmokeScript.test.js`（61 项）、`npm run typecheck:client`、`npm run test:client`（50 项）、`npm run smoke:browser -- --client react --screenshots artifacts/s75-10-browser-acceptance`、docs governance、documentation governance、`git diff --check` 和完整 `npm test`（895 项通过、0 失败）。提交前只读复审未发现 P0/P1/P2；遗留 reduced-motion browser context 与真实专题 `surface` 来源验收已明确留给 S77/S76。下一步是 S76.1 主游戏壳。
- 2026-05-18：执行 S75.9 快捷功能 AI 接入与完善。新增 `quick_action` AI task、provider schema、prompt pack、Mock/provider 接线和只读 `POST /api/ai/quick-actions/:sessionId`；浏览器只发送当前页、短草稿预览和建议数量，服务器从 session 派生 redacted player summary、route view flags、公开 evidence refs 与 actor 工具能力说明。AI 只能生成可点击行动草稿，不提交普通回合、不调用 server resolver、不推进时间、不裁决结果、不写 canonical state，也不持久写 session；schema 校验、身份/工具意图 allowlist、公开 evidence ref 白名单、数量/长度 cap、source 归一化、结果声称拦截和污染清洗都在服务器完成，provider 失败、坏 JSON、越权工具意图、伪造 evidence、声称已裁决结果或污染文本会降级为本地规则建议。`quick_action` 可单独配置 provider、model、输出长度、timeout 和温度，工具预算硬锁为 0，`mayUseTools` / `mayRequestAdjudication` 始终为 false；`mimo-deepseek` 快捷建议由 MiMo 侧转发。React `qianqiuApi`、`gameSessionState`、`GamePage`、`MemorialComposer`、印匣和独立设置页已接入刷新、loading、stale、failed、applied 与来源标记；点击建议仍只写入行动草稿，只有 Enter 或“呈上”才调用 `/api/game/turn`，舆图/专题派生建议会保留 `map-runtime` / `surface` 来源。响应级 `source/status/fallbackReason/generatedAtTurn` 是当前快捷建议安全审计边界；若后续要持久记录 quick action 调动，需另设不破坏 read-only 语义的安全审计方案。Kierkegaard 只读差距审查发现的 P1 工具预算可配置、P2 降级状态不显性、P3 文档待同步均已修正或记录；Dirac 提交前只读复核未发现 P0/P1，其 P2/P3 已修正，补丁后复核未发现新的 P0/P1/P2；剩余 P3 是 `surface` 来源目前为 helper/扩展路径，S75.10 若要玩家可见验收需接实际专题 composer 或明示为预留。收口 browser smoke 发现的旧 S72 `public/mapRenderer.js` Pixi `app.resize()` 兼容问题也已改为 `renderer.resize` fallback。验证已通过 S75.9 后端/AI/route 聚焦测试、prompt pack 测试、client typecheck、client Vitest、client build、S72 map frontend 聚焦测试、docs governance、`git diff --check`、`npm run smoke:browser -- --screenshots artifacts/s75-9-quick-actions-ai` 和完整 `npm test`（894 项通过、0 失败）。
- 2026-05-18：执行 S75.8 底部奏折输入雏形。React `AppShell` shell marker 更新为 `s75-8`；新增 `client/src/components/MemorialComposer.tsx` 和 `client/src/components/quickActionSuggestions.ts`，主卷旧内嵌行动表单改为固定底部奏折/竹简 composer。输入框使用本地输入态，placeholder 按安全玩家身份显示且不再作为可提交 value；Enter 呈上、Shift+Enter 换行，空白行动禁用，普通回合失败保留输入，成功后继续由 `gameSessionState.submitTurn` 清空 UI 草稿。快捷区首个入口“可行事”根据当前安全玩家身份与 route view flags 生成本地 `local-rule` 建议，点击只写入 `role-surface` 行动草稿，不提交行动、不调用 AI/provider、不裁决结果、不写 canonical state；`QuickActionSource` 已预留 `local-rule`、`mock-ai`、`provider-ai`、`map-runtime`、`surface`，AI/Mock 推荐、安全 endpoint、schema 校验、降级和污染过滤留给 S75.9。本轮不改后端 API/schema、provider schema、SQLite schema、存档格式、AI 权限、科举晋级、官职裁决或服务器 canonical state；不暴露 raw state、完整 prompt、provider payload、本地路径、key 或 hidden 信息。Bernoulli medium 子代理先落地早期组件/test patch，Codex 接手后统一 props/helper 接口、补 CSS、scaffold、browser smoke 和文档。Harvey 只读复审未发现 P0/P1/P2；复审中误跑 `npm run test:client -- --runInBand` 因 Vitest 不支持该参数失败，随后用项目原生命令重跑通过。验证已通过 `npm run typecheck:client`、`npm run test:client`（48 项）、`node --test test/reactClientScaffold.test.js`（16 项）、`node --check scripts/clientSmoke.js`、`node --test test/browserSmokeScript.test.js test/reactClientScaffold.test.js`（60 项）、`npm run build:client`、`npm run smoke:browser -- --screenshots artifacts/s75-8-memorial-composer`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check`（仅 LF/CRLF warning，退出码 0）和完整 `npm test`（879 项通过、0 失败）。
- 2026-05-18：按用户要求重排 S75 后续步骤。`docs/DEVELOPMENT_STEPS.md` 已在 S75.8 后新增 S75.9“快捷功能 AI 接入与完善”，用于接入 AI/Mock 推荐、完善“可行事”快捷区、降级、安全校验和测试；原 S75.9“S75 浏览器验收”顺延为 S75.10。`docs/FRONTEND_INK_REDESIGN_ROADMAP.md` 已同步新增 S75.9 规划，明确 AI 只生成可点击草稿建议，不提交行动、不裁决结果、不写 canonical state；安全调用层只能发送 redacted player summary、route view flags、当前页、短草稿预览和必要 session 标识，失败或无 key 回退本地规则建议，返回建议需 schema 校验、长度 cap、敏感词过滤和身份 allowlist，UI 不暴露 raw state、provider payload、完整 prompt、本地路径、key 或 hidden 信息。本轮仅为低风险文档重排与规划澄清，不改运行时代码、API/schema、provider schema、SQLite schema、存档格式、AI 权限、科举晋级、官职裁决或服务器 canonical state；跳过提交前子代理复审。
- 2026-05-18：执行 S75.7 显示偏好本地安全持久化。React `AppShell` shell marker 更新为 `s75-7`；新增 `client/src/state/displayPreferenceStorage.ts`，只把 `motion`、`textSize`、`contrast`、`autoScroll` 和 `mapMotion` 五个白名单字段写入 `qianqiu.displayPreferences.v1`，payload 使用 `display-preferences-v1` schema 版本。`useUiStateStore` 启动、reset 和运行时修改均走清洗层；旧 schema、损坏 JSON、未知字段、非法值和 raw/provider/prompt/path/key 污染文本会被丢弃或回落默认值，不保存 `currentSessionId`、安全玩家投影、行动草稿、raw state、provider payload、完整 prompt、本地路径或 key。`scripts/clientSmoke.js` 已覆盖印匣显示 tab 调整、localStorage 白名单、reload 后 data attributes 恢复和舆图 runtime `data-map-motion="reduced"`；首次 smoke 因 Playwright `getByLabel("动效")` 同时命中“舆图动效”复选框失败，已改为 role 精确定位后重跑通过。Boyle medium 子代理被派发前端存储层任务但超时关闭且未返回最终报告；Codex 审阅工作树并收束实现、测试和文档。Kuhn 只读复审未发现 P0/P1/P2，P3 测试隔离建议已补。验证已通过 `npm run typecheck:client`、`npm run test:client`（43 项）、`node --test test/reactClientScaffold.test.js`（15 项）、`npm run build:client`、`npm run smoke:browser -- --screenshots artifacts/s75-7-display-preferences`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check`（仅 LF/CRLF warning，退出码 0）和完整 `npm test`（878 项通过、0 失败）；P3 修复后已重跑前端 typecheck/test/scaffold 通过。本轮不改后端 API/schema、provider schema、SQLite schema、存档格式、AI 权限、科举晋级、官职裁决或服务器 canonical state；下一步是 S75.8 底部奏折输入雏形。
- 2026-05-18：执行 S75.6 返回首页与继续本局。React `AppShell` shell marker 更新为 `s75-6`；顶部“返回千秋首页”和印匣“返回首页”都会保留当前安全 `currentSessionId`，`returnHome` 只关闭抽屉/弹窗/专题层并清空临时行动草稿，不调用 start/turn/raw state/dev diagnostics，不删除或重写服务器 session。首页新增“当前本局 / 继续本局”条，只在 UI store 的安全玩家投影与可运行 sessionId 匹配时显示，摘要使用安全玩家名、身份/功名/官职、案卷短码和来源，命中 raw/path/key/prompt/provider 污染词时回落中文 fallback。`scripts/clientSmoke.js` 已覆盖真实 Mock 开局 -> 返回首页 -> 继续本局 -> 递送一回合，并继续拦截 unsafe API。Pasteur medium 子代理在用户中断前落地 `continueShelf` 样式和 App 集成测试，Codex 审阅沿用并补 store/scaffold/smoke/文档；Poincare 只读复审指出的继续本局摘要污染覆盖不足已修正，并新增污染 payload 集成测试。验证已通过 `npm run typecheck:client`、`npm run test:client`（39 项）、`node --test test/reactClientScaffold.test.js`（14 项）、`npm run build:client`、`npm run smoke:browser -- --screenshots artifacts/s75-6-continue-session`；初次 `npm run test:client` 因本机 `node_modules` 缺少 `@rolldown/binding-win32-x64-msvc` optional binding 失败，执行 `npm install` 补齐本机依赖后重跑通过且未改 package/lock。本轮不改后端 API/schema、provider schema、SQLite schema、存档格式、AI 权限、科举晋级、官职裁决或显示偏好持久化；下一步是 S75.7 显示偏好本地安全持久化。
- 2026-05-18：执行 S75.5 案卷式存档/读档。React 新增 `SaveCaseList`，首页旧案卷与印匣“旧案”tab 复用同一案卷卡片，显示 sessionId 短码、玩家名、身份/功名/官职、朝代年月旬、回合数、公开摘要和最近更新时间；metadata 缺字段时显示中文 fallback，且玩家名/身份/朝代/摘要命中 raw state、开发诊断、provider payload、完整 prompt、本地路径、key 或 `data/sessions` 污染词时会落回安全 fallback。首页读档进入 `/game/:sessionId` 后由 `GamePage` 继续走 `loadSession` / `GET /api/game/player-state/:sessionId`，印匣读档按钮也只调用 `loadSession` 后跳转主卷；不读取 raw state、开发诊断、provider payload、完整 prompt、本地路径或 key。Ampere medium 子代理按用户要求启动前端 scoped patch，产出未接入的重复 `SaveDossierList`，Codex 审阅后删除并以已测试的 `SaveCaseList` 收束。验证已通过 `npm run typecheck:client`、`npm run test:client`（36 项）、`node --test test/reactClientScaffold.test.js`（13 项）、`npm run build:client`、`npm run smoke:browser -- --screenshots artifacts/s75-5-save-cases`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check`（仅 LF/CRLF warning，退出码 0）和完整 `npm test`（876 项通过、0 失败）。Raman 只读复审发现的 P2 文档验证状态滞后和 save metadata summary 污染防线已修正；本轮不改后端 API/schema、provider schema、SQLite schema、存档格式、AI 权限、科举晋级、官职裁决或显示偏好持久化。下一步是 S75.6 返回首页与继续本局。
- 2026-05-18：执行 S75.4 右上角印匣入口。React `AppShell` 顶部右侧已从分散工具按钮收束为单一“印匣”按钮，`SurfaceHost` 设置抽屉改为印匣 tab：AI 设置、旧案、显示、安全；旧 `openDrawer("saves")` / `openDrawer("display-preferences")` 兼容入口会打开同一印匣并切到对应 tab。印匣内支持保存当前案卷 AI preset、试连 provider、刷新旧案、通过 `loadSession` / `player-state` 载入旧案并跳转主卷、返回首页、调整运行时显示偏好和查看安全摘要；数据继续来自安全 API client、脱敏 saves metadata 和 UI store 安全玩家摘要，不读取 raw state、开发诊断、内部审计原文、模型原始返回、完整提示词、本地路径或 key。Mill medium 子代理按用户要求启动前端 scoped patch 尝试但未产出 patch，已关闭；Codex 完成实现、测试和文档。验证已通过 `npm run typecheck:client`、`npm run test:client`（34 项）、`node --test test/reactClientScaffold.test.js`（12 项）、`npm run build:client`、`npm run smoke:browser -- --screenshots artifacts/s75-4-inkbox`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check`（仅 LF/CRLF warning，退出码 0）和完整 `npm test`（875 项通过、0 失败）。Kepler 只读复审未发现 P0/P1/P2；P3 建议是 S75.10 或 S75.5 后补真实浏览器点击右上角印匣并遍历 tab。本轮不改后端 API/schema、provider schema、SQLite schema、存档格式、AI 权限、科举晋级、官职裁决或显示偏好持久化。下一步是 S75.5 案卷式存档/读档。
- 2026-05-18：执行 S75.3 朱印开始按钮反馈。React 默认 `/` 首页朱印按钮已新增短促落章反馈、loading 扫光、disabled/error 视觉和 `aria-live` 状态说明；提交路径继续只走 `startNewGame` / `POST /api/game/start`，不读取 raw state、开发诊断、provider payload、完整 prompt、本地路径或 key。前端新增同步 `submitLockRef` 防线，连续 Enter、双击或同一提交周期重复 submit 只触发一次 start；低动效同时读取应用显示偏好和系统 `prefers-reduced-motion: reduce`，低动效下不播放落章动画。Epicurus medium 子代理完成前端 scoped patch，Codex 补测试、文档和 smoke；验证已通过 `npm run typecheck:client`、`npm run test:client`（33 项）、`node --test test/reactClientScaffold.test.js`（11 项）、`npm run build:client`、`npm run smoke:browser -- --screenshots artifacts/s75-3-start-seal`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check`（仅 LF/CRLF warning，退出码 0）和完整 `npm test`（874 项通过、0 失败）。Parfit 只读复审未发现 P0/P1/P2；残余说明是系统级 `prefers-reduced-motion` 真实浏览器行为主要由源码/CSS、Vitest 低动效路径和本轮 browser smoke 共同覆盖。本轮不改后端 API/schema、provider schema、SQLite schema、存档格式、AI 权限、科举晋级或官职裁决规则。下一步是 S75.4 右上角印匣入口。
- 2026-05-18：执行 S75.2 开局表单。React 默认 `/` 首页册页表单已补齐朝代、年份、六类身份、姓名、书生家境和自定背景，非书生身份隐藏家境并显示身份底色说明；提交继续只走 `startNewGame` / `POST /api/game/start`，不读取 raw state、开发诊断、provider payload、完整 prompt、本地路径或 key。服务器新增公开 `familyBackground` 归一化，只接受贫寒/普通/世家并且只在 `scholar` 身份保留；自定背景写入公开 `customSetting`，不是 hidden 私档，不授予功名、官职或绕过科举晋级。Hilbert medium 子代理完成前端 scoped patch，Codex 收束 payload，补服务器边界、测试和文档。验证已通过 `npm run typecheck:client`、`npm run test:client`（31 项）、`node --test test/gameStartRole.test.js test/reactClientScaffold.test.js`（18 项）、`npm run build:client`、`npm run smoke:browser -- --screenshots artifacts/s75-2-start-form`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check`（仅 LF/CRLF warning，退出码 0）和完整 `npm test`（873 项通过、0 失败）；Aristotle 只读复审未发现 P0/P1/P2，残余建议是历史公开 `background` 字段仍作为兼容 start 字段保留，若要收紧外部 start schema 需另开兼容评估。下一步是 S75.3 朱印开始按钮反馈。
- 2026-05-18：按用户要求排查 Codex/Git 面板显示的 `+45,442/-27,865` 大量变更来源。确认当前 `HEAD`、`main` 和 `origin/main` 均为 `e65aaa6`，工作区无未提交变更；该数字对应最近 15 个已提交的 React 前端迁移、S73/S74/S75 素材 manifest/QA 与立绘重制累计 diff，不是待提交垃圾。仓库卫生处理：`artifacts/` 本地 browser smoke 截图约 1.9GB、`tmp/` 和 `dist/` 均已删除，它们已在 `.gitignore` 中不会推送到 GitHub；`.env`、`node_modules/` 和 `data/` 本地存档未触碰。另将历史跟踪的 `game-start-*.log` 启动日志移出仓库并加入忽略规则。未改运行时代码、API/schema、provider schema、SQLite schema、存档格式、AI 权限或游戏规则；本轮是低风险仓库卫生和文档记录，跳过子代理复审。
- 2026-05-18：执行 S75.1 首页画卷布局。React 默认 `/` 首页已接入 S73.4 已审核 `home-scroll-landscape-v1`、`home-mist-layer-v1`、`home-register-form-paper-v1`、`home-cinnabar-start-seal-v1`、`home-archive-casefile-v1` 和 `home-static-reduced-motion-v1`，形成标题“千秋”、水墨云雾、中央册页表单和旧案卷提示；`prefers-reduced-motion: reduce` 使用静态底并关闭云雾漂移。首页继续只用现有 start/saves 安全接口，不新增 raw state route、开发诊断、provider payload、完整 prompt、本地路径或 key；本轮不改后端 API/schema、provider schema、SQLite schema、存档格式、AI 权限或游戏规则。Parfit medium 子代理完成前端 scoped patch，Codex 修正旧 `#start-form` DOM 标记以保持 React smoke 防线；桌面/移动 smoke 与 Codex 浏览器实看均无横向溢出或 hidden/raw 文本。验证已通过 `npm run typecheck:client`、`npm run test:client`、`node --test test/reactClientScaffold.test.js`、`npm run build:client`、`npm run smoke:browser -- --screenshots artifacts/s75-1-home-scroll`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check`（仅 LF/CRLF warning，退出码 0）和完整 `npm test`（872 项通过、0 失败）。提交前 Gauss 只读复审未发现 P0/P1/P2；残余风险是 S75.2 扩更多表单字段时需重新验移动端高度、按钮状态和低动效反馈。本步不提前实现 S75.2 完整开局字段或 S75.3 朱印盖章反馈。
- 2026-05-18：执行 S74.7 默认入口验收。`AppShell` 顶部主卷/舆图/人物/史册入口在真实案卷中绑定当前安全 `sessionId`，预览卷仍使用 `s74-preview`；`scripts/clientSmoke.js` 无 `--url` 时显式固定 `AI_PROVIDER=mock`，从默认 `/` 首页表单真实开局，验证当前 session 导航、舆图 canvas、人物立绘懒加载分页、史册/科举/皇榜/朝议/印匣刷新恢复、移动端首页和 unsafe `/api/game/state/*` / `/api/dev/*` 请求防线。README、brief、架构、浏览器验收、AI 控制矩阵、React 迁移契约和前端路线图已同步：`npm run smoke:browser` 现在代表 React 默认入口 smoke，JSON/SQLite parity 与 no-browser 存储验收继续走 `npm run smoke:dual-mode` / `--storage-only`；产品内不保留 `/legacy.html` 或 `/ink-client/` 双入口，失败回退是 Git revert 或恢复上一提交。本轮不改后端 API/schema、provider schema、SQLite schema、存档格式、AI 权限或游戏规则。验证已通过 `node --check scripts/clientSmoke.js`、`npm run typecheck:client`、`npm run test:client`、`node --test test/reactClientScaffold.test.js`、`npm run build:client`、`npm run smoke:browser -- --screenshots artifacts/s74-7-default-entry`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check` 和完整 `npm test`（872 项通过、0 失败）。提交前只读复审未发现 P0/P1/P2，P3 `BROWSER_ACCEPTANCE.md` 最新验收结果旧口径已修正。
- 2026-05-18：执行 S74.6 S72 地图运行时桥。新增 `client/src/components/InkMapRuntimeBridge.tsx`，React 侧动态加载 `/vendor/pixi.min.js` 和 `/mapRenderer.js`，直接复用 S72 `window.MapRenderer`，由 React 管理容器、label、tooltip 和行动草稿；`MapPage` 只在当前安全 session payload 与路由 `sessionId` 一致时渲染 `mapRuntimeView`，行动草稿写入 Zustand `actionDraft`，仍需玩家回主卷提交普通回合。`client/src/api/types.ts` 新增 `MapRuntimeView` 等只读类型；`public/mapRenderer.js` 新增 `motionEnabled` / `setMotionEnabled`，接入显示偏好里的舆图动效降级。S74.6 明确不调用旧 `public/mapPanel.js`、不依赖旧 `public/app.js`、旧 `#action-input` 或旧 `#information-panel`，不保存 raw session、内部审计原文、模型原文、完整 prompt、本地路径或 key。已视觉查看现有 `ink-world-base-v1` 底图，确认水墨案头基调可继续使用，本轮不重做地图素材；后续若截图暴露图标比例或移动端可读性问题，再开地图 v2 polish。验证已通过 `npm run typecheck:client`、`npm run test:client`、`node --test test/reactClientScaffold.test.js test/mapFrontendShell.test.js test/mapAssetsManifest.test.js test/mapRuntimeView.test.js test/mapRuntimeRoute.test.js`、`npm run build:client`、`npm run smoke:browser -- --screenshots artifacts/s74-6-map-runtime-bridge`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check` 和完整 `npm test`（871 项通过、0 失败）。提交前只读复审未发现 P0/P1/P2；P3 建议后续补 session mismatch 与 mapMotion toggle 组件测试，并在地图 view 更新时清空旧 tooltip。
- 2026-05-18：执行 S74.5 资产加载层。新增 `client/src/assets/assetRegistry.ts`、`client/src/assets/useAssetRegistry.ts` 和 `client/src/components/Portrait.tsx`，React runtime 通过 `/assets/ui/ink-ui-manifest.json` 读取 active manifest，按 `runtimeUsableReviewStatuses`、安全 `/assets/ui/` 路径、fallback、缩略图、低清占位和 `allowEagerLoad=false` 建立查询层；未审核素材、planned 矩阵、本地 artifacts 母版和 provider/prompt/raw/hidden 内容不进入组件输出。人物页已按 `usage="people_page"` 分页接入全部 524 张人物页可用立绘，每页只渲染 8 张缩略图；女性高清重制通过 `source.localHighResSource=kept_outside_public_manifest` 自动优先列前，未重制女性立绘继续使用 manifest 原图。验证已通过 `npm run typecheck:client`、`npm run test:client`、`node --test test/reactClientScaffold.test.js`、`node --test test/frontendInkAssetsManifest.test.js`、`npm run qa:frontend-assets`、`npm run qa:portrait-compression`、`npm run qa:single-portrait-overrides`、`npm run qa:portrait-matrix`、`npm run qa:player-female-portraits`、`npm run build:client`、`npm run smoke:browser -- --screenshots artifacts/s74-5-asset-registry-smoke`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`node --check scripts/clientSmoke.js`、`git diff --check` 和完整 `npm test`（870 项通过、0 失败）。提交前只读复审未发现 P0/P1/P2；P3 文档收口标记和 build 体积旧口径已修正。
- 2026-05-18：已按用户要求把 S73.10.7 女性立绘单张重制续作收口提交前状态。本批 24 张已入库：`portrait-s73-10-generic_npc-female-style-palace-03-look03-v1`、玩家女性补充池边关女将 6 张、士族贵女 6 张、高阶宫廷女官 6 张和商贾女东家前 5 张。当前 `portrait-single-override-qa-v1.json` 为 60 张单张覆盖，`artifacts/s73-10-single-portrait-overrides/` 共有 181 张 PNG 母版；公开 runtime WebP、缩略图、低清占位、manifest、单张覆盖 QA、玩家女性 QA、通用 NPC QA 兼容逻辑、frontend asset QA 和 portrait compression QA 均已同步。`test/frontendInkAssetsManifest.test.js` 已更新：继续硬性检查玩家女性身份阶段 36 张完整覆盖，同时允许单张覆盖扩展到玩家女性补充池和 1 张通用 NPC 女性风格图；`scripts/frontendGenericNpcPortraitAssets.js` 也识别 `localHighResSource=kept_outside_public_manifest` 的覆盖标记，不再用旧批次 QA bytes 卡新 runtime 文件。验证已通过 `npm run qa:single-portrait-overrides`、`npm run qa:player-female-portraits`、`npm run qa:generic-npc-portraits`、`npm run qa:frontend-assets`、`npm run qa:portrait-compression`、`npm run qa:portrait-matrix`、`node --test test/frontendInkAssetsManifest.test.js`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check` 和完整 `npm test`（869 项通过、0 失败）。下一个没有母版的队列项为 `portrait-s73-10-player-female-extra-merchant_owner-look06-v1`。
- 2026-05-14：按用户要求归档活动台账中的已完成路线并压缩共享上下文。`docs/DEVELOPMENT_STEPS.md` 已改为只保留治理保护块、归档索引、当前边界、空活动总览和本轮记录；DONE 的 S68-S71 长表与长进度记录不再留在活动台账。`docs/SHARED_CONTEXT.md` 已压缩为必读状态、核心边界、内容保护、实现入口和归档入口。本轮不改运行时代码、API、provider schema、存档格式、SQLite 表结构、提示词、验证脚本或 AI 权限。验证已通过 `npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`；只读子代理复审未发现 P0/P1/P2，确认治理规则、归档入口和 hidden/raw 内容保护边界未被删弱。
- 2026-05-14：按用户新要求启动 S72 PixiJS 水墨地图专项规划，并把当前活动协作从 Claude Code 交接切换为 Codex + Gemini CLI。Codex 负责后端、素材、审核与 Git 提交；Gemini CLI 负责前端 PixiJS patch 且不做 Git 提交。新增 `docs/PIXIJS_INK_MAP_ROADMAP.md` 和 `docs/MAP_ASSET_LEDGER.md`，`docs/DEVELOPMENT_STEPS.md` 已写明 S72.0-S72.8 owner 与验收边界。本轮仍不改运行时代码、API、provider schema、存档格式、SQLite 表结构、提示词、验证脚本或 AI 权限；S72.1 前不得直接接入 PixiJS 依赖。验证已通过 `npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`；只读子代理复审未发现 P0/P1/P2。完整 `npm test` 已运行但未全过，唯一失败为既有 S67 大规模 fixture 性能阈值：`fixtureGenerationMs 11122.608 > 10000`，其余 830 项通过；本轮未改 S67 fixture 或运行时代码。
- 2026-05-14：按用户要求细化 S72 给 Codex/Gemini 直接开发使用。`docs/PIXIJS_INK_MAP_ROADMAP.md` 已扩展为后端 `mapRuntimeView` 草案、前端 PixiJS 文件/图层/接口/动效/性能规格、素材清单、Gemini 交付格式和首个只读任务；新增 `docs/MAP_ASSET_GUIDE.md`，细化 AI 生图、历史参考查找、manifest、许可记录、文件规格和验收；新增根目录 `GEMINI.md` 作为 Gemini CLI 项目指引，并新增 `.geminiignore` 排除敏感和大体积本地文件但保留 `.env.example` 可读。本轮不改运行时代码、API、provider schema、存档格式、SQLite 表结构、提示词、验证脚本或 AI 权限。验证已通过 `npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`；只读子代理复审未发现 P0/P2，P1 台账验证状态已修正。
- 2026-05-14：按用户要求补强 S72 地图素材准入规则。AI 生图统一限定为 Codex 使用 `gpt-image-2`；AI 生成素材和第三方优秀素材入库前都必须由 Codex 使用视觉能力审核游戏基调、历史/水墨适配、缩放可读性、现代元素/水印/误生成文字和同批一致性。已同步 `AGENTS.md`、`GEMINI.md`、`docs/PIXIJS_INK_MAP_ROADMAP.md`、`docs/MAP_ASSET_GUIDE.md`、`docs/MAP_ASSET_LEDGER.md`、`docs/DEVELOPMENT_STEPS.md` 和 brief。本轮只改文档，不改运行时代码、API、provider schema、存档格式、SQLite 表结构、提示词或验证脚本；验证已通过 `npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`。
- 2026-05-14：按用户转交的 Gemini S72.1 前端约束报告完成 PixiJS 依赖治理与 runtime 契约。新增 `docs/PIXIJS_INK_MAP_RUNTIME_CONTRACT.md`，固定 `pixi.js@7.4.3` UMD、本地 `public/vendor/pixi.min.js` 优先、固定 CDN fallback、无 build step、不进入 `package.json`；固定 `mapRuntimeView` 字段、字典式 `actionDrafts`、DOM 插入点、`app.js` 最小接线、地图高度/overlay/reduced-motion 约束和 `ink-map-v1` manifest 边界。同步更新路线图、活动台账、brief 和 `GEMINI.md`。本轮不改运行时代码、API、provider schema、存档格式、SQLite 表结构、提示词、验证脚本、素材文件或 `package.json`。验证已通过 `npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`；只读子代理复审未发现 P0/P1/P2。
- 2026-05-14：按用户澄清修正 Gemini CLI 协作口径。Gemini 不是全程只读；除 S72.1 前置报告这类明确只读任务外，Gemini 可以按任务修改代码、增加前端文件或维护必要前端上下文说明。限制点是不得运行 `git add`、`git commit`、`git push` 或创建 PR；Codex 审核 diff、运行验证、同步文档并提交。本轮只改文档，不改运行时代码、API、provider schema、存档格式、SQLite 表结构、提示词、验证脚本、素材文件或 `package.json`；验证已通过 `npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`。
- 2026-05-14：执行并完成 S72.2 后端地图 runtime view。新增 `src/game/mapRuntimeConfig.js`、`src/game/mapVisualLayoutSeed.js`、`src/game/mapRuntimeView.js`，从安全 `mapContextView` 派生 `mapRuntimeView`，包含显示 layout、layer/style token、route path、event effect 和服务器预渲染 action draft；显示坐标只服务浏览器 UI，不进入 prompt、AI 工具或服务器裁决。`POST /api/game/start`、`GET /api/game/state/:sessionId`、`GET /api/game/player-state/:sessionId`、`POST /api/game/turn`、SSE `state_preview`/`final_state`、`POST /api/exam/question|progress|submit` 均返回该安全投影；dual-mode S70 AI-first parity 也比较 `mapRuntimeView`。本轮不改 PixiJS 前端、素材 manifest、provider schema、SQLite schema 或 `package.json`。验证已通过新增/相关 Node 测试、dual-mode script、docs governance 和 diff check；提交前已进行只读子代理复审。
- 2026-05-14：执行并完成 S72.3 首批水墨地图素材。按用户要求先让 medium 子代理做创意发散/prompt 草案，Codex 负责 prompt 定稿、生成、视觉审核和入库。新增 `public/assets/maps/ink-map-manifest.json`、水墨底图、宣纸纹理、路线笔触、红/青事件涟漪和 9 个透明地图图标；所有素材均由 Codex 视觉审核，确认无文字、水印、现代 UI、本地路径、key、hidden/raw 内容，并登记到 `docs/MAP_ASSET_LEDGER.md`。新增 `test/mapAssetsManifest.test.js` 校验 manifest 本地路径、尺寸、alpha 和敏感字段；本轮不改 PixiJS 前端、provider schema、SQLite schema、存档格式或 `package.json`。
- 2026-05-14：审核并收口 Gemini S72.4 前端地图 patch。新增本地 `public/vendor/pixi.min.js` 与 MIT license 说明、`public/mapRenderer.js` 和 `public/mapPanel.js`，`index.html` 按契约加载 PixiJS vendor、固定 CDN fallback、地图 renderer/panel 和 `app.js`；`app.js` 只保存并传递 `payload.mapRuntimeView`，地图点击只把服务器预渲染 `actionDraft.actionText` 回填到行动输入框。`mapRenderer` 读取 `public/assets/maps/ink-map-manifest.json` 并加载 S72.3 底图、图标和涟漪素材，缺 PixiJS 或素材失败时降级显示，不阻断文字主流程。新增 `test/mapFrontendShell.test.js` 防回归；浏览器临时端口验证 canvas 非空、tooltip/action draft 可用、控制台无错误。
- 2026-05-14：审查并收口 Gemini S72.5 前端联动 patch。`public/mapRenderer.js` 现在在地点、路线和事件涟漪点击时统一绘制选中圈，事件点击只携带安全 `mapRuntimeView` ref/sourceRefs 给 tooltip，且事件贴图 hitArea 不再用透明外框抢占周边 marker；素材异步加载完成后改走 panel 更新回调，避免 DOM 标签重复。`public/mapPanel.js` 只从安全 ref/sourceRefs 匹配已渲染的 `data-entity-id` / `data-event-id` 局势簿卡片，点击“查阅局势簿”会在局势簿现有 DOM 内切换 tab 可见性、重新定位已连接卡片并短暂高亮。行动草稿仍只把服务器 `actionDraft.actionText` 写入 `#action-input`，不自动提交、不调用后端 resolver、不直写状态；新增 `test/mapFrontendShell.test.js` 回归断言防止 unsafe selector、前端自行 turn fetch 和选中态漏接。验证已通过地图/manifest/runtime/route 聚焦测试、docs governance、`git diff --check`、浏览器临时端口手测和 `node --test test/dualModeAcceptanceScript.test.js`；完整 `npm test` 仅遇到既有 S67 large fixture 性能阈值波动 `fixtureGenerationMs 10658.028 > 10000`，单跑失败文件已通过。
- 2026-05-14：审查并收口 Gemini S72.6 前端 polish patch。`public/mapRenderer.js` 新增路线墨线 alpha 呼吸、事件涟漪 scale/alpha 扩散、深朱砂双圈选中态、`prefers-reduced-motion` 降级，以及 `IntersectionObserver`/`visibilitychange` 可见性守卫；Codex 修正了 Gemini patch 中会导致脚本语法失败的末尾残片，并修正路线动效被通用 tick 写入 `baseScale` 导致 `NaN` 的风险。`public/styles.css` 将 tooltip、行动草稿按钮和 fallback 面板调整为纸色渐变、硬边与浅朱砂分隔线。新增 `test/mapFrontendShell.test.js` S72.6 回归断言，并保留 `S72.6_DELIVERY_REPORT.md` 作为 Gemini 交付说明。本轮不改后端、API/schema、SQLite、provider schema、prompt 或素材 manifest；验证已通过前端/manifest/runtime 聚焦测试、docs governance、`git diff --check`、本地 Mock 浏览器桌面/窄屏检查和单跑 `test/dualModeAcceptanceScript.test.js`。完整 `npm test` 仅遇到既有 S67 large fixture 性能阈值波动 `fixtureGenerationMs 10274.08 > 10000`，单跑失败文件已通过；只读子代理复审未发现 P0/P1/P2。
- 2026-05-14：执行并完成 S72.7 PixiJS 地图验收、安全与性能回归。`scripts/browserSmoke.js` 现在真实检查地图面板、canvas 像素非空、label、动画数量、hidden/raw 文本、资源失败静态降级、`prefers-reduced-motion` 禁用动效、恢复读档/新页面恢复和桌面/窄屏布局。S72.7 browser smoke 捕获并修复了地图/书生面板挤压叙事区的问题：地图高度收束为 `clamp(200px, 24vh, 280px)`，桌面游戏态书生信息面板改为较短的可滚动摘要区，`narrative` 不再用固定 100% 高度。`public/mapRenderer.js` 新增只读 debug state、资源失败可观测状态、纸色 fallback base 和 `preserveDrawingBuffer` 以支持 canvas 非空验收；`public/mapPanel.js` 对未知 schemaVersion 降级等待并销毁旧 renderer/canvas，避免旧地图残留。本轮不改后端 API/schema、SQLite、provider schema、prompt 或素材 manifest；地图仍只读安全 `mapRuntimeView`，行动草稿仍需玩家提交普通回合。已通过聚焦 node tests、`npm run smoke:browser -- --screenshots artifacts/s72-browser-smoke`、docs governance、dual-mode storage-only、`git diff --check`；完整 `npm test` 仅遇到既有 S67 large fixture 性能阈值波动 `informationPanelMs 3258.724 > 3000`，随后单跑 `node --test test/dualModeAcceptanceScript.test.js` 已通过。提交前只读子代理复审最初发现 P2 旧 canvas 残留问题，已修复；无剩余 P0/P1/P2。
- 2026-05-14：按用户要求停止与 Gemini CLI 共同开发，后续开发全部改为 Codex-only。已同步 `AGENTS.md`、`CLAUDE.md`、治理锚点、brief、路线图、S72 契约/素材文档和本交接板；删除 Gemini 专用上下文入口 `GEMINI.md` 与 `.geminiignore`。历史记录中已经发生的 Gemini patch 作为事实保留，但不再作为未来协作模式。本轮只改文档、治理检查脚本和 Gemini 专用上下文文件，不改运行时代码、API、provider schema、SQLite schema、素材 manifest 或提示词；纯文档/治理改动，已进行只读子代理复审。
- 2026-05-14：按用户要求完成 S72 PixiJS 水墨地图专项归档。新增 `docs/PIXIJS_INK_MAP_ARCHIVE.md`，并把 `docs/DEVELOPMENT_STEPS.md`、`docs/QIANQIU_DEVELOPMENT_BRIEF.md` 和 `README.md` 的 S72 口径统一改为“已完成归档”；`docs/SHARED_CONTEXT.md` 也已更新为归档后的下一步建议。本轮仍不改运行时代码、API、provider schema、存档格式、SQLite 表结构、提示词、验证脚本或 AI 权限；验证完成后将按 Git 提交收口。
- 2026-05-14：按用户要求启动 S73-S77 前端水墨重构规划。已让 medium 子代理做创意发散/prompt 草案，Codex 收口为 `docs/FRONTEND_INK_REDESIGN_ROADMAP.md`，将用户提出的水墨、手绘、泛黄宣纸/奏折、首页画卷、右上角设置、存档/读档/返回首页、底部奏折输入、科举/放榜全屏、身份/场景专属面板、NPC/玩家立绘长期管线拆成 S73 素材、S74 信息架构拆分、S75 首页 shell、S76 身份/场景专题界面、S77 验收归档。S73-S77 继续纯 HTML/CSS/JS、Mock 默认可玩、S72 地图只读安全 view、存档/AI 设置安全 API 和完整书生路径不回退；立绘边界收束为成人、端庄、身份明确、俊美但不露骨。本轮只改规划/交接文档，不改运行时代码、API、provider schema、SQLite schema、存档格式、提示词、验证脚本或素材文件。验证已通过 `npm run check:docs-governance`、`node --test test/documentationGovernance.test.js` 和 `git diff --check`；只读子代理复审未发现 P0/P1，P2 brief 后段活动方向遗漏已修正，P3 复审状态已闭环。
- 2026-05-14：按用户反馈补强 S73-S77 规划。`docs/FRONTEND_INK_REDESIGN_ROADMAP.md` 已重写为详细开发蓝图：S73 拆成视觉资产指南、manifest/台账、UI 材质、首页资产、场景插画、身份背景、玩家/NPC 立绘基准、动效/fallback 和素材预览/QA；S74 调整为 React + TypeScript + Vite 渐进式接管。本条当时规划 `/ink-client/` 并行前端岛和 S77 切入口，现已由 S73.0d 按用户要求改为新前端直接接管默认 `/`，旧前端不再作为交付入口；S75-S77 拆出首页、印匣、存档/读档、返回首页、底部奏折、身份专属面板、考试/放榜、舆图、立绘接线、安全/性能/可访问性验收。此条中的立绘范围已由 S73.0c 更新为 S73.10 在 S73 内完成 300-400 张全量立绘池。女性立绘边界更新为成年、端庄、高颜值、身份感强，可通过服饰剪裁和姿态体现优雅成熟女性身形比例，但不得露骨、挑逗、幼态或过度暴露。本轮仍只改规划/交接文档，不安装依赖、不生成素材、不改运行时代码或 API。
- 2026-05-14：按用户进一步确认，当前旧前端是单页游戏壳，继续堆地图、科举、NPC、史册、设置和多身份面板会拥挤；S73.0b 已将新前端方向改为 React Router 多页 SPA。当时 S74.0 候选依赖新增 `react-router` 并规划 Data Mode；具体入口口径已由 S73.0d 更新为默认 `/` 直接接管，不再使用 `/ink-client/` 并行入口。不用 Framework Mode 接管 Express；推荐路由包括首页、主叙事、独立舆图、人物、史册/局势簿、科举考试、放榜、皇帝/朝议或官署公文、设置/存档。地图重构为独立“舆图”页，主叙事页只保留入口或摘要；`mapRuntimeView` 仍只供浏览器渲染，不进入 prompt、AI 工具或服务器裁决。本轮仍只改规划/交接文档。
- 2026-05-14：执行 S73.1 前端视觉资产指南。新增 `docs/FRONTEND_VISUAL_ASSET_GUIDE.md`，固定 S73-S77 资产的阅读优先原则、目录命名、页面 usage、颜色材质、文件规格、移动裁切、UI 材质、首页/场景/身份背景、成人端庄立绘边界、动效与 reduced-motion、fallback、审核清单、manifest/台账安全字段和第三方来源记录要求。S73-S77 正式 AI 生图继续限定为 Codex 使用 `gpt-image-2`，AI 生成或第三方候选素材入库前必须完成 Codex 视觉审核；未审核或未通过素材不得进入 manifest 可用集合。本轮只改文档，不安装依赖、不生成素材、不改运行时代码、API、provider schema、SQLite schema、存档格式、提示词或验证脚本；子代理已做只读要点分析，提交前只读复审未发现 P0/P1/P2，P3 历史记录中 S73.1/S73.2 边界旧表述已修正。
- 2026-05-14：按用户指出“台账缺 S73.10，立绘可以在 S73 全部完成且后续步骤都要使用”，执行 S73.0c 文档修正。`docs/DEVELOPMENT_STEPS.md` 已补齐 S73.10，`docs/FRONTEND_INK_REDESIGN_ROADMAP.md` 与 `docs/FRONTEND_VISUAL_ASSET_GUIDE.md` 已把 S73.10 从长期分期改为 S73 内全量立绘生产与入库：300-400 张玩家/NPC 立绘需完成生成、缩略图、压缩、视觉/安全审核、manifest 入库和台账登记；S73.7 只做风格基准，S74-S77 必须通过已审核 `portraitRef` 使用 S73.10 立绘池，仍按需懒加载且不得显示未审核素材。本轮仍只改规划/交接文档，不生成实际立绘、不安装依赖、不改运行时代码或 API。
- 2026-05-14：按用户最新要求“仍保持旧前端可用可以去掉，既然是重构旧的全都可以抛弃”，执行 S73.0d 文档修正。S74-S77 规划已改为新 React/Vite 多页前端直接接管默认 `/`；旧 `public/index.html`、`public/app.js`、`public/styles.css` 可在实施中替换或删除，不再要求旧 `/`、`/ink-client/` 双入口、`/legacy.html` 或旧前端继续可用。回退方式改为 Git revert 或恢复上一提交；仍保留 `npm install && npm start` 可运行、Mock 默认可玩、完整书生路径、S72 地图安全 runtime 和 hidden/raw 防线。本条只改规划/交接文档，不删除旧前端文件、不安装依赖、不改运行时代码或 API；已与 S73.6 身份背景收口拆分边界。
- 2026-05-14：执行 S73.2 前端素材 manifest schema 与素材台账。新增 `public/assets/ui/ink-ui-manifest.json` 草案和 `docs/FRONTEND_ASSET_LEDGER.md`，固定素材字段、审核状态、fallback、usage/role catalog、路径策略、性能字段、缩略图/占位图、安全裁切和 S73.10 全量立绘池的 `portraitRef`、状态/情绪变体、缩略图、低清占位、fallback 与懒加载字段；新增 `test/frontendInkAssetsManifest.test.js` 防止 manifest 进入远程/本地绝对路径、密钥或敏感运行数据，并校验台账关键字段。本轮不生成实际图片、不安装依赖、不改运行时代码、API、provider schema、SQLite schema、存档格式或提示词；验证已通过聚焦素材/地图 manifest 测试、docs governance、documentation governance 和 `git diff --check`。完整 `npm test` 已运行但未全过，失败为既有 S67 large fixture 性能阈值波动 `fixtureGenerationMs 10975.641 > 10000`；本轮未改 S67 fixture、dual-mode 阈值或运行时生成逻辑。提交前只读复审未发现 P0/P1/P2；复审建议的“立绘需继承基础素材字段”已补入 manifest、台账和测试。
- 2026-05-14：执行 S67 规模验收性能波动维护。`scripts/dualModeAcceptance.js` 不再输出旧 `fixtureGenerationMs`；`src/game/worldContentFixtures.js` 的 fixture summary 改为 `rawFixtureGenerationMs`，S67 scale 报告另给 `fixtureBuildMs`。`runScaleRegressionAcceptance()` 复用 fixture 自带 performance baseline，保留 large fixture 数量、防泄漏、分页 cap、prompt budget、SQLite read-repair parity、行为级耗时和 heap guard；`fixtureBuildMs` 作为报告字段，`informationPanelMs` 阈值放宽到 5000ms 以吸收 full-suite 并发资源竞争。本轮不改 API、存档 schema、provider schema、SQLite schema、prompt 或游戏状态规则；验证完成后按 Git 提交收口。
- 2026-05-14：执行 S73.3 UI 材质包。按用户要求由 medium 子代理做创意发散 prompt 草案，Codex 定稿并用 `gpt-image-2` 生成 16 个首批 UI 材质：宣纸、旧绢、奏折纸面、竹简输入条、卷轴面板、残纸卡片、水渍叠加层、淡墨分隔线、角饰笔触、朱印按钮默认/按下态、印匣纹理、试卷界栏、皇榜纸底、朱砂墨迹和纸页边缘阴影。素材已保存到 `public/assets/ui/materials/`，缩略图保存到 `public/assets/ui/thumbs/`；透明素材经 chroma-key 去底、despill 与 WebP alpha 压缩，水渍初版偏绿后已重生成并校色。`ink-ui-manifest.json` 进入 `assets_active`，台账逐项记录来源、prompt 摘要、后处理、许可说明、视觉审核和安全审核；测试已扩展到图片存在性、尺寸、alpha、性能预算、`thumbnailBytes` 和安全字段。验证已通过素材/地图 manifest 测试、docs governance、documentation governance、`npm test` 和 `git diff --check`；提交前只读复审发现的缩略图 bytes P2 已修正，无剩余 P0/P1/P2。本轮不安装依赖、不改运行时代码、API、provider schema、SQLite schema、存档格式、提示词或 AI 权限。
- 2026-05-14：执行 S73.4 首页资产包。按用户要求由 medium 子代理做创意发散 prompt 草案，Codex 定稿并用 `gpt-image-2` 生成 6 个首页素材：首页水墨山水画卷、云雾透明层、题名册/户籍册表单底、无字朱印开始按钮、首页案卷存档素材和 reduced-motion 静态底。素材已保存到 `public/assets/ui/home/`，缩略图保存到 `public/assets/ui/thumbs/`；透明素材经 chroma-key 去底、despill、alpha 软化与 WebP alpha 压缩，云雾层已调为暖灰低对比，朱印首版空心感过强后已重生成厚实无字版本。提交前只读复审发现 P2 透明素材仍有 chroma-key 色边/切口风险，Codex 已重处理册页、朱印和案卷透明边缘，并把云雾层改为基于 AI 雾层的柔化 alpha mask、低频雾团和边界渐隐；已刷新缩略图和 manifest bytes，并新增 `public/assets/ui/home/home-transparency-qa-v1.json` 记录透明素材 SHA-256、纸色/深色合成复审背景和色边/边界指标。S73.9 QA 范围已补入纸色/深色合成与高饱和绿/紫色边检查。`ink-ui-manifest.json` 继续保持 `assets_active`，台账逐项记录来源、prompt 摘要、后处理、许可说明、视觉审核和安全审核；测试已扩展到 S73.4 图片存在性、尺寸、alpha、性能预算、`thumbnailBytes`、透明 QA sidecar 和安全字段。验证已通过 `node --test test/frontendInkAssetsManifest.test.js`、`node --test test/mapAssetsManifest.test.js`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check`（仅 CRLF 提示）和 `npm test`（849 项通过、0 失败；此前一次完整运行遇到 `test/historicalEventArchive.test.js` 的 Node test runner `spawn EPERM` 瞬时错误，单跑该文件通过，随后完整重跑已通过）。最终只读复审确认无剩余 P0/P1/P2。本轮不安装依赖、不改运行时代码、API、provider schema、SQLite schema、存档格式、提示词或 AI 权限。
- 2026-05-14：执行 S73.5 场景插画包。按用户要求由 medium 子代理做创意发散 prompt 草案，Codex 定稿并用 `gpt-image-2` 生成 10 个场景插画：书斋、贡院号舍、放榜、殿试、县衙、公堂、军帐、御案、城市街巷和部院公文。素材已保存到 `public/assets/ui/scenes/`，缩略图保存到 `public/assets/ui/thumbs/`；放榜、军帐和部院公文早稿因伪文字/军器灰区/印章标记风险未入库，已重生成更干净版本。`ink-ui-manifest.json` 继续保持 `assets_active`，台账逐项记录来源、prompt 摘要、后处理、许可说明、视觉审核、安全审核、safeArea 和移动裁切；`test/frontendInkAssetsManifest.test.js` 已扩展到 S73.5 场景数量、阶段、分类、尺寸、缩略图 bytes、场景 token 和安全字段。本轮不安装依赖、不改运行时代码、API、provider schema、SQLite schema、存档格式、提示词或 AI 权限；验证完成后按 Git 提交收口。
- 2026-05-14：执行 S73.6 身份背景包。按用户要求由 medium 子代理做创意发散 prompt 草案，Codex 定稿并用 `gpt-image-2` 生成 6 个身份背景：书生、地方官、入仕官员、大臣、将领和皇帝。素材已保存到 `public/assets/ui/roles/`，缩略图保存到 `public/assets/ui/thumbs/`；地方官早稿因疑似伪文字未入库，已重生成干净版本。`ink-ui-manifest.json` 继续保持 `assets_active`，台账逐项记录来源、prompt 摘要、后处理、许可说明、视觉审核、安全审核、safeArea、移动裁切、色彩权重和面板材料建议；`test/frontendInkAssetsManifest.test.js` 已扩展到 S73.6 身份背景数量、阶段、分类、role token、roleStyle、尺寸、缩略图 bytes 和安全字段。验证已通过聚焦 manifest/map 测试、docs governance、documentation governance、`git diff --check`（仅 CRLF 提示）和完整 `npm test`（849 项通过、0 失败）；提交前只读复审未发现 P0/P1/P2。本轮不安装依赖、不改运行时代码、API、provider schema、SQLite schema、存档格式、提示词或 AI 权限。
- 2026-05-15：执行 S73.7 玩家/NPC 立绘风格基准。按用户要求由 medium 子代理做创意发散 prompt 草案，Codex 定稿并用 `gpt-image-2` 生成 24 张成人、端庄、高颜值、身份明确且不露骨的基准立绘，覆盖玩家书生/举人/进士/初任官员、县令/吏员/部院郎官/大臣/将领/摄政/皇帝、老师/考官/同年、女书生/女官/宫廷人物/商贾/市井人物。素材已保存到 `public/assets/ui/portraits/`，缩略图保存到 `public/assets/ui/thumbs/`，低清占位保存到 `public/assets/ui/portraits/placeholders/`，QA sidecar 为 `public/assets/ui/portraits/portrait-baseline-qa-v1.json`；本步采用极淡宣纸底而非强制透明，先锁定小尺寸可读性、成人端庄安全边界、`portraitRef`、fallback 和懒加载规则。`ink-ui-manifest.json` 继续保持 `assets_active`，台账逐项记录立绘矩阵、视觉/安全审核、低清占位和懒加载分组；`test/frontendInkAssetsManifest.test.js` 已扩展到 S73.7 portrait assets 与 QA sidecar。验证已通过聚焦 manifest/map 测试、docs governance、documentation governance、`git diff --check`（仅 CRLF 提示）和完整 `npm test`（850 项通过、0 失败）；提交前只读复审待完成。本轮不安装依赖、不改运行时代码、API、provider schema、SQLite schema、存档格式、AI 权限或游戏规则。
- 2026-05-15：执行 S73.8 动效与 fallback 包。按用户要求由 medium 子代理做创意发散 prompt 草案，Codex 定稿并用 `gpt-image-2` 生成 8 个动效/fallback 素材：水墨云雾缓入层、墨迹扩散圆晕、无字朱印落章、纸页展开横幅、榜文揭示纸幕、考试交卷朱封、淡墨分场擦拭和卷边揭页角标。素材已保存到 `public/assets/ui/effects/`，缩略图保存到 `public/assets/ui/thumbs/`，QA sidecar 为 `public/assets/ui/effects/effect-motion-qa-v1.json`；Codex 视觉审核确认无可读文字、水印、现代 UI/器物、本地路径、key、raw/hidden 内容或未公开剧情事实。`ink-ui-manifest.json` 继续保持 `assets_active`，台账逐项记录 effect usage、scene、motion 建议、fallback、reduced-motion fallback 和性能预算；`test/frontendInkAssetsManifest.test.js` 已扩展到 S73.8 effect assets 与 QA sidecar。验证已通过聚焦 manifest/map 测试、docs governance、documentation governance 和 `git diff --check`；最新完整 `npm test` 运行中读到 S73.7 旧口径导致 `70 !== 62`，已在最终提交事务中写回 S73.8 并通过聚焦验证，完整重跑留待 S73.9 或后续长跑复核。本轮不安装依赖、不改运行时代码、API、provider schema、SQLite schema、存档格式、AI 权限或游戏规则。
- 2026-05-15：执行 S73.9 素材预览与 QA。新增 `scripts/frontendAssetQa.js`、`public/assets/ui/asset-qa-preview.html`、`public/assets/ui/asset-qa-report-v1.json` 和 `qa:frontend-assets` / `qa:frontend-assets:write` 命令，把 manifest active 素材检查收束为可重复流程：路径、尺寸、alpha、bytes、缩略图、fallback、审核状态、性能预算、立绘低清占位、敏感字段和未审核状态禁用；`--write --pixel` 复用现有 Playwright/Chrome 工具链采样 19 个透明素材的宣纸底/深色底合成、高饱和绿/紫像素、边界 alpha 和硬 alpha 跳变。S73.9 当时 QA 报告覆盖 70 个 active 素材，S73.10.2 后当时已刷新为 142 个 active 素材；`test/frontendInkAssetsManifest.test.js` 已扩展到 S73.9 报告和预览入口。本轮不新增图片、不安装依赖、不改游戏运行时代码、API、provider schema、SQLite schema、存档格式、提示词或 AI 权限。验证已通过 `node scripts/frontendAssetQa.js --write --pixel`、`npm run qa:frontend-assets`、`node --test test/frontendInkAssetsManifest.test.js`、`node --test test/mapAssetsManifest.test.js`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check` 和完整 `npm test`（853 项通过、0 失败）；只读子代理复审未发现 P0/P1/P2，P3 台账记录已补齐。
- 2026-05-15：执行 S73.10.1 全量立绘矩阵定稿。按用户要求由 medium 子代理做创意发散 prompt 草案，Codex 定稿矩阵、prompt 母版、审核字段和测试；新增 `docs/FRONTEND_PORTRAIT_MATRIX.md`、`public/assets/ui/portraits/portrait-pool-matrix-v1.json`、`scripts/frontendPortraitMatrix.js` 和 `qa:portrait-matrix` / `qa:portrait-matrix:write`。矩阵锁定 336 张 planned 立绘：玩家身份阶段 72、通用 NPC 120、重要 NPC 72、状态姿态 48、场景锚点 24；所有条目 `runtimeUsable=false`，不进入 `ink-ui-manifest.json` 可用资产集合，不保存 provider 原始响应、本地路径、key、raw audit、hidden 私档或未公开剧情事实。本轮不生成实际图片、不安装依赖、不改游戏运行时代码、API、provider schema、SQLite schema、存档格式、AI 权限或游戏规则；验证已通过 `npm run qa:portrait-matrix`、`npm run qa:frontend-assets`、`node --test test/frontendInkAssetsManifest.test.js`、`node --test test/mapAssetsManifest.test.js`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check` 和完整 `npm test`（855 项通过、0 失败）。提交前只读复审未发现 P0/P1；P2 文档验证状态滞后已修正，重要 NPC 不混入通用池已补测试断言。
- 2026-05-15：执行 S73.10.2 玩家立绘池。按用户要求由 medium 子代理做创意发散 prompt 草案，Codex 定稿 12 个身份阶段 prompt 并用 `gpt-image-2` 生成 12 张 3x2 源图页，裁切为 72 张玩家身份阶段立绘，覆盖书生、童试考生、秀才、举人、贡士、进士、初任官、地方官、京官、大臣、将领、皇帝/摄政，每阶段 `m01/m02/m03/f01/f02/f03` 六个变体。新增 `scripts/frontendPlayerPortraitAssets.js`、`qa:player-portraits` / `qa:player-portraits:write`、`public/assets/ui/portraits/s73-10/` 主图、对应缩略图/低清占位和 `public/assets/ui/portraits/portrait-player-pool-qa-v1.json`；`ink-ui-manifest.json` 当时 active 素材数为 142，S73.9 QA 报告已刷新为 142 个 active 素材、19 个透明素材、0 errors、0 warnings。Codex 已通过总览图视觉审核：成人端庄、身份阶段递进清楚、无现代器物/水印/可读文字/露骨/挑逗/幼态问题。本轮不安装依赖、不改游戏运行时代码、API、provider schema、SQLite schema、存档格式、AI 权限或游戏规则；验证已通过 `npm run qa:player-portraits`、`npm run qa:frontend-assets`、`npm run qa:portrait-matrix`、`node --test test/frontendInkAssetsManifest.test.js`、`node --test test/mapAssetsManifest.test.js`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check` 和完整 `npm test`（856 项通过、0 失败）。提交前只读复审未发现 P0/P1/P2；P3 历史 S73.9 口径已修正为当时/当前表述。
- 2026-05-15：按用户反馈执行 S73.10.2a 玩家女性立绘重置与风格补充。S73.10.2 中 36 张女性身份阶段立绘已用新源页覆盖旧图；另新增 60 张玩家可选女性风格立绘，登记为 `player_female_style_pool` 与 `portrait_pool_player_female_extra_s73_10`，不是 NPC 通用池。新增 `scripts/frontendPlayerFemalePortraitAssets.js`、`qa:player-female-portraits` / `qa:player-female-portraits:write` 和 `public/assets/ui/portraits/portrait-player-female-reset-qa-v1.json`；开发规范已补明这批额外立绘供玩家选角使用，女性特征只能通过完整服饰、衣料层次、腰封细腰、服饰层次和姿态表现，不得低胸、透视、裸露、挑逗、幼态、现代化、水印或可读文字。验证已通过 `npm run qa:player-female-portraits`、`npm run qa:player-portraits`、`npm run qa:frontend-assets`、聚焦 manifest/map 测试、docs governance、documentation governance、`git diff --check` 和完整 `npm test`（857 项通过、0 失败）；提交前只读复审未发现 P0/P1/P2，P3 standalone QA SHA 校验和 Markdown BOM 已修正。
- 2026-05-15：按用户反馈“男的呢”执行 S73.10.2b 玩家男性风格补充。新增 60 张玩家可选男性风格立绘，登记为 `player_male_style_pool` 与 `portrait_pool_player_male_extra_s73_10`，不是 NPC 通用池；覆盖书院文士、唐风圆领袍、朝堂文官、宫廷贵胄、中枢重臣、边塞将领、禁军武官、士族公子、商贾东家和行旅策士 10 类，每类 6 张。新增 `scripts/frontendPlayerMalePortraitAssets.js`、`qa:player-male-portraits` / `qa:player-male-portraits:write` 和 `public/assets/ui/portraits/portrait-player-male-extra-qa-v1.json`；玩家池当前为 72 张身份阶段 + 60 张女性风格补充 + 60 张男性风格补充，男女玩家可选立绘各 96 张。男性角色要求成年端庄、服饰完整，可通过肩颈与上身轮廓、冠帽、腰带、甲衣、胡须和姿态表现成年男性气质，不得裸露、挑逗、幼态、现代偶像化、水印或可读文字。验证随本轮提交记录同步。
- 2026-05-15：执行 S73.10.3 通用 NPC 立绘池。按用户要求由 medium 子代理做创意发散 prompt 草案，Codex 定稿并用 `gpt-image-2` 生成/筛选通用 NPC 源图页，裁切入库 188 张立绘：一开始的 120 张矩阵通用 NPC 完整保留，20 张旧版源页作为 `bonus_generic_npc` 继续使用，48 张宫装/唐装女性风格扩展作为 `female_style_pack` 额外补充；同时确认 S73.10.2 既有 60 张女性玩家风格补充继续保留，不浪费已生成素材。新增 `scripts/frontendGenericNpcPortraitAssets.js`、`qa:generic-npc-portraits` / `qa:generic-npc-portraits:write`、`public/assets/ui/portraits/portrait-generic-npc-pool-qa-v1.json` 和对应主图、缩略图、低清占位；`ink-ui-manifest.json` 当时 active 素材数为 390，S73.9 QA 报告当时刷新为 390 个 active 素材、19 个透明素材、0 errors、0 warnings。女性扩展只保留宫装/唐装方向，角色 token 为 `palace-lady` 与 `tang-lady`，成年年轻至轻熟、苗条、上身服饰层次清楚、腰身收束，不露胸、不挑逗、不幼态、不发福化。Codex 已通过三张总览图视觉审核。本轮不安装依赖、不改游戏运行时代码、API、provider schema、SQLite schema、存档格式、AI 权限或游戏规则；验证随提交记录同步。
- 2026-05-16：执行 S73.10.4 重要 NPC 专属立绘池。按用户要求由 medium 子代理做创意发散 prompt 草案，Codex 定稿并用 `gpt-image-2` 生成 12 张 3x2 源图页，裁切入库 72 张重要 NPC 专属立绘，覆盖皇帝/太后、皇后/摄政、首辅/大司马、礼部/兵部重臣、都御史/总督、名将/名臣、清流领袖/权宦、权臣/豪商、地方望族/名师、名主考/宿敌、知己/红颜蓝颜知交、宫廷谋主/边地使者。新增 `scripts/frontendSignatureNpcPortraitAssets.js`、`qa:signature-npc-portraits` / `qa:signature-npc-portraits:write`、`public/assets/ui/portraits/portrait-signature-npc-pool-qa-v1.json` 和对应主图、缩略图、低清占位；`ink-ui-manifest.json` 当前 active 素材数为 522，S73.9 QA 报告刷新为 522 个 active 素材、19 个透明素材、0 errors、0 warnings。重要 NPC 使用 `signature_npc_pool` 与 `portrait_pool_signature_npc_s73_10` 单独隔离，不混入通用头像池；未进入公开剧情视野的人物只暴露安全 `portraitRef`。女性角色不做中性化处理，通过高髻、簪钗、层叠礼服、腰封细腰、肩颈线和端庄仪态体现成年女性特征，无低胸、透视、裸露、挑逗、幼态或现代化。Codex 已通过总览图视觉审核。本轮不安装依赖、不改游戏运行时代码、API、provider schema、SQLite schema、存档格式、AI 权限或游戏规则；验证已通过 `npm run qa:signature-npc-portraits`、`npm run qa:frontend-assets:write`、`npm run qa:frontend-assets`、`npm run qa:portrait-matrix`、`node --test test/frontendInkAssetsManifest.test.js test/mapAssetsManifest.test.js`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check` 和完整 `npm test`（858 项通过、0 失败）。提交前只读复审未发现 P0/P1/P2/P3，确认 72 张重要 NPC 专属池未混入通用 NPC 池、manifest/QA/test/文档口径一致、女性角色要求已锁定、未发现 hidden/private/raw/provider/local path/key/prompt 泄漏。
- 2026-05-16：执行 S73.10.5 状态/姿态与场景锚点池。按用户要求由 medium 子代理做创意发散 prompt 草案以避免 prompt 过度工程化，Codex 定稿并用 `gpt-image-2` 生成 12 张 3x2 源图页，裁切入库 72 张立绘：48 张状态/姿态变体覆盖沉思、拱手、执卷、阅卷、持笏、执笔、按剑、疲惫、病中、肃立、含怒、微笑、惊疑、决断、受审和请罪，24 张场景锚点覆盖科举答卷、放榜相逢、县衙问案、朝议陈奏、军帐筹谋、书斋夜读、街市交涉和宫廷召见。新增 `scripts/frontendStateScenePortraitAssets.js`、`qa:state-scene-portraits` / `qa:state-scene-portraits:write`、`public/assets/ui/portraits/portrait-state-scene-pool-qa-v1.json` 和对应主图、缩略图、低清占位；`ink-ui-manifest.json` 当前 active 素材数为 594，S73.9 QA 报告刷新为 594 个 active 素材、19 个透明素材、0 errors、0 warnings。女性角色按用户要求不做中性化处理，通过发髻、簪钗、衣料层次、腰封细腰、肩颈线和端庄姿态体现成年女性特征，同时保持完整衣着、无露骨、无挑逗、无幼态或现代化。Codex 已通过 12 张源页视觉审核；验证已通过 `npm run qa:state-scene-portraits`、`npm run qa:frontend-assets`、`npm run qa:portrait-matrix`、`node --test test/frontendInkAssetsManifest.test.js`、`node --test test/mapAssetsManifest.test.js`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check` 和完整 `npm test`（859 项通过、0 失败）。本轮不安装依赖、不改游戏运行时代码、API、provider schema、SQLite schema、存档格式、AI 权限或游戏规则；提交前只读复审未发现 P0/P1/P2/P3，确认 manifest/QA/matrix 计数一致、脚本只按新资产 ID 精确替换、未发现 hidden/private/raw/provider/local path/key/prompt 泄漏。
- 2026-05-16：执行 S73.10.6 缩略图与压缩总括 QA。新增 `scripts/frontendPortraitCompressionQa.js`、`qa:portrait-compression` / `qa:portrait-compression:write` 和 `public/assets/ui/portraits/portrait-compression-qa-v1.json`，统一校验 548 张 active 立绘、其中 524 张 S73.10 立绘的 1024x1536 主图、384x576 缩略图、64x96 低清占位、SHA-256、bytes、safeArea、focalPoint、mobileCrop、文件预算和 `allowEagerLoad=false`；`test/frontendInkAssetsManifest.test.js` 新增 S73.10.6 命名测试。Hubble 只读差距扫描确认实现层已有缩略图/占位/预算覆盖，主要缺口是 S73.10.6 独立口径和 README 旧计数，本轮已同步 README、brief、前端路线图、立绘矩阵说明、素材台账、本台账和共享上下文。本轮不生成新图片、不安装依赖、不改游戏运行时代码、API、provider schema、SQLite schema、存档格式、AI 权限或游戏规则；验证已通过 `npm run qa:portrait-compression:write`、`npm run qa:portrait-compression`、`npm run qa:frontend-assets`、`npm run qa:portrait-matrix`、`node --test test/frontendInkAssetsManifest.test.js`、`node --test test/mapAssetsManifest.test.js`、`npm run check:docs-governance`、`node --test test/documentationGovernance.test.js`、`git diff --check` 和完整 `npm test`（860 项通过、0 失败）。提交前只读复审发现的 P2 台账验证状态与历史 450 素材“当前”口径已修正。
- 2026-05-16：执行 S73.10.7 年轻女性补充池。按用户要求“不要中年女性、女性特征不能中性化”，Codex 用 `gpt-image-2` 先生成 4 张 3x2 源图页并逐页视觉审核；未发现中年女性、发福老态、男性化或中性化候选。后续同日又按用户参考画风追加 vivid 批次，使 `young_female_style_pool` 与 `portrait_pool_young_female_s73_10_7` 扩为 48 张，覆盖宫署/女官、书院/才女、商事/市井和边关/军务年轻成年女性立绘；最终计数和 QA 状态见下一条单张重制记录。
- 2026-05-16：继续执行 S73.10.7 女性立绘清洁与单张重制。按用户反馈将年轻女性补充池扩为 48 张，追加更接近参考画风的高对比 vivid 批次；新增 `scripts/frontendSinglePortraitOverrides.js`、`qa:single-portrait-overrides` / `qa:single-portrait-overrides:write` 和 `public/assets/ui/portraits/portrait-single-override-qa-v1.json`。2026-05-17 继续按用户要求补齐玩家女性身份阶段单张高清重制，当前 12 个身份阶段的 `f01/f02/f03` 共 36 张已全部由单张 PNG 母版覆盖，优先修复脏噪、模糊、人物过小、比例拉伸和姿势模板化。重制策略保持：尽量用压缩 WebP 或 manifest 构图信息作参考，不上传源图/大图；已重制图少审以节约流量；公开 manifest/QA 只登记 `/assets/ui/` 运行时 WebP、缩略图、低清占位和本地母版不公开标记，不列 artifacts 母版路径；疑似小面积文字风险不再阻塞，除非明显水印、现代标识或大面积可读字。`scripts/frontendSinglePortraitOverrides.js` 已补主图预算兜底并在兜底失败时显式报错；`npm test` 已收窄为 `node --test test/*.test.js`，React TS/Vitest 测试继续用 `npm run test:client`。S73.9 QA 报告刷新为 642 个 active 素材、19 个透明素材、0 errors、0 warnings，S73.10.6 压缩 QA 刷新为 596 张 active 立绘、其中 572 张 S73.10 立绘；`qa:player-female-portraits` 已识别单张覆盖优先级，不再用旧网格切片哈希误报。
- 2026-05-16：执行 S74.0 依赖治理与 React 默认前端契约。按用户要求在 S73 素材仍运行时并行推进且不触碰 S73 素材路径；当前工作树中的 `public/assets/ui/**`、立绘 QA 和 S73 脚本变动视为 S73 外部素材生产改动，本轮只改 `package.json`、`package-lock.json`、README 和文档。已安装精确版本 React/React DOM/React Router/Zustand/Lucide runtime 与 TypeScript/Vite/`@vitejs/plugin-react`/Vitest/Testing Library/jsdom/type packages dev 依赖；新增 `docs/FRONTEND_REACT_MIGRATION_CONTRACT.md`，固定 React Router Data Mode、Node `20.19+` / `22.13+` / `24.0+`、URL 安全边界、Zustand 存储边界、Vite 构建输出隔离、S72 地图桥和 S73.10 立绘懒加载边界。S74.0 不创建 `client/`、不改 Express fallback、不替换当前 `public/` 前端；默认 `/` 新前端接管从 S74.1 开始。验证已通过 `npm install` 两次安装命令、`npm audit --omit=dev`、docs governance、documentation governance、`npm start` 健康检查和 `git diff --check`；完整 `npm test` 已运行但因 S73 素材生产中的计数漂移失败 3 项（642/48/596 对旧断言 618/24/572），其余 858 项通过，本轮未修正 S73 测试或素材。
- 2026-05-16：执行 S74.1 Vite/TypeScript 默认前端。按用户要求在 S73 素材继续运行时并行推进且不触碰 S73 素材路径；当前工作树中的 `public/assets/ui/**`、立绘 QA 和 S73 脚本变动仍视为 S73 外部素材生产改动，本轮只改前端基础设施、server fallback、focused smoke、测试和文档。新增 `client/` React + TypeScript 源码、`vite.config.mjs`、`tsconfig.client.json`、`vitest.config.mjs`、`scripts/clientSmoke.js`、`test/reactClientScaffold.test.js` 和 `.gitignore dist/`；`package.json` 新增 `prestart` 与 client scripts，`smoke:browser` 改为 S74.1 focused React smoke，旧原生 smoke 保留为 `smoke:browser:legacy`。`server.js` 现在 API 优先，随后服务 `dist/client` 和 `public`，只对 HTML 导航 GET/HEAD 做 React history fallback，并显式排除 `/assets`、`/client-assets`、`/vendor`、`/mapRenderer.js`、`/mapPanel.js` 静态命名空间，避免缺失静态资源被 React HTML 吞掉。验证已通过 `npm run typecheck:client`、`npm run test:client`、`node --test test/reactClientScaffold.test.js`、`node --test test/serverCors.test.js test/browserSmokeScript.test.js`、`npm run build:client`、`npm run smoke:browser -- --screenshots artifacts/s74-frontend-smoke`、`npm start` 临时端口 3202 启动检查、docs governance、documentation governance、`git diff --check`（仅 `.gitignore` LF/CRLF 提示），并用 Browser 插件打开 `http://127.0.0.1:3200/` 与 `/game/s74-browser/map` 确认 React entry、history fallback、无横向溢出和无 hidden token 泄漏；完整 `npm test` 已运行但因 S73 素材生产中的计数/bytes/hash 漂移失败 5 项，其余 860 项通过，本轮未修正 S73 测试或素材；提交前只读复审初次发现 P2 静态资源无扩展名路径可能被 history fallback 吞掉，已修正并补测试，复审确认未发现新的 P0/P1/P2。
- 2026-05-16：执行 S74.2 安全 API client。按用户要求在 S73 素材仍运行时并行推进且不触碰 S73 素材路径；当前工作树中的 `public/assets/ui/**`、立绘 QA、S73 脚本和 `package.json` 变动仍视为 S73 外部素材生产改动，本轮只改 React client、client 测试、scaffold 测试和文档。新增 `client/src/api/qianqiuClient.ts`、`client/src/api/types.ts`、`client/src/state/gameSessionState.ts` 和 `client/src/routes/sessionId.ts`，封装 start/saves/player-state/turn/exam/AI settings/connection-test 安全接口并显式拒绝 raw state/developer diagnostics/未知 endpoint；首页、主卷、科举和印匣已最小接线，预览/烟测非 UUID route 不自动读档。验证已通过 `npm run typecheck:client`、`npm run test:client`、`node --test test/reactClientScaffold.test.js`、`npm run build:client`、`npm run smoke:browser -- --screenshots artifacts/s74-2-api-client-smoke`；本轮未运行素材 QA、完整 `npm test` 或会刷新/断言 S73 manifest/hash 的命令。
- 2026-05-16：执行 S74.3 前端状态层补全。按用户要求在 S73 素材仍运行时并行推进且不触碰 S73 素材路径；当前工作树中的 `public/assets/ui/**`、立绘 QA、S73 脚本和 `package.json` 变动仍视为 S73 外部素材生产改动，本轮只改 React client、client 测试、scaffold 测试和文档。`client/src/state/uiState.ts` 现在是 Zustand UI store，管理 route page、`sessionId`、安全玩家摘要、drawer/modal/surface、tab、action draft 和 display preferences；`gameSessionState` 在开局、读档、普通回合和交卷成功后同步安全摘要，普通回合成功会清空行动草稿；`App` 新增 route-state bridge、显示偏好抽屉和安全摘要 modal，`GamePage` 的奏折输入改用全局 action draft。验证已通过 `npm run typecheck:client`、`npm run test:client`、`node --test test/reactClientScaffold.test.js`、`npm run build:client`、`npm run smoke:browser -- --screenshots artifacts/s74-3-ui-state-smoke`、docs governance 和 documentation governance；本轮未运行素材 QA、`qa:*:write` 或完整 `npm test`，以避免干扰 S73 素材生产。
- 2026-05-16：执行 S74.4 多页 shell 与 surface registry。按用户要求在 S73 素材仍运行时并行推进且不触碰 S73 素材路径；当前工作树中的 `public/assets/ui/**`、立绘 QA、S73 脚本和 `package.json` 变动仍视为 S73 外部素材生产改动，本轮只改 React shell/components/surfaces、client 测试、scaffold 测试、README 和文档。`client/src/App.tsx` 已瘦身为 `AppShell` 入口；新增 `client/src/components/AppShell.tsx`、`SurfaceHost.tsx`、`overlayFocus.ts` 和 `client/src/surfaces/surfaceRegistry.tsx`。设置/存档/显示偏好抽屉、安全 modal、人物/圣旨/奏折/舆图专题层均由 registry 管理，支持 Esc 关闭、焦点回收、页面滚动锁定和路由滚动恢复；专题层只显示安全占位与行动草稿入口，不读取 raw state、内部审计原文、模型原文、完整 prompt、本地路径或 key，不导入全量立绘池。验证已通过 `npm run typecheck:client`、`npm run test:client`、`node --test test/reactClientScaffold.test.js`、`npm run build:client`、`npm run smoke:browser -- --screenshots artifacts/s74-4-shell-smoke`、docs governance、documentation governance 和 `git diff --check`；提交前只读复审未发现 P0/P1，P2 文档验证状态滞后已修正。本轮未运行素材 QA、`qa:*:write` 或完整 `npm test`，以避免干扰 S73 素材生产。

## Next Recommended Step

主线下一步进入 S77.6 可访问性：在 S77.5 性能资源预算通过的基础上，继续检查 Enter/Shift+Enter/Esc、焦点回收、浏览器级 `prefers-reduced-motion`、字体大小、对比度和按钮文本不溢出。随后按 S77.7-S77.8 继续做前端水墨重构归档和总验证。

S76.12 已完成 S76 总验收。React browser smoke 当前覆盖书生、地方官、入仕官员、大臣、将领、皇帝六类身份代表草稿行动，逐个打开奏折队列、拟圣旨、朝议、堂审、军议、人物档案六类专题，并继续覆盖考试/放榜、独立舆图、人物谱牒、移动端和 unsafe API 防线；完整书生科举晋级链由 `npm run smoke:exam-s69` 验证到入仕。S78 也已完成官署专题玩法化，六类专题读取 `topicSurfaceView` 安全材料，支持证据勾选、`topic_draft` AI/Mock 草稿、玩家改稿和写入底部奏折；专题草稿仍不提交回合、不推进时间、不写 canonical state，真实后果继续由普通 `/api/game/turn` 和服务器裁决链处理。

若用户继续本轮女性立绘续作，先从 `portrait-s73-10-player-female-extra-merchant_owner-look06-v1` 开始补下一批仍没有母版的女性立绘；继续直接用公开 runtime WebP 作单张参考，完成一个可审核批次后再派生 runtime WebP、缩略图、低清占位、QA 和 manifest，并删除对应本地参考副本以免误导后续队列。后续页面继续只使用已审核 `/assets/ui/` 素材、安全 route/store 数据、已审核 `portraitRef`、缩略图、低清占位和 fallback；未进入公开 manifest/QA 的 artifacts 单张 PNG 不得被前端直接引用或视为已审核 runtime 素材。
