# AI 调动与控制审查矩阵

S44.1 固定本项目“AI 影响游戏，但不拥有最终裁决”的系统边界。后续新增 prompt pack、状态字段、浏览器面板、真实 provider 验收或多实体世界模型时，必须先检查本矩阵；如果新增系统能被 AI 读取、生成、建议、排序或解释，就要同步补一行。

## 权限术语

- 可生成：AI 可以写叙事、题面、评语、来函口吻、传闻文本或 JSON 中的展示性字段；这些内容仍需 schema 校验。
- 可建议：AI 可以通过受限 `statePatch`、`relationshipChanges` 或 `examTrigger` 提出变化方向；服务器可以夹断、忽略、改写或拒绝。
- 可排序：AI 可以给出候选解释、倾向或显性排序建议，但榜单、职位、考试晋级、事件结局等 canonical 顺序仍由服务器生成。
- 可评分：AI 可以给出科举五维分数和考官评语；本地反作弊、扣分、排名、晋级和处分仍由服务器应用。
- 可解释：AI 可以解释当前局势、角色反馈和行动阻力；不能把隐藏状态伪装成角色已知事实。
- 可请求工具：AI 可以按 actor 身份请求 read/proposal/request-adjudication 工具；工具调用不是已发生事实，落地必须由服务器 resolver 裁决。
- 不可写：AI 输出不得进入该持久字段；schema、remote normalization 和 `applyStatePatch()` 都应拒绝或忽略。
- 不可裁决：AI 不能决定晋级、任免、作弊处罚、存档写入、隐藏信息公开、长期事件结局、世界 tick 或回滚。

## 总原则

- 模型只拿到玩家可见或 prompt 专用的压缩摘要；隐藏联系人、隐藏 thread、官场 `hiddenNotes`、provider 配置和 `.env` 不进入 prompt/UI。
- 普通回合只允许 provider patch `src/ai/schemas.js` 和 `src/game/stateRules.js` 中的 provider-facing 字段；`turnCount`、`year`、`month`、`tenDayPeriod` 和其他服务器独占字段必须通过服务器模块用 `allowServerOwnedPatchKeys` 写入。
- 路由持久化前必须完成 schema 校验、patch 白名单、clamp、服务器后续结算、玩家可见 view 构造和 session 写入。
- 新增浏览器面板只能渲染 route payload 中的 player-facing view，不能从原始隐藏 ledger 或 provider 原文扫描关键状态。`public/app.js` 中少量 raw `worldState` 兼容 fallback 只用于旧 payload/开发兜底，不是后续面板的允许路径。

当前威胁模型是本地开发与本机浏览器游玩：游戏状态 API 会返回完整 `worldState` 给同一个本地客户端，浏览器约束是“玩家可见 UI 不从隐藏 ledger 渲染”。若以后支持不可信客户端、多人访问或远程托管，必须新增脱敏状态 API，不能继续把完整 `worldState` 当成公网玩家响应。另一个边界是普通真实 provider 调用可能在 adapter 层失败后 fallback 到 Mock 以保持可玩；`POST /api/ai/connection-test` 和 provider smoke 才是检查真实 provider 连通性与配置失败的依据。

## 系统矩阵

| 系统 | AI 权限 | 服务器裁决与持久化 | 玩家可见性 | 当前证据 | S44.2 红队缺口 |
| --- | --- | --- | --- | --- | --- |
| AI 连接诊断 | 可生成一次开局 JSON，用于连通性与 schema 检查；可解释脱敏错误。 | `src/ai/diagnostics.js` 直接构造指定 provider，不创建 session，不写存档，不用 Mock fallback 掩盖真实 provider 失败；错误经 `redactSecrets()`，覆盖 OpenAI、DeepSeek、MiMo、MiMo+DeepSeek 和 Anthropic key。 | 开局页“AI 连接”显示 provider、模型摘要、耗时、streaming 能力、预览或脱敏错误。 | `src/routes/ai.js`、`test/aiDiagnostics.test.js`、`test/aiConnectionRoute.test.js`、`test/providerRouteHealthScript.test.js`。 | 增加 partial/transformed key 泄漏、错误堆栈泄漏、指定 provider 与当前 provider 混淆探针。 |
| 开局叙事 | 可生成 `narrative` 和少量 `events`；不可写初始身份、年份、属性或隐藏状态。 | `createInitialState()` 校验身份和初始状态；`POST /api/game/start` 只在 opening 成功后写 session；route 构造各类 player-facing view。 | 玩家看到开局叙事、公开状态、身份面板和可见 view。 | `src/game/initialState.js`、`src/routes/game.js`、`src/ai/schemas.js`。 | 增加 opening 试图暴露隐藏 provider 配置、伪造身份权限、写入状态字段的 fixture。 |
| 普通自由回合 | 可生成叙事、`events`、展示性 `attributeChanges`；可建议 provider 白名单内的世界/玩家数值和 `relationshipChanges`；可建议 `examTrigger`。 | `normalizeProviderStatePatch()`、turn schema、`applyStatePatch()`、clamp 和 route follow-up 共同裁决；remote helper 可先丢弃 schema 不兼容的展示性属性变化和关系建议，但关系目标可见性、存在性、最终夹断和持久化仍归 route。服务器按固定顺序执行关系、考试门禁、NPC、身份联动、tick、长期事件、官场和世界议程。若已有写卷考试，route 在调用 provider 前转入考试场景分支。 | 叙事、属性变化、人脉反馈、考试触发反馈、旬度/月度/大势/官场/议程 view；写卷中则显示科场局部反馈。 | `src/routes/game.js`、`src/ai/providers/remoteHelpers.js`、`src/game/stateRules.js`、`test/remoteHelpers.test.js`、`test/gameTurnRelationships.test.js`、`test/stateRules.test.js`。 | 增加一次性混合越权 patch 探针：同时伪造官职、考试名位、长期事件、议题和隐藏关系，确认全部被拒绝且安全字段仍可生效。 |
| S60 大世界内容契约与规模 fixture | AI 可以读取服务器按身份、地点、关系、官署、事件和 query 排序后的 capped 大世界摘要；可以在叙事中解释可见国家、城市、NPC、官职、事件、情报、市场和军事压力；可以提交受限传闻、关系或事件 proposal。AI 不可成为 seed/source，不可直接生成或写入数据库总量，不可把 fixture hidden canary 当作玩家已知事实。 | S60.1 的 `docs/HUGE_DYNAMIC_WORLD_CONTENT_CONTRACT.md` 固定小/中/大规模档位、seed 分层、数据库/fixture 总量与 route view/prompt cap 分离、hidden 私档不回填当前 raw route `worldState`、S60.2 fixture 数量、prompt 全局预算和 hidden canary 验收。后续 S60.2 generator、SQLite import/repair、prompt retrieval 和浏览器分页都必须由服务器 helper、schema、visibility filter、content hash、adapter transaction 和审计 sanitizer 执行；AI 可以通过领域工具提交 proposal，但不能执行 SQL、不能写 canonical 状态、`geo_*`、`people_*`、`office_*`、`event_archive_index`、`prompt_retrieval_index`、`event_log` 或 `ai_change_proposals`。 | 玩家只看到服务器 view、分页 projection、传闻或角色可见摘要；浏览器和 prompt 不读 raw table、raw audit、provider proposal、完整 prompt、本地路径、密钥、hidden notes、hidden intent、未公开关系/任所、密札考成或邻国真实虚实。当前 state route 仍返回完整本地 `worldState`，因此真正 hidden 私档必须留在未来 redacted API 之外的服务器私档层。 | `docs/HUGE_DYNAMIC_WORLD_CONTENT_CONTRACT.md`、`docs/DEVELOPMENT_STEPS.md`、`docs/DYNAMIC_WORLD_DATABASE_PLAN.md`、`docs/QIANQIU_DEVELOPMENT_BRIEF.md`。当前是 S60.1 文档契约，S60.2 再补运行时 fixture 和 tests。 | S60.2 必须补数量门槛、JSON/SQLite parity、prompt budget 不线性膨胀、读档修复单向、浏览器分页/overflow、hidden canary 扫描、raw table/audit/proposal token、假 key、SQLite path、`last_event_id`、`contentHash` 和低可信情报真值防泄漏探针。 |
| S70 AI actor / 工具编排规划 | S70 允许 narrator、actor_mind、planner、domain_specialist、critic、safety 等模型角色读取各自身份可见摘要、请求 read/proposal/request-adjudication 工具或提交结构化 proposal；critic/safety 只能输出风险、拒绝理由、二次建议和审查标签，不能直接改状态。 | 规划入口见 `docs/AI_ORCHESTRATION_ROADMAP.md`。未来实现必须通过 `game_ai_tools` registry、actor 权限层、tool envelope schema、proposal/result schema、resolver、审计 hook 和 Mock runner；模型只请求工具，服务器才执行工具、裁决后果、写 `worldState`、SQLite 派生表、审计和 revision。工具不是 SQL 代理，tool call 不能被当作已发生事实；多数模型同意也不能绕过 schema、白名单、clamp、visibility filter、科举晋级、官职任免、战争/外交 resolver 或持久化事务。 | 玩家只看到服务器接受后的公开结果、角色可见后果和必要的传闻/奏报/案牍/朝议反馈；开发者可在 hidden-safe 诊断里看到 tool name、actor、状态、拒绝原因和耗时摘要，不得看到完整 prompt、密钥、本地路径、hidden ledger 或 raw provider proposal。 | `docs/AI_ORCHESTRATION_ROADMAP.md`、`docs/DEVELOPMENT_STEPS.md`、`docs/QIANQIU_DEVELOPMENT_BRIEF.md`。当前是提前规划，暂无运行时代码。 | S70 实施时必须补越权工具探针：书生调用皇帝诏令、县令宣战、将领擅自出兵、皇帝无罪杀臣、critic/safety 直写状态、MCP/外部工具泄漏 raw session、多个模型同时夹带 SQL/table 写入意图、工具伪装 raw session patch 或 raw audit insert、Mock/no-key 降级和 JSON/SQLite view parity。 |
| S68-S69 科举深化规划 | AI 可扮演老师、保人、同年、县府/学政、房官、同考官、主考官、吏部意见和皇帝问策，生成题目、读书计划、文卷点评、科场事故、初评批语、复核疑点和授官 proposal；不可直接写县试/府试/院试资格、乡试/会试三场结果、解元/会元/状元/榜眼/探花、官职或任免事实。 | 规划入口见 `docs/IMPERIAL_EXAM_DEEPENING_ROADMAP.md`。后续实现必须由服务器维护 `studyProfileView`、`examProcedureView`、`examinerPanelView`、`examHonorView` 和 `appointmentTrackView`，并由 resolver 裁决保结、搜检、号舍、弥封、誊录、对读、磨勘、复核、房官/同考官/主考官建议、canonical ranking、甲第、馆选、庶吉士、观政、铨选、候缺、外放和籍贯回避。 | 玩家可见读书计划、老师批语、科场 phase、脱敏卷件、公开榜单、同年关系和授官轨迹；不得看到弥封前隐藏身份、考官 hidden intent、raw provider proposal、hidden 私档、raw table 或完整 prompt。 | `docs/IMPERIAL_EXAM_DEEPENING_ROADMAP.md`、`docs/DEVELOPMENT_STEPS.md`、`docs/QIANQIU_DEVELOPMENT_BRIEF.md`。当前是提前规划，暂无运行时代码。 | 实施时补红队：AI 夹带 `statePatch` 直接授官、房官把玩家定为解元/会元/状元、主考绕过服务器榜单、誊录/复核泄漏姓名或 hidden notes、授官 proposal 绕过官缺/回避/甲第、老师凭空制造真实关系、Mock/no-key 路径缺失。 |
| 检索式 prompt context assembler | AI 可读取 `retrievalContext` 中按任务/玩家行动排序的可见国家、城市、路线、NPC、关系、官署、官职、任所、考成、迁转、实体、案牍、军务外交态势和事件摘要；不可把排序结果当作任免、地方事务结局、外交战事结局、事件结局或隐藏信息公开裁决。 | S53.1 的 `src/ai/promptContextAssembler.js` 只从服务器可见 view 和 prompt summary helper 组装上下文，保留原 capped summary 字段并新增只读 ranked index。S57.1 后，`retrievalContext.events.recentEvents` 与 `src/ai/prompts.js` 顶层 `recentEvents` 都读取 `eventArchiveView` 安全条目，而不是 raw `eventHistory` 文本。S57.2 的审计公开 projection 工具是本地诊断出口，不进入 prompt assembler。S58.1 起，SQLite 读档会挂载非枚举安全来源，assembler 可从 `prompt_retrieval_index` 读取由 `worldGeographyView`、`worldPeopleView`、`officialPostingsView`、`localAffairsDocketView` 和 `eventArchiveView` 生成的 compact 可见检索行；该索引带内容 hash，读档从 `world_state_json -> server views` 单向修复。S63.2 的 `events.localDockets` 只取地方案牍 compact rows，不读取 raw 城市/官职/SQLite 表。S64.1 的 `events.militaryReports` 只取 `militaryDiplomacyView` 派生 compact rows，给高权力/军务身份看边镇、驻军、粮道、使节和边患预警，不读取 raw hidden ledger 或 raw SQLite 表。它不读取 raw `worldState.worldGeography`、raw `worldPeople`、raw `officialPostings`、raw `worldThreads`、raw `longTermEvents.queue`、raw audit sidecar、SQLite 审计表或 raw business tables；provider schema 和稳定前缀不变。 | 这是 provider-only prompt 对象，不直接进入浏览器 UI。S53.2 浏览器信息面板仍必须读取 route view，而不是 `retrievalContext` 或 `prompt_retrieval_index`。 | `src/ai/promptContextAssembler.js`、`src/ai/promptContextSource.js`、`src/storage/sqlitePromptRetrievalTables.js`、`src/game/militaryDiplomacy.js`、`src/ai/prompts.js`、`test/promptContextAssembler.test.js`、`test/prompts.test.js`、`test/sqlitePromptRetrieval.test.js`、`test/militaryDiplomacy.test.js`。 | S57.1 已补安全事件条目、分页和 hidden-token 检索探针；S57.2 的 raw audit 转公开事件工具不并入 prompt；S58.1 已补 SQLite prompt 索引同 id 内容污染、缺失行修复、raw audit/raw business table 防读取和 JSON fallback parity 探针；S63.2 已补 local-docket prompt row、污染 payload 修复和书生不可见回归；S64.1 已补军务外交 view 可见性、prompt rows、SQLite repair 和 hidden 军情/使节防泄漏。后续若扩展 SQLite prompt 检索范围，仍必须先经过 sanitized projection、内容 hash、hidden-token corpus 和 raw-ledger/audit 泄漏探针。 |
| 浏览器信息面板 | AI 不直接控制 DOM，不决定面板内容，也不能把 prompt-only `retrievalContext` 当作 UI 数据源。 | S53.2 的 `docs/BROWSER_INFORMATION_PANEL_PLAN.md` 固定后续面板只读 route player-facing view：天下格局读 `worldGeographyView`，任所地理读 `officialPostingsView` + `worldGeographyView`，人物谱牒读 `worldPeopleView`，官职簿读 `officialPostingsView`，地方案牍读 `localAffairsDocketView`，事件档案读服务器 `eventArchiveView`。S53.3 的 `#information-panel` 只缓存这些 route view 并渲染 tab 壳和计数；S53.4 的天下格局/任所地理细卡只读取 `worldGeographyView`、`officialPostingsView` 和可见地理查表；S53.5 的人物/官职簿细卡只读取 `worldPeopleView`、`officialPostingsView` 和可见辅助标签；S53.6 的事件档案只读取 `eventArchiveView`，不读取 raw ledger 或 raw audit；S57.1 起事件档案 view 带分页 metadata，SQLite `event_archive_index` 也只是同一 view 的派生索引；S57.2 审计公开 projection 仍是本地 CLI 输出，不是浏览器面板来源。S58.2 起，`#event-archive-panel` 暴露稳定分页 `data-*` 供验收读取，`smoke:browser -- --information-parity` 会对 JSON/SQLite 两种 adapter 比对局势簿 DOM 与 route view 摘要，但仍不读取 raw index 或 `retrievalContext`。S61.2 的任所考成事件条目只从 `officialPostingsView.assessmentRecords` 派生；S63.2 的 `local_docket` 条目只从 `localAffairsDocketView` 派生。 | 玩家可看到“局势簿”分类壳、安全计数、天下格局卡、任所地理卡、人物谱牒卡、官职簿卡和事件档案卡；后续案牍面板也必须读取 `localAffairsDocketView`。面板不得读取 raw ledger、raw audit sidecar、SQLite audit table、provider proposal、prompt、本地路径或 key；事件档案只展示公开近事、议程、长期事件、官场履历、科场档案、可见任所考成和地方案牍的 sanitized projection。 | `docs/BROWSER_INFORMATION_PANEL_PLAN.md`、`docs/BROWSER_ACCEPTANCE.md`、`src/game/localAffairsDockets.js`、`src/game/eventArchive.js`、`src/storage/sqliteEventArchiveTables.js`、`public/app.js`、`scripts/browserSmoke.js`、`test/localAffairsDockets.test.js`、`test/eventArchive.test.js`、`test/gameTurnEventArchive.test.js`、`test/browserSmokeScript.test.js`、`test/sessionStoreAdapterContract.test.js`、`test/auditEventArchiveTool.test.js`。 | S57.1 已覆盖分页、raw index 污染修复和 prompt 近事防泄漏；S57.2 已覆盖 JSON/SQLite 审计 projection 脱敏但不并入浏览器；S58.2 已覆盖 JSON/SQLite 局势簿 parity、事件分页 DOM metadata、raw table/index/prompt hidden token 和 event archive grid overflow；S63.2 已覆盖 local-docket route/SSE 与书生不可见。后续扩展任何信息面板或把 audit 检索并入事件档案时，仍需同步 hidden-token corpus、raw-ledger/audit 探针、分页权限和 overflow。 |
| SSE 流式回合 | 可流式生成顶层 `narrative` 字符串；不可在流式中提前提交状态。 | `streamTurn()` 缓冲完整 JSON，只有最终 payload 通过 schema/服务器结算后写 session；`streamingJson` 只抽顶层 `narrative`；后续失败不写存档。 | 浏览器先显示 pending 叙事；失败时移除未提交文本并显示错误。 | `src/utils/streamingJson.js`、`src/routes/game.js`、`test/streamingJson.test.js`、`test/streamingTurnRoute.test.js`。 | 增加 nested `statePatch.narrative`、半截 JSON、错误后重复 final_state、隐藏文本先流出再失败的浏览器/route 探针。 |
| 世界 tick / 全局时间 | AI 不可写 `turnCount`、日历推进、`tenDayPeriod` 或自然漂移；可在叙事中解释当旬/当月民情。 | `src/game/time.js` 归一化年月旬和格式化日期；`src/game/worldTick.js` 用服务器状态推进 上旬 -> 中旬 -> 下旬 -> 下月上旬，非月末返回轻量 `旬度` 反馈与小幅自然漂移，下旬 rollover 才执行完整月度资源/派系结算；route 用 server-owned patch 应用，并用 `worldTick.completedMonth` 门控长期事件与官场任期月份。 | 玩家看到 `[旬度]` 或 `[月度]` 反馈、事件、状态栏、存档和考试 UI 的年月旬；browser smoke 会检查关键日期面板包含 `上旬/中旬/下旬`。 | `docs/WORLD_TICK_CONTRACT.md`、`src/game/time.js`、`src/game/worldTick.js`、`test/time.test.js`、`test/worldTick.test.js`、`test/gameTurnTick.test.js`。 | 保持 provider 试图 patch `turnCount/year/month/tenDayPeriod` 同时触发考试窗口的跨模块探针，并覆盖非月末长期事件不递减。 |
| 普通回合考试触发 | AI 可建议 `examTrigger.shouldStart/level/reason`；不可直接创建或覆盖考试。 | `applyExamTrigger()` 检查 active writing exam、`canEnterExam()`、`canOpenExamInCalendar()`；拒绝原因进入 response，不写非法 `activeExam`。 | 玩家看到可入场或被拒原因，浏览器可打开合法考试。 | `src/routes/game.js`、`src/game/exams.js`、`src/game/examCalendar.js`、`test/providerLongRunScript.test.js`。 | 增加“已在写卷时 provider 请求另一场考试”“关闭考期最后一月流式触发失败”探针。 |
| 科举出题 | 可生成题面、题型、难度、要求、字数和推荐通过线；不可决定玩家是否有资格入场。 | `POST /api/exam/question` 先做考试等级、资格、日历窗口、盘费/赶考准备和未交卷复用；随后保存 server-owned `activeExam`。 | 玩家看到题面、要求、字数、准备情况和考试日历摘要。 | `src/routes/exam.js`、`src/game/examTravel.js`、`src/game/examCalendar.js`、`test/examTravel.test.js`、`test/examCalendar.test.js`。 | 增加 provider 题面夹带 `statePatch`、越级考试题、现代制度题、重复刷题绕过盘费探针。 |
| 科举局部时间 | AI 不裁决科场阶段、时辰、入场/交卷年月旬；浏览器按钮或写卷中 free-text 只作为玩家局部动作。 | `src/game/examSceneTime.js` 写 `activeExam.sceneTime`；`/api/exam/progress` 与写卷中的 `/api/game/turn` 不调用普通 provider、不运行全局 tick、不推进 `turnCount/year/month/tenDayPeriod`。 | 玩家看到场内阶段、局部步数、考试场景叙事和 `scene` cadence 反馈。 | `src/game/examSceneTime.js`、`src/routes/exam.js`、`src/routes/game.js`、`test/examSceneTime.test.js`、`test/examTravel.test.js`、`test/gameTurnExamTrigger.test.js`。 | 增加 provider/客户端试图跳阶段、错 examId 推进、写卷中 SSE 普通回合仍不滚旬的探针。 |
| 科举评分 | 可生成五维评分、考官评语和模型侧 authenticity echo；不可独自处罚或晋级。 | `checkEssayAuthenticity()` 本地复核；`applyAuthenticityPenalties()` 改写总分与评语；`applyExamPromotion()` 决定通过、降档、殿试入仕和严重作弊后果。 | 放榜页显示分数、复核、榜单、晋级/黜落原因和考试档案。 | `src/routes/exam.js`、`src/game/essayChecks.js`、`src/game/promotions.js`、`test/examRules.test.js`、`test/aiEvalFixtures.test.js`。 | 增加“AI 判抄袭但本地无证据”“AI 放过现代词”“AI 给 101 分/负分”“殿试严重作弊仍授官”的回归组合。 |
| 虚拟考生与榜单 | AI 当前不拥有 canonical 虚拟考生或榜单；grade schema 可承载 provider-shaped 样本，但 route 使用服务器生成。 | `generateVirtualCandidates()`、persistent cohort 和 `buildRanking()` 生成同场考生、文卷、榜次和同年记忆。 | 玩家看到同场文卷、名次、评语、强弱项和后续 rival 记忆。 | `src/game/candidates.js`、`src/game/examCalendar.js`、`src/routes/exam.js`。 | 增加 grade payload 夹带 provider 排名但服务器榜单不采纳的 route-level 探针。 |
| 关系变化 | AI 可建议顶层 `relationshipChanges`，只能指向既有、可见 character/faction，变化幅度受限；不可直接写 ledger。 | `ensureRelationshipLedger()` 归一化；`applyRelationshipChanges()` clamp、丢弃隐藏/未知目标、追加可见 notes；raw ledger 不进普通 patch。 | 玩家看到人脉簿、最近关系变动和必要的隐藏提示。 | `src/game/relationships.js`、`test/relationshipLedger.test.js`、`test/gameTurnRelationships.test.js`。 | 增加 provider 对 hidden contact/faction、超界 delta、伪造新 NPC、note 泄漏 hidden intent 的综合探针。 |
| 主动 NPC 请托 | AI 可生成相关叙事和关系建议；不可创建、替换、接受、拒绝或过期请求。 | `runActiveNpcRequestStep()` 调度、回应、过期和事件顺序；请求状态由服务器写 `worldState.activeNpcRequest`。 | 浏览器 `#active-request-panel` 显示可见来函/请托/期限；隐藏意图不公开。 | `src/game/activeRequests.js`、`test/gameTurnLongTermEvents.test.js`、`test/gameTurnRelationships.test.js`。 | 增加 provider 伪造 active request、把隐藏目标写进叙事、请求过期同回合越权接受的探针。 |
| 人物、家族、资产与田产 schema / bridge | AI 可在叙事中解释可见人物、人情、公开谱系、姻亲、门生故旧、同乡同年、派系、家产或田产压力；当前只能沿用 `relationshipChanges` 建议既有可见关系，不可写人物/家产/田产/关系表，也不可裁决婚姻、死亡、迁居、任免或生命周期结果。 | S51.1 的 `worldPeopleSchemas` 归一化独立 schema bundle、过滤 hidden 行和 nested hidden refs、构造 capped prompt summary。S51.2 的 `worldPeople` 从旧 `characters`、`relationshipLedger` 和可见 active request 近期札记派生 `worldState.worldPeople`、`worldPeopleView` 与 prompt summary；它只保存当前可见 projection，不存新增 hidden 私档。S55.2 在 SQLite 模式通过 `src/storage/sqlitePeopleTables.js` 创建并同步 `people_npcs`、`people_households`、`people_assets`、`people_estates`、`people_relationships`，但只从规范化后的可见 bridge projection 写入；读档修复从 `world_sessions.world_state_json` 单向重建，不把 raw `people_*` 行回填进 route state。S55.3 的 `src/game/worldPeopleEvents.js` 只根据服务器已应用的可见 people delta 生成安全人物事件，SQLite `people_*.last_event_id` 只在 storage context 中关联审计事件，不进入 route `worldPeople`。S62.1 的 `src/game/worldPeoplePopulation.js` 只由服务器/fixture helper 生成可见 NPC 人口、家族谱系和社会关系 projection，供 small route-safe fixture 与 medium/large storage-only 侧车复用；不新增 hidden 私档、不写 raw `people_*`，也不放宽 prompt cap。普通 `statePatch.worldPeople` 被 schema、remote normalization、`applyStatePatch()`、provider long-run 和 route red-team 拒绝或忽略；审计脱敏会 redacts `worldPeople` / `npcs` / `assets` 等 proposal 内容。 | 游戏、SSE 和考试 route 返回 `worldPeopleView`；现有 `relationshipView` / `activeNpcRequestView` 不变。S53.5 浏览器人物谱牒读取服务器 view 和可见关系标签，不能读 raw ledger、旧 `characters`、`relationshipLedger`、raw `people_*` 表或 `last_event_id`。S62.1 的中/大样本人口总量只在 storage-only fixture/page helper 中体现，真实浏览器搜索分页仍归 S66.2。 | `docs/NPC_HOUSEHOLD_ASSET_RELATIONSHIP_CONTRACT.md`、`src/game/worldPeopleSchemas.js`、`src/game/worldPeople.js`、`src/game/worldPeoplePopulation.js`、`src/game/worldPeopleEvents.js`、`src/storage/sqlitePeopleTables.js`、`test/worldPeoplePopulation.test.js`、`test/worldPeopleSchemas.test.js`、`test/worldPeopleBridge.test.js`、`test/worldPeopleEvents.test.js`、`test/gameTurnRelationships.test.js`、`test/prompts.test.js`、`test/aiControlRedTeam.test.js`、`test/sessionStoreAdapterContract.test.js`。 | S55.2 已覆盖 JSON/SQLite `worldPeopleView` / prompt parity、hidden token、hidden raw `people_*` row、可见 bridge 缺失、错 `row_id`、relationship mutate、stale revision 和 import/delete 探针；S55.3 已覆盖关系/请托人物事件、生命周期/资产/田产可见 delta 分类、SQLite `last_event_id` 关联和 view/prompt/retrieval 不显示事件 id。S62.1 已覆盖身份类型、亲缘/婚姻/门生/同乡同年/派系网络、prompt cap、medium/large storage-only 总量和 hidden token 不回填。真正 hidden 私档或安全事件索引仍需继续保持 route redaction/view-first 边界。 |
| 长期事件 | AI 可解释灾荒、边报、廷争等可见后果；不可排程、解决、删除或直接写队列。 | `runLongTermEventStep()` 依据服务器日历/指标调度和结算；server-owned patch 写入队列、冷却、近期归档和数值后果。 | 玩家看到 `[大势]` 反馈、长期事件 view 和世界议程派生摘要。 | `docs/LONG_TERM_EVENTS_CONTRACT.md`、`src/game/longTermEvents.js`、`test/longTermEvents.test.js`、`test/gameTurnLongTermEvents.test.js`。 | 增加 provider patch `longTermEvents.queue/recentResolved/cooldowns` 与隐藏归档泄漏探针。 |
| 官场任免与履历 | AI 可生成公文口吻、来函、传闻、关系态度和受限官场 meter 建议；不可授官、升降、弹劾成案、处分、起复或公开 hidden notes。 | `officialCatalog` 推断官署；`runOfficialCareerStep()` 推进差事/考成/弹劾流程并结算任免；`player.officeTitle`、`officialCareer` 和明显官职 `position` 由服务器保护。 | `officialCareerView` 和浏览器官场面板显示官署、差事、考成、关系、风险和履历档案。 | `docs/OFFICIAL_CAREER_CONTRACT.md`、`src/game/officialCareer.js`、`src/game/officialCatalog.js`、`test/gameTurnOfficialCareer.test.js`。 | 增加更多幻觉官职变体：空格、繁简、别称、低频官名、`position` 与 `officeTitle` 双通道混写。 |
| 官职任所数据库契约 / bridge | AI 可读取 capped 可见官署、官职、任所、考成、迁转、S61 城市压力摘要和 S63.1 任命池线索并解释公开职责或传闻；不可写 `officialPostings`、任命记录、考成记录、迁转记录或 `office_*` SQLite business rows，也不可用城市指标或官缺压力直接裁决考成、补缺、起复、弹劾或任免。 | S52.1 的 `officialPostingSchemas` 定义 schema normalization、hidden-filtered view 和 prompt cap。S52.2 的 `officialPostings` 从 `officialCatalog`、`officialCareer`、地方官 role state 和可见 `worldGeographyView` 派生 `worldState.officialPostings`、`officialPostingsView` 与 prompt summary；存储的 raw ledger 也只是可见 projection。S61.2 起，bridge 的 `localMetrics` 优先读取可见城市税基、词讼、水利、士绅、灾害、驻军、书院等指标，当前任所考成追加“任所奏报”。S63.1 起，bridge 用 `officialEcosystemConfig` 派生上级堂官、属官/胥吏/幕友接口、空缺、候补、补授、试署、外放、丁忧、起复和弹劾候勘等少量可见任命池 projection；这些内容只影响 view/prompt/SQLite 派生索引背景，不改变 `officialCareer` 的 merit、risk、recommendation 或任免结算。`eventArchiveView` 只从真实任所考成派生 `official_assessment` 条目，排除 `assessment-s63-*` 任命池压力行。S56.1 在契约中固定 `office_bureaus`、`office_catalog`、`office_city_jurisdictions`、`office_postings`、`office_assessments`、`office_transfers` 表形状、公共列、hidden 边界、holder/地理/人物引用和 one-way repair 规则；S56.2 已由 `src/storage/sqliteOfficialPostingTables.js` 在 SQLite 模式创建并同步这些派生表；S56.3 为派生行加入 `metadata_json.contentHash` 漂移探针，读档时只从 `world_state_json` 修复缺失、陈旧、错行、同 id/同 revision 内容污染或旧行缺指纹。普通 `statePatch.officialPostings` 会被 schema、remote normalization、`applyStatePatch()`、audit redaction 和 provider long-run 拒绝、忽略或脱敏。实际任免仍由 `officialCareer` 裁决。 | 游戏、SSE 和考试 route 返回 `officialPostingsView`；S53.4 已用 `officialPostingsView` 加可见 `worldGeographyView` 渲染任所地理，S53.5 已用同一 view 渲染官职簿。浏览器 UI 和事件档案必须读取服务器 view，不能读 raw ledger 或 raw `office_*` table。hidden 任所、密札考成、未公开调任、hidden holder refs、hidden people refs 和 hidden 地理 refs 不进入 view/prompt；S63.1 `role_visible` 官缺线索只对行政身份开放。 | `docs/OFFICIAL_POSTING_DATABASE_CONTRACT.md`、`src/game/officialEcosystemConfig.js`、`src/game/officialPostingSchemas.js`、`src/game/officialPostings.js`、`src/game/eventArchive.js`、`src/storage/sqliteOfficialPostingTables.js`、`test/officialPostingSchemas.test.js`、`test/officialPostings.test.js`、`test/eventArchive.test.js`、`test/gameTurnOfficialPostings.test.js`、`test/prompts.test.js`、`test/auditRoute.test.js`、`test/providerLongRunScript.test.js`、`test/sessionStoreAdapterContract.test.js`。 | S56.3 已覆盖 same row/same revision raw table pollution、hidden geo/people refs、旧行缺 hash 安全升级和 prompt/view/retrieval 防泄漏；S61.2 已覆盖城市深度进入 localMetrics、考成公开摘要、prompt 摘要和事件档案安全条目；S63.1 已覆盖任命池幂等生成、候补/丁忧/起复/弹劾摘要、`assignmentIds` cap、事件档案排除任命池压力和 JSON/SQLite parity。后续若保存真正 hidden 官员私档、密札考成或未公开调任，必须先新增 route redaction 或非 raw player state API，并补浏览器 hidden-token corpus、raw-ledger fallback 探针和 SQLite 表级权限边界；若扩展 SQLite 索引读取，也只能读取安全 capped projection。 |
| 地方事务案牍 / local affairs docket | AI 可读取 capped `localAffairsDocketView` 与 `events.localDockets` 摘要，解释钱粮、刑名、灾赈、水利、盗匪、徭役、士绅、疫病和任所收束压力；可写叙事或后续受限 proposal，但不可直接写城市指标、案牍结局、官场考成、任免、`event_archive_index`、`prompt_retrieval_index`、`geo_*`、`office_*` 或其他 SQLite 原始表。 | S63.2 的 `src/game/localAffairsDockets.js` 从 `worldGeographyView` 城市深度指标和 `officialPostingsView` 任所/辖区行派生 `localAffairsDocketView`，模板、严重度阈值、辖区 cap 和 prompt/archive cap 集中在 `src/game/localAffairsDocketConfig.js`。它只返回可见案牍、公开摘要、metricRefs 和非裁决性的 `assessmentHint`；不修改 `worldState`、`officialCareer`、`officialPostings` 或任何 SQLite 表。`eventArchiveView` 的 `local_docket` 条目只从该 view 生成；`prompt_retrieval_index` 的 local-docket rows 也只从 server view compact projection 同步并按内容 hash 修复。 | 游戏、SSE 和考试 route 返回 `localAffairsDocketView`；书生默认为空，地方官/入仕官员/大臣/皇帝/将领按行政身份和辖区 cap 读取。浏览器或后续案牍面板必须读取该 route view，不得扫描 raw geography/posting ledger、raw SQLite rows、raw audit、provider proposal、prompt、本地路径或 key。 | `src/game/localAffairsDocketConfig.js`、`src/game/localAffairsDockets.js`、`src/game/eventArchive.js`、`src/ai/promptContextAssembler.js`、`src/storage/sqlitePromptRetrievalTables.js`、`src/routes/game.js`、`src/routes/exam.js`、`test/localAffairsDockets.test.js`、`test/eventArchive.test.js`、`test/gameTurnEventArchive.test.js`、`test/promptContextAssembler.test.js`、`test/prompts.test.js`、`test/sqlitePromptRetrieval.test.js`。 | S63.2 已覆盖行政身份/书生可见性、案牍不变更 career/geography、事件档案 `local_docket`、prompt rows、SQLite same-row 污染修复和 route/SSE payload。后续若新增案牍 resolver、官场考成影响、浏览器面板或 AI 工具，必须另补 proposal schema、服务器裁决、审计事件、hidden-token corpus、分页权限和 JSON/SQLite parity。 |
| 外交军务态势 / military diplomacy | AI 可读取 capped `militaryDiplomacyView`、顶层 `militaryDiplomacy` prompt 摘要与 `events.militaryReports`，解释边防战区、驻军、粮道、战备、邻国使节和边患预警；可写叙事或后续受限 proposal，但不可直接宣战、和议、调兵、任免统帅、结算战役、改变军粮、公开隐藏情报或写 `geo_*`、`people_*`、`office_*`、`event_archive_index`、`prompt_retrieval_index` 等 SQLite 原始/派生表。 | S64.1 的 `src/game/militaryDiplomacy.js` 从可见 `worldGeographyView`、`worldPeopleView` 和 `officialPostingsView` 派生只读态势，配置集中在 `src/game/militaryDiplomacyConfig.js`。它按角色 cap、任所/边面相关性、文本脱敏和情报可信度过滤，返回战区、驻军、粮道、外交接触与边患预警；不修改 `worldState`，不写持久表。`eventArchiveView` 的 `military_diplomacy` 条目只从 `militaryDiplomacyView.frontierIncidents` 派生；`prompt_retrieval_index` 的 military rows 只同步 compact server view，并用内容 hash 修复污染。 | 游戏、SSE 和考试 route 返回 `militaryDiplomacyView`；书生默认为空，地方官/入仕官员/大臣/皇帝/将领按身份上限读取。当前不新增浏览器军务面板；后续面板必须只读 route view，不得扫描 raw geography/people/posting ledger、raw SQLite rows、raw audit、provider proposal、prompt、本地路径或 key。 | `src/game/militaryDiplomacyConfig.js`、`src/game/militaryDiplomacy.js`、`src/game/eventArchive.js`、`src/ai/promptContextAssembler.js`、`src/storage/sqlitePromptRetrievalTables.js`、`src/routes/game.js`、`src/routes/exam.js`、`test/militaryDiplomacy.test.js`、`test/eventArchive.test.js`、`test/gameTurnEventArchive.test.js`、`test/promptContextAssembler.test.js`、`test/prompts.test.js`、`test/sqlitePromptRetrieval.test.js`。 | S64.1 已覆盖书生不可见、将领可见态势、route/SSE/exam payload、事件档案 `military_diplomacy`、prompt rows、SQLite same-row 污染修复和 hidden 军情/使节 token 防泄漏。后续若新增战役/外交 resolver、军务浏览器面板或 AI 工具，必须另补 proposal schema、服务器裁决、审计事件、分页权限、JSON/SQLite parity 和越权调兵/宣战/和议红队。 |
| 身份与世界联动 | AI 可叙述角色行动背景并建议普通 meter；不可决定 compound 世界后果或 cooldown。 | `runRoleWorldCouplingStep()` 识别地方水利、将领战役、皇帝任免、大臣弹劾等关键行动，服务器写近期影响和冷却。 | 玩家看到 `[联动]` 行、相关状态、人脉与世界议程。 | `docs/ROLE_WORLD_COUPLING_CONTRACT.md`、`src/game/roleWorldCoupling.js`、`test/gameTurnRoleWorldCoupling.test.js`。 | 增加 provider patch cooldown/recentImpacts、重复触发绕过 cooldown、隐藏关系牵连泄漏探针。 |
| 多实体世界模型 / World Entities | AI 可读取可见 `worldEntities` prompt 摘要，并在叙事中解释朝廷衙门、地方士绅、书院、边镇、盐漕税赋和赈务压力；不可写实体账本、隐藏札记、`worldEntityImpacts` 或实体结局。 | `src/game/worldEntities.js` 创建/归一化 server-owned `worldState.worldEntities`，clamp/cap 实体指标并构造 `worldEntityView`；S45.2 的 `deriveWorldEntityInfluences()` / `applyWorldEntityInfluences()` 只从已经应用的服务器来源生成 bounded 影响；普通 `statePatch.worldEntities` 被 schema、remote normalization 和 `applyStatePatch()` 拒绝或忽略。 | 路由返回 `worldEntityView`，turn payload 额外返回本回合 server-owned `worldEntityImpacts`；隐藏实体、`hiddenNotes` 与 `recentNotes` 不进入 view/prompt。 | `docs/WORLD_ENTITIES_CONTRACT.md`、`src/game/worldEntities.js`、`test/worldEntities.test.js`、`test/gameTurnWorldEntities.test.js`、`test/prompts.test.js`。 | 后续若新增浏览器实体面板，要用 `worldEntityView` 而不是 raw ledger，并补 smoke 隐藏 token 扫描；`role_visible` 仍可作为后续身份视野切片。 |
| 天下地理账本 | AI 可读取 capped `worldGeography` prompt 摘要，并在叙事中解释公开地名、路线、边面、贡道、官署辖区，以及 S61 新增的国家财政/军备/国威/继承/外交/情报可靠度和城市税粮/市价/士绅/词讼/徭役/水利/灾害/交通/驻军/书院压力；不可写 `worldGeography` 或 `geo_*` SQLite business rows，不可裁决战争、外交、城市财政、城市治理或隐藏情报公开。 | `src/game/worldGeographyConfig.js` 集中 S61 深度指标默认值、字段 keys、动态刷新权重和文本上限；`src/game/worldGeographySeeds.js` 提供静态 seed；`src/game/worldGeography.js` 创建/归一化 server-owned `worldState.worldGeography`，从现有顶层指标刷新轻量压力和 S61 深度 projection，并构造 `worldGeographyView` 与 prompt summary。S61.2 城市指标以 seed 为基线，随财政、粮储、治安、腐败和军情压力由服务器幂等刷新；同一 `turnCount` 的 route view 不会反复漂移。普通 `statePatch.worldGeography` 被 schema、remote normalization、`applyStatePatch()` 和 provider long-run 检查拒绝或忽略。S54.1 在 `docs/WORLD_GEOGRAPHY_SEED_CONTRACT.md` 固定 `geo_countries`、`geo_regions`、`geo_cities`、`geo_routes`、`geo_frontier_zones`、`geo_office_jurisdictions` 契约；S54.2 由 `src/storage/sqliteGeographyTables.js` 和 SQLite adapter transaction 同步这些表，并从 `world_state_json` 修复缺失/陈旧地理行。S61 不新增 STRICT 表列；`geo_countries.metadata_json.s61CountryDepth` 与 `geo_cities.metadata_json.s61CityDepth` 只保存安全派生摘要，prompt/UI 仍只读 server view 或安全 `prompt_retrieval_index`。S61.2 已让 `officialPostings` 和 `eventArchiveView` 通过服务器 view 消费城市深度摘要，不反向读取 raw `geo_*` 表。 | 路由返回 `worldGeographyView`；隐藏路线/边面、hidden 国家/城市深度文本、`hiddenNotes` / `hidden_notes_json`、hidden nested refs 和书生不可见的 `role_visible` 行不进入 view/prompt。现有 route 仍返回完整本地 `worldState` 以保持开发兼容；S53.4 浏览器天下格局已读取 view 而不是 raw ledger，S54.2/S54.3/S61 不新增 raw table route/prompt/UI 暴露。 | `docs/WORLD_GEOGRAPHY_SEED_CONTRACT.md`、`src/game/worldGeographyConfig.js`、`src/game/worldGeographySeeds.js`、`src/game/worldGeography.js`、`src/game/officialPostings.js`、`src/game/eventArchive.js`、`src/storage/sqliteGeographyTables.js`、`src/storage/sqlitePromptRetrievalTables.js`、`scripts/sqliteGeographyTool.js`、`test/worldGeographySeeds.test.js`、`test/worldGeography.test.js`、`test/worldContentFixtures.test.js`、`test/officialPostings.test.js`、`test/eventArchive.test.js`、`test/sqlitePromptRetrieval.test.js`、`test/sessionStoreAdapterContract.test.js`。 | S61 已覆盖国家/城市深度 projection、任所考成/事件档案联动、SQLite event/prompt 污染修复和城市动态刷新。后续 S64/S65/S66 继续保持 AI 只能读 projection、不能写 SQL/table rows；仍需补浏览器分页、role-visible 精细权限、大 fixture prompt budget 探针和更完整的经济/军事/灾害 resolver。 |
| 世界议程 / World Threads | AI 可读取可见 prompt 摘要并解释议题；不可写 `worldThreads`、不可解决议题、不可替代来源系统。 | `ensureWorldThreadState()` 从 server-owned 来源派生、去重、排序、过滤隐藏 row 并构造 `worldThreadView`；S45.2 允许高压可见实体派生 `world_entity` 议题，并给议题挂 `relatedEntitySummaries`。 | 浏览器 `#world-thread-panel` 显示目标、期限、风险、相关对象、相关实体、介入提示和近归档余波；相关实体摘要来自可见 `worldEntityView`。 | `docs/WORLD_THREADS_CONTRACT.md`、`src/game/worldThreads.js`、`test/worldThreads.test.js`、`test/gameTurnWorldThreads.test.js`。 | 增加 legacy hidden `threads`、hidden `recentResolved`、provider `statePatch.worldThreads`、prompt 摘要泄漏的组合 fixture；若未来实体议题过多，再补去重/降噪规则。 |
| 事件日志与 AI proposal 审计 | AI 不可写审计表、不可决定审计 status；它的输出只作为被审查 proposal 的输入摘要。 | S49.4 由 `src/game/audit.js` 在服务器裁决后生成摘要；`src/storage/sessionAudit.js` 统一脱敏 API key、本地路径、隐藏字段和 raw `worldState`；JSON adapter 尽力写 `data/audit/*.jsonl` 诊断 sidecar，SQLite adapter 在同一事务写 `event_log` / `ai_change_proposals`，route 通过 `mutateSession()` context 排队，`skipWrite` 会丢弃队列。S53.6 的 `eventArchiveView` 不读取这些 raw audit/proposal 记录；S57.1 的 `event_archive_index` 也只从 `eventArchiveView` 安全条目派生，不从 raw audit 反向生成玩家信息。S57.2 的 `src/game/auditPublicProjection.js` 和 `scripts/auditEventArchiveTool.js` 可在本地读取 JSON sidecar 或 SQLite 审计，但只投影 `visibility: "public"` 摘要、related/applied allowlist 和统计；`ai_change_proposals` 只计数，不输出 proposal 原文。 | 审计记录本身不暴露玩家 API；浏览器事件档案读取 `eventArchiveView`，不能直接展示 raw audit payload。`storage:audit-events` 输出面向本地开发/调试，默认只含安全 public projection，不回填 route state、prompt、浏览器或 `event_archive_index`。 | `src/storage/sessionAudit.js`、`src/game/audit.js`、`src/game/eventArchive.js`、`src/game/auditPublicProjection.js`、`scripts/auditEventArchiveTool.js`、`src/storage/jsonSessionAdapter.js`、`src/storage/sqliteSessionAdapter.js`、`src/storage/sqliteEventArchiveTables.js`、`test/auditRoute.test.js`、`test/sessionStoreAdapterContract.test.js`、`test/eventArchive.test.js`、`test/auditPublicProjection.test.js`、`test/auditEventArchiveTool.test.js`。 | S57.2 已覆盖 JSON sidecar / SQLite audit 双来源、hidden notes、prompt、path、key、raw audit table token 和 AI proposal 原文防泄漏；后续若把工具输出接入 UI/prompt，必须另补权限、分页和 hidden-token 验收，且不得跳过 `eventArchiveView` sanitizer。 |
| 存档与存档列表 | AI 不可写 session envelope、revision、metadata、文件路径或 provider 配置。 | `sessionStore` 负责安全 session id、envelope、atomic rename、revision、lock、metadata 脱敏和 skipped 文件。 | `GET /api/game/saves` 只显示 metadata；载入完整状态必须走 state route。 | `src/storage/sessionStore.js`、`test/sessionStore.test.js`、`test/saveListRoute.test.js`。 | 增加 provider 输出中夹带 session 路径/metadata、未来 schema 存档、并发 revision 冲突后的 AI 重试边界探针。 |
| 隐藏信息与视图过滤 | AI 只能读取 compact visible summary；不可公开隐藏关系、隐藏议题、隐藏实体、官场 hidden notes、密钥、provider 原始错误。 | 各 `build*View()` 与 prompt summary 负责过滤；diagnostics 错误脱敏；browser 只消费 view。 | 玩家可见信息需标明来源语气：公文、奏报、传闻、私信、角色自知或开发者诊断。 | `src/game/relationships.js`、`src/game/worldEntities.js`、`src/game/worldThreads.js`、`src/ai/prompts.js`、`test/prompts.test.js`、`test/browserSmokeScript.test.js`。 | 增加跨视图 hidden token corpus：关系 notes、NPC hidden intent、官场 hiddenNotes、world entity hiddenNotes、world thread hidden rows、provider key 片段。 |
| 浏览器显示 | AI 不直接控制 DOM 或读取 raw hidden ledger；只能通过 route payload 中的 narrative/view 间接呈现。 | `public/app.js` 渲染服务器 view；smoke 检查关键 selector、隐藏 token 和桌面/移动溢出。 | 玩家看到叙事、面板、考试、存档簿、AI 连接诊断；开发者看到 smoke 截图和测试输出。 | `public/app.js`、`scripts/browserSmoke.js`、`test/browserSmokeScript.test.js`、`docs/BROWSER_ACCEPTANCE.md`。 | 增加 AI 连接按钮浏览器 smoke、streaming 错误 UI、隐藏 token 多面板扫描和 save-list metadata 泄漏检查。 |

## 当前保护层

| 层级 | 作用 | 主要文件 |
| --- | --- | --- |
| Prompt pack | 在模型调用前声明 JSON 严格性、时代语气、隐藏信息限制和服务器边界。 | `src/ai/promptPacks.js`、`src/ai/prompts.js` |
| Prompt context assembler | 从服务器可见 projection 组装动态 prompt 摘要和 ranked `retrievalContext`，避免 prompt 直接读取 raw hidden ledger 或 raw audit payload。 | `src/ai/promptContextAssembler.js`、`test/promptContextAssembler.test.js` |
| Model schema | 拒绝 provider-shaped payload 中的未知根字段、越权 `statePatch`、非法考试等级和超界评分。 | `src/ai/schemas.js`、`test/aiSchemas.test.js` |
| Remote normalization | 真实 provider 原文解析后先丢弃未知/服务器独占 patch，再交给统一 schema。 | `src/ai/providers/remoteHelpers.js`、`test/remoteHelpers.test.js` |
| State merge | `applyStatePatch()` 执行 provider/server 白名单、数值 clamp、事件裁剪和 official `position` 官职伪造过滤。 | `src/game/stateRules.js`、`test/stateRules.test.js` |
| Server follow-up | route 在 provider 后按固定顺序执行关系、考试门禁、NPC、身份联动、tick、长期事件、官场和议程同步。 | `src/routes/game.js`、`src/routes/exam.js` |
| Player-facing view | 各模块用 view 暴露可见摘要，隐藏 ledger 不直接给浏览器和普通 prompt。 | `src/game/*` view builders、`public/app.js` |
| Offline/keyed acceptance | no-network eval、route tests、provider smoke、browser smoke 分层检查 schema、语气、越权、布局和真实 provider 可达性。 | `testdata/aiEvalFixtures.js`、`scripts/providerSmoke.js`、`scripts/providerLongRun.js`、`scripts/browserSmoke.js` |

## S44.2 优先测试清单

1. 越权 patch 组合包：一个普通 turn 同时伪造 `activeExam`、`examCalendar`、`longTermEvents`、`officialCareer`、`roleWorldCoupling`、`worldEntities`、`worldThreads`、`player.examRank`、`player.officeTitle` 和隐藏关系目标，确认全部被 schema/normalizer/merge 层挡下。
2. 幻觉官职扩展：对 `player.position` 与 `player.officeTitle` 分别注入官职别称、空格拆分、繁简变体、目录外高官和“署理/护理/兼管”文本，确认 official 玩家不能被普通 provider 任免。
3. 非法晋级与考试门禁：provider 试图通过 `examTrigger` 越级、闭考期入场、已有写卷覆盖、殿试前直接入仕，确认 route 返回安全拒绝且 session 不变。
4. 隐藏信息泄漏：建立统一 hidden token fixture，覆盖关系、主动请求、长期事件、官场 notes、World Threads、provider 配置、浏览器多面板扫描和旧 payload/raw `worldState` fallback。
5. 作弊误判边界：AI 评分和本地 `checkEssayAuthenticity()` 意见不一致时，以本地复核和服务器晋级规则为准；严重作弊不得入仕。
6. 流式失败：流出顶层叙事后 provider 失败、流出 nested `statePatch.narrative`、半截 JSON 和重复 final payload 都不得写 session 或留下 pending 文本。

S44.2 baseline 已覆盖：

- `test/aiControlRedTeam.test.js` 用 route-level 恶意 provider 覆盖混合越权 patch（含 `worldEntities`）、隐藏关系目标、非法考试触发、provider-owned 科举候选/榜单、AI 与本地反作弊意见冲突、严重作弊不得晋级，以及流式失败后不落盘。
- `testdata/aiEvalFixtures.js` 与 `test/aiEvalFixtures.test.js` 增加混合越权离线 fixture，确认 opening/turn schema 拒绝跨系统状态写入。
- `test/stateRules.test.js` 扩大官职幻觉 guard，覆盖空格、繁体、署理/护理/兼管和常见英文官名。
- `test/aiDiagnostics.test.js` 覆盖长密钥片段脱敏；`src/routes/game.js` 的 SSE `error` 事件也复用 `redactSecrets()`，避免 provider 错误文本直接泄漏已配置 key 或片段。

仍留给后续的更细验收：

- 浏览器自动化仍主要通过既有 smoke helper 验证 pending 文本回滚；S44.2 新增的是 route 层不落盘和 error 脱敏，不保证失败前已流出的网络文本从未到达客户端。
- `POST /api/exam/question` 仍依赖 provider schema 校验题面结构；若未来要防御自定义 provider 绕过 adapter，需要为题面 `passScore/wordCount/requirements` 增加 route-level sanity clamp。
- 统一 hidden token corpus 目前覆盖 prompt/eval、relationship/world thread/browser helper 与 S44 route views；完整 `worldState` 本地 API 仍按当前威胁模型保留内部状态。

## 维护规则

- 新增 AI 可读摘要时，必须说明隐藏字段如何过滤，并补 prompt/input 测试。
- 新增 AI 可写/可建议字段时，必须同时更新 schema、remote normalization、`applyStatePatch()` 白名单、clamp 和 S44 矩阵。
- 新增服务器拥有 ledger 时，必须补“provider 不能 patch 该 ledger”的 schema/eval/route 测试，并通过 view 暴露玩家可见部分。
- 新增浏览器面板时，必须只读 route view，补 hidden token 和响应式 smoke；不得扫描 raw `worldState` 中的隐藏 ledger。
